# D-017/D-018 turn-2 prep

Coordinator notes only. Not a decision. Not a review. Not the subject.
Written while turn-1 reviews were still in flight, after Codex's
in-progress measurements named two load-bearing problems. Apply only
after both verdict files exist, and only to findings those verdicts
actually raise.

## Likely D-017 MUST-FIX: "exactly one" consumption rule

Draft §3 says an item becomes live work only by being written into
**exactly one** of: D-000 entry, register amendment, Route B disposition.

Not exclusive: D-010 is a D-000 entry that also amends the register.
Not exhaustive under D-001: Route A (V1 successor through coop process)
and Route C (product disposition) are lawful surfaces D-001 already
names. Route C is usually a D-000 entry, but Route A is not in the list.

Intended repair, if the verdicts raise it:

- Replace "exactly one of" with "at least one lawful surface already
  named by D-001 or by file 08."
- Enumerate the D-001 routes by reference, not a new closed set:
  Route A, Route B, Route C / D-000 entry, and a file-08 row or
  amendment.
- Keep the negative: conversation, this draft, and file 11 itself
  cannot satisfy consumption.
- If a count-pin is kept, pin the negative (those three non-surfaces),
  not a new exclusive positive set that will drift against D-001.

## Likely D-018 MUST-FIX: Route B selection vs DR-003

The honesty-about-the-trade says preview design is no longer serialized
behind Phase-1A, TM-complete closure, G19, and the evidence/retention
join. The decision also says DR-003 is not waved through and a scoped
threat model remains required.

If DR-003 stays full Route A (V10/custody + G19 + final disposition),
condition 1 still cannot close for the preview, and selecting Route B
for DR-002/004/005/008 does not produce the stated benefit.

Intended repair, if the verdicts raise it:

- Select Route B for a **scoped preview threat model** under DR-003,
  covering what the preview actually ships: signed delivery, permission
  broker, doctor probes, bundled Node closure.
- Full TM / V10 / G19 / publication-block closure stays Route A on the
  authoritative-MVP path.
- This is a fifth selected Route B, or a re-cut of §4 into "four
  authoritative-closure rows" plus "one scoped TM row." Do not bury
  the TM selection inside the trade paragraph.
- Do not write the scoped TM disposition in D-018.
- Recite that condition 1 remains unmet until those later dispositions
  land. The trade is authorization to *author* them, not a claim that
  condition 1 is now reachable without TM.

## Do not apply until both verdicts are on disk
