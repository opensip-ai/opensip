{
  "artifact": "component-manifest-schemas.v8",
  "version": 8,
  "date": "2026-08-14",
  "documentClass": "DESIGN-CONTRACT-CANDIDATE",
  "status": "CANDIDATE-NOT-APPLIED",
  "reviewStatus": "AWAITING-INDEPENDENT-REVIEW",
  "sealRecommendation": "DO-NOT-SEAL",
  "binds": "NOTHING",
  "authorityClaim": "NONE. Authored under the adopted D-001 plan as DR-103 candidate acceptance material. This artifact is the author-side candidate for DR-103's acceptance-evidence cell ('Reviewed canonical schemas, duplicate/path rejection, signatures, exact-byte test corpus', file 08 DR-103 row). It closes nothing: DR-103 closes only on independent review plus the register's own process. Nothing here is QUALIFIED or DEMONSTRATED.",
  "purpose": "Define the canonical COMPONENT MANIFEST, INDEX, and LOCK schemas for V2 distribution, field by field, with typed rejection rules, exact-byte test-corpus requirements, named identity dependencies on open surfaces, and named differences from the pinned prototype baseline. Every empirical claim below is measured or cited at digest (EIR discipline); every design choice that could go another way carries an alternatives note.",
  "basedOn": {
    "predecessor": "docs/coop/artifacts/component-manifest-schemas.v7.json",
    "sha256": "a887001807402dc37d698288b3852a84b24faeef1af54b57c72192d2eb09fb79",
    "measuredAtAuthoring": true,
    "method": "v8 was produced from v7's exact bytes by targeted operations only. Every path the v8 repairLog (V7-B1, V7-S1, V7-A1, V7-A2, META-v8) does not name is unchanged from v7. The roster is closed; a later finding requires a new successor, not an in-place fold."
  },
  "appliesFindingsOf": {
    "codexV7": {
      "path": "docs/coop/artifacts/component-manifest-schemas.v7.review-independent.codex.json",
      "sha256": "70860e0583909967e17f49f1967d98a2ddaf6e824ff418bcf1ddac8516b22d79",
      "verdict": "REJECT",
      "blockers": [
        "V7-B1"
      ],
      "shouldFix": [],
      "advisories": []
    },
    "claude2V7": {
      "path": "docs/coop/artifacts/component-manifest-schemas.v7.review-independent.claude2.json",
      "sha256": "aeb0e25bcb48ad588aca85990e5b2f85765804c178960bd710dc81c9e891935e",
      "verdict": "REJECT",
      "blockers": [],
      "shouldFix": [
        "V7-S1"
      ],
      "advisories": [
        "V7-A1",
        "V7-A2"
      ]
    },
    "measuredAtAuthoring": true,
    "standing": "Codex REJECT of v7 (V7-B1) and Claude 2 REJECT of v7 (V7-S1) folded. Does not SATISFY DR-103.",
    "notFoldedThisSuccessor": [
      "CLAUDE-V6-A2 (three gate expressions) and CLAUDE-V6-A3 (conditionalRequires array shape) remain OPEN as OD-V8-1 on this surface. They are not folded because changing the corpus-class container shape here would reopen V5-B1-class enumeration risk. Owner: this schema surface (DR-103). Activation: a later successor of this artifact, not a SATISFIED-GRADE corpus review by itself."
    ]
  },
  "repairLog": [
    {
      "finding": "B1",
      "verdictTitle": "D-012 clause 6's DECIDED in-class ownership-transfer element silently dropped; RJ-1 as written contradicts it",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /indexSchema/fields/2/entryShape/ownershipTransfers - clause 6's decided element in schema form: host-authorized in-class transfer records under the SAME two-custody update discipline, slice-1 class bound on both publishers, the third-party half left explicitly deferred (DR-117 successor, DR-010 process)",
        "AMEND /indexSchema/fields/2/entryShape/provenance - post-admission provenance moves ONLY by a recorded in-class transfer",
        "AMEND /rejectionRules/rules/0/trigger, ADD /rejectionRules/rules/0/exception, and AMEND /rejectionRules/rules/0/warrant (the exception's warrant names clause 6 via SRC-D) - lawful post-transfer state (same stableId, changed publisher through the recorded procedure) is admissible; an unrecorded publisher change and pre-transfer-provenance releases still refuse; the retiredIds clause is untouched by the exception",
        "AMEND /testCorpusRequirements/classes/1/requires (TC-DUP) - the RJ-1 duplicate fixture qualified 'no recorded transfer'; the lawful post-transfer ACCEPT case and the pre-transfer-provenance refusal case added"
      ]
    },
    {
      "finding": "B2",
      "verdictTitle": "TC-NAME's clause-7 completeness warrant false (stale-alias absent); clause 7's typed doctor remediation has no binding point",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /testCorpusRequirements/classes/2/requires - stale-alias fixture class added (dual-clock window exercised at both edges: inside the window the alias must still resolve; after retirement past the window it must not, typed, never a silent rebind)",
        "AMEND /testCorpusRequirements/classes/2/warrant - the universal claim now states where every clause-7 member lives, so the enumeration walk is checkable member by member",
        "AMEND /indexSchema/fields/2/entryShape/deprecation - doctorRemediation added as a RESERVED BINDING POINT riding DR-114: clause 7's closing REQUIREMENT declared here, the remediation record's shape owned by DR-114",
        "ADD /identityDependencies/dependencies/9 (ID-DEP-10) - the DR-114 ride named as a dependency row",
        "AMEND /rejectionRules/rules/5/trigger - the reserved-binding-point enumeration extended with deprecation.doctorRemediation"
      ]
    },
    {
      "finding": "B3",
      "verdictTitle": "Five measured prototype command-manifest fields diverge silently with no DIFF entry; requires->permissions narrowing unregistered",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /namedDifferencesFromPrototype/8 (DIFF-9) - output/rawStreamReason REPLACED by outputModes, described as the semantic substitution it is, with the dispatch datum's route named (DR-102 role subprotocol, ID-DEP-6)",
        "ADD /namedDifferencesFromPrototype/9 (DIFF-10) - commonFlags dropped; the unminted host flag vocabulary named as the reason, candidate owners named",
        "ADD /namedDifferencesFromPrototype/10 (DIFF-11) - producesVerdict dropped; the suite-eligibility datum's stated route is the role/capabilities path consumed by DR-118/DR-122",
        "ADD /namedDifferencesFromPrototype/11 (DIFF-12) - producesEvidenceSnapshot dropped; the evidence lineage owns any successor carrier",
        "ADD /namedDifferencesFromPrototype/12 (DIFF-13) - languages (a CONSUMED prototype field, tools-list discovery) dropped; the inventory-filtering loss stated as a loss, DR-120/DR-118 named as candidate owners",
        "ADD /namedDifferencesFromPrototype/13 (DIFF-14) - the requires->permissions narrowing regularized from the inline note into this register",
        "AMEND /manifestSchema/fields/8/commandSpecFields/8/note - outputModes names DIFF-9 at the field",
        "ADD /manifestSchema/fields/14/namedDifference - permissions names DIFF-14 at the field"
      ]
    },
    {
      "finding": "B4",
      "verdictTitle": "ENVELOPE_MISMATCH and TC-SIG's wrong-preimage fixture sit on the wrong side of the lawful signature split",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /rejectionRules/rules/3/subcodes - ENVELOPE_MISMATCH removed from the unconditional list; UNSIGNED and DIGEST_MISMATCH remain",
        "ADD /rejectionRules/rules/3/conditionalSubcode - ENVELOPE_MISMATCH declared and INACTIVE behind ID-DEP-1 (canonical-JSON profile) and ID-DEP-5 (DR-112 envelope semantics), in the D-002 symmetry-clause form the artifact already uses at TC-BYTE-EXACT",
        "AMEND /rejectionRules/rules/3/trigger - the envelope-verification clause removed from the unconditional trigger, so the byte-level scope note is now true as written",
        "AMEND /testCorpusRequirements/classes/4 (TC-SIG) - the wrong-preimage fixture moved from requires to a conditionalRequires member under the same conditionality"
      ]
    },
    {
      "finding": "B5",
      "verdictTitle": "manifest.name <-> mounted-root-command-name binding unstated; root-grammar double-mount admissible under RJ-2's literal universe",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /manifestSchema/fields/8/rootNameBinding - the parent-less entry's name MUST equal manifest.name, making indexSchema entries[].mountedRootCommand a pure custody echo; violation is RJ-2/ROOT_COMMAND_NAME_MISMATCH (prototype precedent measured at PB-6's bytes)",
        "AMEND /rejectionRules/rules/1/subcodes and /rejectionRules/rules/1/trigger - ROOT_COMMAND_NAME_MISMATCH added; the collision universe widened to live entries' mounted root command names, so the invariant is structural at BOTH ends (DIFF-2's own lesson applied to the pair the collapse left behind)",
        "AMEND /indexSchema/fields/2/entryShape/namesSnapshot - the mounted root command name carried explicitly in the snapshot",
        "AMEND /testCorpusRequirements/classes/2/requires - the root-name-mismatch fixture and the verdict's hostile double-mount pair added"
      ]
    },
    {
      "finding": "B6",
      "verdictTitle": "No duplicate-JSON-key refusal class or fixture class despite the corpus's own measured freeze section 7.5 attack lineage",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /sources/bindingSources/4 (SRC-FRZ) - IMPLEMENTATION-FREEZE.md cited at digest 93a4f74f... for section 7.5's measured duplicate-key sweep",
        "AMEND /rejectionRules/rules/5/subcodes - DUPLICATE_JSON_KEY added",
        "AMEND /rejectionRules/rules/5/trigger - a duplicate JSON key anywhere in manifest, index, or lock bytes refuses; the refusal NAMES the duplicated key (section 7.5: naming the key is part of the fix); the reader-vs-instrument divergence mechanism stated against the exact-byte admission digest",
        "AMEND /testCorpusRequirements/classes/7/requires (TC-BYTE-EXACT) - a digest-pinned duplicate-key fixture added, existing ONLY as pinned bytes like the NFC classes"
      ]
    },
    {
      "finding": "A1",
      "verdictTitle": "PB-1 citation imprecision: the user-global sidecar is documented at ToolSource lines 72-76, not the fileoverview",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /measuredPrototypeBaselines/files/0/measuredShape/manifestDeclarationSite - the user-global sidecar cited at its true site (ToolSource doc comment, manifest.ts lines 72-76, re-measured this authoring); the fileoverview citation kept only for what it actually supports"
      ]
    },
    {
      "finding": "A2",
      "verdictTitle": "TC-PATH has no '.'-segment fixture although RJ-3 refuses '.' segments; Windows-class deferral unnamed",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /testCorpusRequirements/classes/3/requires - the '.'-segment fixture added",
        "ADD /testCorpusRequirements/classes/3/scopeNote - the Windows-only-classes deferral (D-002) named so the omission is visible"
      ]
    },
    {
      "finding": "A3",
      "verdictTitle": "capabilities required with no mayBeEmpty ruling; the artifact's own worked example undecidable",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /manifestSchema/fields/9/mayBeEmpty and AMEND /manifestSchema/fields/9/semantics - the empty-array ruling stated; PB-2's cargo-clippy translation is now decidable (admits as capabilities: [])"
      ]
    },
    {
      "finding": "A4",
      "verdictTitle": "Relation between entries[].status ('retired'/'revoked') and the retiredIds ledger unspecified",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /indexSchema/fields/3/semantics - the defining relation stated: one act writes both records; LIVE defined ('active' or 'deprecated-alias-window'); RJ-1's two consultations named; 'revoked' carries the same never-readmit standing here, with any further revocation semantics DR-112's (ID-DEP-5)"
      ]
    },
    {
      "finding": "A5",
      "verdictTitle": "ID-DEP-4's consequence understates its ride: lock RESOLUTION, not just admission, cannot evaluate versionConstraint",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /identityDependencies/dependencies/3/consequence - stated at full width: until DR-111 closes, no lock over a constrained request is producible at all, because resolutionInputs.request and the closure rule both require constraint evaluation"
      ]
    },
    {
      "finding": "A6",
      "verdictTitle": "No stated caps: manifest size, tree entry count, path length, alias count",
      "disposition": "REPAIRED-AS-NAMED-OPEN-DECISION",
      "operations": [
        "ADD /namedOpenDecisions (OD-1) - the absence of caps recorded as a named open decision with candidate owners DR-115 (threshold machinery) and DR-120 (packaging contract); the verdict allows 'an answer or a named owner' and this is the named-owner form",
        "AMEND /manifestSchema/fields/10/treeCommitmentShape/alternatives - the self-flag now points at OD-1 instead of leaving the question rhetorical"
      ]
    },
    {
      "finding": "A7",
      "verdictTitle": "DIFF-2 slightly overstates the prototype drift guard's equality set",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /namedDifferencesFromPrototype/1/prototype - the equality set corrected to what PB-6 holds at bytes (id == identity.name == runtime tool name; declared stableId == runtime id); the display 'name' field removed from the claimed set"
      ]
    },
    {
      "finding": "A8",
      "verdictTitle": "Two host-owned vocabularies the refusals ride are not in identityDependencies",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /identityDependencies/dependencies/10 (ID-DEP-11) - the permission token vocabulary riding DR-105/DR-G09; UNKNOWN_PERMISSION_TOKEN undecidable until the vocabulary is minted",
        "ADD /identityDependencies/dependencies/11 (ID-DEP-12) - the configuration classification vocabulary recorded UNOWNED-AND-NEEDED: no register row mints it, and naming that gap is the honest carrier"
      ]
    },
    {
      "finding": "A9",
      "verdictTitle": "PB-5's enforced-by parenthetical merges ADR-0159's enforced-by list with its enforcement-reason mechanism",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /measuredPrototypeBaselines/files/4/measuredShape - the yaml enforced-by list (script:suites-reserved-names.test.ts, script:reserved-names.test.ts) stated separately from the enforcement-reason's admission gate (reject-host-command-collisions.ts), both re-measured this authoring"
      ]
    },
    {
      "finding": "META",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v2 succession",
        "ADD /basedOn, /appliesFindingsOf, /repairLog - predecessor digest, verdict digest, and this complete diff account",
        "AMEND /sources/corpusHeadAtAuthoring and ADD /sources/reMeasurementAtV2 - every v1 source digest and the SRC-PROTO pin re-measured and reproduced exactly at v2 authoring (prototype HEAD a62509d623173155d0946e9f5d5ca90c839893e0, working tree clean); the corpus head at v2 authoring is d31df3866157d4edaa316a371128c83eef43f2f5",
        "AMEND /reviewGuidance/forTheIndependentReviewer - a repairLog-walk item appended"
      ]
    },
    {
      "finding": "V2-A1",
      "verdictTitle": "The collision universe never states its same-component exclusion, and entryShape.stableId's 'unique among LIVE entries per (stableId)' phrasing sits in tension with the multi-version coexistence pair both records require",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /indexSchema/collisionUniverse - V2-A1's defining sentence: collisions and the live-name/root-name uniqueness quantify over entries of a DIFFERENT stableId; index entries are keyed per (stableId, version) with the component-binding uniqueness constraint holding per stableId",
        "AMEND /indexSchema/fields/2/entryShape/stableId - uniqueness restated as unique among LIVE entries of a different (stableId) at the same version; same (stableId, provenance) at a new version is a release, not a collision",
        "AMEND /manifestSchema/fields name type - unique among live names of a DIFFERENT stableId at admission",
        "AMEND /rejectionRules/rules/1/trigger - RJ-2 collision universe explicitly excludes the admitting component's own same-stableId versions"
      ]
    },
    {
      "finding": "ID-DEP-4-LOCK-PRODUCIBILITY",
      "verdictTitle": "ID-DEP-4 forbids any lock over a constrained request while DR-111 is open, which made TC-ACCEPT's required lock fixture unproducible",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/fields/1/type and /lockSchema/fields/1/semantics - request may be exact-pin pairs (stableId, version) that copy already-admitted index tuples and do not evaluate versionConstraint; versionConstraint remains a constrained request and stays unproducible until DR-111 closes",
        "AMEND /identityDependencies/dependencies/3/consequence - same split stated at full width",
        "AMEND /testCorpusRequirements/classes/0/requires - the required lock fixture is an exact-pin multi-component closure, not a constrained-request lock"
      ]
    },
    {
      "finding": "META-v3",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v3 succession",
        "AMEND /basedOn and /appliesFindingsOf - predecessor is v2 73114dde... and the v2 ACCEPT verdict 42004c95..."
      ]
    },
    {
      "finding": "V3-B1",
      "verdictTitle": "collisionUniverse names RJ-1 and exempts every same-stableId case from it, which unfires RJ-1; two landing sites drop the provenance qualifier",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /indexSchema/collisionUniverse - provenance-qualified coexistence; RJ-1 remains a same-stableId rule and is unaffected",
        "AMEND /rejectionRules/rules/1/trigger - trailing sentence is same-(stableId, provenance) coexistence, not same-stableId outright"
      ]
    },
    {
      "finding": "V3-B2",
      "verdictTitle": "(stableId, version) entry key cannot represent D-012 clause 4 dual-scope admission and contradicts shadowedBy",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /indexSchema/collisionUniverse - entries keyed per (stableId, version, scope)",
        "AMEND /indexSchema/fields/2/entryShape/stableId - same (stableId, version, scope) key",
        "AMEND /indexSchema/fields/2/entryShape/shadowedBy - reference is (stableId, version, scope) so it addresses a unique entry"
      ]
    },
    {
      "finding": "V3-B3",
      "verdictTitle": "exact-pin request does not make a multi-component closure producible while dependency edges are versionConstraint-typed",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/fields/1/semantics - exact-pin is producible only for a request whose admitted manifests declare no versionConstraint edges (empty dependencies). Multi-component / constrained-edge locks remain unproducible until DR-111",
        "AMEND /testCorpusRequirements/classes/0/requires/5 - required lock is a single-component exact-pin over a dependency-free ACCEPT fixture; multi-component lock named deferred to DR-111"
      ]
    },
    {
      "finding": "V3-B4",
      "verdictTitle": "v3 reverts the closure-rule half of v2 A5 without disclosing the removal",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /identityDependencies/dependencies/3/consequence - restore A5's closure-rule width: ANY lock whose production requires versionConstraint evaluation remains unproducible; exact-pin of a dependency-free single component is the only producible lock form until DR-111"
      ]
    },
    {
      "finding": "V3-S1",
      "verdictTitle": "repairLog completeness (Claude SHOULD-FIX; overlaps Codex B2)",
      "disposition": "REPAIRED",
      "operations": [
        "This v4 repairLog names every v3-to-v4 leaf at a resolving JSON Pointer. v3's incomplete v2-to-v3 account is historical and is not rewritten."
      ]
    },
    {
      "finding": "V3-S2",
      "verdictTitle": "ID-DEP-4-LOCK-PRODUCIBILITY was author-initiated, not a verdict finding (not a verdict finding)",
      "disposition": "RECORDED",
      "operations": [
        "No v3 bytes rewritten for this accounting defect. v4 lock changes are labelled as repairs of V3-B3/V3-B4, which are verdict findings."
      ]
    },
    {
      "finding": "Codex-S1",
      "verdictTitle": "DIFFERENT-stableId qualifier is over-broad at the name-field site because reserved commands have no stableId",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /manifestSchema/fields/3/type - unique among live names of a DIFFERENT stableId; reserved-list uniqueness is unconditional",
        "AMEND /manifestSchema/fields/3/semantics - reserved-root collisions stay unconditional; the DIFFERENT-stableId carve-out is only for cross-entry live name/alias/root comparisons"
      ]
    },
    {
      "finding": "META-v4",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v4 succession",
        "AMEND /basedOn and /appliesFindingsOf - predecessor is v3 159c6089...; dual REJECT verdicts 0d2a5217... and aa05c571...",
        "AMEND /reviewGuidance/forTheIndependentReviewer - append the v4 walk item"
      ]
    },
    {
      "finding": "V4-B1",
      "verdictTitle": "The purported single-component-only lock rule still permits a multi-component exact-pin lock",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/fields/1/semantics - no lock is producible until DR-111, including exact-pin requests of any cardinality",
        "AMEND /testCorpusRequirements/classes/0/requires/5 - lock member of TC-ACCEPT is DEFERRED to DR-111; present evidence is index custody"
      ]
    },
    {
      "finding": "V4-B2",
      "verdictTitle": "The single-component exact pin still ignores the DR-111-owned compatibility meaning",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /identityDependencies/dependencies/3/consequence - restore full width: hostCoreVersion/platform/compatibility meaning and versionConstraint evaluation are DR-111-owned; no lock of any form is a completed compatible resolution until DR-111 closes",
        "AMEND /lockSchema/fields/1/semantics - a non-validating pin is not a lock"
      ]
    },
    {
      "finding": "V4-S1",
      "verdictTitle": "basedOn.method names a repairLog entry that does not exist",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /basedOn/method - v5 names only real v5 repairLog finding ids"
      ]
    },
    {
      "finding": "V4-S2",
      "verdictTitle": "Codex S2 successor custody only half-addressed",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /sources/reMeasurementAtV5 - v2/v3/v4 measurement fields remain historical; this authoring records its own predecessor digest only. Live SRC-08/SRC-D/SRC-FRZ whole-document pins are not re-claimed as this-authoring measurements."
      ]
    },
    {
      "finding": "META-v5",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v5 succession",
        "AMEND /basedOn and /appliesFindingsOf - predecessor is v4 63ea2c47...; Codex v4 REJECT d54b0e65...; Claude 2 v4 verdict NOT-RECEIVED",
        "AMEND /reviewGuidance/forTheIndependentReviewer - append the v5 walk item"
      ]
    },
    {
      "finding": "V5-B1",
      "verdictTitle": "The no-lock-until-DR-111 rule conflicts with still-required lock fixtures",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /testCorpusRequirements/classes/4/requires - remove lock-digest mismatch from unconditional requires",
        "AMEND /testCorpusRequirements/classes/4/conditionalRequires - lock-digest mismatch enters only after DR-111",
        "AMEND /testCorpusRequirements/classes/7/requires/0 - lock digest dropped from the pre-DR-111 recompute fixture; admission+index only",
        "AMEND /testCorpusRequirements/classes/7/scopeNote - lock-bearing recompute is conditional on DR-111"
      ]
    },
    {
      "finding": "V5-S1",
      "verdictTitle": "V4-S2 custody accounting remains contradictory after reMeasurementAtV5",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /sources/bindingSources/*/measuredAtAuthoring - renamed/scoped to measuredAtV2Authoring",
        "AMEND /sources/citationDiscipline - whole-document digests are v2-authoring measurements; named properties are what to re-verify if pins moved",
        "ADD /sources/reMeasurementAtV6 - predecessor is v5 c58f062d...; SRC-08/SRC-D/SRC-FRZ live pins recorded as live, not as this-authoring whole-document claims"
      ]
    },
    {
      "finding": "V5-A1",
      "verdictTitle": "basedOn.method should not imply an in-place future Claude fold",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /basedOn/method - closed roster of real v6 repairLog finding ids only; no successor-hedge; a later finding requires a new successor"
      ]
    },
    {
      "finding": "V5-B2",
      "verdictTitle": "Codex V4-S2 marked REPAIRED while measuredAtAuthoring stayed true on moved SRC-08/SRC-D/SRC-FRZ",
      "disposition": "REPAIRED",
      "operations": [
        "The v5 repairLog entry V4-S2 is left byte-identical (historical; no landed repair is rewritten).",
        "The field work Claude named is completed by V5-S1: measuredAtAuthoring renamed to measuredAtV2Authoring on every binding source; citationDiscipline scoped to v2 authoring; live SRC-08/SRC-D/SRC-FRZ pins recorded only at /sources/reMeasurementAtV6."
      ]
    },
    {
      "finding": "CLAUDE-V5-S1",
      "verdictTitle": "basedOn.method roster must be enumerable; no trailing unbounded clause",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /basedOn/method - drop the draft hedge 'plus any Claude v5 findings folded in a later successor'; the roster is the closed list of real v6 repairLog ids"
      ]
    },
    {
      "finding": "V5-S2",
      "verdictTitle": "lockSchema.resolved cannot address a (stableId, version, scope)-keyed index entry",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/fields/2/type - add scope ('project'|'global') to the resolved tuple",
        "AMEND /lockSchema/fields/2/semantics - the lock's custody join to the index is by (stableId, version, scope)",
        "AMEND /lockSchema/fields/1/semantics - ordering rule, when locks become producible, is (stableId, version, scope)",
        "AMEND /lockSchema/purpose - selected tuples include scope"
      ]
    },
    {
      "finding": "CLAUDE-V5-A1",
      "verdictTitle": "lockSchema.purpose still describes a producible record with no deferral marker",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/purpose - add the no-lock-until-DR-111 clause at the purpose site, not only in resolutionInputs.semantics"
      ]
    },
    {
      "finding": "V5-A2",
      "verdictTitle": "resolutionInputs.type still offers exact-pin and constrained request forms as live alternatives",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/fields/1/type - both request forms are schema shapes for a future lock; neither is producible until DR-111"
      ]
    },
    {
      "finding": "V5-A3",
      "verdictTitle": "dual-scope exclusion is not provenance-qualified while the multi-version exclusion is",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /indexSchema/collisionUniverse - dual-scope exclusion is same-(stableId, provenance, version); a different-provenance claimant at the same stableId is RJ-1 regardless of scope",
        "AMEND /rejectionRules/rules/1/trigger - matching dual-scope exclusion text"
      ]
    },
    {
      "finding": "V5-A4",
      "verdictTitle": "collisionUniverse shadowedBy phrasing is stale ('plus version')",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /indexSchema/collisionUniverse - shadowedBy is the (stableId, version, scope) triple; drop 'plus version'"
      ]
    },
    {
      "finding": "V5-A5",
      "verdictTitle": "RJ-2 DIFFERENT-(stableId, provenance) qualifier attached only to the last enumerated item",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /rejectionRules/rules/1/trigger - the DIFFERENT-(stableId, provenance) qualifier covers live names/aliases AND mounted root command names"
      ]
    },
    {
      "finding": "META-v6",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v6 succession",
        "AMEND /basedOn and /appliesFindingsOf - predecessor is v5 c58f062d...; Codex v5 REJECT 112b522d...; Claude 2 v5 REJECT c5b300e8...",
        "AMEND /reviewGuidance/forTheIndependentReviewer - append the v6 walk item"
      ]
    },
    {
      "finding": "V6-S1",
      "verdictTitle": "The non-qualification clause falsely says no fixture exists yet",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /whatThisDoesNotDo/1 - drop the existence claim; this artifact authors no fixtures; a separate fixture-corpus candidate is not accepted, qualified, or applied here"
      ]
    },
    {
      "finding": "CLAUDE-V6-A1",
      "verdictTitle": "RJ-4 lock-digest trigger arm is dormant until DR-111 and carries no marker",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /rejectionRules/rules/3/trigger - mark the lock-digest arm as activating only when DR-111 closes; the index-recorded arm remains live"
      ]
    },
    {
      "finding": "CLAUDE-V6-A4",
      "verdictTitle": "SRC-PROTO 'this authoring' wording was not scoped with the measuredAtV2Authoring rename",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /sources/bindingSources[SRC-PROTO]/verifiedAtAuthoring and custodyCaveats - v2-authoring measurement; successor re-verification is a review obligation, not a v7 whole-document claim"
      ]
    },
    {
      "finding": "CLAUDE-V6-A5",
      "verdictTitle": "corpusHeadAtAuthoring keeps a bare AtAuthoring name",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /sources/corpusHeadAtAuthoring - renamed corpusHeadAtV2Authoring"
      ]
    },
    {
      "finding": "CODEX-V6-A1",
      "verdictTitle": "Define a total tuple comparator before lock production is enabled",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /lockSchema/fields/1/semantics - sorted-by names the key only; total comparator (canonical encodings + exact-byte tie-breaker) is RESERVED until lock production is enabled"
      ]
    },
    {
      "finding": "META-v7",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v7 succession",
        "AMEND /basedOn and /appliesFindingsOf - predecessor is v6 51b0a0b7...; Codex v6 REJECT a75d0af6...; Claude 2 v6 ACCEPT-WITH-ADVISORIES 1e9bf51b...",
        "AMEND /reviewGuidance/forTheIndependentReviewer - append the v7 walk item"
      ]
    },
    {
      "finding": "V7-B1",
      "verdictTitle": "Three v6-to-v7 source paths changed outside the v7 repairLog",
      "disposition": "REPAIRED",
      "operations": [
        "ADD /sources/reMeasurementAtV8 - v8 authoring custody; predecessor is v7 a8870018...; live pins unchanged from reMeasurementAtV7",
        "The v7 key /sources/reMeasurementAtV7 is left byte-identical (historical v7 authoring note)."
      ]
    },
    {
      "finding": "V7-S1",
      "verdictTitle": "Three /sources paths changed without any repairLog operation naming them",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /sources/reMeasurementAtV2 - append a v8 name-resolution clause only; the measured v2 head value is unchanged",
        "AMEND /sources/reMeasurementAtV5 - append a v8 name-resolution clause only; the v5-era scoping is unchanged",
        "These two historical notes are updated for field-name resolution only (corpusHeadAtV2Authoring). The v7 ADD of reMeasurementAtV7 remains historical and is now accounted by V7-B1's ADD of reMeasurementAtV8."
      ]
    },
    {
      "finding": "V7-A1",
      "verdictTitle": "whatThisDoesNotDo[1] hedges on fixture existence where the measured fact was available",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /whatThisDoesNotDo/1 - replace 'may exist' with a measured non-binding citation of the fixture-corpus candidates; still not accepted, qualified, or applied"
      ]
    },
    {
      "finding": "V7-A2",
      "verdictTitle": "The carry reason routes a schema-shape decision to a corpus milestone without naming an owning row",
      "disposition": "REPAIRED",
      "operations": [
        "AMEND /appliesFindingsOf/notFoldedThisSuccessor - CLAUDE-V6-A2 and CLAUDE-V6-A3 remain open as named open decision OD-V8-1 on this surface; not routed to an unnamed corpus milestone"
      ]
    },
    {
      "finding": "META-v8",
      "verdictTitle": "Succession bookkeeping (not a verdict finding)",
      "disposition": "AUTHORING-METADATA",
      "operations": [
        "AMEND /artifact and /version - v8 succession",
        "AMEND /basedOn and /appliesFindingsOf - predecessor is v7 a8870018...; Codex v7 REJECT 70860e05...; Claude 2 v7 REJECT aeb0e25b...",
        "AMEND /reviewGuidance/forTheIndependentReviewer - append the v8 walk item",
        "ADD /openDecisions[OD-V8-1] - named owner for carried CLAUDE-V6-A2/A3"
      ]
    }
  ],
  "sources": {
    "bindingSources": [
      {
        "id": "SRC-02",
        "path": "docs/v2/architecture/02-distribution-and-components.md",
        "sha256": "1811c682cf293e1e0b255be82c62f7ed3c439f0873eb7922bfb0ad965b43f7db",
        "measuredAtV2Authoring": true,
        "note": "Byte-identical to the DR-205 frozen digest and to the digest DR-117's row re-verified (file 08 DR-117 row). The distribution/packaging/manifest sections bound this artifact; the operative manifest sentence is quoted at manifestSchema.coverageObligation."
      },
      {
        "id": "SRC-03",
        "path": "docs/v2/architecture/03-configuration-and-security.md",
        "sha256": "52158469e805ca2c2e9a053282ac8428b9adf75184190c1cb6b4ebe2fce8425a",
        "measuredAtV2Authoring": true,
        "note": "Supplies the lock's owning sentence ('The project component lock records selected signed inputs and deterministic resolution, not secret values, user trust decisions, or self-granted authority. Its concrete schema is DR-103.', lines 37-39), the independent-release trust enumeration (delegated publisher/component namespaces, SBOM/attestation digests inside the signed closure, last-known revocation/expiry), and the permission law ('A manifest, repository, or publisher cannot grant authority to itself'; deny wins; absence denies)."
      },
      {
        "id": "SRC-08",
        "path": "docs/v2/architecture/08-decision-and-readiness-register.md",
        "sha256": "f0d72536eb1c55e62e43f02ca859a32aa5a8fd096719199d087d8aa665f7b00d",
        "measuredAtV2Authoring": true,
        "note": "DR-103 row (decision: 'Canonical component manifest/index/lock schemas, delegated roles, IDs, provenance/SBOM/attestation binding'; acceptance evidence: 'Reviewed canonical schemas, duplicate/path rejection, signatures, exact-byte test corpus'; status OPEN). Also DR-006 park coverage naming capabilityManifestId and typescriptStdlibMerkleRoot as identity gaps with no producing rule; DR-104/115/118/119/123 decided rows; DR-111/DR-112 open rows this artifact declares dependencies on."
      },
      {
        "id": "SRC-D",
        "path": "docs/coop/COORDINATOR-DECISIONS.md",
        "sha256": "a3d0d26675bfa0512daebd2f5fcc818346aec7cc1ac2eb07878647ce5e2ddb8f",
        "measuredAtV2Authoring": true,
        "note": "D-002 (slice, identity-dependencies section and its symmetry clause), D-008 (DR-119 rule accepted universally: manifest declarations of licenses/SBOM/attestation/platform/capability/performance; typed absence/failure for approved exceptions), D-012 (DR-104 identity/namespace policy, clauses 1-7 plus the four routed turn-2 observations: per-install uniqueness universe, (ID, provenance) discriminator, host-core release train for the alias window, per-item landing discipline)."
      },
      {
        "id": "SRC-FRZ",
        "path": "docs/coop/IMPLEMENTATION-FREEZE.md",
        "sha256": "93a4f74f744a49e09a8ccc38b8355bdeb85dccc5c99c193eda97659ad8e8e1ab",
        "measuredAtV2Authoring": true,
        "note": "Section 7.5 ('Measured: duplicate JSON keys across the checker corpus') supplies the duplicate-key attack lineage RJ-6/DUPLICATE_JSON_KEY and TC-BYTE-EXACT's duplicate-key fixture class cite: an exhaustive sweep (493 checker executions, nothing sampled) measured that json parsing without a pairs hook keeps the LAST duplicate key, 'so a document can say one thing to a reader and another to every instrument while the parsed object stays byte-identical to the honest one' - and that naming the duplicated key is part of the fix."
      },
      {
        "id": "SRC-PROTO",
        "repository": "/Users/sb/code/opensip-ai/opensip-cli",
        "commit": "a62509d623173155d0946e9f5d5ca90c839893e0",
        "custodyCaveats": "DR-203's recorded caveats carry: SHA-1 commit identity; out-of-corpus custody; the pin is re-verified at each measurement time. The working-tree-clean measurement is a v2-authoring claim, not a v7 whole-document claim. Independent review re-verifies HEAD at review time.",
        "verifiedAtV2Authoring": "git rev-parse HEAD returned exactly this commit; working tree clean at measurement time"
      }
    ],
    "reMeasurementAtV2": "All bindingSources digests above and the SRC-PROTO pin were re-measured at v2 authoring and reproduced exactly (SRC-02/03/08/D/FRZ byte-identical; prototype HEAD a62509d623173155d0946e9f5d5ca90c839893e0, working tree clean). corpusHeadAtV2Authoring is the v2 authoring head; v1's was a2ad75d018c7b0c1d46b1d0eb0417dcb337162a7. v8 name-resolution only: the live field name is corpusHeadAtV2Authoring; the measured head value is unchanged from v2 authoring.",
    "reMeasurementAtV5": "HISTORICAL: corpusHeadAtV2Authoring and reMeasurementAtV2 remain v2-era measurements and are not this-authoring claims. v5 predecessor is v4 63ea2c47ad2f5580954a5dbf673105696c3323d19c7b9c91100fa6398f31abc8. Live whole-document pins of SRC-08, SRC-D, and SRC-FRZ may have moved since v2; they are not re-claimed here. SRC-02 1811c682\u2026 and SRC-03 52158469\u2026 were re-measured at v5 authoring and still match. v8 name-resolution only: the live field name is corpusHeadAtV2Authoring; this note remains a v5-era historical scoping record.",
    "reMeasurementAtV6": "Predecessor is v5 c58f062dd9974cd7df7edb18f47eab238eea6e4bceed28c0c66a7a4ce0fb3835. bindingSources whole-document digests remain v2-authoring measurements (measuredAtV2Authoring). Live pins at this authoring, for named-property re-verification only, not as replacement whole-document claims: SRC-08 1cdcf9d4071a6c5eaa7cd7d28fac288eaced796c9ee906b6f1db0f7a4b1f95b3; SRC-D 12d192f758f48f692e69cb410a1d7a9bf776c765257c8e910272d3de457ec3e3; SRC-FRZ e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd. SRC-02 and SRC-03 still match their recorded v2 hashes.",
    "citationDiscipline": "Whole-document digests in bindingSources are v2-authoring measurements (measuredAtV2Authoring), not v6-authoring claims. Per file 08's pin-move record, whole-document pins strand on any edit of the cited file. Independent review re-verifies the named property if a digest has moved. Live pins at v6 are recorded only at reMeasurementAtV6.",
    "corpusHeadAtV2Authoring": "d31df3866157d4edaa316a371128c83eef43f2f5",
    "reMeasurementAtV7": "Predecessor is v6 51b0a0b7c884dc106b89768661d9550597b941064454b2155f138f6dd164f401. bindingSources whole-document digests remain v2-authoring measurements. Live pins unchanged from reMeasurementAtV6: SRC-08 1cdcf9d4071a6c5eaa7cd7d28fac288eaced796c9ee906b6f1db0f7a4b1f95b3; SRC-D 12d192f758f48f692e69cb410a1d7a9bf776c765257c8e910272d3de457ec3e3; SRC-FRZ e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd. SRC-02 and SRC-03 still match their recorded v2 hashes.",
    "reMeasurementAtV8": "Predecessor is v7 a887001807402dc37d698288b3852a84b24faeef1af54b57c72192d2eb09fb79. bindingSources whole-document digests remain v2-authoring measurements. Live pins unchanged from reMeasurementAtV7: SRC-08 1cdcf9d4071a6c5eaa7cd7d28fac288eaced796c9ee906b6f1db0f7a4b1f95b3; SRC-D 12d192f758f48f692e69cb410a1d7a9bf776c765257c8e910272d3de457ec3e3; SRC-FRZ e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd. SRC-02 and SRC-03 still match their recorded v2 hashes."
  },
  "measuredPrototypeBaselines": {
    "note": "These are the empirical baselines every 'the prototype does X' claim below rests on. All paths are relative to SRC-PROTO at commit a62509d6.",
    "files": [
      {
        "id": "PB-1",
        "path": "packages/core/src/tools/manifest.ts",
        "sha256": "e0f47e6f39766f7c8394029a8aa87b3adbdec5b290f18717334b3a35ebde7db2",
        "lines": 302,
        "measuredShape": {
          "manifestDeclarationSite": "package.json#opensipTools for bundled + installed tools, and JSON sidecar (opensip-tool.manifest.json) for project-local tools (fileoverview comment, lines 1-24); the user-global sidecar is documented at the ToolSource doc comment, lines 72-76 (~/.opensip-cli/tools/<name>/), with project-local's deny-by-default posture at lines 77-82 - the sidecar claim spans BOTH citation sites, not the fileoverview alone",
          "identityFields": "kind:'tool' discriminator; identity (ToolIdentity block, id must equal identity.name); id (human/programmatic key, 'declared' identifier, backward-compat key per ADR-0048); stableId OPTIONAL ('Stable machine identity (real UUID) ... Declared additively by tools that opt into durable identity', lines 185-190); name (display); version (display semver, 'NOT the contract epoch')",
          "contractEpoch": "apiVersion: single coarse integer epoch; admission is an integer range check [MIN_SUPPORTED_PLUGIN_API_VERSION, PLUGIN_API_VERSION] = [1,1] at the pin; 'no semver range maths'; a missing apiVersion is representable (RawToolPluginManifest) so the gate can diagnose before rejecting (lines 49-61, 264-280)",
          "commands": "ToolCommandManifest: name, description, aliases?, visibility? ('public'|'internal'), parent? (verb nesting), commonFlags?, options? (OptionSpec MINUS the parse closure - the one non-serializable field, a documented deliberate narrowing), args?, scope?, output?, rawStreamReason?, producesVerdict?, producesEvidenceSnapshot? (lines 98-146). Host mounts external commands from the manifest ALONE - no runtime import",
          "capabilities": "optional ToolCapabilityDeclaration[]; host stamps ownerToolId = stableId ?? id (lines 202-211)",
          "otherFields": "config? (coarse draft-07-subset JSON-Schema descriptor validated host-side before forking), pluginLayout?, requires? (ToolResourceRequirement[], declaration-only, structurally validated at admission), languages?; reserved unknown-shaped fields: dashboard, sessions, pluginDomains, compatibility, distribution, extensionMetadata (lines 212-261)",
          "provenance": "ToolProvenance: source ('bundled'|'installed'|'user-global'|'project-local' trust classes, project-local deny-by-default), id, stableId?, version, packageName?, resolvedPath?, manifestHash ('Stable hash of the manifest bytes the host read - tamper/identity check') (lines 63-84, 287-302)"
        }
      },
      {
        "id": "PB-2",
        "path": "packages/tool-cargo-clippy/package.json",
        "sha256": "d52fd32d8b50221b7f41893b2529ebeebedd520428eed20bfd96bdbc6dfc18fd",
        "measuredShape": "Worked opensipTools instance: kind 'tool'; id 'cargo-clippy'; identity {name:'cargo-clippy', aliases:['clippy']}; stableId '66cb4afb-783c-42e7-b893-bb922ff8a72c' (a real UUID, present in a first-party tool as ADR-0048 directs); apiVersion 1; three commands, two of which ('doctor', 'version') declare parent 'cargo-clippy' - the declarative sub-grammar D-012 clause 2 adopts; requires [{resource:'subprocess', reason}, {resource:'filesystem', reason}]; config {namespace, schema}; license at package.json level only"
      },
      {
        "id": "PB-3",
        "path": "packages/cli/src/bootstrap/bundled-tools.manifest.json",
        "sha256": "f607ec0607f1a8d4d3d0c7c91d2338551831ad125b7e96227cee4f875766d1ad",
        "lines": 30,
        "measuredShape": "First-party bundled-set manifest: bundledPackages (5 package names), scaffoldingToolIds, bundledCapabilityPacks (two named pack groups). Data-driven source the CLI composition root loads; the closest prototype analogue to an INDEX - an unsigned name list with no digests, no custody fields, no admission records"
      },
      {
        "id": "PB-4",
        "path": "docs/decisions/ADR-0048-tool-stable-uuid-identity.md",
        "sha256": "f87284a73b99695b8caff02d7b42a0b24f40e8e7b393f277f975945327525826",
        "measuredShape": "Stable UUID identity decision: tools get a stable machine UUID with the human key separate; motivation is community collision-proofing and rename-without-history-loss; enforcement 'no placeholder UUIDs' is mechanizable"
      },
      {
        "id": "PB-5",
        "path": "docs/decisions/ADR-0159-reserved-host-command-and-suite-names.md",
        "sha256": "7e672be4c89169f22748821750b169f50affa83eab4f584d6d64d189ab823a63",
        "measuredShape": "Reserved host root-command names: a Tool may not claim any host-owned root command name or alias; colliding tools are rejected at admission, before mounting; a parity test asserts the reserved list stays in parity with the mounted host surface. Citation split per the ADR's own bytes: the yaml enforced-by list names script:suites-reserved-names.test.ts and script:reserved-names.test.ts; reject-host-command-collisions.ts is named in the enforcement-reason prose as the admission gate (PB-7), not in the enforced-by list"
      },
      {
        "id": "PB-6",
        "path": "packages/core/src/tools/manifest-assert.ts",
        "sha256": "32e9ac8bacae4547b4f200cc671a14bd6b2e285b9d7fa395513500fe237cdbdd",
        "measuredShape": "Load-time drift guard: manifest.id must equal runtime tool name; declared stableId must equal runtime tool id; manifest.id must equal identity.name - typed ValidationError refusals, not warnings"
      },
      {
        "id": "PB-7",
        "path": "packages/cli/src/bootstrap/reject-host-command-collisions.ts",
        "sha256": "2e454c8f3a6266ea1dc4bc0f3a0d077dfb81f71bb6e92b9f01e725b93e13c827",
        "measuredShape": "The admission gate ADR-0159 names as its CLI-side enforcement point (existence and digest measured; cited as the pinned mechanism behind D-012 clause 3)"
      }
    ]
  },
  "manifestSchema": {
    "name": "opensip-component-manifest",
    "schemaVersionField": "manifestSchemaVersion",
    "coverageObligation": "SRC-02 (at 1811c682), 'One component architecture' section, verbatim: 'A manifest eventually binds commands, capabilities, dependencies/prerequisites, compatibility, exact immutable tree and entrypoint, publisher/provenance, SBOM/attestations, permissions, configuration classification, state/migration, and update data. Concrete schemas remain DR-103.' Every member of that enumeration appears below either as a designed field group or as an explicitly reserved binding point; none is silently dropped.",
    "encodingRule": "A manifest is a single JSON document. Its ADMISSION identity is the sha256 of its exact bytes as stored (a plain measurement, no canonicalization involved). Its SIGNING preimage is the canonicalized form under the canonical-JSON dependency declared at identityDependencies ID-DEP-1; this artifact declares THAT manifests are canonicalized for signing and does NOT define the canonicalization (see ID-DEP-1).",
    "fields": [
      {
        "name": "manifestSchemaVersion",
        "type": "integer >= 1",
        "required": true,
        "semantics": "Version of THIS schema. Compatibility windows for the manifest schema surface are a DR-111 per-surface matrix; this field is the surface's version datum, not a semver range.",
        "sourceWarrant": "SRC-02 'no required same-version coupling ... Compatibility is expressed by separate per-surface matrices under DR-111'; prototype precedent PB-1 apiVersion (single integer, no range maths)",
        "alternatives": "A semver string was rejected: the prototype's own comment rejects 'semver range maths' at the admission gate, and DR-111 owns window semantics - a bare integer keeps the admission comparison trivial and pushes window policy to its owner."
      },
      {
        "name": "kind",
        "type": "closed string enum, initial vocabulary: 'component'",
        "required": true,
        "semantics": "Discriminator for the document class. The prototype's value is 'tool' (PB-1 line 167); V2 uses 'component' because file 02's one-component-architecture section makes 'component' the covering term for all optional roles.",
        "sourceWarrant": "PB-1 (kind:'tool'); SRC-02 'One component architecture'",
        "namedDifference": "DIFF-7"
      },
      {
        "name": "stableId",
        "type": "UUID string (RFC 4122 textual form, lowercase)",
        "required": true,
        "semantics": "The component's identity: an author-declared immutable UUID. Never derived from mutable state. The host enforces uniqueness at admission against its persisted admission registry (the index custody below) - two custody records with a uniqueness constraint between them, per-install uniqueness universe, (ID, provenance) pair as the same-vs-different-component discriminator. A retired stableId is retained in the registry and never readmitted for a different component. Display names and aliases are mutable metadata resolving THROUGH this ID, never the reverse.",
        "sourceWarrant": "D-012 clause 1 (SRC-D) verbatim policy; prototype baseline PB-1 stableId + PB-2 worked UUID + PB-4 ADR-0048",
        "namedDifference": "DIFF-1"
      },
      {
        "name": "name",
        "type": "string, non-empty, host-defined safe-charset (lowercase alphanumeric plus '-'), unique among reserved root commands unconditionally, and unique among live names/aliases/mounted roots of a DIFFERENT (stableId, provenance) at admission",
        "required": true,
        "semantics": "Human/programmatic display and command key. Mutable across releases (rename governance is D-012 clause 5); resolves through stableId. Reserved-list collisions are unconditional (reserved commands have no stableId). Cross-entry live name/alias/root collisions quantify over a DIFFERENT (stableId, provenance). Same-(stableId, provenance) multi-version or dual-scope coexistence is not an RJ-2 name collision.",
        "sourceWarrant": "D-012 clauses 1, 2, 5; PB-1 id/name split; PB-6 drift guard",
        "alternatives": "The prototype carries BOTH a programmatic 'id' (human key) and a display 'name'. V2 collapses them to one 'name' plus 'displayName' optional, because the prototype's id-must-equal-identity.name drift guard (PB-6) shows the two-key shape only ever holds by assertion; see DIFF-2."
      },
      {
        "name": "displayName",
        "type": "string",
        "required": false,
        "semantics": "Optional human-facing label when it differs from 'name'. Pure metadata; participates in no resolution.",
        "sourceWarrant": "PB-1 name field ('Human-facing display name')"
      },
      {
        "name": "aliases",
        "type": "array of strings, same charset as name, no duplicates within the array",
        "required": false,
        "semantics": "Alternate resolution keys through stableId. An alias may never shadow any live name or reserved name; alias cycles are refused at admission; a rename keeps the old name as a deprecated alias for at least one host-core minor release cycle AND no fewer than 90 days from the deprecating release, whichever is longer (deprecation metadata lives in the index entry, not the manifest, because the manifest is immutable per release).",
        "sourceWarrant": "D-012 clause 5 verbatim (including the turn-2 observation that 'one minor release cycle' means the host core's release train); PB-2 aliases:['clippy']"
      },
      {
        "name": "version",
        "type": "semver string",
        "required": true,
        "semantics": "Display/release version of the component itself. Distinct from stableId (ID/version distinctness is a D-012 negative-test class) and distinct from every contract-surface version (DR-111). Multi-version coexistence is expressed in the index/lock, not by mutating the manifest.",
        "sourceWarrant": "PB-1 version ('Display semver ... NOT the contract epoch'); D-012 clause 7 (ID/version distinctness, multi-version coexistence)"
      },
      {
        "name": "role",
        "type": "closed string enum owned by the host, initial slice-1 vocabulary: 'analyzer'; vocabulary extension is a host decision, not a manifest right",
        "required": true,
        "semantics": "The delegated role class this component fills (DR-103 row: 'delegated roles'). A role names which host contracts apply (e.g. an analyzer negotiates an opaque named role subprotocol per SRC-02's control-plane demarcation). The role vocabulary is host-owned; a manifest cannot mint a role.",
        "sourceWarrant": "SRC-08 DR-103 decision column; SRC-02 control plane ('negotiate an opaque named role subprotocol and version')",
        "alternatives": "A free-form role string was rejected: file 02's control plane only carries NAMED role subprotocols, and an open vocabulary would let a manifest invent a contract surface no host owns - the same class of self-granted authority file 03 forbids."
      },
      {
        "name": "commands",
        "type": "array of CommandSpec objects (below)",
        "required": true,
        "semantics": "The declarative command grammar this component asks the host to mount. The ROOT namespace is host-owned and flat: exactly one entry may omit 'parent' and that entry is the component's mounted root command; every other entry must name a parent within this manifest. The component declares; the host owns all parsing and dispatch; discovery never hands a component raw root parser authority. Help/completion/inventory are served from this declaration without executing component code.",
        "sourceWarrant": "D-012 clause 2 verbatim; SRC-02 'Help, completion, and inventory read authenticated metadata only'; PB-1 ToolCommandManifest; PB-2 parent nesting worked example",
        "rootNameBinding": "The parent-less entry's name MUST equal manifest.name: the component's mounted root command IS its component name, so indexSchema entries[].mountedRootCommand is a pure custody echo of the entry's name, never an independent grammar claim. A parent-less command whose name differs from manifest.name is RJ-2/ROOT_COMMAND_NAME_MISMATCH. This closes the root-grammar double-mount left admissible in v1 (component A, name 'aaa', mounting root 'zzz'; component B, name 'zzz', admitting because 'zzz' was in no namesSnapshot - the exact defect ADR-0159/PB-7 exists to refuse): under the binding every mounted root name IS a live component name inside RJ-2's universe, and RJ-2's universe is ALSO widened to include mounted root command names explicitly, so the invariant is structural at both ends rather than held by assertion - DIFF-2's own lesson, applied to the pair the collapse left behind. Prototype precedent, measured at PB-6's bytes this authoring: the drift guard's assertPrimaryAliases (manifest-assert.ts lines 83-92) resolves the primary spec as the parent-less command whose name equals the runtime tool name.",
        "commandSpecFields": [
          {
            "name": "name",
            "type": "string, command charset",
            "required": true
          },
          {
            "name": "description",
            "type": "string",
            "required": true
          },
          {
            "name": "aliases",
            "type": "array of strings",
            "required": false,
            "note": "Sub-command aliases; same shadowing/cycle refusals as component aliases, scoped to the mounted subtree."
          },
          {
            "name": "parent",
            "type": "string naming another command in THIS manifest",
            "required": "absent exactly once (the mounted root)",
            "note": "Parent linkage must form a tree rooted at the mounted root command: unknown parent, self-parent, cycle, or duplicate (parent,name) pair is a typed refusal (RJ-2). Namespace depth beyond the mounted-root model is EXPLICITLY DEFERRED to the DR-117 successor process per D-012 clause 6 - this schema therefore permits arbitrary depth BELOW the single mounted root and nothing above it."
          },
          {
            "name": "visibility",
            "type": "'public' | 'internal'",
            "required": false,
            "default": "public"
          },
          {
            "name": "options",
            "type": "array of pure-data option descriptors (flag, description, valueKind, repeatable, default)",
            "required": false,
            "note": "Pure data by construction. The prototype had to EXCLUDE the one non-serializable field (the parse closure, PB-1 ManifestOptionDescriptor); V2 manifests are JSON documents, so the exclusion is structural, not a carve-out - see DIFF-5."
          },
          {
            "name": "args",
            "type": "array of positional-arg descriptors (name, description, required, variadic)",
            "required": false
          },
          {
            "name": "scope",
            "type": "'project' | 'none'",
            "required": false,
            "default": "project",
            "note": "Whether the host enters a project scope before dispatch (PB-1 CommandScopeRequirement mirror)."
          },
          {
            "name": "outputModes",
            "type": "array over 'human' | 'json' | 'sarif'",
            "required": false,
            "default": "['human','json']",
            "note": "Which HOST-OWNED projections the command's results feed. Declaring 'sarif' is an applicability advertisement under DR-122/DR-G17 and carries D-002's SARIF identity dependency; it is not a right to render. Components never render product UI directly (SRC-02 developer contract). The prototype's output/rawStreamReason dispatch datum is NOT carried by this field - a named semantic substitution, see DIFF-9."
          }
        ]
      },
      {
        "name": "capabilities",
        "type": "array of capability declaration objects: { capabilityId: string (host-registered vocabulary), roleSubprotocol: string, subprotocolVersion: integer, declarationData: schema-checked object }",
        "required": true,
        "mayBeEmpty": true,
        "semantics": "The capability domains this component serves, consumed metadata-only at admission and by DR-118's matrix (D-007: 'the manifest is the boundary source' for matrix rows). Subprotocol names/versions are opaque tokens here; negotiation semantics are DR-102's. No identity is derived from this block by this schema: capabilityManifestId is a NAMED IDENTITY GAP owned elsewhere (ID-DEP-2). An EMPTY array is a VALID declaration meaning this component serves no capability domains - the prototype's no-capabilities case made explicit rather than optional - so PB-2's cargo-clippy worked example (which declares no capabilities) translates decidably as capabilities: [] and ADMITS; only the member's outright ABSENCE is RJ-6/MISSING_REQUIRED_FIELD.",
        "sourceWarrant": "PB-1 capabilities (ToolCapabilityDeclaration[]); D-007 matrix boundary (SRC-D); SRC-08 DR-006 park coverage naming capabilityManifestId"
      },
      {
        "name": "platforms",
        "type": "array of platform objects: { os: string, arch: string, tree: TreeCommitment (below), entrypoint: relative path into that tree }, at least one entry",
        "required": true,
        "semantics": "Explicit supported-platform alternatives (SRC-02 packaging contract). Each platform binds its own exact immutable tree and entrypoint. A platform absent from this array is unsupported - typed unavailability, never a fallback (D-008 rule). The slice-1 platform vocabulary is D-002's four (macos/arm64, macos/x86_64, linux/x86_64, linux/arm64); the schema is platform-open, the product decision is not made here.",
        "sourceWarrant": "SRC-02 'explicit supported-platform alternatives and per-surface compatibility'; D-002 platforms (SRC-D)",
        "treeCommitmentShape": {
          "definition": "TreeCommitment = { entries: array of { path: string, type: 'file'|'dir'|'symlink', mode: octal string, length: integer (files only), sha256: hex string (files only), target: relative path (symlinks only) } }",
          "semantics": "Full-tree path/type/mode/length/digest commitment including entrypoint and every required runtime/tool byte (SRC-02 packaging contract; DR-119's 'declares every bundled runtime/tool byte and exact tree'). Every per-entry sha256 is a plain measurement of bytes. This schema deliberately does NOT mint a single tree-root digest: a Merkle-style root would be a DERIVED identity requiring a producing recipe, and the freeze's identity-recipe property (DR-006) owns that class - the corpus specifically records typescriptStdlibMerkleRoot as a root digest with no producing rule. A treeRootDigest field is RESERVED as a binding point that activates only on the DR-006/EIR-lineage closure it rides (ID-DEP-3).",
          "pathRule": "Every path and symlink target is relative, forward-slash separated, NFC-composed, case-sensitively unique AND unique under case-folding and Unicode normalization within the tree, with no '.' or '..' segments, no leading '/', no drive letters, no backslashes, no NUL, no empty segments. Violation is RJ-3. Symlink targets must resolve inside the tree.",
          "alternatives": "A single root digest (simpler to sign, one-line lock entries) was rejected for the identity-recipe reason above; an entries list of plain measurements carries the same information without minting a recipe, at the cost of manifest size. Size bounds are the named open decision OD-1 (namedOpenDecisions): no cap is minted here, and the absence is owned, not silent."
        }
      },
      {
        "name": "dependencies",
        "type": "array of { stableId: UUID, versionConstraint: string (constraint language is a DR-111 surface), reason: string }",
        "required": true,
        "mayBeEmpty": true,
        "semantics": "The component's complete declared dependency closure over other components. Completeness is load-bearing: resolution refuses any closure that requires an undeclared edge (RJ-5), and admission never fetches implicitly (D-008 refusal of ambient/implicit downloads). System-ABI/loader dependencies are DR-126's allowlist surface, not manifest edges.",
        "sourceWarrant": "SRC-02 'dependencies/prerequisites'; D-008 (SRC-D); SRC-08 DR-126"
      },
      {
        "name": "prerequisites",
        "type": "array of typed prerequisite objects; slice-1 vocabulary: the DR-119 approved-exception shape below",
        "required": false,
        "semantics": "Non-bundleable external prerequisites. Under D-008's accepted rule the ONLY lawful member is an explicitly product-approved customer-owned external-system exception, and it must carry: { exceptionApprovalRef: string (the product approval record), ownership: string, trust: string, network: string, prerequisiteExpectations: string, doctorContract: string (the doctor surface that reports it), typedAbsenceBehavior: refusal-code ref, typedFailureBehavior: refusal-code ref }. Visible in manifest and doctor output; never marketed or silently treated as self-contained.",
        "sourceWarrant": "D-008 accepted rule, restated at full source fidelity in SRC-D; SRC-02 Language runtime/toolchain UX section"
      },
      {
        "name": "declarations",
        "type": "object with required members: licenses, sbom, attestation, platformQualification, capabilityParityEvidence, performanceBaseline",
        "required": true,
        "semantics": "The DR-119/D-008 declaration set. Each member is either (a) a digest reference { path: relative path into the platform tree OR sibling release artifact name, sha256: hex } binding the declared inventory/evidence bytes into the signed closure (SRC-03: 'SBOM and attestation digests inside the signed closure'), or (b) for approved exceptions only, a TYPED ABSENCE object { absent: true, exceptionApprovalRef: string, typedAbsenceBehavior: refusal-code ref } - absence is a claim, never a default (D-007's absence-as-claim discipline). licenses additionally carries the notice inventory (SRC-02: 'license, and notice inventory').",
        "sourceWarrant": "D-008 verbatim ('MANIFEST DECLARATIONS of licenses/SBOM/attestation/platform/capability/performance; ... typed absence/failure' for approved exceptions); SRC-02 packaging contract bullets",
        "namedDifference": "DIFF-6"
      },
      {
        "name": "permissions",
        "type": "array of { permission: host-owned closed vocabulary token, reason: string }",
        "required": true,
        "mayBeEmpty": true,
        "semantics": "REQUESTED permissions only. A manifest cannot grant authority to itself; deny wins; absence denies (SRC-03 verbatim law). Grant/denial/enforcement records are the host's DR-105/DR-G09 surface. The prototype's requires[] (resource + reason, PB-2) is the evidentiary ancestor; V2 narrows it to a closed host vocabulary.",
        "sourceWarrant": "SRC-03 permissions section; PB-1 requires (ToolResourceRequirement, 'Declaration-only ... structurally validated at admission')",
        "namedDifference": "DIFF-14"
      },
      {
        "name": "configuration",
        "type": "object { namespace: string, schema: JSON-Schema-subset object, classifications: map field -> host-owned classification token }",
        "required": false,
        "semantics": "Namespaced, schema-closed component configuration with per-field analysis classification, mapped by the host into existing resolution/Plan semantics. A component cannot add a private precedence layer or classify its own hidden semantic input (SRC-03) - therefore 'classifications' values come from a host-owned vocabulary and the host validates the mapping at admission.",
        "sourceWarrant": "SRC-03 lines 31-35; SRC-02 'configuration classification'; PB-1 config descriptor precedent"
      },
      {
        "name": "provenance",
        "type": "object { publisher: string (delegated publisher/component namespace token; namespace semantics are DR-112's), sourceClass: closed enum, slice-1 vocabulary: 'first-party' | 'explicitly-trusted' }",
        "required": true,
        "semantics": "The provenance half of the (ID, provenance) discriminator. Slice 1 admits first-party/explicitly-trusted only (D-002/DR-128); the enum widens only by the DR-117/DR-010 successor process, never by schema edit here. The signature itself is NOT a manifest field - see signatureBinding.",
        "sourceWarrant": "D-012 (ID, provenance) discriminator; D-002 components clause; PB-1 ToolSource trust classes as the prototype baseline",
        "namedDifference": "DIFF-4"
      },
      {
        "name": "stateMigration",
        "type": "RESERVED BINDING POINT (typed null: { reserved: true, ridesOn: ['DR-124', 'DR-106', 'DR-109', 'DR-113'] })",
        "required": true,
        "semantics": "File 02's manifest enumeration includes 'state/migration'. DR-106/109/113 are deferred WHOLLY by D-002 (their acceptance cells begin with applied DR-002..008 successors), so this schema RESERVES the member and designs no semantics - the DR-202 F2 lesson: a slice-1 schema must not force wholly-deferred rows into design. The reserved marker is required so the omission is visible, never silent.",
        "sourceWarrant": "SRC-02 manifest sentence; D-002 MF-03 wholly-deferred ruling (SRC-D); SRC-08 DR-202 row (F2 rescoping)"
      },
      {
        "name": "updateData",
        "type": "RESERVED BINDING POINT (typed null: { reserved: true, ridesOn: ['DR-110', 'DR-112'] })",
        "required": true,
        "semantics": "File 02's 'deterministic update, rollback, repair, and revocation metadata'. Slice 1's install is a fresh signed download (D-002's DR-110 deferral); the member is reserved, visible, and undesigned here.",
        "sourceWarrant": "SRC-02 packaging contract; D-002 explicit deferrals (SRC-D)"
      },
      {
        "name": "compatibility",
        "type": "object mapping surface name -> declared version datum; surface vocabulary and window semantics are DR-111's",
        "required": true,
        "semantics": "Per-surface compatibility declarations replacing the prototype's single integer epoch. The manifest DECLARES data; DR-111's matrices own meaning. No shared same-version assumption may be encoded (SRC-02: 'no required same-version coupling').",
        "sourceWarrant": "SRC-02 compatibility paragraph; SRC-08 DR-111 row; PB-1 apiVersion baseline",
        "namedDifference": "DIFF-3"
      }
    ],
    "signatureBinding": {
      "rule": "A manifest file itself carries NO signature field. Each component release ships the manifest plus a DETACHED signature envelope over the manifest's canonicalized bytes (ID-DEP-1), under the publisher's delegated namespace. Signature format, key custody, thresholds, rotation, expiry, revocation and the ceremony are DR-112's surface; this schema defines only the binding points: (a) the manifest's admission digest (sha256 of stored bytes), (b) the canonicalized signing preimage (dependency declared, not defined), (c) the envelope's association to the index entry (indexSchema.entries[].signatureRef).",
      "alternatives": "An embedded signature field (self-describing single file) was rejected: embedding forces a sign-then-mutate ordering problem (the signed bytes differ from the stored bytes), while the detached envelope keeps 'verified bytes equal executed bytes' (DR-G07) trivially statable. The prototype has no manifest signature at all (PB-1 manifestHash is a post-hoc integrity note, not a signature) - see DIFF-4."
    }
  },
  "indexSchema": {
    "name": "opensip-component-index",
    "purpose": "The signed collection of ADMITTED manifests plus the host registry custody records - the second custody record of D-012's two-record identity shape. The index is what help/completion/inventory read ('authenticated metadata only', SRC-02); it is the admission registry that enforces stableId uniqueness; and it is the uniqueness universe, which is PER-INSTALL (D-012 routed observation), not global.",
    "fields": [
      {
        "name": "indexSchemaVersion",
        "type": "integer >= 1",
        "required": true,
        "semantics": "DR-111 surface datum, same rule as manifestSchemaVersion."
      },
      {
        "name": "installId",
        "type": "UUID",
        "required": true,
        "semantics": "Names the per-install uniqueness universe this index governs. Minted by the host at install time; participates in no cross-install identity."
      },
      {
        "name": "entries",
        "type": "array of admission records",
        "required": true,
        "entryShape": {
          "stableId": "UUID - index entries are keyed per (stableId, version, scope). The component-binding uniqueness constraint holds per stableId. A second LIVE admission of the same (stableId, provenance) at a new version is a release, not a duplicate. A second LIVE admission of the same (stableId, version) at a different scope is D-012 clause 4 dual-scope admission, not a duplicate. unique-among-LIVE name/alias/root comparisons apply to a DIFFERENT (stableId, provenance). Discriminated from retired holders by (stableId, provenance)",
          "provenance": "copy of the admitted manifest's provenance object at admission - the discriminator's second half. After admission it moves ONLY by a recorded in-class ownership transfer (see ownershipTransfers)",
          "ownershipTransfers": "array, may be empty, of transfer records { fromPublisher: string, toPublisher: string, transferredAt: timestamp, hostAuthorityRecordRef: string } - D-012 clause 6's DECIDED element in schema form: transfer WITHIN the first-party/explicitly-trusted class is registry bookkeeping under host authority with the SAME two-custody update discipline as admission. The transfer act updates this entry's provenance.publisher to the transferee and appends the record here in one act (registry custody); the transferee's next admitted release declares the new publisher in its manifest (manifest custody); the stableId uniqueness constraint holds throughout - same component, new steward, unchanged identity. Between the transfer act and the transferee's first release the entry's provenance lawfully differs from the last admitted manifest's declared publisher, and the transfer record IS the accounting for that divergence - the custody join (manifestDigest) is untouched by transfer. Both fromPublisher and toPublisher must lie within slice 1's sourceClass universe ('first-party'/'explicitly-trusted'); THIRD-PARTY transfer/dispute policy is the clause's explicitly DEFERRED half (DR-117 successor, DR-010 process) and has no carrier here",
          "version": "the admitted release's semver",
          "manifestDigest": "sha256 of the admitted manifest's exact stored bytes - the custody join to the first record",
          "signatureRef": "reference to the detached signature envelope verified at admission (envelope identity = sha256 of envelope bytes); verification semantics are DR-112's",
          "admittedAt": "timestamp of admission",
          "scope": "'project' | 'global' - D-012 clause 4: a component admitted at both scopes resolves project-shadows-global, and the index entry for the shadowed admission carries shadowedBy so the REQUIRED DISCLOSURE at resolution is computable from custody data alone",
          "shadowedBy": "optional (stableId, version, scope) of the shadowing admission - present exactly when this entry is shadowed; the triple matches the entry key so the reference is unique",
          "mountedRootCommand": "the single root command name mounted for this component - the flat host-owned root namespace record",
          "namesSnapshot": "the name + aliases + mounted root command name the admission bound (the root name equals the component name by the manifest schema's rootNameBinding rule; it is carried EXPLICITLY so RJ-2's collision universe contains it by construction, not by inference), so collision computation is metadata-only against custody (D-012 clause 7) without re-reading manifests",
          "status": "'active' | 'deprecated-alias-window' | 'retired' | 'revoked'",
          "deprecation": "optional { oldName: string, deprecatedAtRelease: host-core release id, deprecatedAtDate: date, windowEndsNoEarlierThan: date, doctorRemediation: RESERVED BINDING POINT ({ reserved: true, ridesOn: ['DR-114'] }) } - carries D-012 clause 5's dual clock (>= 1 host-core minor cycle AND >= 90 days). D-012 clause 7's closing sentence DECIDES that namespace migrations require a recorded deprecation entry WITH typed doctor remediation; that requirement is declared here, and the remediation record's SHAPE is DR-114's doctor-schema surface (ID-DEP-10) - the member activates on DR-114's closure, and populating it before then is RJ-6/RESERVED_FIELD_POPULATED"
        }
      },
      {
        "name": "retiredIds",
        "type": "array of { stableId: UUID, provenance: object, retiredAt: timestamp, reason: string }",
        "required": true,
        "mayBeEmpty": true,
        "semantics": "The never-readmit ledger: a retired ID is retained and never readmitted for a DIFFERENT component (same (ID, provenance) may return; a different provenance claiming a retired ID is RJ-1). DEFINING RELATION to entries[].status: a retirement or revocation act sets the entry's status ('retired'/'revoked') AND appends the (stableId, provenance) row here in the SAME act - the entry is retained as custody history, this ledger row is the never-readmit datum, and every retired/revoked component appears in both records. LIVE, wherever these schemas use it, means status 'active' or 'deprecated-alias-window'; RJ-1 consults live entries for its duplicate check and this ledger for its never-readmit check. 'revoked' carries the SAME never-readmit standing as 'retired' at this surface; any revocation-specific trust or readmission semantics beyond that are DR-112's (ID-DEP-5)."
      },
      {
        "name": "reservedRootCommands",
        "type": "array of strings",
        "required": true,
        "semantics": "The host's static reserved root-command list (D-012 clause 3, the prototype's ADR-0159 shape, PB-5). Recorded IN the index so reservation checks are metadata-only; a CI parity test between this list and the live host grammar is part of the negative-test evidence (TC-NAME-1)."
      },
      {
        "name": "trustMetadata",
        "type": "RESERVED BINDING POINT (typed null: { reserved: true, ridesOn: ['DR-112'] })",
        "required": true,
        "semantics": "Index signature envelope, delegated publisher/component namespace policy, metadata expiry, last-known revocation for air-gap/removable media, quorum/rotation/recovery state (SRC-03 independent-release trust enumeration). The index IS signed - that requirement is declared here - but every ceremony/state-machine semantic is DR-112's. The index's signing preimage rides ID-DEP-1 like the manifest's."
      }
    ],
    "custodyRule": "Admission writes the manifest custody record (the manifest bytes at manifestDigest) and the registry custody record (the entry above) in one admission act; the uniqueness constraint holds between them (D-012 clause 1). Metadata-only admission: every check needed to admit or refuse - uniqueness, collisions, reservation, path rules, declaration presence, signature-digest match - is computable from manifest bytes plus this index, with NO component execution during discovery or admission (D-012 clause 7).",
    "collisionUniverse": "Live-name/alias/root uniqueness (RJ-2's cross-entry half) quantifies over entries of a DIFFERENT (stableId, provenance). Same-(stableId, provenance) multi-version coexistence and same-(stableId, provenance, version) dual-scope admission are excluded from that half (dual-scope is the same identity at two scopes; a different-provenance claimant at the same stableId is RJ-1 regardless of scope). Reserved-list and in-manifest command-tree collisions remain unconditional. RJ-1 is unaffected: it is a SAME-stableId rule by construction and still refuses a different-provenance claimant of a live or retired ID. Index entries are keyed per (stableId, version, scope), which is also how shadowedBy - the same triple - identifies a unique entry. This is the V2-A1 defining sentence as repaired after v3 REJECT."
  },
  "lockSchema": {
    "name": "opensip-component-lock",
    "purpose": "The exact-resolution record for a project/operation: which admitted component releases a resolution selected, as (stableId, provenance, version, scope, digest) tuples, deterministically derived from declared inputs. NO lock is producible until DR-111 closes; this field describes the deferred record, not a presently producible artifact. Owning sentence, SRC-03 lines 37-39 verbatim: 'The project component lock records selected signed inputs and deterministic resolution, not secret values, user trust decisions, or self-granted authority. Its concrete schema is DR-103.'",
    "fields": [
      {
        "name": "lockSchemaVersion",
        "type": "integer >= 1",
        "required": true,
        "semantics": "DR-111 surface datum."
      },
      {
        "name": "resolutionInputs",
        "type": "object { installId: UUID, indexDigest: sha256 of the index bytes consulted, platform: { os, arch }, hostCoreVersion: string, request: either exact-pin pairs { stableId, version } copied from already-admitted index entries, or constrained pairs { stableId, versionConstraint } }. Both request forms are SCHEMA SHAPES for a future lock; neither form is producible until DR-111 closes.",
        "required": true,
        "semantics": "The complete declared input set. NO lock is producible until DR-111 closes: resolutionInputs include hostCoreVersion and platform, compatibility values are DR-111-owned, versionConstraint edges are DR-111-owned, and the closure rule requires those evaluations. An exact-pin of one or many admitted tuples is a custody copy, not a completed compatible resolution, and is not a lock. Determinism of a future lock: identical resolutionInputs must yield a byte-identical lock - which is only fully statable once ID-DEP-1 and DR-111 close; until then even tuple-level lock production is unproducible. Ordering rule, when locks become producible: entries sorted by (stableId, version, scope). That names the key only. A total comparator (canonical UUID / semver / scope encodings and an exact-byte tie-breaker; semantic-version precedence does not distinguish build metadata) is RESERVED until lock production is enabled."
      },
      {
        "name": "resolved",
        "type": "array of tuples { stableId: UUID, provenance: object, version: semver, scope: 'project'|'global', manifestDigest: sha256, platformTreeEntryCount: integer, artifactDigests: array of { artifact: string, sha256: hex } }",
        "required": true,
        "semantics": "One tuple per selected component release: the (ID, provenance, version, scope, digest) record the task's cell names, widened with the release's per-artifact digests (archive, detached signature envelope, declaration artifacts) so DR-G07 exact-byte verification and offline install (SRC-02 authoritative-closure bullets) are computable from the lock alone. The custody join to the index is by (stableId, version, scope), matching entryShape and shadowedBy. Every digest is a plain sha256-of-bytes measurement.",
        "closureRule": "The resolved set must be dependency-closed over the manifests' declared dependencies. If closing the set would require an edge no manifest declares, resolution REFUSES (RJ-5) and no lock is produced - a lock is never a record of an improvised closure."
      },
      {
        "name": "exclusionsRecorded",
        "type": "statement",
        "required": true,
        "semantics": "The lock records NO secret values, NO user trust decisions, and NO self-granted authority (SRC-03 verbatim). Trust decisions live in host consent/permission records (DR-105/DR-G09); secrets live behind handles (SRC-03 secret-value scope)."
      }
    ],
    "alternatives": "Recording only direct dependencies (smaller lock, npm-style peer ambiguity) was rejected: SRC-02's offline-closure bullets require the exact selection installable on a clean machine, which needs the full closure in one signed-input record. Recording resolved TREES inline (self-contained lock) was rejected: manifestDigest already pins the tree commitment through the manifest bytes; duplicating it would create a second copy that can drift - the corpus's recorded drift mechanism."
  },
  "rejectionRules": {
    "discipline": "Every rule is a TYPED REFUSAL: a stable refusal-code identity plus the refusing datum, emitted loudly at admission or resolution. Never silent shadowing, auto-renaming, warn-and-continue, partial admission, or silent fallback (D-012 clause 2; D-008 anti-fallback rule). Refusal codes are typed identities WITHIN this surface; their mapping to process exits/D9 is the host's D9/exit surface and is NOT defined here (DR-007 forbids inventing typed result/exit integration - ID-DEP-7).",
    "rules": [
      {
        "id": "RJ-1",
        "code": "REFUSE_DUPLICATE_STABLE_ID",
        "trigger": "Admission of a manifest whose stableId already has a live index entry with different provenance AND no recorded in-class ownership transfer accounting for the change, or matches a retiredIds entry with different provenance (the never-readmit ledger). Same (ID, provenance) at a new version is not a duplicate - it is a release.",
        "exception": "D-012 clause 6's DECIDED in-class ownership transfer: a publisher change for a LIVE entry executed as host-authorized registry bookkeeping under the two-custody update discipline (recorded at entries[].ownershipTransfers, both publishers within slice 1's sourceClass universe) is LAWFUL, not a duplicate. After the recorded transfer the entry's provenance is the transferee's: releases matching the post-transfer provenance admit as ordinary releases, and releases matching the PRE-transfer provenance now differ from the entry's provenance and refuse under the ordinary trigger. An unrecorded publisher change remains REFUSE_DUPLICATE_STABLE_ID. The retiredIds clause is untouched by this exception - transfer is bookkeeping on LIVE entries, and no transfer semantics for retired IDs are defined or implied here.",
        "warrant": "D-012 clause 1 + per-install universe + (ID, provenance) discriminator; the exception is D-012 clause 6's decided in-class transfer element (SRC-D)"
      },
      {
        "id": "RJ-2",
        "code": "REFUSE_NAME_COLLISION",
        "subcodes": [
          "RESERVED_ROOT_COMMAND",
          "LIVE_NAME_COLLISION",
          "ALIAS_COLLISION",
          "ALIAS_SHADOWS_LIVE_OR_RESERVED",
          "ALIAS_CYCLE",
          "PARENT_LINKAGE_COLLISION",
          "PARENT_UNKNOWN_OR_CYCLIC",
          "MULTIPLE_ROOT_COMMANDS",
          "ROOT_COMMAND_NAME_MISMATCH",
          "ID_VERSION_INDISTINCT"
        ],
        "trigger": "Any collision at admission over name, alias, or parent linkage against the reserved list (unconditional); against live names/aliases and live entries' MOUNTED ROOT COMMAND NAMES of a DIFFERENT (stableId, provenance); or within the manifest's own command tree; more than one parent-less command; a parent-less command whose name differs from manifest.name (the rootNameBinding rule); a version datum used as an identity key. Same-(stableId, provenance) multi-version coexistence and same-(stableId, provenance, version) dual-scope admission are excluded from the cross-entry half (indexSchema.collisionUniverse). RJ-1 is not this rule.",
        "warrant": "D-012 clauses 2, 3, 5; prototype mechanism PB-5/PB-7 (rejected at admission, before mounting)"
      },
      {
        "id": "RJ-3",
        "code": "REFUSE_PATH_VIOLATION",
        "trigger": "Any file reference anywhere in the manifest (tree entry paths, symlink targets, entrypoint, declaration paths, doctor-contract references) violating the pathRule: '..' or '.' segments, absolute paths, drive letters, backslashes, NUL, empty segments, non-NFC bytes, symlink escaping the tree, duplicate paths (byte, case-fold, or Unicode-normalization duplicate), or an entrypoint not present in the committed tree.",
        "warrant": "SRC-02 exact-byte admission + DR-G07 hostile archive/path corpus; DR-120 hostile-path acceptance tests"
      },
      {
        "id": "RJ-4",
        "code": "REFUSE_SIGNATURE_INVALID",
        "subcodes": [
          "UNSIGNED",
          "DIGEST_MISMATCH"
        ],
        "conditionalSubcode": "ENVELOPE_MISMATCH (an envelope that does not verify over the canonicalized preimage) is DECLARED here and INACTIVE: deciding it requires the canonical-JSON profile (ID-DEP-1, CANDIDATE-NOT-APPLIED, UR-1..5 open) AND enough of DR-112's envelope/key semantics to run a verification at all (ID-DEP-5). It activates only when both close; if either closes by scoped disposition it follows that disposition's terms - the D-002 symmetry-clause form TC-BYTE-EXACT's scopeNote already uses. Until then no admission can emit it, and no fixture is required for it (TC-SIG conditionalRequires).",
        "trigger": "A release presented without its detached signature envelope where the trust policy requires one; index-recorded manifestDigest not equal to the sha256 of the presented manifest bytes; artifact bytes not matching an index-recorded digest, or (lock-digest arm, INACTIVE until DR-111 closes and a lock is producible) artifact bytes not matching a lock-recorded digest. NOTE: which keys/namespaces/thresholds make an envelope VALID is DR-112's; this rule's ACTIVE subcodes own only the byte-level mismatch and absence classes, which are decidable without the ceremony or the open canonicalization - envelope verification itself sits behind the conditional subcode above.",
        "warrant": "SRC-03 preserved signed-delivery laws; DR-G07 'verified bytes equal executed bytes'; scope split per whatThisDoesNotDo item 3"
      },
      {
        "id": "RJ-5",
        "code": "REFUSE_UNDECLARED_CLOSURE",
        "trigger": "Resolution requiring a dependency edge no manifest declares; admission of a component whose declared closure references a stableId absent from the index with no lawful acquisition path; any attempt to satisfy a closure by ambient lookup or implicit download.",
        "warrant": "D-008 refusal of ambient/implicit downloads AND typed remediation as separate obligations; SRC-02 offline-closure bullets"
      },
      {
        "id": "RJ-6",
        "code": "REFUSE_MALFORMED_MANIFEST",
        "subcodes": [
          "UNKNOWN_KIND",
          "MISSING_REQUIRED_FIELD",
          "NON_UUID_STABLE_ID",
          "UNKNOWN_ROLE",
          "UNKNOWN_PERMISSION_TOKEN",
          "SELF_CLASSIFIED_CONFIGURATION",
          "RESERVED_FIELD_POPULATED",
          "DUPLICATE_JSON_KEY"
        ],
        "trigger": "Structural invalidity, including populating a RESERVED BINDING POINT (stateMigration/updateData/treeRootDigest/trustMetadata/deprecation.doctorRemediation) before its owning surface closes - reserved means reserved - and including a DUPLICATE JSON KEY anywhere in manifest, index, or lock bytes: admission parses with a duplicate-rejecting pairs hook and the refusal NAMES the duplicated key. Warrant for the class, measured at SRC-FRZ section 7.5 (493 checker executions, nothing sampled): last-key-wins parsing lets a document say one thing to a reader and another to every instrument while its bytes - and therefore its admission digest, the sha256 of exact bytes - stay unchanged; for an exact-byte admission cell that means the custody join would verify while the PARSED identity, name set, tree entries, or declarations diverge between the admitting host and any later consumer, and section 7.5's own finding that naming the key is part of the fix is carried into the refusal shape. Malformed inputs remain REPRESENTABLE for diagnosis (the prototype's raw-vs-admitted pattern, PB-1 RawToolPluginManifest) but are never admitted.",
        "warrant": "PB-1 raw/admitted split; SRC-03 self-granted-authority and self-classification prohibitions"
      }
    ]
  },
  "testCorpusRequirements": {
    "discipline": "These are REQUIREMENTS for the exact-byte acceptance corpus DR-103's cell names - fixture classes, not fixtures. Every fixture, when authored, is digest-pinned per file; every fixture states its expected outcome (ACCEPT, or exactly one refusal code + subcode); acceptance evidence retains the fixture bytes, the run, and the digests (file 08's no-evidence-no-claim rule). Authoring the fixtures is qualification-side work under DR-120's fixture-generation contract; this artifact defines what classes must exist for DR-103's cell to be satisfiable.",
    "classes": [
      {
        "id": "TC-ACCEPT",
        "requires": [
          "minimal valid manifest (every required field, empty optionals)",
          "maximal manifest (every designed field populated)",
          "approved-exception manifest (DR-119 typed-absence declarations + prerequisite block)",
          "multi-version coexistence pair (same stableId+provenance, two versions, both admitted - file 05's goldens class per D-012 clause 7; lawful under collisionUniverse)",
          "index carrying all of the above with custody records",
          "lock resolving a multi-component closure is a required class member DEFERRED until DR-111; no lock fixture is required or producible until then; present ACCEPT evidence is index custody of the admitted set"
        ],
        "byteRule": "Each accept fixture's stored bytes are digest-pinned; the admission digest recomputes to the pin."
      },
      {
        "id": "TC-DUP",
        "requires": [
          "duplicate stableId, different provenance, NO recorded transfer (RJ-1)",
          "retired-ID readmission by different provenance (RJ-1)",
          "same (ID, provenance) re-release (must ACCEPT - the discriminator's positive case)",
          "recorded in-class ownership transfer, then a release under the transferee's provenance (must ACCEPT - D-012 clause 6's decided positive case)",
          "release under the PRE-transfer provenance after a recorded transfer (RJ-1 - the ordinary trigger, post-transfer)"
        ]
      },
      {
        "id": "TC-NAME",
        "requires": [
          "reserved root-command claim (name and alias forms)",
          "live-name collision",
          "alias collision and alias-shadows-live",
          "alias cycle",
          "stale-alias: the deprecated-alias dual-clock window exercised at both edges - inside the window (either clock unexpired) the alias MUST still resolve, and premature removal is itself a violation of D-012 clause 5's floor; after retirement past windowEndsNoEarlierThan (both clocks elapsed) the alias MUST NOT resolve and the outcome is typed, never a silent rebind",
          "parent-linkage collision and unknown/cyclic parent",
          "multiple root commands",
          "parent-less command whose name differs from manifest.name (RJ-2/ROOT_COMMAND_NAME_MISMATCH)",
          "the verdict's hostile double-mount pair: a component whose name equals a live entry's mounted root command name - refused through the widened collision universe, and equivalently as LIVE_NAME_COLLISION since the rootNameBinding makes every mounted root a live name",
          "ID/version distinctness violation",
          "scope-precedence disclosure: dual-scope admission whose index custody yields the required shadowing disclosure",
          "TC-NAME-1: the reserved-list/live-grammar CI parity check (D-012 clause 3)"
        ],
        "warrant": "the full D-012 clause 7 negative-test enumeration, every member present and the walk checkable member by member: collision, cycle, shadow, STALE-ALIAS, parent-linkage collision, reserved-name claim, scope-precedence disclosure, and ID/version distinctness have fixture classes above; multi-version coexistence (file 05's goldens class) is TC-ACCEPT's coexistence pair; the no-execution-during-admission probe is TC-METADATA-ONLY's class; and clause 7's closing sentence (recorded deprecation entry with typed doctor remediation) is carried at the index deprecation shape riding DR-114 (ID-DEP-10)"
      },
      {
        "id": "TC-PATH",
        "requires": [
          "'..' segment",
          "'.' segment",
          "absolute path",
          "backslash separator",
          "drive letter",
          "NUL byte",
          "empty segment",
          "non-NFC path bytes",
          "symlink escaping the tree",
          "byte-duplicate path",
          "case-fold duplicate path",
          "Unicode-normalization duplicate path",
          "entrypoint absent from tree"
        ],
        "byteRule": "Path fixtures are byte-exact by definition - several classes (NFC, case-fold, NUL) are indistinguishable at the rendered-text level and exist only as pinned bytes.",
        "scopeNote": "Windows-only path classes (reserved device names, trailing dot or space) are DELIBERATELY absent: Windows is deferred by D-002, and the deferral is named here so the omission is visible - those classes enter with the platform, not silently before it."
      },
      {
        "id": "TC-SIG",
        "requires": [
          "unsigned release where policy requires a signature (RJ-4/UNSIGNED)",
          "single-byte flip in manifest bytes against the recorded manifestDigest (RJ-4/DIGEST_MISMATCH)"
        ],
        "conditionalRequires": "envelope-over-wrong-preimage mismatch (RJ-4's conditional ENVELOPE_MISMATCH) - required of THIS corpus only after the canonical-JSON surface (ID-DEP-1) and DR-112's envelope semantics (ID-DEP-5) close; if either closes by scoped disposition, the fixture class follows that disposition's terms - the same conditionality TC-BYTE-EXACT's scopeNote applies to signing-preimage vectors. ALSO: artifact-bytes-versus-lock-digest mismatch - required of THIS corpus only after DR-111 closes, because no lock is producible until then; if DR-111 closes by scoped disposition, the fixture follows that disposition's terms.",
        "scopeNote": "Ceremony-validity fixtures (wrong key, expired metadata, revoked namespace, threshold failure) belong to DR-112's corpus; requiring them here would design the ceremony. The boundary is stated, not silent."
      },
      {
        "id": "TC-CLOSURE",
        "requires": [
          "undeclared dependency edge discovered at resolution (RJ-5)",
          "closure referencing an absent stableId (RJ-5)",
          "implicit-download attempt refused with typed remediation (D-008's two separate obligations: the refusal AND the remediation)"
        ]
      },
      {
        "id": "TC-METADATA-ONLY",
        "requires": [
          "no-execution-during-admission probe: an admission/discovery/help/completion/inventory pass over a fixture set containing an executable that records execution; the recorder must show zero executions (D-012 clause 7's probe)"
        ]
      },
      {
        "id": "TC-BYTE-EXACT",
        "requires": [
          "a fixture whose admission digest and index custody digest recompute from stored bytes (no lock digest until DR-111)",
          "a reserved-field-populated fixture refused RJ-6/RESERVED_FIELD_POPULATED",
          "a duplicate-JSON-key fixture: manifest bytes carrying the same key twice with divergent values, refused RJ-6/DUPLICATE_JSON_KEY with the duplicated key NAMED - existing ONLY as pinned bytes like the NFC classes, because SRC-FRZ section 7.5's attack is invisible at the parsed level by construction"
        ],
        "scopeNote": "Canonicalization vectors (signing preimages) are OWNED by the canonical-JSON surface (ID-DEP-1) and are required of THIS corpus only after that surface closes with a binding profile; if it closes by disposition, the signing-preimage fixture class follows that disposition's terms - the D-002 symmetry clause applied to a test class. A lock-digest recompute fixture is required only after DR-111 closes and a lock is producible; until then it is not a required member."
      }
    ]
  },
  "identityDependencies": {
    "pattern": "Named per the D-002 symmetry-clause pattern: each dependency names the open surface it rides, presumes nothing settled, and if the named surface closes by scoped disposition rather than a binding artifact, the dependent feature here ships reduced, re-scoped, or waits, by that disposition's terms; none of it survives on prose.",
    "dependencies": [
      {
        "id": "ID-DEP-1",
        "feature": "Signing preimages for manifest and index ('manifests are canonicalized for signing' - declared, not defined)",
        "ridesOn": "The canonical-JSON owning surface. Measured standing: canonical-json-profile.v1.json (sha256 6f7c906cb739a1313d68f5dd8bc31285d5f213c63649d3d54e1f9e88e5496710) is status CANDIDATE-NOT-APPLIED, binds NOTHING, with UR-1 (key-ordering plane) plus UR-2..UR-5 recorded UNDETERMINED at the vector level. This artifact invents NO canonicalization rule: no key-ordering, escaping, numeric, or normalization semantics are defined or implied here.",
        "consequence": "Until that surface closes, signatures are specified over canonicalized bytes ONLY as a binding point; the lock's byte-level determinism claim is conditional (stated at lockSchema.resolutionInputs); tuple-level determinism does not ride it."
      },
      {
        "id": "ID-DEP-2",
        "feature": "Any identity derived from the capabilities block",
        "ridesOn": "DR-006's park coverage, which names capabilityManifestId as a related identity gap with no producing rule (SRC-08). This schema derives no identity from capability declarations; the field is data, not a commitment.",
        "consequence": "If a capability-manifest identity is ever needed (e.g. for DR-118 matrix pinning), it enters ONLY through the DR-006/EIR-lineage closure, and this schema gains it by successor, not by reinterpretation."
      },
      {
        "id": "ID-DEP-3",
        "feature": "Single-root tree commitment (treeRootDigest)",
        "ridesOn": "DR-006/the identity-recipe property: a minted Merkle-style root is a derived identity requiring a producing recipe; the corpus records typescriptStdlibMerkleRoot as exactly this class with no rule (SRC-08, Exact DR-006 park coverage). The designed TreeCommitment is a list of plain per-entry sha256 measurements precisely so no recipe is minted.",
        "consequence": "treeRootDigest stays a RESERVED BINDING POINT; populating it before its recipe closes is RJ-6."
      },
      {
        "id": "ID-DEP-4",
        "feature": "compatibility field value semantics and constraint language in dependencies[].versionConstraint",
        "ridesOn": "DR-111 (per-surface compatibility matrices, OPEN). The manifest declares data; the matrices own meaning.",
        "consequence": "Admission may check structural validity of these fields but must not evaluate windows until DR-111 closes. Lock RESOLUTION is stated at full width (v2 A5 restored and extended by V4-B2): lockSchema.resolutionInputs.request, hostCoreVersion/platform, compatibility values, AND the resolved closure rule all require DR-111-owned evaluation to produce ANY lock. No lock of any form - exact-pin or constrained, one component or many - is producible until DR-111 closes. A non-validating custody copy of admitted tuples is not a lock. The TC-ACCEPT lock member is deferred to DR-111."
      },
      {
        "id": "ID-DEP-5",
        "feature": "Signature envelope validity, delegated publisher/component namespaces, index trust metadata (expiry, last-known revocation, rotation, quorum, recovery)",
        "ridesOn": "DR-112 (OPEN) and the preserved DELIVERY signed-delivery laws (SRC-03). RJ-4 owns only byte-level mismatch/absence, decidable without the ceremony.",
        "consequence": "trustMetadata stays reserved; TC-SIG's ceremony classes belong to DR-112's corpus."
      },
      {
        "id": "ID-DEP-6",
        "feature": "capabilities[].roleSubprotocol / subprotocolVersion semantics",
        "ridesOn": "DR-102 (common control transport and opaque subprotocol negotiation, OPEN). Tokens here are opaque names.",
        "consequence": "No negotiation, framing, or translation semantics are implied by the manifest fields."
      },
      {
        "id": "ID-DEP-7",
        "feature": "Mapping refusal codes to process exits / D9 / typed results",
        "ridesOn": "DR-007's successor chain (D9 gaps; 'V2 cannot define replay/purge/degradation typed result or exit') and DR-114's doctor schema.",
        "consequence": "Refusal codes are surface-local identities; exit/D9 integration enters by the owning successor only."
      },
      {
        "id": "ID-DEP-8",
        "feature": "Producing manifests from language builds; conformance of adapters to this schema; fixture generation for the test corpus",
        "ridesOn": "DR-120 (packaging contract and build-adapter template, OPEN) and DR-G15.",
        "consequence": "This artifact defines the contract adapters must emit; the adapter template and its ergonomics are DR-120's to design."
      },
      {
        "id": "ID-DEP-9",
        "feature": "SARIF applicability advertisement in commands[].outputModes",
        "ridesOn": "D-002's identity-dependencies section: SARIF for analyze rides DR-006's RunId/fingerprint closure; DR-122/DR-G17 own the projection contract.",
        "consequence": "Declaring 'sarif' advertises applicability only; if DR-006 closes by disposition, dependent SARIF surfaces follow that disposition's terms (D-002's symmetry clause verbatim)."
      },
      {
        "id": "ID-DEP-10",
        "feature": "Typed doctor remediation for namespace-migration deprecation entries (indexSchema entries[].deprecation.doctorRemediation)",
        "ridesOn": "DR-114 (doctor stable machine schema, OPEN). D-012 clause 7's closing sentence decides THAT a namespace migration requires a recorded deprecation entry with typed doctor remediation; DR-114 owns what a typed remediation record IS.",
        "consequence": "The member is a reserved binding point until DR-114 closes (populating it earlier is RJ-6/RESERVED_FIELD_POPULATED); if DR-114 closes by scoped disposition, the member follows that disposition's terms (D-002 symmetry clause). The decided REQUIREMENT is carried now; only its shape waits."
      },
      {
        "id": "ID-DEP-11",
        "feature": "The permission token vocabulary (permissions[].permission - the deciding universe for RJ-6/UNKNOWN_PERMISSION_TOKEN)",
        "ridesOn": "DR-105 (platform permission truth tables, OPEN) and DR-G09 (permissions gate): the host surface that owns requested/granted/denied/enforced/disclosed semantics owns the token universe those records quantify over. Named inline at the permissions field and in whatThisDoesNotDo since v1; carried as a dependency ROW here so the ride is regular rather than mentioned-without-carried.",
        "consequence": "Until the vocabulary is minted, admission checks the permissions block structurally (array shape, reason present, the deny-wins/absence-denies law restated) and UNKNOWN_PERMISSION_TOKEN is UNDECIDABLE - the subcode activates with the vocabulary, by its owner's closure or that closure's disposition terms."
      },
      {
        "id": "ID-DEP-12",
        "feature": "The configuration classification vocabulary (configuration.classifications values)",
        "ridesOn": "The host-owned analysis-classification surface SRC-03's component-configuration section states ('The host reviews every field's analysis classification and maps admitted values into the existing resolution/Plan semantics'). NO register row has minted the vocabulary; this row records it as UNOWNED-AND-NEEDED rather than pretending an owner exists - minting it here would be the self-classification authority SRC-03 forbids components, exercised by a schema instead.",
        "consequence": "RJ-6/SELF_CLASSIFIED_CONFIGURATION's structural half (a manifest minting classification tokens or mapping outside the host review) is decidable from manifest bytes now; validating a classification VALUE against the vocabulary is UNDECIDABLE until a surface mints it. The gap is named; the refusal subcode's value check activates with the vocabulary."
      }
    ]
  },
  "namedOpenDecisions": [
    {
      "id": "OD-1",
      "decision": "Size caps: manifest byte size, tree entry count, path length, alias count",
      "standing": "NO caps are stated in these schemas, and that absence is a NAMED OPEN DECISION, not a default. Unbounded manifests are a denial-of-service surface at metadata-only admission - the treeCommitmentShape alternatives note's own self-flag, answered here with an owner rather than left rhetorical.",
      "candidateOwners": "DR-115's numeric-threshold machinery (which already owns the core's size/startup/memory numbers, DECIDED at D-006, with the measurement half at qualification) or DR-120's packaging contract (which owns what an adapter may emit). This artifact declines to mint the numbers: a cap is a product threshold, and thresholds in this corpus are product-owned, measured, and waiver-formed (the D-006 pattern).",
      "consequence": "Until an owner states caps, admission enforces structural rules only, and oversized-input refusal is UNSPECIFIED, not implied - a fixture author must not assume it."
    }
  ],
  "namedDifferencesFromPrototype": [
    {
      "id": "DIFF-1",
      "prototype": "stableId is OPTIONAL and additive ('Declared additively by tools that opt into durable identity', PB-1 lines 185-190); the primary manifest key is the human string id.",
      "v2": "stableId is REQUIRED and is THE component identity; the human name is mutable metadata resolving through it.",
      "reason": "D-012 clause 1 adopted the author-declared immutable UUID as the identity model with host-registry second custody; an optional identity cannot anchor the (ID, provenance) discriminator or the never-readmit ledger. ADR-0048 (PB-4) itself records the prototype's direction of travel toward exactly this."
    },
    {
      "id": "DIFF-2",
      "prototype": "Dual human keys: id (programmatic) AND identity.name, held equal to each other and to the runtime tool name by a load-time drift guard that throws when they diverge, with declared stableId held equal to the runtime tool id (PB-6, re-measured at bytes this authoring); the display 'name' field is NOT in the guard's equality set - it is unconstrained display metadata.",
      "v2": "One name plus optional displayName.",
      "reason": "The prototype's guard proves the three-way shape only holds by continuous assertion - a drift mechanism held shut by a runtime check. Collapsing the keys removes the invariant instead of guarding it. Alternative (keep the split for prototype-file compatibility) rejected: V2 manifests are a new surface; DR-130 owns migration posture, and slice 1 claims no upgrade continuity (D-010)."
    },
    {
      "id": "DIFF-3",
      "prototype": "Single integer contract epoch apiVersion with a [min,max] admission range (PB-1 lines 49-61).",
      "v2": "manifestSchemaVersion for THIS document's schema plus a per-surface compatibility object; window semantics owned by DR-111.",
      "reason": "SRC-02 forbids the shared same-version assumption the single epoch encodes ('no required same-version coupling among distribution core, component release, ... Compatibility is expressed by separate per-surface matrices under DR-111'). The prototype's integer-not-semver-range lesson is KEPT (both version data are bare integers/data, no range maths in the manifest)."
    },
    {
      "id": "DIFF-4",
      "prototype": "Provenance is a runtime-attached record: a source enum inferred from discovery location ('bundled'|'installed'|'user-global'|'project-local'), plus manifestHash as a post-hoc integrity note. No signatures anywhere in the manifest path (PB-1 lines 63-84, 287-302).",
      "v2": "Provenance is a signed, author/publisher-declared manifest field (publisher namespace + closed sourceClass), verified against a detached signature envelope at admission; manifestDigest becomes index custody data.",
      "reason": "SRC-02's packaging contract requires 'signed publisher provenance plus bound SBOM, attestation, license, and notice inventory'; location-inferred trust cannot express delegated namespaces (SRC-03) or survive offline/air-gap transport. The prototype's deny-by-default posture for the least-trusted class is retained as the general rule: nothing is admitted on location alone."
    },
    {
      "id": "DIFF-5",
      "prototype": "Command manifests mirror runtime CommandSpec MINUS two non-serializable members (handler, OptionSpec.parse) - a documented deliberate narrowing (PB-1 lines 87-146).",
      "v2": "CommandSpec is pure data by construction; there is no runtime twin to narrow from, and admission is metadata-only with a no-execution probe (TC-METADATA-ONLY).",
      "reason": "D-012 clause 7 makes metadata-only admission a policy clause, not an implementation convenience. The prototype already mounts external commands 'from the manifest ALONE'; V2 removes the possibility of the closure-shaped exception rather than documenting it."
    },
    {
      "id": "DIFF-6",
      "prototype": "No manifest declarations of licenses/SBOM/attestation/platform-qualification/capability-parity/performance; license exists only as a package.json field (PB-2); typed-but-not-consumed placeholders reserved for future catalog metadata (PB-1 lines 246-261).",
      "v2": "The declarations object is REQUIRED with all six DR-119 members, each a digest-bound artifact reference or a typed absence under an approved exception.",
      "reason": "D-008 accepted the DR-119 rule universally; the declaration set is its manifest half verbatim. The prototype's placeholder pattern (reserve fields, let the consuming release define shape) is retained for the members that remain open here (stateMigration, updateData, trustMetadata, treeRootDigest) - with the V2 hardening that populating a reserved field is a typed refusal instead of silently ignored data."
    },
    {
      "id": "DIFF-7",
      "prototype": "kind: 'tool'; the manifest lives in package.json#opensipTools or an npm-ecosystem sidecar; discovery walks node package structures (PB-1 fileoverview).",
      "v2": "kind: 'component'; the manifest is a standalone document inside the signed release; no package-manager semantics reach admission.",
      "reason": "SRC-02's adapter-boundary rule verbatim: 'Variation ends at the adapter boundary: admission, signing, exact-byte verification, lifecycle, offline use, and measurement consume the common contract rather than language-specific package-manager semantics.' The npm shapes remain as build-adapter INPUTS under DR-120."
    },
    {
      "id": "DIFF-8",
      "prototype": "The bundled-set list (PB-3) is an unsigned name array with no digests, custody, or admission records.",
      "v2": "The index is a signed custody registry with per-entry digests, admission records, scope/shadowing disclosure data, a retired-ID ledger, and the reserved-command list.",
      "reason": "D-012 clause 1's second custody record and SRC-02's 'authenticated metadata only' both require the index to be evidence, not configuration. The prototype file is retained as evidence of the first-party-set concept only."
    },
    {
      "id": "DIFF-9",
      "prototype": "Per-command output (CommandOutputMode - 'How the host dispatches the handler's return', PB-1 line 134) plus rawStreamReason (required when output is 'raw-stream', line 136).",
      "v2": "Per-command outputModes - an advertisement of which HOST-OWNED projections the command's results feed. No carrier for the dispatch datum.",
      "reason": "A semantic SUBSTITUTION, not a subset, and named as such: the prototype datum (dispatch mode of the handler's return) is a runtime-contract datum between host and handler, while the V2 datum (projection applicability) is admission metadata. Metadata-only admission (D-012 clause 7) has no consumer for a dispatch mode, and V2's handler contract lives in the negotiated role subprotocol (DR-102, ID-DEP-6) - if that surface needs a declared dispatch datum, it enters there, not here. Alternative (carry output/rawStreamReason alongside outputModes) rejected: two output vocabularies on one command spec, one consumed by no admission check, is the typed-but-not-consumed drift shape this schema refuses elsewhere (RESERVED_FIELD_POPULATED discipline)."
    },
    {
      "id": "DIFF-10",
      "prototype": "Per-command commonFlags (CommonFlagKey[] - which of the host's common flags the command exposes, PB-1 line 126).",
      "v2": "Dropped; no carrier.",
      "reason": "The datum references the HOST's common-flag vocabulary, a host-CLI runtime surface no V2 decision row has minted; a manifest field over an unminted host vocabulary would be undecidable at admission - the same class ID-DEP-11 records for permission tokens. When a V2 surface mints the flag vocabulary (candidates: DR-123's baseline-UX surface or DR-120's adapter contract), a carrier enters by successor with the owner named. Alternative (carry the tokens opaquely now) rejected: unlike the capabilities block's opaque tokens, which DR-118's matrix consumes as data, an opaque flag list would be consumed by nothing and checkable against nothing."
    },
    {
      "id": "DIFF-11",
      "prototype": "Per-command producesVerdict (PB-1 lines 137-143): carried through the manifest precisely so the host - which synthesizes external tools from the manifest WITHOUT importing the runtime - knows an external scan command is suite-eligible.",
      "v2": "Dropped; no carrier.",
      "reason": "The prototype datum answers 'is this command suite-eligible' metadata-only - a purpose V2 must still serve, and the stated route is: verdict/suite semantics in V2 are the DR-122/DR-G17 projection contract's, and eligibility is a role/capability-level datum (the role field names which host contracts apply; the capabilities block feeds DR-118's matrix). If those surfaces need a per-command datum this schema cannot express, it enters by successor with its consumer named - the datum is rehomed with its owner, not lost silently. Alternative (keep the boolean) rejected: a verdict-semantics datum with no closed owning surface would pre-commit DR-122's contract from the manifest side - the self-granted-contract shape SRC-03 forbids."
    },
    {
      "id": "DIFF-12",
      "prototype": "Per-command producesEvidenceSnapshot (PB-1 lines 144-145).",
      "v2": "Dropped; no carrier.",
      "reason": "Evidence-snapshot semantics in V2 belong to the evidence/EIR lineage (DR-006's identity recipes and the evidence surface), none of it closed; a manifest boolean claiming evidence production would mint a claim no V2 surface can yet consume or verify. The route mirrors DIFF-11: when the owning evidence surface closes, a carrier enters by successor. Alternative (a reserved binding point instead of omission) rejected: the reserved-point pattern here is used for members file 02's enumeration REQUIRES (stateMigration, updateData); this datum carries no such source obligation."
    },
    {
      "id": "DIFF-13",
      "prototype": "Manifest-level languages (PB-1 lines 238-244) - a CONSUMED field, not a reserved placeholder: canonical source-language ids driving 'opensip tools list --available --lang <l>' discovery, derived for external-tool adapters from the adapter's declared languages with a parity gate.",
      "v2": "Dropped; no carrier.",
      "reason": "The datum is real (inventory/discovery filtering) and its V2 home is not this schema's to seize: the language-id vocabulary is unminted in V2, and the prototype itself DERIVES the field for external adapters - which places it with DR-120's build-adapter contract (ID-DEP-8), with DR-118's applicability matrix as the consuming candidate. Until an owner mints the vocabulary, a languages field would repeat DIFF-10's unminted-vocabulary defect. Named consequence, stated as a loss: V2 inventory cannot filter by language until that owner closes. Alternative (an opaque token list now) rejected for the same consumed-by-nothing reason as DIFF-10."
    },
    {
      "id": "DIFF-14",
      "prototype": "requires[] - ToolResourceRequirement (resource + reason), declaration-only, structurally validated at admission (PB-1 line 237; PB-2's worked instance declares subprocess and filesystem with reasons).",
      "v2": "permissions[] - { permission: host-owned closed vocabulary token, reason: string }, requested-only, deny-wins, absence-denies.",
      "reason": "Regularized into this register from the inline note at the permissions field: the narrowing was described there but unnamed here - the silent-divergence class this register exists to prevent, applied to the register itself. The narrowing: the prototype's open resource strings become a CLOSED host vocabulary, because SRC-03's law that a manifest cannot grant authority to itself requires the host to own the token universe, and grant/denial/enforcement records move wholly to DR-105/DR-G09 (ID-DEP-11). The declaration-plus-reason shape is retained verbatim from the prototype. Alternative (keep open resource strings) rejected: an open vocabulary lets a manifest mint a permission class no host table governs."
    }
  ],
  "whatThisDoesNotDo": [
    "Does NOT close DR-103. The row closes only on independent review of this candidate plus the register's own process; this artifact is author-side material and its own header says AWAITING-INDEPENDENT-REVIEW.",
    "Does NOT qualify anything. No claim here is QUALIFIED or DEMONSTRATED; the test-corpus section states requirements for future retained evidence. This artifact authors no fixtures. Separate fixture-corpus candidates exist at component-manifest-fixture-corpus.v1.json (d81a4ef9ed26405a07e84ae0be9a3d2048fd9a6126b1f93d139a5a499204b542, independently REJECTED) and component-manifest-fixture-corpus.v2.json (draft against this schema lineage). Neither is accepted, qualified, or applied here, and their existence does not SATISFY DR-103.",
    "Does NOT define the signing ceremony: key custody, thresholds, rotation, expiry, revocation, quorum loss, recovery, and envelope validity are DR-112's surface. This artifact defines binding points and byte-level mismatch refusals only.",
    "Does NOT invent canonicalization. It declares that manifests/index are canonicalized for signing and names the dependency on the canonical-JSON owning surface (ID-DEP-1), whose profile artifact is CANDIDATE-NOT-APPLIED with UR-1 OPEN; no ordering, escaping, numeric, or normalization rule is defined or implied here.",
    "Does NOT design state/migration, update/rollback/repair, replay/purge, or storage semantics: stateMigration and updateData are reserved binding points because DR-106/109/110/113 are deferred (D-002), and DR-007 forbids inventing typed result/exit integration.",
    "Does NOT mint any identity recipe: no tree root digest, no capability-manifest identity, no release-commitment digest. Every digest in these schemas is a plain sha256 over exact stored bytes; derived identities are reserved to the DR-006/EIR lineage.",
    "Does NOT extend the provenance/trust universe beyond slice 1's first-party/explicitly-trusted classes; widening is the DR-117/DR-010 successor's (with DR-116 for support/vulnerability policy), per D-012 clause 6.",
    "Does NOT choose packagers, build tools, or adapter mechanics (DR-120), and does NOT decide DR-111 windows, DR-102 negotiation, DR-105 permission truth tables, or DR-114 doctor schemas.",
    "Does NOT bind the host to these schemas: binds NOTHING. Application, if it ever happens, is a separate reviewed act under the standing route-A acceptance property (D-001 T2-02)."
  ],
  "reviewGuidance": {
    "forTheIndependentReviewer": [
      "Re-measure every digest in sources and measuredPrototypeBaselines; the prototype pin must equal a62509d623173155d0946e9f5d5ca90c839893e0 at review time (DR-203 custody caveats apply).",
      "Check the coverageObligation claim: every member of SRC-02's manifest enumeration maps to a designed field or an explicitly reserved binding point - the claim is falsifiable by enumeration walk.",
      "Check no clause contradicts D-012's adopted policy text (the D-012 review rejected two drafts for contradicting the pin; the clauses are restated here at their operative strength, not paraphrased down).",
      "Check that no canonicalization semantics leak in anywhere (the encodingRule, signatureBinding, lockSchema determinism clause, and TC-BYTE-EXACT scopeNote are the four places to probe).",
      "Check the rejection rules against D-012 clause 7's negative-test enumeration for completeness (TC-NAME claims every member present).",
      "Probe the alternatives notes: each names a real other design and a reason; a note that cannot be argued back is a decoration, not an alternative.",
      "Walk the repairLog against the verdict at 27965f38...: every finding B1-B6/A1-A9 names its operations at JSON-Pointer paths - verify each pointer resolves, each repair does what the verdict's FULL finding text prescribes, and every block the repairLog does NOT name is byte-identical to v1 (2733d766...).",
      "v4 walk: measure predecessor v3 159c6089... and both REJECT verdicts (Claude 0d2a5217..., Codex aa05c571...). Confirm V3-B1 provenance qualifier, V3-B2 (stableId, version, scope) key plus shadowedBy triple, V3-B3/V3-B4 single-component exact-pin only, Codex-S1 reserved-list unconditional. Attack any still-producible multi-component lock. Attack any RJ-1 unfiring. Every v3-to-v4 path the v4 repairLog does not name must be byte-identical to v3.",
      "v5 walk: measure predecessor v4 63ea2c47... and Codex v4 REJECT d54b0e65.... Confirm NO lock is producible until DR-111 (V4-B1 cardinality and V4-B2 compatibility). Attack any still-producible lock. Fold Claude 2's v4 findings if that verdict exists. Every v4-to-v5 path the v5 repairLog does not name must be byte-identical to v4.",
      "v6 walk: measure predecessor v5 c58f062d..., Codex v5 REJECT 112b522d..., and Claude 2 v5 REJECT c5b300e8.... Confirm V5-B1 lock requirements deferred at TC-SIG and TC-BYTE-EXACT; V5-B2/V5-S1 (Codex) custody flags scoped to v2 authoring; V5-S2 resolved tuple and ordering carry scope; Claude V5-S1/Codex V5-A1 method roster is closed; Claude V5-A1 purpose carries the deferral. Attack any still-producible lock. Attack any lockSchema join that cannot address a (stableId, version, scope) index entry. Attack any open-ended method roster. Every v5-to-v6 path the v6 repairLog does not name must be byte-identical to v5.",
      "v7 walk: measure predecessor v6 51b0a0b7..., Codex v6 REJECT a75d0af6..., and Claude 2 v6 ACCEPT-WITH-ADVISORIES 1e9bf51b.... Confirm V6-S1 no longer asserts fixture non-existence. Confirm RJ-4 lock arm is marked inactive until DR-111. Confirm SRC-PROTO and corpusHeadAtV2Authoring are v2-scoped. Confirm the ordering rule reserves a total comparator. Attack any still-producible lock. Attack any SATISFIED/QUALIFIED/implementation-authorization claim. Every v6-to-v7 path the v7 repairLog does not name must be byte-identical to v6. Claude V6-A2 and V6-A3 are carried, not folded; attack that accounting if those findings required a container-shape change to meet the ACCEPT bar.",
      "v8 walk: measure predecessor v7 a8870018..., Codex v7 REJECT 70860e05..., Claude 2 v7 REJECT aeb0e25b.... Confirm every v7-to-v8 path is named by V7-B1, V7-S1, V7-A1, V7-A2, or META-v8. Confirm reMeasurementAtV8 exists and reMeasurementAtV7 is unchanged. Confirm whatThisDoesNotDo[1] cites measured corpus candidates without accepting them. Attack any still-producible lock. Attack any SATISFIED/QUALIFIED/implementation-authorization claim. Attack any unnamed v7-to-v8 path."
    ]
  },
  "openDecisions": [
    {
      "id": "OD-V8-1",
      "finding": [
        "CLAUDE-V6-A2",
        "CLAUDE-V6-A3"
      ],
      "decision": "Whether to normalize TC-ACCEPT/TC-SIG/TC-BYTE-EXACT lock deferral onto a single conditionalRequires array-of-{member,gate} shape.",
      "standing": "OPEN. Not folded at v8.",
      "owner": "this schema surface (DR-103)",
      "doesNotBlockLockDeferral": "Each class already states the DR-111 gate unambiguously."
    }
  ]
}
