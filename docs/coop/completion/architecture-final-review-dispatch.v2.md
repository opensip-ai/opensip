# Independent whole-architecture and per-row application review

FROZEN CORRECTIVE DISPATCH — application freeze receipt BIND-V2.
Application SHA e8906a952435c6b8b1de963197c856a0c7bd84edb4ad169b8277db05b6d53610; supplement SHA be569fb045b96a94f35c111faadab7806b77e717a15f811dab7461e9ad0e7480.
Dispatch is now ready for the independent whole-architecture review.

Recipient: the independent whole-architecture auditor in Herdr wH:p3, who
has authored none of the architecture application or incorporated design units.

## Review boundary

Review the exact integrated `architecture-application.v1.json`, all of its
operative unit receipts and independent verdicts, the exact proposed register
and twelve-document publication, and the accepted reference handoff. Historical
scoped evidence remains scoped; the final signed G15/policy and broker/courier
successors must complete their actual integration, not inherit wider acceptance
from earlier fixtures. Do not modify any subject. Write findings or acceptance
as an external frozen verdict so no application/review digest cycle exists.

This is a design-completion application for the existing D-002/D-134 affected
set, not product qualification or implementation authorization. Check active
fixture/schema/numeric/actor/custody joins are authored and independently
accepted. Only exact accepted scope rides may remain with re-entry. Remaining
implementation execution and actual platform measurement must have named gates
and owners; lack of a design answer cannot be renamed execution work.

## Counting and source completeness

Independently resolve every source pointer against its pinned bytes and check:

- Exactly 23 affected rows, nine whole-row deferrals, six already SATISFIED rows
  and seventeen newly proposed row completions; no row removed to finish.
- Exactly 47 inherited obligations, identified by row plus obligation ID against
  the pinned obligation map. Confirm every disposition on its merits.
- The original 86 source-qualified ID-DEP definitions concern new-row contracts;
  the supplement covers incorporated already-SATISFIED contracts and other
  named conditions/claim rides. Owning contract, row and identifier define
  identity, with exact source SHA and JSON pointer. Repeated literals in
  different owning contracts are distinct. Independently enumerate the whole
  operative incorporated set and compare each category to the final declared
  totals: 94 ID-DEP definitions (86 plus eight DR-102), eleven named open
  conditions, 38 inherited V1 decision dependencies and five separate evidence
  custody edges. Delivery v4 authoring conditions are editing provenance only if
  their actual content confirms that classification. No definition may be lost
  by excluding an older version as lineage.
  Verify recorded successor authority and carry/retirement of any old-only
  definitions. Classify every inherited claim ride against actual D-002,
  D-018, D-077 or D-078 scope bytes. The historical security inventory v1 is
  neither the original86 nor the complete denominator.
- Exactly seventeen MF-6 row edits and all register summary/roster edits form
  the declared full before-to-after byte patch. Required-now gates become 29
  with the explicit G13 owner and unchanged unqualified status.
- All 68 grades are independently supplied: application, D-056 gate 2,
  D-056 gate 3 and SATISFIED for each of seventeen rows. An overall verdict or
  accepted unit cannot substitute for a missing row grade.

Check every original WA-1..WA-17 whole-audit finding against the final bytes.
The author's resolution map is navigation, not independent closure. Retain
new findings even if predecessor checkers pass. Check reciprocal protocol,
permission, trust, lifecycle, resolver, SDK, doctor and host-output boundaries
through concrete admitted/refused examples.

## Security exception history

The application must preserve three ordinary security exchanges, the UPHOLD
adjudication, the failed first bounded confirmation, the independently reviewed
and recorded D-367 delegated-owner case exception to D-368, and the same
adjudicator's second FAILED bounded confirmation. Then check the recorded
lead-correction v2 procedural successor, its independent consent, the exact user
notification and fresh complete LEAD-CORRECTION-REVIEW acceptance. All dispatches
and verdicts retain the cumulative three ordinary + UPHOLD + two failed counts. M2/M6 retain their confirmed
positions; M1/M3/M4/M5 and SEC4-REG-M5-OUTCOME require actual correction. No count
reset, fourth ordinary exchange, erased failure or fabricated assent is allowed.
The separate N1/cap review has only its declared scope. Check the final security
acceptance chain, not just an author's passing checker.

## Publication and replay

The proposed register is an exact post-image, not a live edit. Independently
replay the complete byte patch and check all seventeen row before/after hashes.
Review all twelve exact document before/after bodies, including completion
language and the reference handoff. No claim of shipping, implementation
permission or V1 authority may leak into the completion text.

The fourteen-path `architecture-publication-before.v1/snapshot-manifest.json`
retains the original register, twelve documentation files and decision book.
Original hashes and selectors remain mandatory. The structural checker routes
historical reads through those exact bytes while checking live register/docs
are the exact before or after images, rejecting drift. Frozen evidence remains
at its original source paths. Verify this distinction rather than assuming
successful replay proves actual publication.

Run the retained checker only with reports outside the frozen subject. Its
module docstring and `architecture-application-checker.v1.md` give the required
external verdict shape. A pre-review nonzero result solely because the external
whole verdict is absent is expected; it is not acceptance. Any other FAIL or
PENDING must be investigated.

## Verdict requirements

Use `architecture-whole-review.v1.json` and a concise Markdown companion.
Record reviewer identity/independence, HEAD at open, the exact application SHA,
all reviewed receipt pins, resolved original findings, remaining findings and
limits. On acceptance use the documented `independent-whole-application-review`
shape with numeric zero MUST/SHOULD, all row grade objects and MF-6 hashes,
registerImage, documentationImage and handoffImage. Also bind enactmentImage
and supply ownerActGrades for the seven ACT entries and scopeApplicationGrades
for SD-1..8. These approve the exact proposed acts; they do not assert that the
lead has already enacted them. The accepted application remains an immutable
proposal snapshot; the D-369 record and checked live images establish enactment. Additional rationale and
probe evidence may be retained beside those required fields. Do not author
acceptance fields if the underlying review does not warrant them. On objection,
identify concrete counterexamples, affected rows and exact required repairs.

No register or document mutation is proposed by the reviewer. The lead performs
recording only after independent acceptance and structural replay.
