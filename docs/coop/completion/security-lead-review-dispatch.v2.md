# LEAD-CORRECTION-REVIEW 2 — v7 repair and integration

Cumulative history: three ordinary exchanges, one UPHOLD adjudication, two failed
bounded confirmations, LEAD-CORRECTION-REVIEW 1 OBJECT (2 MUST/1 SHOULD). This is
lead review 2, not an ordinary exchange or bounded confirmation reset.
Reviewer /root/security_lead_review authored none of the subject bytes; author
Codex lead with attributed inherited design. The v7 document quotes the user
notification sent before this repair round. The same recorded lead-correction
v2 procedure applies, with every earlier verdict retained.

Subject: security-freeze.v7.json SHA-256 b289fb968d8958c99d22990547ee0e1cb3faab939f33013a20ac0c5931752efd, 72 frozen files.
Review the exact v6→v7 diff and complete affected boundary composition, with all
three first lead findings explicitly disposed. Raw policy closed schema and
canonical admission occur inside merge_policy, exceptions/refusals yield no
effective grants, project stateClass cannot create a value absent globally;
missing files are mapped only by the explicit higher-level adapter. Envelopes
are fully canonical-profile checked before schema/signature use; no float
version verifies. §7.5 names scratchDirSt/fileSt exactly. The v7 mutation suite
includes policy and envelope inputs, all earlier failures/controls remain.

Replay check-security-unit.v7.py to /tmp output (864 checks). Preserve v6/v7.
The original review harness replayed with version-path changes yields119/121:
the old documented-context probe hardcodes the retired Stat spelling, and its
policy-to-grant projection raises StopIteration because v7 correctly returns
no grant. Adapt these two test harness assumptions explicitly, not production
semantics: use the current documented names and assert no grant/no host effect
when merge refuses. The v7 owned suite tests both properties separately.

Write security-lead-review.v2.json and .md with subject/diff pins, independence,
exact probes, prior finding dispositions, numeric MUST/SHOULD counts, cumulative
history and actual verdict. Accept only zero rejecting findings; no register
edit, implementation permission, platform qualification or adoption follows.
