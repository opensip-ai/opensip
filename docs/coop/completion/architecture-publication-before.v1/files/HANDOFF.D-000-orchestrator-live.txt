# D-000 orchestrator live handoff — paste this entire file as the first user message to the incoming orchestrator (Grok).

You are the live D-000 orchestrator for OpenSIP V2 architecture completion. The sole human authority is the user. Between 2026-08-26 and 2026-08-28 the Claude orchestrator held this role (Grok was out of credits); this file is Claude's handoff back to Grok. Read it fully, then verify every pin below against live bytes before acting.

Record only at independent dual ACCEPT 0/0 (Stage A) and dual CONSENT 0/0 (Stage B) from Claude and Codex via Herdr. CANDIDATE-NOT-APPLIED binds NOTHING. Do not invent leftover-design, verdicts, fixture bytes outside the D-293 delegation, headings, identifiers, reserved numbers or lists. Do not edit file 08 except by a reviewed MF-6 act. Do not flatten DR-118 `DECIDED-V1-NOT-INTEGRATED` or DR-107 `PROPOSED-CLOSED-FOR-REVIEW` to OPEN. Do not SATISFY DR-117 / DR-131 / DR-133 (Class A openings are owner-controlled entries and none has landed).

## Live authorities at handoff (2026-08-28 18:18 )

- HEAD: `f3456575071928022a1f0e3a77e531a87157b365` (`D-294: adopt the cross-lineage leftover-join citation convention`)
- Last COORD heading: **D-294** (279 headings). COORD sha256 `31746810f9be78f697d66eb94d9cd50a95a51218998f97a154596363039fb9b6`.
- File 08 sha256 `e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e` (unchanged since D-236). required-now 28; owners 32 of 32; last gate DR-G32; Condition 2 **5 of 32** (DR-102, DR-104, DR-115, DR-119, DR-123); Condition 5 last; no `docs/v2/implementation/`.
- Git: local and `origin/main` in sync at handoff (verify with `git fetch && git status -sb`). **Push per decision is now authorized** by the owner (D-293 Decision 9; D-000 clause 4) — every recorded act is committed and pushed, non-force, after `git fetch` and a fast-forward check.

## What happened during the Claude tenure (do not replay)

1. D-282..D-292 (2026-08-26/27): eleven leftover-join remasurements at dual ACCEPT/CONSENT 0/0 — component-manifest v9, permission v12, state-class v4, doctor-actor v12, exact-bytes v7, distribution-core v9, g09 v12, g12 v5, g15 v6, g19 v5, g21 v13. Both mechanical queues (occupancy-stale and GATE-cites-superseded-ROW) are exhausted.
2. The decisions phase: every item of `DECISIONS-NEEDED.md` (A1–A4, B1–B3, C1–C9, D1, E2, F1) got a Claude+Codex reconciled recommendation (`DECISIONS-RECOMMENDED.md`, packets and round files under `DECISION-PACKETS/`, manifest `DECISION-PACKETS/MANIFEST.sha256`).
3. **D-293** (user-made entry, D-054/D-132 class, commit `c10319d`): the owner adopted every agreed recommendation. Read it in full — it is the warrant for everything below. Key content: A1 (D-273 stands, D-272 parked), A2 (nine parked CONTESTED headings confirmed historical), A3 (identity-namespace v7 authorized, lowest priority), A4 (convention adopted in principle → D-294), B1–B3 programmes authorized (Class A openings remain separate owner-controlled entries), C1–C9 dispositions, D1 fixture-authoring delegation with a reserved list, E2 push, F1 docs-rewrite plan adopted for after sealing, and the commit of the working files plus the 31 dispatch texts.
4. **D-294** (RULE-GOVERNED, turn 3 dual CONSENT 0/0, commit `f345657`): cross-lineage leftover-join citations are custody at recording, not standing currency claims. A recorded join needs a successor only when (a) an occupancy it cites as specification is superseded, (b) a sibling successor changes a projected value the citing join relies on (leftoverDesign partition; registerRow / file08StatusToken / liveGateOwners where the cited version carries them; per named obligation leftoverDesign / existingGate / rideStanding / executionObligationOwnerToday), or (c) its own lineage is superseded. A successor issued for any reason refreshes its cross-lineage citations to the versions current at its dispatch. **Consequence: the old "GATE citer remasurement" queue no longer exists; do not re-open it.**
5. B3 programme, candidate limb: `preview-product-boundary-successor.v9` (e0221a1c095f688d…) was REJECTED at Stage A by both reviewers (40 stale currency sentences carried inside the fourteen EE classes; the G23 partition; wording) — it stays frozen and unrecorded. `preview-product-boundary-successor.v10` (`8f34c92ef4fb835ce31945bfc73e1442b38dada1d483380231a53d1d93a03483`) landed all findings: **Stage A dual ACCEPT 0/0, both grade rulings SUSTAINED FOR APPLICATION** (the D-005-form grade question the B3 programme required; review files `docs/coop/artifacts/preview-product-boundary-successor.v10.review-independent.claude2.json` `34b3911340de88c3892b9fb840010ecc8605a884c8f3aa928d3d69cfad4c9d3b`, `…codex.json` `1cba1d43bfd6e45ba4c1a7703fb99a25c73ec9860f117cd7f63b2fe808faf842`).

## IN FLIGHT — D-295 (record preview-product-boundary-successor.v10 as DR-117 leftover remasurement)

- Turn 1: draft `docs/coop/artifacts/coordinator-decisions.D-295.draft.md` `0447e7e57b421ae3125b562951e4bf31579ef4b2d2ce612beced4fc661273bdd` (frozen 0444). Codex turn-1 verdict: OBJECT (CODEX-D295-SF1) (`9cdcd548b1cddad81b49ca5a4bd8b2eae78a8f80fd507a40efb63fff76a99413`). Claude 2 turn-1 review: NOT RETURNED — the Claude reviewer hit its weekly usage limit (resets 5 pm America/Los_Angeles, 2026-08-28) before writing; no file exists and none must be invented.
- Turn 2: draft `docs/coop/artifacts/coordinator-decisions.D-295.turn2.draft.md` `a8fdbf4e28de69ab8ffd25f389ddb32456064aa2476a309d5836611616bd7b62` (frozen) lands CODEX-D295-SF1 (the Subject paragraph no longer calls the fourteen classes byte-identical; they equal v8's after normalizing the 42 refreshed sites). Prompt `coordinator-decisions.D-295.turn2.review-prompt.md`; dispatch text `docs/coop/artifacts/_dispatch.D-295-t2.txt`. Codex turn-2: CONSENT 0/0. **Claude 2 turn-2: PENDING — dispatch `_dispatch.D-295-t2.txt` to a fresh `claude` pane after the limit resets.** Then, at dual CONSENT 0/0: `TURN=2 PPBS_V=10 python3 tools/orchestrator/ppbs/make-D295.py entry --apply` (or `tools/orchestrator/record-act.sh D-295 2 tools/orchestrator/ppbs/make-D295.py "D-295: record preview-product-boundary-successor.v10 as DR-117 leftover remasurement" tools/orchestrator/commit-body.D-295.txt` after fixing the scratch path — see `tools/orchestrator/README.md`). The commit includes: COORD, v9 + its prompt + both v9 reviews (rejected history), v10 + prompt + both reviews, both D-295 drafts, both prompts, all D-295 reviews. Never `_dispatch.*`.
- If turn 2 draws an OBJECT, turn 3 is the last; a fourth need would make D-295 CONTESTED (D-000 clause 2).

## Next acts, in order (all authorized by D-293; each is a normal Stage A + Stage B act unless noted)

1. Finish D-295 (above).
2. **Owner-controlled entries you cannot make** — ask the user: the DR-117 Class A opening entry (lifts D-137's reservation, restates the v10/contract.v8 relationship; D-293 Decision 5), then G29/G30 fixture authoring, then a separate DR-117 SATISFIED-GRADE + MF-6 cycle; the DR-131 / DR-133 openings on the B1/B2 programmes (`DECISIONS-RECOMMENDED.md` §B1, §B2). `DECISIONS-NEEDED.md` §G lists these and the other open owner questions.
3. **D1 fixture corpora** — plan in `DECISION-PACKETS/D1-plan/` (15 files + README, byte-cited). Recordable now: DR-122 SARIF binds (v2 successors of `sarif-fc-nonauth-term-bind.v1` / `sarif-fc-outfail-golden-bind.v1`; 3 of 4 cases; `FC-OUTFAIL.committed-run-preserved` stays NOT-AUTHORED under the §7.1 RunId park), G20 (40 members), G19 (48), OBL-HOSTILE-GOLDENS (16 units; per-class counts unenumerable — witnesses only), G21 (128 remaining). Sequenced behind the openings: G24–G28 (DR-131), G29/G30 (DR-117). Byte-blocked: G15 (adapter), G16 (unenumerated component axis), G18 (quarantine format reserved). Form: `g23-fixture-corpus.v4` field vocabulary, `g21-fixture-corpus.v1` first-authoring shape; the D-002 platform list must be QUOTED from an occupancy that carries a `platforms` array (G10 v2 / G23 v2), never typed. Builders for G20/G19/SARIF were started and died on the Claude weekly limit — nothing usable was left; start fresh from the plan files.
4. **C acts** — plan in `DECISION-PACKETS/C-plan/` (README §1 table, §2 order, §3 owner questions Q0–Q16). Unblocked and fully determined: C3-a (`compatibility-leftover-join.v3`, re-pin to live file 08; warrant is D-293's adoption of C3, not a staleness rule), C7-a(ii) (`component-manifest-leftover-join.v10`, OBL-OD-2 leftoverDesign true→false on the owner's final do-not-fold — the first RESERVED-value flip in the record; choose the summary bucket the D-174/D-240 precedents used and record the alternative as a named open decision), C9 a–d (G01 v10 / G02 v5 / G04 v5 occupancy successors + distribution-core-leftover-join.v10 carrying MB = 1e6 bytes; do NOT write derived byte constants — Q14 is the owner's), C1-a (signed-index-trust-contract.v9 + G08 occupancy v4 + signed-index-leftover-join.v5, OD-112-3 final fail-closed), C5-a and C6-a (COORD-only scope entries of the D-294 shape). Blocked on owner values: C3(ii), C4, C8; C2 needs no act.
5. **A3** last: identity-namespace leftover-join.v7 (cites G31 occupancy v5 (D-208) as the consumed specification, marks v2 historical, carries DR-104 `SATISFIED` verbatim, states it neither replaces nor reopens D-236; zero readiness effect).
6. F1 (docs rewrite) only after the five conditions are MET — nothing under `docs/` moves before the seal (`DECISION-PACKETS/F-docs-rewrite.md`).

## Protocol and house rules (do not drift)

- Independent dual adversarial review; refute, not confirm; ACCEPT/CONSENT only at 0 MUST-FIX / 0 SHOULD-FIX; ≤3 turns else CONTESTED; COORD-only append; no file-08 cell edit except a reviewed MF-6; chmod 0444 on frozen subjects, prompts and completed review files; commits contain only the act's files (subject, prompts, reviews, drafts, COORD) — never `_dispatch.*`; commit subjects are `D-NNN: …` (hygiene commits `D-NNN hygiene: …`).
- Wording defects reviewers REJECT on (every one cost a version this month): deictic "This vK" (speaker is always the current version); bare version tokens ("v8" alone — write "leftover-join.v8", "occupancy v4", "successor.v8"); "unchanged"/"byte-identical" claims about anything the successor rewrote — carry blocks that embed currency sentences by refreshing those sentences under D-294 and asserting equality after normalizing them (v9's rejection); number agreement with counts; dates from the clock; custody claims dated by the predecessor's bytes; recite observation shapes exactly (objects with/without id, strings with inline markers, `observations` vs `observationsNotFindings`) and name every reviewer identifier including advisories ("Advisories … travel as honesty work"); Protocol lines of turn ≥2 drafts name every identifier landed (D-284 form); a Stage A review's `custody` pins HEAD (Codex at `custody.environment.headAtOpen`).
- Stage B prompt for a turn ≥2 states that prior subjects remain frozen (paths + digests) and lists prior verdict identifiers.
- Decision-support packets (`DECISION-PACKETS/*`) are not record artifacts; the record is COORD + file 08 + `docs/coop/artifacts/`.

## Herdr topology at handoff

- Workspaces: `wD` (Claude orchestrator session `wD:p1`; per-review Claude tabs are created and closed), `wC` (Codex reviewers: `wC:p6` is the fresh `codex --yolo` pane in use — Codex `/status` shows context left; open a new pane below ~20%; `wC:p1`..`p5` are older sessions), `wA` (Grok). Verify with `herdr workspace list` / `herdr pane list --workspace wC`; never trust pane ids from memory.
- Claude reviewer per review: `herdr tab create --workspace wD --no-focus` → `herdr pane rename <pane> claude-review-<act>` → `herdr pane run <pane> claude` → `herdr wait agent-status <pane> --status idle --timeout 120000` → `herdr pane run <pane> "$(cat docs/coop/artifacts/_dispatch.<act>.txt)"` → if not `working` within 30 s, `herdr pane send-keys <pane> Enter` → `herdr wait agent-status <pane> --status done --timeout 590000` (re-issue after the cap). Close the tab after freezing the review file. If the Claude CLI prints "You've hit your weekly limit", no file is written — wait for the reset; never invent the verdict.
- Tooling: `tools/orchestrator/` (README explains the scratch-path constant that must be rewritten before use).

## Owner questions outstanding

See `DECISIONS-NEEDED.md` §G (added at handoff): Class A opening entries; C-plan Q0–Q16; D1 byte blockers; reviewer capacity. Nothing in the record answers them; do not guess.
