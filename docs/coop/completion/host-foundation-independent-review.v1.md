# Independent host foundation evidence review

**NO OBJECTION within the stated evidence scope.** Zero MUST-FIX and zero
SHOULD-FIX findings. Reviewer: Codex protocol fixture author, who authored none
of the six subject files. The reviewer authored older lock-schema dependencies;
this verdict applies to the new evidence unit and does not independently
reapprove those dependencies or the lead's normative document.

All six subject hashes and ten dependency hashes match the frozen roster.
The 210-check, 182-case replay passes and reproduces the retained report exactly.
Twenty-three independently authored probes also pass; their exact inputs and
outputs are retained in `host-foundation-independent-probes.v1.json`.

The probes challenge escaped duplicate JSON keys, nonfinite/oversized numbers,
fractional budgets, escaped strings, surrogates, invalid compiled/environment
inputs, layer-4 CI omission, interactive local-file errors, budget/provenance
precedence, presentation separation, global component restrictions, identical
pin/hold refusal at the actual solver boundary, filesystem I/O versus unsupported
classification, doctor no-fallback behavior, root ancestor/absence handling,
policy private modes, leaf symlinks, pre-read fencing and repository-policy
exclusion. They produced no uncontrolled exception or false admission within
these assertions.

The model's `CONFIGURATION-ONLY`, `analysisAdmitted: false` and `solverExecuted`
markers correctly distinguish resolution evidence from dependency admission.
Native account, executable and filesystem observations are expressly injected;
the temporary policy reader verifies leaf custody while ancestor custody and
fencing are trusted observations. This review makes no physical race,
filesystem, durable publication or supported-platform qualification claim.
Per-file policy digests are explicitly separate from the pending security v3
combined effective-policy digest. That integration remains outside this verdict.

The exact frozen subject roster, replay result and independent probe hashes are
in `host-foundation-independent-review.v1.json`. No reviewed bytes or live
register entries were changed.
