# Independent security review, turn 1

Reviewer: Codex lead; author of none of the security unit bytes. HEAD: 12f8783.
Subject: security-completion.v1.md SHA-256 a5bdd005660b7a566fdb3af60278f8bc140d26e4c493916b6cfbca47210e9004 and its dispatched vectors/checker/four fixture files. Proposed register edit: none.

Verdict: OBJECT, 8 MUST-FIX, 2 SHOULD-FIX. Independent checker replay passes its stated 15 vectors; that result does not discharge the findings below.

1. **SEC-M1 — evaluation time can move backwards (§4.2).** `max(wallClock,lastAcceptedIssuedAt)` does not preserve the highest evaluated time. After observing expiry in January without accepting another document, resetting the clock to December makes the same document unexpired. Persist a security evaluation high-water time before decisions; define restart/durability and future-time handling, and retain forward-then-back cases. Also reconcile §4.7's January continuation example with simultaneous stale revocation: the stated 90-day rule makes every role stale, so COMPONENT/CORE are not trusted as claimed.
2. **SEC-M2 — witness update violates locking and has an unprotected crash gap (§5.4).** A project-lock writer updates trust.sqlite, whose sole writer requires the lifecycle lock; acquiring it violates the specified order. Further, journal commit followed by witness update permits a crash between them and subsequent lost committed tail below the stale witness to pass. Define one implementable ordered writer protocol, including every durable transition and crash point. Separate databases are acceptable, but no cross-database WAL atomicity may be assumed. A write-ahead reservation/witness with fail-closed reconciliation is one possible repair. Do not perform the external effect until its authorization is protected by the completed protocol.
3. **SEC-M3 — restore rule cannot detect whole-store rollback (§5.1).** Restoring old trust.sqlite, documents and journal together makes all comparisons agree. State the actual rollback threat boundary and concrete backup/restore protocol. Preserve trust floors outside lifecycle backups and either require independently current authority on restore or use an independent witness. Do not claim global filesystem rollback detection from mutually restored files. Retain whole-set rollback and ordinary generation rollback cases.
4. **SEC-M4 — Linux selected profile contradicts v48 (§8.4).** v48.kexecAbsenceDerivation and selectedCapability require structurally kexec-absent kernel bytes plus builder attestation; presentState explicitly requires `/proc/sys/kernel/kexec_load_disabled` ABSENT and refuses its presence. Draft requires it equal 1 and Ubuntu stock kernel. This profile cannot qualify. Choose an actually implementable kernel/profile or an explicit reviewed semantic successor with equivalent current-kernel binding. Runtime disable alone is not the accepted proof. Also demonstrate a feasible macOS running-kernel attestation acquisition or name and justify a feasible successor; a future measurement placeholder cannot stand in for an unavailable acquisition API.
5. **SEC-M5 — carrier/body schemas remain authoring obligations (§§3.1,4.5,4.6,5.4,8).** Root, revocation, payload, envelope and journal are prose fragments, not complete closed schemas. Journal body has no per-record shapes; hash chain lacks exact byte encoding/genesis and omits a clear binding of all authorization-bearing columns; locator projectKey escaping is unspecified; operation_ref cannot depend on parked DR-006 while required in preview. Supply closed typed carriers, canonical chain preimage/locator rules, preview operational correlation semantics and valid/invalid examples. Supply four complete profile templates validated against the actual v48 grammar, with only explicitly typed release measurements deferred. `verifiedInstallRoot` is the actual preselection canonical host path, not automatically `/`. Clarify the two Linux certificate members versus §8.2's blanket absence claim.
6. **SEC-M6 — signing ceremony conflicts with the selected Node runtime (§3.2).** The prohibition on JIT/unsigned-memory entitlements covers every Mach-O, including bundled V8 Node. Apple documents that hardened runtime rejects MAP_JIT without allow-jit. Decide and substantiate per-binary/per-architecture entitlements or an explicit jitless runtime qualified by G13. Core may retain zero exceptions. Source: https://developer.apple.com/documentation/Apple-Silicon/porting-just-in-time-compilers-to-apple-silicon . Also remove the unconditional claim that tar extraction avoids Gatekeeper: extraction/tool/quarantine behavior is not a product security invariant.
7. **SEC-M7 — full manifest uses the wrong host capability registry (§2.2).** Five IDs parse-fidelity/semantic-resolution/graph-construction/facts/coverage with declarationData language/rungs/relations do not match analysis-quality-completion §2's typescript.imports/references/calls/types/reachability and closed {relation,rung}. Syntactic v11 validation alone is insufficient. Rebuild pair against that registry and retained admission checker, including a wrong-declaration negative. Keep [0.1.0,0.2.0), which is correct.
8. **SEC-M8 — unsigned envelope routing fields lack a closed binding (§2.2).** kind/domain/role/namespace are used to choose authorization but signature signs only the domain-separated subject digest. Specify and validate the closed kind-to-domain-to-role mapping and payload namespace/publisher binding, including namespace mismatch and role confusion negatives. Define malformed envelope precedence so a valid signature cannot be reinterpreted as authority for another role/namespace.

SHOULD-FIX:

- **SEC-S1:** freeze a retained machine-readable checker report with input/checker hashes and environment; PASS stdout alone is harder to join into a SATISFIED package. Explicitly disclose reference Unicode version versus production Unicode 15.1.
- **SEC-S2:** distinguish empty default PR-6 trigger set from explicit policy requiring confinement. If such a configured requirement is admissible, unenforceable enforcement must still refuse; trusted first-party status does not itself enforce confinement. If absent by closed preview schema, show that absence.

Accepted direction, subject to repairs: static-PIE musl core, SC-TRUST distinct from lifecycle rollback state, scoped consented trust refresh with root-bound SPKI, child-only execution, separated host/component outcomes, 2-of-3 ordinary thresholds and 3-of-5 recovery. Existing-scope deferred sub-obligations ride with re-entry. Fixture authoring in §6.7/§10 still must be completed before row application; no SATISFIED inference follows from this review.
