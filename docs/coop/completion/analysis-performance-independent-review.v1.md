# Analysis performance independent review v1

**OBJECT — 2 MUST, 0 SHOULD.** Reviewer authored none of the four frozen subjects.

Freeze SHA-256: `a236239b2cd00cf9e9b4c598f33f24309f7193cd42cf8f0a67027b92915f8a9a`. Four subjects and six direct dependencies match before/after review. All1,037 inherited quality-unit pins match. External replay is20/20, byte-identical.

- **APR-1 — Actual runner observations must satisfy the selected class.** The validator accepts a `macos-15` runner with independently observed RAM of1TiB,1byte, or OS version26.0, provided candidate and inventory agree. Trust establishes what was observed; it does not authorize changing D-102's class. Enforce the adopted7/14/16GiB±0.75GiB RAM bands and macOS15/Linux24.04 membership, with an explicit native-architecture/class acquisition precondition. Add observed out-of-class negatives; the retained forged-RAM test exercises only disagreement.
- **APR-2 — Repair the compiled matrix join.** The current pinned matrix still declares `workerVcpu:4` and `workerRamBytes:8589934592`. The successor replaces the prose sentence/schema, while its validator hashes and silently ignores these contradictory machine fields. Explicitly supersede those exact fields and validate the effective policy, or publish a matrix successor preserving all24 rows and native-corpus links.

Thirteen independent probes produced the three runner-policy counterexamples above; ten other checks confirm candidate-only mismatch refusal, matrix/corpus custody, absolute equality/overflow, rank29 behavior, RSS10% equality/plus-one and baseline-image mismatch. Exact inputs are retained in the JSON review.

WA-17's chain-only performance scope is explicit and defensible: the1000-module acyclic benchmark supplies performance observations; small native cyclic/acyclic fixtures remain mandatory correctness evidence. No numeric ceiling or regression weakening was found. Production acquisition, profile qualification and real measurements remain separate; no subject/register edits or SATISFIED claim.
