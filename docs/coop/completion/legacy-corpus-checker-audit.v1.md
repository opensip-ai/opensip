# Legacy corpus checker audit v1

**Universal legacy-corpus coverage is not yet demonstrated.** This bounded audit traced the47-obligation/86-ID-DEP evidence map and inspected active G01–G05, G07, G08, G12 and G22 joins. No large suites were rerun and no live record changed.

D-368 requires a retained checker and digest-pinned PASS report for fixture corpora used by the new package. An occupancy specification or initial-state naming catalog is not itself an authored executable corpus. Historical occupancy review counts establish specification consistency; they establish neither input/golden implementation nor product qualification.

| Gate | Current evidence | Remaining evidence action |
|---|---|---|
| G01–G05 | Active occupancies v11/v6/v5/v5/v4; old catalogs name13/13/22/16/22 states. G02 has6 accounting cases in the71-check distribution report; doctor G04 has a scorer in the159-check doctor/cache report. | No full active-family checker/report found. Classify every named state as design evidence or execution output. If claiming scoring completeness, retain bounded synthetic G01 container/size/exclusion, G03 timing/no-load, G04 help/version and G05 paired-delta cases. Reuse existing G02 and doctor scoring. |
| G07 | Checked archive/path vectors, lifecycle identity/lease/race checks and signed-byte evidence exist. | No complete23-state crosswalk found. Map8 archive/3 path states to current result IDs and show7 loader-substitution/5 TOCTOU states in existing or arriving host/lifecycle probes. Add only genuinely missing synthetic cases. |
| G08 | Behavior v3 covers all19 signed-trust classes;1415/1415 retained report. G15v2 adds actual TEST-signed metadata boundaries and3251/3251 report. | Pin exact result mappings for the4 connectivity classes and3 role histories; platform replication is execution, not another claimed run. Final security-v3 PASS report is still absent at this snapshot. |
| G12 | Behavior v3 maps all12 doctor classes with concrete case IDs; doctor/cache adds159 checks. | No class-level checker gap found. Carry those exact successor mappings and final security/doctor semantics into the application; do not reclassify real doctor execution as authoring. |
| G22 | Security v3 checker validates4 TCB profile templates. | Template validity alone does not prove hostile-event admission. Map or retain synthetic declared/incorrect identity, undeclared system-class, alternate-loader and missing/partial-trace cases. Actual build identities and loader traces remain qualification. |

The companion JSON pins active occupancies and lists exact legacy state IDs and identified gaps. It also records the existing D-106/G15 inherited schema crosswalk, which supplies substantially stronger evidence than a bare occupancy count.

For G01–G05, a small new design unit is sufficient: keep the five active occupancy pins, all86 named state IDs, an explicit DESIGN-MAPPED/EXECUTION-OUTPUT classification, synthetic scoring inputs and independent expected results, plus a retained checker/report. Consume the already decided D-006 bounds, G02 accounting and doctor G04 successor. Do not invent thresholds or claim real signed downloads, OS timings or RSS. In particular, retain or explicitly map the two named FC-G03 hostile fixture inputs; they are design inputs rather than timing measurements.

These are evidence-join and bounded authoring actions, not a request for production qualification. The final application reviewer must decide the exact state quantifiers; this audit makes no SATISFIED/closed finding.
