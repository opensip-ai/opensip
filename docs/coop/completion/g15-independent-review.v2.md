# G15 repair v2 independent review

**NO-OBJECTION-WITHIN-REPAIR-SCOPE — 0 MUST, 0 SHOULD.** G15-M1 is resolved. The reviewer authored none of this unit, including the parent-authored retained adversarial fixture.

Freeze SHA-256: `40ee1076f2f8026780513f0d57a1d47c2f3420bb657744586aa118fd17bbe55c`. All nine repair subjects, fifteen retained predecessor pins and source pins matched before and after review; the companion JSON records all 56 unique file hashes.

External replay passes **3,251/3,251**, byte-identical to the frozen report. All sixty original case objects are unchanged; the 960-slot matrix differs only in successor corpus paths. The separate 149-check resolver-core replay remains byte-identical.

Twenty-two independent probes pass. With valid authorized signatures and current catalog/root/store/policy custody, age 90 days minus one second and exact equality accept; plus one second and the exact retained 150-day repro refuse. Moving only the externally supplied evaluation time crosses the same boundary. Invalid calendar dates, year zero, leap second, offset spelling, trailing newline and malformed host-time values refuse. A fresh list with a broken threshold signature also refuses. Every refusal exposes no partial resolved array or lock fields, and tested input objects remain unchanged.

The admissibility preconditions are explicit: valid signature and custody, matching root/version/floors, valid UTC seconds, and `0 <= trusted evaluation time - issuedAt <= 90 days`. Expected outcomes were not used as authorizing inputs.

Root semantic admission remains a separate **pending security-v3 integration dependency**. This selector still imports its pinned v2 security library and consumes a fixed, already trusted host root. The contract assigns full trust-state/root rotation to the security owner and requires reviewed dependency bytes or a reviewed successor. Security v3’s `admit_root` is not executed by this G15 unit; this verdict does not close arbitrary-root admission or the eventual successor join. The unrelated parent security unit needs no reauthoring for this bounded repair verdict.

This is reference-model design evidence. Persistent clock publication, host authority, lifecycle durability, native OS qualification and release activation retain their separate owners. No subject or register was edited and no acceptance/SATISFIED state was assigned.
