# Decisions — agreed recommendations (Claude orchestrator + Codex), 2026-08-28

Process: `DECISION-PACKETS/RECOMMENDATION-PROTOCOL.md`. For each item: the byte-cited evidence packet, Claude's recommendation, Codex's independent adversarial review, and up to three rounds of reconciliation. **Nothing here is decided; every item is yours.** A verdict of AGREED means the latest Claude round was accepted by Codex without further amendment; SPLIT means both positions are shown; PENDING means a round is still in progress.


## Summary

| Item | Subject | Status |
|---|---|---|
| A1/A2 | Process rulings: D-272 (A1) and the nine superseded CONTESTED entries (A2) | AGREED |
| A3 | identity-namespace leftover-join.v6 (DR-104): remasure or leave | AGREED |
| A4 | Cross-lineage citation convention (precedent vs content-based) | AGREED (adopt; Codex round-3 amendments applied post-round — see note) |
| B1 | DR-131 Class A opening (preview-analyze-contract.v2) | AGREED |
| B2 | DR-133 Class A opening (provider-only-output-contract.v3) | AGREED |
| B3 | DR-117 Class A opening (preview-product-boundary-successor.v8) | AGREED |
| C1–C4 | Reserved numbers/lists: DR-112, DR-118, DR-111, DR-126 | AGREED |
| C5–C9 | Reserved encodings/owners/units: DR-121, DR-107, DR-103 OD-1/OD-2, DR-101 OD-101-1/2, DR-115/D-006 | AGREED |
| D1 | Fixture-authoring delegation (39 measurements) | AGREED |
| E2 | Push the 12 unpushed commits | AGREED |
| F1 | Documentation rewrite after sealing | AGREED (adopt; Codex round-3 amendments applied post-round — see note) |


## A1/A2 — Process rulings: D-272 (A1) and the nine superseded CONTESTED entries (A2)

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/A-process-rulings.md`


### Recommendation (Claude, round 2)

# A1 / A2 — Claude round 2 (answering Codex's AGREE-WITH-AMENDMENT)
**Both amendments adopted. Agreed recommendation:** A1 → option **(a) only** (D-273 stands; D-272 stays CONTESTED-parked; no D-000 amendment — D-000 clause 2 already mandates termination after three exchanges, and D-093 is a same-turn freeze-precondition re-dispatch, not a fourth-turn exception, so option (c1) would add ambiguity rather than remove it). A2 → option **(ii)**: one direct user-made entry (D-054 form, recorded verbatim) confirming that the nine parked CONTESTED headings remain historical, that their ADOPTED resolvers stand, and that none is reopened; no heading is relabelled or rewritten. Confidence: high.


### Codex (round 2): AGREE — confidence high

A1: choose option (a) only; D-273 stands, D-272 remains CONTESTED history, and D-000 is not amended. A2: choose option (ii); use one direct user-made entry in the D-054 form to confirm that the nine parked CONTESTED headings remain historical, their adopted resolvers stand, and none is reopened, without relabelling or rewriting any heading.

*Rationale:* Claude's round-2 text adopts both round-1 amendments faithfully. It no longer treats D-093 as a fourth-turn exception or proposes option (c1), matching D-000's mandatory three-exchange termination language and D-093's express same-turn freeze-precondition characterization. It also preserves the distinct A2 recommendation: a direct user disposition records the completed batch review while leaving the parked entries and their adopted successor chains unchanged. I found no new factual claim or omitted condition that conflicts with D-000, D-054, D-093, D-272, D-273, or the mapped A2 resolver entries.


## A3 — identity-namespace leftover-join.v6 (DR-104): remasure or leave

**Status: AGREED**


### Recommendation (Claude, round 2)

# A3 — identity-namespace leftover-join.v6 — Claude round 2 (answering Codex's DISAGREE)

**Claude adopts Codex's position. Agreed recommendation: REMASURE identity-namespace leftover-join.v6 → v7 as a bookkeeping successor** that (i) cites G31 occupancy v5 (D-208) as the consumed specification and marks v2 historical, (ii) re-pins live file 08 and carries DR-104's live token `SATISFIED` verbatim, (iii) states explicitly that it neither replaces nor reopens D-236 (which already says "leftover-join.v6 binds NOTHING and is not this SATISFIED"), and (iv) goes through the normal Stage A + Stage B dual review with zero readiness effect.

Refutations accepted:
1. "Every remasurement D-261..D-292 targeted an OPEN row" — false as stated: D-273 (DR-118 `DECIDED-V1-NOT-INTEGRATED`) and D-275 (DR-107 `PROPOSED-CLOSED-FOR-REVIEW`) targeted non-OPEN rows. Corrected claim: none targeted a `SATISFIED` row — which argues for explicit scope language in the entry, not for leaving the mismatch.
2. Leaving the sole remaining occupancy-specification mismatch in place would be an unexplained exception to the occupancy-supersession trigger that the A4 proposal itself retains; the record is cleaner with the trigger applied uniformly.
3. The "~40 min" cost and the "material CONTESTED risk" were my estimates, not bytes; withdrawn.

Amendment kept from Claude's round 1: this is the lowest-priority act in the queue (zero readiness effect); it should run after the owner's decisions are recorded, not before.
Confidence: high.


### Codex (round 2): AGREE — confidence high

REMASURE identity-namespace-leftover-join.v6 as v7, at the lowest priority, solely as a bookkeeping successor. The successor should consume the current G31 occupancy v5 recorded at D-208, mark v2 historical, re-pin live file 08 and carry DR-104's live SATISFIED token verbatim, state explicitly that it neither replaces nor reopens D-236, and pass the normal Stage A plus Stage B dual review with zero readiness effect.

*Rationale:* The contrary case does not survive the current bytes. identity-namespace-leftover-join.v6 consumes G31 specification v2, while D-208 records v5 as current and v2 as historical. Its own file-08 pin and DECIDED-V1-NOT-INTEGRATED token also predate D-236. D-236 then cleanly separates the lineages: it records DR-104 SATISFIED, leaves G31 execution at condition 4, and says leftover-join.v6 binds NOTHING and is not the SATISFIED recording. A v7 can therefore repair current custody without touching the decision that supplies SATISFIED. Zero readiness effect supports Claude's retained lowest-priority ordering; it does not support preserving the sole occupancy-specification mismatch.


## A4 — Cross-lineage citation convention (precedent vs content-based)

**Status: AGREED (adopt; Codex round-3 amendments applied post-round — see note)**


### Recommendation (Claude, round 3)

# A4 — Claude round 3 (final; answering Codex's round-2 AGREE-WITH-AMENDMENT)
All round-2 corrections applied to the generator and the proposal regenerated (`PROPOSAL.cross-citation-convention.md`):
1. Provenance table path is `docs/coop/COORDINATOR-DECISIONS.md`; "Overturn: C-D293" (token derived from the entry number, not carried).
2. D-282 through D-287 described as five ROW successors plus the DR-G07 GATE successor (exact-bytes).
3. Citation grammar is semantic: `remains/remain/is/are (the) current` and `still measure(s)`, dotted or spaced version spelling; sites recomputed (26 present-tense sites across the ten pairs; the named-obligation extraction re-run over all of them).
4. ROW→ROW tolerance precedents recorded: D-282 kept packaging v4 current while superseding component-manifest v6; D-285 kept permission v12 current while superseding doctor-actor v11.
5. D-270, D-271 and D-274 classified with the citation-driven GATE successors (each per its own basedOn.predecessor role); the "occupancy remasurements that refreshed in passing" list reduced to D-261, D-264, D-282–D-286.
6. Mechanical projection widened and computed for every pair: leftoverDesign partition equality; the cited join's registerRow unchanged; named-obligation presence and value; every "still measures … leftoverDesign true/false" assertion checked against the successor's flag; and the entry states that any other asserted cross-lineage fact (owner, status token, non-stealing statement) must read the same in the successor's bytes.
Agreed recommendation unchanged: ADOPT the symmetric content-based reading via one RULE-GOVERNED entry through the dual-CONSENT cycle. Confidence: high.


### Codex (round 3): AGREE-WITH-AMENDMENT — confidence high

ADOPT the symmetric recording-time/content-based citation convention only after correcting the citation grammar/count and completing the promised mechanical projection. A sibling version change alone should not require a successor; own-lineage supersession, supersession of an occupancy consumed as specification, or a change in any relied-on cross-lineage fact should require one.

*Rationale:* The central convention remains the better reading: the recorded D-282 and D-285 ROW-to-ROW tolerance precedents join the earlier directional precedents, and all ten currently disputed predecessor/successor pairs preserve registerRow and leftoverDesign while the extracted obligation assertions preserve presence and value. Claude's provenance path, five-ROW-plus-DR-G07 wording, D-270/D-271/D-274 classification, ROW-to-ROW precedent discussion, and C-D293 token now match the bytes. The round-3 claim that every amendment was applied nevertheless fails in two material places. First, the stated dotted-or-spaced semantic grammar produces 27 sites, not 26: the packaging row omits its spaced-form remeasurementClause citation, and neither the measurement-rule paragraph nor Decision clause 1 actually states the full widened grammar. Second, clause 2(b) gives exact comparisons for partitions, registerRow, and obligation flags but leaves owner, status-token, and non-stealing facts to the undefined phrase 'reads the same'; that is not a mechanical field/value projection. The convention should therefore be adopted with those corrections, not in the regenerated form unchanged.


*Amendments:*
- State the widened grammar normatively in both the measurement rule and Decision clause 1, covering remains/remain/is/are with optional 'the' before current and still measure/measures for dotted or spaced version spelling; change the packaging Sites value to 3 and the total to 27.
- Replace clause 2(b)'s 'reads the same' catch-all with exact source fields and equality rules for any asserted owner, status-token, routing, and non-stealing fact, and require a successor whenever any projected value changes.


### Post-round note (Claude)

# A4 — post-round note (Claude; not a fourth round)

Codex's round-3 verdict was AGREE-WITH-AMENDMENT on the proposal text (it agrees the convention should be adopted). The protocol allows no fourth exchange, so the two residual amendments were applied to the generator and to `PROPOSAL.cross-citation-convention.md` without a further Codex reply, for the owner to see:

1. **Citation grammar stated normatively** in both the measurement rule and Decision clause 1 — `remains/remain/is/are (the) current` and `still measure(s)`, over dotted (`leftover-join.vN`) and spaced (`leftover-join vN`) version spelling; sites recomputed from bytes.
2. **Projection fields named exactly** in Decision clause 2(b): for the cited join — `registerRow`, `file08StatusToken`, `liveGateOwners`, each compared wherever the cited version carries the field (a field the successor adds is not a value the citer relied on: component-manifest leftover-join.v6 carries no `file08StatusToken`, v9 carries `OPEN`); for every obligation id a citing sentence names — presence, `leftoverDesign`, `existingGate`, `rideStanding`, `executionObligationOwnerToday`; plus the asserted value of every "still measures … leftoverDesign true/false" sentence. A successor is required whenever any projected value changes. The generator computes and asserts every one of these for all current pairs.

If the owner adopts A4, the D-000 cycle on this proposal is where Codex re-checks these two points as a reviewer.

Numbering: the exchange above refers to the convention draft as D-293. The owner's decision entry took D-293 on 2026-08-28, so the draft is regenerated as **D-294** (its number is assigned only when it is dispatched; nothing else in it changes).


## B1 — DR-131 Class A opening (preview-analyze-contract.v2)

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/B1-DR-131-class-A.md`


### Recommendation (Claude, round 2)

# B1 — DR-131 — Claude round 2 (answering Codex's AGREE-WITH-AMENDMENT)
**All four amendments adopted. Agreed recommendation:** Option 3(a) + 3(b), then Option 1, as one programme shared with DR-133 and DR-117:
1. One shared gate-2 entry that states a common interpretation of D-056 gate 2 (declaring whether it interprets D-056 or is a scoped successor amendment) and then measures each of DR-131, DR-133, DR-117 separately from bytes; it reconciles the row-level findings (D-154; D-147; D-207) with the current gate joins' FX-AUTHORING flags expressly, and does not describe D-249..D-253 as silently overturning D-154.
2. A fresh application-grade dual review with the NEUTRAL question of the exact final DR-131 contract bytes the opening entry will cite (v2 `081ff7fb…` unless a successor is authored, in which case the successor).
3. The T2-02 opening entry per row, citing those verdicts and the owner's product word.
4. G24–G28 fixture authoring (D1) before the separate DR-131 SATISFIED-GRADE + MF-6 cycle — as conservative sequencing, not as a precedent-proven automatic reopening of gate 2.
Withdrawn from round 1: the categorical claim that D-154 and D-249..D-253 are contradictory, and that the opening has no value beyond unlocking SATISFIED-GRADE (it also resolves the disputed T2-02 property and records the owner-controlled disposition). Confidence: medium-high.


### Codex (round 2): AGREE — confidence medium

Choose Option 3(a) and Option 3(b), then Option 1. Use one shared Gate-2 entry only as a common rule with separately evidenced findings for DR-131, DR-133, and DR-117, expressly stating whether it interprets D-056 or is a scoped successor amendment. Obtain fresh application-grade dual review of the exact final DR-131 contract bytes cited by the opening entry. Then use a separate DR-131 opening entry, author the G24-G28 fixtures as conservative sequencing, and conduct a later separate DR-131 SATISFIED-GRADE plus MF-6 cycle.

*Rationale:* Claude's round-2 text adopts all four amendments faithfully and remains consistent with the bytes. It withdraws the claimed automatic contradiction between D-154 and D-249 through D-253, requires the shared ruling to reconcile row-level closure with gate-side FX-AUTHORING, binds the fresh review to the final artifact digest, and labels pre-cycle fixture authoring as conservative rather than as a precedent-proven Gate-2 reopening. It also withdraws the claim that the opening has no independent legal value. D-000 contains no one-row-per-entry restriction, while D-056 still requires the later SATISFIED-GRADE review of each row, so the revised shared-entry/per-row-cycle structure is lawful as stated.


## B2 — DR-133 Class A opening (provider-only-output-contract.v3)

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/B2-DR-133-class-A.md`


### Recommendation (Claude, round 2)

# B2 — DR-133 — Claude round 2 (answering Codex's AGREE-WITH-AMENDMENT)
**All four amendments adopted. Agreed recommendation:** Option 3 → Class A opening → Option 2's pre-SATISFIED-GRADE sequencing, with:
- the fresh application-grade dual review targeting whichever artifact the opening entry cites (v3 `ef2a7416…`, or a v4 successor if the three advisories are landed);
- a separate reviewed disposition of the candidate's proposed file-01 preview-role delta (the existing Claude verdict says recording it is a separate reviewed act and D-136 did not perform it), or removal of reliance on that delta, before the SATISFIED-GRADE cycle;
- NT-6 authoring treated as established work, and the NT-4/NT-7 fixture standing at G20 byte-resolved before deciding what authoring is required there;
- in the shared gate-2 entry, reconciliation of D-147's row-level finding with the current G20/G21 joins rather than any "silent supersession" language.
Withdrawn: "Option 4 has no byte-recorded 'what must change'" (the file-01 delta and NT-4/NT-7 standing are such items). Confidence: medium-high.


### Codex (round 2): AGREE — confidence medium

Choose Option 3, then the Class A opening, with Option 2's pre-SATISFIED-GRADE sequencing. The fresh application-grade dual review must target the exact artifact version cited by the opening entry. Before the later per-row cycle, separately record the proposed file-01 preview-role delta or remove reliance on it, author NT-6, byte-resolve NT-4/NT-7 fixture standing at G20 and author what remains unauthored, and make the shared Gate-2 entry reconcile D-147 with the current G20/G21 joins through a distinct DR-133 finding.

*Rationale:* Claude's round-2 text adopts all four amendments faithfully. It preserves final-byte review binding, adds the separate file-01 delta disposition or removal of reliance, keeps NT-6 authoring and the NT-4/NT-7 byte check in the pre-cycle work, and rejects silent supersession of D-147. The reference to Option 2 makes clear that NT-6 authoring is work to perform before the later cycle, not a claim that the fixture already exists. It also withdraws the false round-1 statement that Option 4 had no byte-recorded change candidates beyond advisories. I found no remaining mismatch with the v3 reviews, D-136, D-147, D-292, or the current G20/G21 join bytes.


## B3 — DR-117 Class A opening (preview-product-boundary-successor.v8)

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/B3-DR-117-class-A.md`


### Recommendation (Claude, round 2)

# B3 — DR-117 — Claude round 2 (answering Codex's AGREE-WITH-AMENDMENT)
**All four amendments adopted. Agreed recommendation:** Option C with **both C1 and C2 on the current record, C1 first**: author `preview-product-boundary-successor.v9` re-citing the twelve current joins and stating its relationship to `product-boundary-successor-contract.v8` (D-116); then a fresh application-grade dual review bound to v9's final digest; then the owner-controlled opening entry (which lifts D-137's express reservation with the owner's word and restates the v9/contract.v8 relationship); then G29/G30 fixture authoring (D1); then a separate DR-117 SATISFIED-GRADE + MF-6 cycle. The shared gate-2 entry carries a distinct DR-117 finding.
Withdrawn: making C1 conditional on A4 — A4 is not adopted, is written for recorded leftover-join citations, and the record does not say it reaches v8, which has no leftoverDesign partition; and "the only gate-1 issue is the missing verdict" — D-137's reservation must also be lifted by the owner-controlled act. Confidence: medium-high.


### Codex (round 2): AGREE — confidence medium

Choose Option C with both C1 and C2 on the current record, in that order: author preview-product-boundary-successor.v9 against the twelve current joins and state its relationship to product-boundary-successor-contract.v8; obtain fresh application-grade dual review bound to v9's final digest; use the owner-controlled opening entry to lift D-137's reservation and preserve that artifact relationship; author the G29/G30 fixtures; then use a separate DR-117 SATISFIED-GRADE plus MF-6 cycle. The shared Gate-2 entry must contain a distinct DR-117 finding.

*Rationale:* Claude's round-2 text adopts all four amendments faithfully. It removes the unsupported A4 waiver, orders C1 before C2, binds application-grade review to v9 rather than superseded v8 bytes, requires both v9 and the opening entry to resolve the D-116 contract relationship, and retains a distinct DR-117 Gate-2 finding followed by a separate row cycle. It also withdraws the claim that review grade was the only Gate-1 issue and expressly carries the owner-controlled lift of D-137's reservation. These statements remain consistent with D-207, the two v8 verdicts, v8's remeasurement clause, the twelve recorded join successors, and the G29/G30 fixture-authoring bytes.


## C1–C4 — Reserved numbers/lists: DR-112, DR-118, DR-111, DR-126

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/C1-4-reserved-numbers-security-quality.md`


### Recommendation (Claude, round 3)

# C1–C4 — Claude round 3 (final; answering Codex's round-2 AGREE-WITH-AMENDMENT)
All three amendments adopted on top of round 2:
- **C1 authority split:** OD-112-1 and OD-112-2 stay under DR-112's `Security + operations` authority; OD-112-4 is expressly reserved to product/release (signed-index-trust-contract.v8 `namedOpenDecisions`); OD-112-3 recorded now as the final fail-closed policy. Any later parking disposition for OD-112-1/2/4 must name a real trigger and state that it has no Condition-2 or D-056 eligibility effect without a separate reviewed act and a successor join.
- **C2, C3:** unchanged from round 2.
- **C4:** the "G22 qualification evidence" phrase for `ikconfigParserVectors` / `NT-TCB-KEXEC` is candidate standing only, not an operative routing: on the current join they remain unresolved design (existingGate none, execution owner none, not-capable-of-riding) unless governing evidence is authored; owner assignment, then an application-grade TCB successor that makes the grammar governing, then complete profile population.
Confidence: high.


### Codex (round 3): AGREE — confidence high

Adopt Claude's round-3 recommendation on top of round 2. For C1, record OD-112-3 as final fail-closed; keep OD-112-1 and OD-112-2 under Security + operations and OD-112-4 under product/release, with any later parking naming a real trigger and having no Condition-2 or D-056 eligibility effect absent a separate reviewed act and successor join. Retain the agreed C2 matrix/corpus, threshold-approval, and G13 sequence and the agreed C3 live-file-08 remasurement plus coherent evaluable windows. For C4, treat the G22-evidence wording as non-operative candidate standing only; assign the population owner, adopt an application-grade successor that makes the grammar governing, populate complete profiles, and leave ikconfigParserVectors and NT-TCB-KEXEC unresolved unless their governing evidence is authored.

*Rationale:* Claude's final text now states all three round-2 amendments faithfully. File 08 assigns DR-112 to Security + operations, while signed-index-trust-contract.v8 namedOpenDecisions and auditAndWaiver reserve OD-112-4 and its duration to product/release; OD-112-3 remains the named preview-refuse policy that the recommendation would make final. signed-index-leftover-join.v4 keeps the reserved-number obligation outside any existing execution route. The C2 and C3 positions remain supported by their current joins and owner cells. For C4, platform-tcb-leftover-join.v9 places the named vectors/tests inside OBL-RESERVED-TABLES with leftoverDesign true, existingGate none, execution owner none, and not-capable-of-riding, while platform-tcb-contract.v45 is CANDIDATE-NOT-APPLIED and binds NOTHING. Claude now expressly preserves that distinction and conditions any governing effect on the application-grade successor and later evidence authoring.


## C5–C9 — Reserved encodings/owners/units: DR-121, DR-107, DR-103 OD-1/OD-2, DR-101 OD-101-1/2, DR-115/D-006

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/C5-9-reserved-encodings-owners-units.md`


### Recommendation (Claude, round 2)

# C5–C9 — Claude round 2 (answering Codex's DISAGREE)

**Claude concedes the sufficiency point (as in C1–C4) and adopts Codex's per-sub-item positions.** A D-006-shaped sub-row deferral records a decision; it does not by itself move a leftoverDesign obligation out of D-056 gate 2 or count toward Condition 2. Agreed:
- **C5 DR-121:** a reviewed DR-121 architecture-scope act classifies the six `reservedForBlueprint` members as implementation encodings (ownership record stays the impact authority); concrete encodings move after Condition 5 only if a scoped D-056/Condition-2 successor expressly accepts that classification and a successor join remeasures; until then `OBL-CI-ENCODING-RESERVED` stays leftoverDesign true.
- **C6 DR-107:** the same architecture-scope/eligibility sequence for the on-disk formats, lock grammar, lease, solver, layout. **No new atomic-rename-equivalent admissibility policy** — live file 04 already permits "same-filesystem atomic rename or a reviewed equivalent" and any successor must prove the recorded properties regardless of mechanism (my round-1 addition withdrawn).
- **C7 OD-1:** assign to DR-115's `Product + release engineering` authority as a scoped D-006 threshold-family successor; do not import DR-G05's first-component-acceptance deferral automatically; record measured manifest-byte / tree-entry / path-length / alias-count caps before authoring oversized-input fixtures; if values are not available, leave the numeric limb open and say so. **C7 OD-2:** a final **do-not-fold** disposition (the candidate schema itself says the class-specific DR-111 statements are unambiguous); remeasure `OBL-OD-2` closed by that design choice; name OD-2 with OD-1 at the next MF-6. My round-1 "fold, delegated" withdrawn.
- **C8 OD-101-1:** resolve the core implementation language before the core-implementation blueprint through a dedicated D-000 successor that compares candidates against DR-101's constraints, recorded under `Architecture + release engineering` (PREFERENCE-LADEN, cheap overturn); the owner may instead state the choice directly — both are lawful; "the owner's alone" and "gates G01–G05 fixtures" withdrawn. **C8 OD-101-2:** its own DR-101 successor covering core code-signing and platform notarization, owned separately from DR-112's trust-recovery mechanics (reviews may be coordinated with C1 but not merged); a deferral to C1 does not close `OBL-D2`.
- **C9 DR-115/D-006:** record now that MB means 1e6 bytes for the D-006 G01/G02/G04 quantities (the record-named decimal reading); leave G02 installed-tree accounting open until `Product + release engineering` and `Architecture + release` record a complete rule covering logical lengths, allocated blocks, metadata/xattrs, links, and deduplicated inventory nodes; do not invent it.
Cross-cutting amendments adopted: any post-Condition-5 encoding disposition is paired with an explicit scope/eligibility ruling and successor-join measurement; no CANDIDATE-NOT-APPLIED contract is treated as binding authority without an application-grade successor act; DR-101 core signing and DR-112 trust recovery stay separately owned.
Withdrawn: "D-132 makes OD-101-1 the user's alone", "D-134 supplies deferral authority". Confidence: high.


### Codex (round 2): AGREE — confidence high

Adopt Claude's round-2 recommendation. For C5 and C6, classify the reserved encodings through reviewed architecture-scope acts and require an express scope/eligibility successor plus successor-join remeasurement before treating any concrete encoding as post-Condition-5 implementation work; preserve the live atomic-rename-or-reviewed-equivalent rule. For C7, assign OD-1 to DR-115's Product + release engineering authority, require measured caps before oversized-input fixtures, and resolve OD-2 by a final do-not-fold disposition. For C8, decide OD-101-1 before the core implementation blueprint under Architecture + release engineering, normally through a dedicated D-000 successor, while keeping OD-101-2 in its own DR-101 signing/notarization successor rather than merging it with DR-112. For C9, record MB as 1e6 bytes for the D-006 G01/G02/G04 quantities and leave G02 installed-tree accounting open until both named authorities supply a complete rule for every recorded accounting dimension.

*Rationale:* Claude now preserves every material round-1 position. monorepo-leftover-join.v4 and lifecycle-leftover-join.v4 keep their encoding obligations as leftoverDesign with no gate, no execution owner, and not-capable-of-riding, while their contracts are CANDIDATE-NOT-APPLIED or otherwise require a successor to prove the recorded properties. Live file 04 already admits same-filesystem atomic rename or a reviewed equivalent, so no new admissibility policy is needed. component-manifest-schemas.v11 makes OD-1's four caps product-owned and leaves oversized-input refusal unspecified until caps exist; its OD-2 object says the class-specific DR-111 statements are already unambiguous, and the current join keeps both decisions open pending a recorded act and remeasurement. File 08 assigns DR-115 to Product + release engineering and DR-101 to Architecture + release engineering. distribution-core-inventory-contract.v16 and distribution-core-leftover-join.v9 keep OD-101-1 and OD-101-2 separate, with DR-112 and DR-110 adjacent rather than owning core signing/notarization. core-gate-harness-specifications.v4 separately records the MB conversion and G02 accounting as undecided and names the same unresolved accounting dimensions Claude preserves. D-056's execution-only gates and D-132/D-134's limited scopes support the corrected cross-cutting treatment.


## D1 — Fixture-authoring delegation (39 measurements)

**Status: AGREED**

Evidence packet: `DECISION-PACKETS/D1-fixture-authoring-delegation.md`


### Recommendation (Claude, round 3)

# D1 — Claude round 3 (final; answering Codex's round-2 AGREE-WITH-AMENDMENT)
Amendment adopted: **`OBL-UNICODE-NORM` (the DR-103 schema-successor obligation, component-manifest-leftover-join.v9: leftoverDesign true, existingGate none, execution owner none, not-capable-of-riding) is added explicitly to the owner-reserved list.** The G09/DR-105 decision-record envelope is a separate reserved item, not a substitute for it. My round-1 "delegate the OBL-UNICODE-NORM schema successor" is withdrawn.
Final agreed reserved list: gate obligations at G07, G08, G09, G12, G14, G22 (each applying to the same obligation id on every current ROW twin); `OBL-WINDOWS-PATH`, `OBL-ENVELOPE-MISMATCH`, `OBL-UNICODE-NORM`, `OBL-JOIN-FX-AUTHORING`, `OBL-R10-AUTHORING`, `OBL-R6-AUTHORING`; the G09/DR-105 decision-record envelope. G10 stays selector-blocked (not security-owned); G22 stays reserved until C4 resolves. Everything else in round 2 stands. Confidence: high.


### Codex (round 3): AGREE — confidence high

Adopt Claude's round-3 recommendation together with the unchanged round-2 dependency-aware Option (c) split. Delegate only the enumerated G15, G16, G18, G19, G20, G21, G24-G30, OBL-HOSTILE-GOLDENS, and two DR-122 SARIF fixture obligations under the agreed semantic, coverage, dependency, and D-000 constraints. Reserve G07, G08, G09, G12, G14, and G22 with every current same-id ROW twin; also reserve OBL-WINDOWS-PATH, OBL-ENVELOPE-MISMATCH, OBL-UNICODE-NORM, OBL-JOIN-FX-AUTHORING, OBL-R10-AUTHORING, OBL-R6-AUTHORING, and the separate G09/DR-105 decision-record envelope. Keep G10 selector-blocked rather than security-reserved, keep G22 reserved until its C4 dependency is resolved, and retain C7's no-fold disposition for OD-2.

*Rationale:* Claude's final reserved list now faithfully includes the omitted distinct obligation and preserves the rest of the agreed split. component-manifest-leftover-join.v9 records OBL-UNICODE-NORM with leftoverDesign true, existingGate none, executionObligationOwnerToday none, and rideStanding not-capable-of-riding; its reason requires a later schema successor before the member can be scored. The G09/DR-105 decision-record envelope remains a separate obligation rather than a substitute. File 08 names security-inclusive owners for G07, G08, G09, G12, G14, and G22, while G10 is owned by Protocol + semantic owners and is hard-blocked pending selector refresh. No identifier, owner classification, or obligation standing in Claude's final text conflicts with those bytes.


## E2 — Push the 12 unpushed commits

**Status: AGREED**


### Recommendation (Claude, round 2)

# E2 — Claude round 2 (answering Codex's AGREE-WITH-AMENDMENT)
**Amendment adopted. Agreed recommendation: do not push now; push only after the owner has read DECISIONS-RECOMMENDED.md and explicitly approves publishing the exact record at HEAD `4abb961aad98525ca8b992a24609a6286964a451`.** Preconditions at push time: `git fetch origin` and require `main` to be a non-diverged fast-forward of `origin/main` (if either ref moved, re-audit the range before pushing); then a normal non-force `git push` — no rebase, no squash — preserving all twelve commit identities (D-282..D-292 plus `9d04151`, whose full hash is pinned by `coordinator-decisions.D-286.draft.md` and `exact-bytes-leftover-join.v7.json`). Withdrawn: "pushing before review would only matter if another machine needed the record" (the record does not establish exclusivity). Confidence: high.


### Codex (round 2): AGREE — confidence high

Do not push now. Push only after the owner has read DECISIONS-RECOMMENDED.md and explicitly approved publishing the exact record at HEAD 4abb961aad98525ca8b992a24609a6286964a451. At push time, run git fetch origin, require local main to be a non-diverged fast-forward of origin/main, and re-audit if either ref moved. Then use a normal non-force push with no rebase or squash, preserving all twelve commit identities.

*Rationale:* Claude's round-2 recommendation adopts every material round-1 amendment: explicit owner approval of the exact HEAD, a fresh origin fetch, a non-divergence/fast-forward check, re-audit on ref movement, a non-force push, and preservation of the twelve commit identities. It also withdraws the unsupported claim that an early push could matter only to another machine. The adversarial countercase therefore has no remaining byte or operational basis. HEAD remains 4abb961aad98525ca8b992a24609a6286964a451 and the current cached graph remains 0 behind and 12 ahead, but the required push-time fetch correctly treats that graph as non-authoritative for future remote state. The current instruction still forbids pushing.


## F1 — Documentation rewrite after sealing

**Status: AGREED (adopt; Codex round-3 amendments applied post-round — see note)**

Evidence packet: `DECISION-PACKETS/F-docs-rewrite.md`


### Recommendation (Claude, round 3)

# F1 — Claude round 3 (final; answering Codex's round-2 AGREE-WITH-AMENDMENT)

All three round-2 amendments are correct and are adopted. An independent re-verification of the round-1 repairs (`DECISION-PACKETS/.verify/F-docs-rewrite.md.reverify2.json`) reached the same conclusion as amendment 1 by another route and added six defects of its own; all are repaired in the packet together (the dispatch text pins the new sha256; §0.3 gains one row per repair). Nothing else in the reconciled plan changes.

## Codex round-2 amendments — adopted

| Codex r2 # | Point | Adopted as |
|---|---|---|
| 1 (refutations 4, 5, 6) | The measured commit cannot be both the already-known input to the measurement and the seal's parent, because the D-000 measurement act is its own decision commit; the moved archive is the seal commit's `docs/` tree, not the measured one; a mapping predicate keyed to the measured commit misses the measurement artifact's own path | **Four states, four names.** `<P>` = the already-known measured commit (five conditions `MET` there); `<R>` = the D-000 measurement-record commit (measurement artifact + penultimate COORD heading, both pinned to `<P>`); `<S>` = the user-seal commit (last heading; parent `<R>`; cites `<P>` and the measurement digest, never its own id nor `<M>`); `<M>` = the pure move. The archive is byte-identical to `docs/` **at `<S>`** (terminal COORD and the measurement artifact included). Path mapping applies to **tracked paths present in the `<S>` snapshot immediately before the move**; the absent `docs/v2/implementation/` remains the explicit literal exception (5-A as amended; 5-C only for a genuinely different live path). `docs/ARCHIVE.md` records `<P>`, `<R>`, `<S>`, `<M>` separately; the archive rule reads "nothing changes after the authorized pure move". |
| 2 (refutations 8, 9) | D-000 clause 4 is "commit **and push** per decision" (COORD L39–L42); the two-revert overturn is true only before dependent work exists | **Pushes in the sequence**: commit+push `<R>` before the seal; commit+push `<S>` before `<M>`; `<M>` and the later `docs/` commits pushed under the same rule. **Overturn qualified and ordered**: revert the front-door/metadata commits, then `<M>`, then `<S>`; once derived pages or successor decisions exist they are reversed or superseded first. |
| 3 (refutation 7) | §0.3 cited the intermediate, unretained packet state `3af2c81e…` as "pre-correction" | **Provenance corrected**: §0.3 cites the round-1 pinned subject `29d7a11c9a9ecd59ec9507d484a89eb1bfd4f2f48c86e415f5242efa4ec0d202` (the bytes Codex reviewed) and names the intermediate state honestly: after round 1, an independent re-verification of the 15 verifier fixes (`.verify/F-docs-rewrite.md.reverify1.json`) produced three wording corrections applied *before* the Codex-driven repairs; that intermediate state is not retained and was never a reviewed subject. |

Refutations 1–3 and 10 hold as Codex found.

## Further repairs from the independent re-verification (all applied)

1. `<S>` and `<M>` no longer appear inside the sealed bytes: the D-SEAL draft's Commit and Reversibility fields describe those commits ("the commit carrying this entry", "the rename commit that immediately follows") instead of naming placeholders that could never be filled after the register closes.
2. §4.3 gains the check that `<S>` changed exactly one file (`git diff --name-only <R> <S>` = `docs/coop/COORDINATOR-DECISIONS.md`), and that `<R>` added only the measurement artifact and the COORD append.
3. The untracked-overlay guard is `git status --porcelain --ignored -- docs` (plain porcelain cannot see the gitignored `docs/coop/artifacts/__pycache__/`, 29 `.pyc` files at HEAD 4abb961, which `git mv` would carry into the archive); the same check runs under `<ARCHIVE>/` after `<M>`.
4. The §2 placeholder legend defines `<P>`, `<R>`, `<S>`, `<M>`, `<ARCHIVE>`; the stale `<SEAL>` token is replaced throughout §2/§3 (`<P>` where the measured state is meant, `<S>` where the archived snapshot is meant).
5. `docs/ARCHIVE.md` is a fifth root page in §2/§2.1/§5.1 (**baseline 128 pages**, 149 with candidate ADRs); `docs/archive-manifest.v1.json` is listed in the tree and validated by script, not dual-reviewed.
6. D-SEAL clause 1 is bracketed with clause 4 so that choosing §6.10 option (a) amends both consistently; two citation ranges corrected (D-001 pin-note COORD L119–L122; D-033 tuple L1823).

## Final agreed sequence

0. Owner records the A–E decisions they choose; commits `DECISION-PACKETS/` at the repository root; disposes of the 31 untracked dispatch texts (E4); `git status --porcelain --ignored -- docs` is empty.
1. **`<P>`** — a pushed commit at which file 08 shows all five conditions `MET` (08:L414–L418 form).
2. **`<R>`** — D-000 cycle: measurement artifact pinned to `<P>` (file 08 sha256; COORD sha256 through the last pre-measurement heading; condition table verbatim; required-now; last gate row; last heading), dual-reviewed; recorded as the penultimate COORD heading; committed **and pushed**.
3. **`<S>`** — the owner's verbatim seal (D-000 / D-054 / D-132 class), the last COORD heading: cites `<P>` and the measurement digest; amends D-000 explicitly (register closed here; continues in the COORD-form successor register); states the path-mapping rule for tracked paths present in this snapshot; preserves `docs/v2/implementation/` literally and absent unless a 5-C amendment was recorded before `<P>`; committed **and pushed**; touches only COORD.
4. **`<M>`** — `git mv docs docs-sealed`, renames only; pushed.
5. Front door `docs/README.md`, `docs/ARCHIVE.md` (`<P>`, `<R>`, `<S>`, `<M>`, mapping, rules), external `docs/archive-manifest.v1.json` hashing every file under `docs-sealed/`; read-only validators confirm it.
6. Derived tree page by page under the packet's §3 templates and §4.6–§4.7 review form; owner touchpoints after `register/*`, after `contracts/*` + `gates/*`, and at the end.

## Final agreed option positions (unchanged from round 2 except 6.5/6.7 wording)

6.1 (b) `docs-sealed/` (owner may keep `docs-old/`; sibling, never nested) · 6.2 (a) link only, full-file sha256 validators · 6.3 (b) 49 derived, non-authoritative ADR groups with the D-NNN reverse map · 6.4 (b) candidates included and labelled, standing from the sealed bytes · 6.5 5-A as amended (mapping over the terminal `<S>` snapshot; absent path preserved literally) by default, 5-C for a different live path · 6.6 (a) link; canonical catalog of the 26 join-named corpora on DR-105/DR-114, cross-referenced from G09/G12 · 6.7 (c) two decision acts over three distinct states (`<P>`, `<R>`, `<S>`) · 6.8 (a) commit the checked dispatch texts before `<P>`; `DECISION-PACKETS/` at the root, never in the archive · 6.9 (a) pointers only · 6.10 (b) COORD-form successor register continuing the D-series with D-000's commit-and-push field intact; ADRs derived.

Amendments A–C as re-scoped in round 2 (disposable pilot yielding samples; read-only validators first; 128 a provisional baseline, 149 with candidate ADRs, effort unmeasured).

**Agreed recommendation: ADOPT** the corrected plan and the option positions above. Confidence: high.


### Codex (round 3): AGREE-WITH-AMENDMENT — confidence high

Adopt the corrected four-state sequence and the reconciled option positions only after six final consistency repairs. Keep `<P>` as the already-known measured commit, `<R>` as the pushed measurement-record commit, `<S>` as the pushed terminal seal, and `<M>` as the pure move; derive the live documentation only after `<M>`. Make D-SEAL's operative path rule cover the same tracked pre-seal citations that `docs/ARCHIVE.md` and §6.8 claim it covers; describe the condition-5 path as authorized but not yet created once condition 5 has been met; place the recommended COORD-form successor register in the post-move tree and allow final sign-off to follow any earlier post-seal dispositions; never regenerate the archive manifest to bless changed archive bytes; disambiguate the 49-ADR baseline from the optional candidate-ADR expansion; and correct Claude's ignored-bytecode count to the measured value. These repairs do not change the recommended archive name, link-only model, pure-move mechanics, review discipline, or owner-option selections.

*Rationale:* Claude faithfully adopted the three round-2 amendments, and the corrected packet implements their core: the placeholder legend separates `<P>`, `<R>`, `<S>`, and `<M>`; the D-SEAL bytes contain neither `<S>` nor `<M>`; the archive is the tree at `<S>`; `<R>` and `<S>` have exact-delta checks and are pushed in order; rollback is ordered and qualified; the ignored-overlay guard uses `--ignored`; `docs/ARCHIVE.md` is reviewed; and §0.3 now cites the retained round-1 subject `29d7a11c…`. The corrected packet and Claude reply also match their pinned hashes. Adversarial re-reading nevertheless finds inconsistencies outside that repaired core. D-SEAL clause 2 maps citations only in COORD, file 08, and frozen artifacts, while `docs/ARCHIVE.md` and §6.8 say the rule also maps `DECISION-PACKETS/` and the other committed root files. The default condition-5 clause carries the word `reserved` past the very authorization that must precede sealing. Recommended option 6.10(b) creates `docs/DECISIONS.md`, and §4.8 requires an entry there, but the file is absent from the proposed tree and post-move sequence; additionally, final sign-off cannot always be its first entry if a CONTESTED-page disposition was already required. The manifest rule permits regeneration precisely after archive drift, which would re-pin the defect instead of detecting and reverting it. Finally, the packet ties the optional 21 candidate ADRs to §6.3/§6.4 while simultaneously recommending 49 ADRs plus candidate inclusion and calling 128 the recommended baseline; the intended distinction needs to be explicit. Claude's separate `30+` bytecode count is directly refuted by the 29 files on disk at the pinned HEAD. These are bounded amendments, so disagreement with the overall plan is not warranted.


*Amendments:*
- Make the authoritative scope of D-SEAL clause 2 match the later mapping table and §6.8. The clean rule is: a `docs/<p>` citation written before the seal in any tracked repository file present at `<S>` maps to `<ARCHIVE>/<p>` when `docs/<p>` is a tracked path in the `<S>` tree; later citations do not. Alternatively, remove the broader claims and explicitly rewrite every committed root file after `<M>`, but do not let non-authoritative `docs/ARCHIVE.md` silently enlarge D-SEAL.
- In D-SEAL clause 3 and every 5-A repetition, distinguish the historical reservation quote from the post-authorization state. Once condition 5 is MET, describe an absent path as `authorized but not yet created`, preserved literally as the prospective live path; do not say it remains reserved after the authority event that ends `Until then`.
- Add the option-6.10(b) successor register to the proposed post-move tree and creation sequence, while keeping it distinct from the 128 derived pages subject to F1's page-review cycles. Require the first actual successor entry to cite D-SEAL and `<P>` as clause 4 says; make final owner sign-off the first entry only if no earlier post-seal decision or CONTESTED-page disposition exists, otherwise record it after those entries.
- Pin `docs/archive-manifest.v1.json` to the archive at `<M>` and never regenerate it merely because archive bytes drift. On a mismatch, revert or restore the archive and the original manifest; any intentional new archive state requires an explicit new authorization and manifest version, not normalization of a defect.
- Resolve the candidate-ADR trigger without changing arithmetic: either state that recommended §6.4(b) includes candidates on contract/gate/design pages while recommended §6.3(b) keeps exactly 49 ADRs and 128 derived review pages, or explicitly select the additional 21 candidate ADR groups and use the packet's 149-page scenario. Remove `if candidates are included` wherever it conflates those two choices.
- Replace Claude round 3's `30+` ignored `.pyc` claim with the measured 29 at HEAD `4abb961`, or omit the volatile count while retaining the required `--ignored` guard.


*Codex positions on the owner options:*

| § | Position |
|---|---|
| 6.1 | AGREE: recommend sibling `docs-sealed/`, with `docs-old/` remaining the owner's naming alternative and no nested archive. |
| 6.2 | AGREE: link only, with independently specified full-file sha256 validation and no generalization of D-033. |
| 6.3 | AGREE-WITH-AMENDMENT: recommend the 49 BINDING-DESIGN ADR groups and complete D-NNN reverse map; state explicitly that the optional 21 candidate-CUSTODY groups are not selected merely by including candidate contract/gate content under 6.4(b). |
| 6.4 | AGREE-WITH-AMENDMENT: include candidates with conspicuous standing labels on their contract, gate, and design pages; separately state whether candidate ADRs are excluded under the 49-group choice or are an additional selected expansion. |
| 6.5 | AGREE-WITH-AMENDMENT: retain 5-A as amended by default and 5-C for a different live path, but after condition-5 authorization call an absent literal path authorized and not yet created, not still reserved. |
| 6.6 | AGREE: link fixture/corpus bytes; keep the canonical catalogs on DR-105/DR-114 and only cross-reference them from G09/G12. |
| 6.7 | AGREE: option (c), with the reviewed measurement and user seal as distinct acts over `<P>`, `<R>`, and `<S>`, followed by pure move `<M>`. |
| 6.8 | AGREE-WITH-AMENDMENT: commit the checked dispatch texts and root packet corpus before `<P>` and keep them outside the archive; align D-SEAL's citation-mapping scope with the promised treatment of those tracked root files. |
| 6.9 | AGREE: V1 pointers only. |
| 6.10 | AGREE-WITH-AMENDMENT: use a COORD-form authoritative successor with D-000's fields and commit-and-push rule intact; add it to the actual post-move tree/sequence and make final sign-off ordering conditional on earlier successor entries. |


### Post-round note (Claude)

# F1 — post-round note (Claude; not a fourth round)

Codex's round-3 verdict was AGREE-WITH-AMENDMENT (high confidence): it accepts the four-state sequence (`<P>` measured commit → `<R>` measurement-record commit → `<S>` seal → `<M>` pure move → derived tree), the integrity model, and all ten option positions, and asked for six bounded consistency repairs. The protocol allows no fourth exchange, so they were applied to the packet and to the round-3 reply without a further Codex reply, for the owner to see (each is a row in the packet's §0.3):

1. **Mapping scope aligned.** D-SEAL clause 2 now states the rule that `docs/ARCHIVE.md` and §6.8 rely on: a `docs/<p>` citation written in or before the sealing entry, in any tracked file present at `<S>`, maps to `<ARCHIVE>/<p>` when `docs/<p>` is a tracked path in the `<S>` tree; citations first written after the seal do not.
2. **Condition-5 wording after authorization.** Once condition 5 is MET, an absent `docs/v2/implementation/` is "authorized but not yet created", preserved literally as the prospective live path — the pre-authorization word "reserved" (08:L395 "Until then …") is quoted as history, not carried forward as current standing.
3. **Successor register in the tree.** `docs/DECISIONS.md` (option 6.10 b, COORD form) is added to the proposed tree and the post-move sequence, distinct from the 128 review pages; its first entry cites D-SEAL and `<P>`; the owner's final sign-off is its first entry only if no earlier post-seal decision or CONTESTED-page disposition exists.
4. **Manifest never regenerated over drift.** `docs/archive-manifest.v1.json` is pinned to the archive at `<M>`; a mismatch is fixed by restoring the bytes or the original manifest, never by minting matching digests; an intentional new archive state needs an explicit authorization and a new manifest version.
5. **Candidate-ADR trigger disambiguated.** Recommended 6.4(b) includes candidates on contract/gate/design pages; recommended 6.3(b) keeps exactly 49 ADRs and the 128-page baseline; the 21 candidate-ADR groups are a separate, unselected expansion (149 pages) — "if candidates are included" no longer conflates the two.
6. **Count corrected.** The round-3 reply said "30+" ignored `.pyc` files; the measured value at HEAD `4abb961` is 29 (the packet already said 29).

**Independent re-verification alongside round 3.** A third read-only re-verifier (`DECISION-PACKETS/.verify/F-docs-rewrite.md.reverify3.json`, verdict REJECT against the pre-amendment state) confirmed the round-2 repairs and found two further defects and six nits, all repaired in the same post-round pass:
- **Defect — the `<P>→<R>` check assumed a two-file commit.** Every real D-000 cycle commit in this record carries the cycle's draft, review prompts and reviewer verdicts under `docs/coop/artifacts/` (measured: `4abb961` 13 files; `cb8bd16`, `20e6d2d`, `8d0cf09` 9 each). The check now expects COORD, `<MEASUREMENT-PATH>` and that cycle's artifacts, each enumerated by path in the measurement entry, and nothing else; the archive-delta description, R6 and R10 name those artifacts; the `<R>→<S>` one-file check is stated to assume the user-made seal form.
- **Defect — D-SEAL clause 1 hard-coded option 6.10(b) outside its bracket.** Clause 1 (and clause 6's gloss) now take clause 4's form, with both option texts supplied (under (a): four amendments of D-000, including the entry form).
- **Nits:** `<MEASUREMENT-PATH>` required under `docs/coop/artifacts/`; the COORD prefix digest defined by byte offset with its recomputation procedure and a zero-deletion `--numstat` check; R6 worded "at any point up to `<M>`"; §9's D-000 locator corrected to L26–L45 (clauses 1–5 L29–L45); clause 2's qualifier attached unambiguously to the path; the mapping-scope nit had already been resolved by amendment 1.

A fourth, scoped re-verification of that pass (`DECISION-PACKETS/.verify/F-docs-rewrite.md.reverify4.json`) confirmed all 13 repairs and found one more defect — clause 2 said "written **before** this entry", which excluded the sealing entry's own citations of file 08 and of the register — and five wording nits (a non-byte-exact quote in §0.3; `--numstat` zero deletions read as proof of *appending*; `<R>`'s parent unstated; `DECISIONS.md` called a "non-page file"; a stale "F1 NOT STARTED"). The orchestrator applied these by hand (clause 2 now reads "written in or before this entry"; parents `<P>`→`<R>`→`<S>`→`<M>` stated), byte-checked them, and recorded them as the last two §0.3 rows; this final hand pass was **not** independently re-verified. Final packet state: `sha256 db7f09914903b8f034bf0a86f0f12ae15bfdb2d87a4ed3112fc3c6a5e59cbc88` (180,880 bytes; 4 verifier records under `DECISION-PACKETS/.verify/`).

If the owner adopts F1, the D-000 cycles on the measurement act and on each derived page are where Codex re-checks these points as a reviewer.
