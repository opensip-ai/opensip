# C1–C4 — reserved numbers/lists (DR-112, DR-118, DR-111, DR-126) — Claude recommendation (round 1)
Packet: `C1-4-reserved-numbers-security-quality.md` (fill-in forms §1.6, §2.6, §3.6, §4.6; skeletons §x.7).

Cross-cutting: where a value is not in hand, record an **explicit deferral disposition in the D-006 clause-5 shape (trigger + owning condition-4/DR-G* obligation + owner)** — the record's own reviewed form for parking a number (§0.1–0.2) — rather than leaving the reservation implicit. Prefer the user-made verbatim form (D-132) for the numbers themselves; D-000 cycles for anything the orchestrator drafts.

- **C1 DR-112 (OD-112-1..4):** decide OD-112-3 now as "preview refuse stands" (a decision, no number invented); for OD-112-1/2/4 either set values or record deferral dispositions naming trigger and owning gate (G08). Rationale §1.4: no candidate values exist in the record; §1.5 constraints (expiry + audit + measured residual for waivers) bound any later number.
- **C2 DR-118:** confirm "parity-or-improvement per row, product-approved at matrix acceptance" as the decided rule; defer numeric thresholds by explicit disposition (trigger: DR-125 closure/disposition, then first matrix acceptance); defer corpus source to qualification with the D-007 item-4 discipline; leave G13 `reserved, not named` and say so. Rationale: D-007's rejection of THRESHOLDS-NOW still holds (no measured denominator).
- **C3 DR-111:** decide the **window unit** now (this is the one choice that makes any later number evaluable and unblocks OBL-LOCK-JOIN / DR-103's lock rule); set the four surface windows if in hand, otherwise defer with disposition; name the deciding authority as both cells say (`Versioning authority` / `Product/versioning`); remasure compatibility-leftover-join first only if reviewers require it (§3.6 precondition — my view: not required for a user-made verbatim entry).
- **C4 DR-126:** assign the population-packet owner (default: the row's owner cell `Security + release + platform owners`) and set the four filesystem selectors now (pure choices inside a closed grammar); confirm by disposition that per-OS member tables, `ikconfigParserVectors` and `NT-TCB-KEXEC` stay `RESERVED as G22 qualification evidence`; accept the single-exact-build limitation for slice 1 unless the owner wants a total-order successor first.
Confidence: medium (the values themselves are the owner's; the shapes are the record's).
