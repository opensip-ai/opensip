# D1 — fixture-authoring delegation — Claude recommendation (round 1)
Packet: `D1-fixture-authoring-delegation.md` (§3 inventory: 30 distinct GATE ids across 20 gates + 8 ROW-only + 2 schema items; §6 options).

**Recommendation: Option (c) — mixed.** Delegate to the orchestrator, under D-000 dual adversarial review with the §6(b) default policies as written: (i) all *corpus content* ids (15) and the *derivable byte-set* ids for the non-security gates (G14–G21 except where security-owned; G24–G30 preview gates), (ii) the DR-103 `OBL-UNICODE-NORM` schema successor and the OD-2 fold. **Reserve to the owner** the security-owned set the file-08 owner cells name: G07, G08, G09, G12, G22 (and G22 in any case until C4 is decided), G10 until its selector refresh, plus the ROW-only DR-103/DR-105/DR-114 items and the two *envelope-shape* ids (G09/DR-105 decision-record envelope; OD-2 shape is delegable under "projection of an accepted schema, no new top-level members").
Rationale: (1) fixture authoring is the largest remaining effort-bound block (39 measurements) and cannot start at all until this is answered; (2) the record already has precedent for orchestrator-chosen fixture bytes surviving dual review (§4: D-239/D-243/D-247 corpora), so CONTESTED risk is real but bounded; (3) the security-owned gates are exactly where a wrong byte-set has consequences a reviewer cannot fully judge, so the owner's inputs there are worth the wait; (4) the delegation must be recorded as a user-made D-000 entry listing the delegated and reserved ids and stating that the handoff's "do not invent" line is lifted for the delegated set only (packet §6 "what any answer must contain").
Confidence: medium-high.
