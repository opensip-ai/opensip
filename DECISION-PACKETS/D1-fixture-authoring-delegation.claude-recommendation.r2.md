# D1 — fixture-authoring delegation — Claude round 2 (answering Codex's AGREE-WITH-AMENDMENT)
**All seven amendments adopted. Agreed recommendation: Option (c), dependency-aware split, recorded as a user-made D-000 entry.**
- **Delegated to the orchestrator under separate D-000 review:** fixture choices for G15, G16, G18, G19, G20, G21 and G24–G30; `OBL-HOSTILE-GOLDENS`; the two DR-122 SARIF fixture obligations. Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep their recorded prohibitions (adapter, CI encoding, journal).
- **Reserved to the owner:** the security-inclusive gate obligations at G07, G08, G09, G12, **G14** (owner cell `Product + language + release + security`) and G22 — each reservation applying automatically to the same obligation id on every current ROW twin — plus `OBL-WINDOWS-PATH`, `OBL-ENVELOPE-MISMATCH`, `OBL-JOIN-FX-AUTHORING`, `OBL-R10-AUTHORING`, `OBL-R6-AUTHORING`, and the G09/DR-105 decision-record envelope. **G10** is not security-owned (`Protocol + semantic owners`); it stays blocked on its selector refresh, separately. **G22** stays reserved until C4's population dependency is resolved.
- **Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved; for delegated byte sets, concrete witness bytes may be selected only within already-recorded schemas and fates, and any choice that would create a new semantic member, identifier, value, list or implementation stays outside the grant (recorded as a named open decision instead).
- **OD-2:** resolved by the final no-fold disposition agreed under C7; if the owner orders a fold, that is a separate DR-103 schema decision, not part of this delegation.
- **The delegation entry** lists the delegated and reserved obligation ids and states that the handoff's do-not-invent instruction (line 81) and its do-not-steal list (line 83) are lifted only for the enumerated delegated obligations and only under these constraints.
Withdrawn: the round-1 reserve list (G10 in, G14 out), the one-case-per-member policy, the OD-2 double treatment, and the claim that D-239/D-243/D-247 bound the CONTESTED risk of underdetermined bytes. Confidence: high.
