# F1 — Documentation rewrite after sealing — Claude recommendation (round 1)

Packet: `DECISION-PACKETS/F-docs-rewrite.md` (byte-cited; adversarially verified by a fresh subagent, whose 15 findings — 2 MUST-FIX, 9 SHOULD-FIX, 4 observations, all wording/citation/arithmetic, none touching the tree or the process — were applied before this recommendation was dispatched; the dispatch text pins the packet's sha256). Owner item: `DECISIONS-NEEDED.md` F1 (L62–L70).

## Recommendation

**ADOPT the packet's plan as the target shape, decide the ten §6 options now (positions below), and do nothing to `docs/` until D-SEAL exists.** Concretely:

1. **Sequence (packet §4.1–§4.3, §4.8).** D-SEAL (last COORD heading, sealing file 08 + COORD + a `seal-manifest.v1.json` of every tracked file under `docs/`) → one rename commit `git mv docs <ARCHIVE>` with a minimal `docs/README.md` front door → the derived tree, page by page, each page dual-reviewed for fidelity. Two separate commits so each reverts cleanly (§4.2 Reversibility). Preconditions are the packet's five (§4.1): all five conditions MET at HEAD, the owner's A–E decisions recorded, no untracked files left under `docs/`, D-SEAL adopted, HEAD pushed.
2. **Tree (packet §2).** `docs/{README,AUTHORITY,GLOSSARY}.md`, `design/00–06`, `contracts/DR-xxx.<lineage>.md` (+ `README` index), `gates/DR-Gxx.<name>.md` (+ index), `register/{README,inherited-v1-prerequisites,v2-decisions,review-findings,release-gates,readiness}.md`, `decisions/ADR-NNNN-*.md` (+ index with the D-NNN → ADR reverse map), `v1/README.md` (pointers only). Every artifact of §1.5a–c reachable from exactly one page or listed "archive only" (§4.8 item 4).
3. **Templates (packet §3.1–§3.6) and the citation rule (§3.0)**: every sentence cites `<ARCHIVE>/<path>` + sha256 + line (or is a marked `[editorial]` sentence); tokens, versions, digests, owners, dates, obligation ids and counts verbatim; a mandatory verbatim "Does not …" section in every ADR (the record's negations are load-bearing — §7 R2).
4. **Review (packet §4.6–§4.7)**: automated pre-checks (paths exist, digests recompute, `L<n>` in range, every `D-NNN`/`DR-xxx`/`v<N>` resolves, no uncited sentence) gate every dispatch; then the D-000 form per page — two independent reviewers in fresh panes, ≤3 turns, ACCEPT 0/0 or CONTESTED-and-parked; verdicts in a review ledger inside the new tree (the only process material allowed there). Order: `AUTHORITY`+`GLOSSARY` → `register/*` → `contracts/*` → `gates/*` → `decisions/*` → `design/*` → READMEs; owner touchpoints after `register/*`, after `contracts/*`+`gates/*`, and at the end.

## Positions on the ten owner options (packet §6)

| § | Option | Claude position | Why (packet ref) |
|---|---|---|---|
| 6.1 | Archive name | **`docs-sealed/`**; `docs-old/` acceptable if the owner prefers the name already in F1 — mechanics identical. Not (c) `docs/archive/` (self-move, and the archive would sit under the tree that cites it); not (d) TREE-ENDSTATE re-layout (per-file re-pathing breaks the single mapping rule; its own §7.1 forbids moves during churn). | The archive governs on conflict (§4.6); "old" reads as deprecated. The name appears in D-SEAL clause 2 and every citation — choose once. |
| 6.2 | JSON copied or linked | **(a) link only**, sha256 on every page, a script that verifies them. | Single copy of every byte; the archive is frozen so links cannot rot; the record's own convention is path + digest (D-033 property pins, COORD L1820). Copying creates a second (and, after any post-seal successor, a third) home. |
| 6.3 | ADR granularity | **(b) 49 ADR groups** with the reverse map D-NNN → ADR in `decisions/README.md`. | 1:1 lookup survives; avoids 30 near-duplicate ADRs for the select/record/owner-record triplets. |
| 6.4 | Candidates in the new docs | **(b) include, labelled** `CANDIDATE-NOT-APPLIED — "binds NOTHING"` with a banner. | Excluding them (a) would drop 22 of 26 contract lineages, all 28 occupancies and all 43 corpora — the only recorded design for those rows. Note the packet's own point: sealing presupposes condition 2 MET, so by then most candidates will have been re-recorded under D-056/D-133 and the label question shrinks to the deferred rows and the gate occupancies. |
| 6.5 | Condition-5 literal path `docs/v2/implementation/` | **5-C** — one D-000 cycle (MF-6) amends condition 5's path text in file 08 before the seal, exactly as D-010/C-D010 amended condition 2's wording under D-001's own new-row rule (D-001 pin-note, COORD L119–L121); fallback 5-B (mapping clause in D-SEAL) only if the owner refuses the extra cycle. | 5-C is the only option that leaves no divergence between sealed text and live tree; the cost is one cycle. 5-A/5-D leave a `v2/` stub or a pointer directory in the new tree; 5-E archives the implementation directory at birth. |
| 6.6 | Fixture bytes; DR-join-named corpora | **(a) link only**; list the DR-join-named corpora on **both** the gate page (G09/G12) and the row page (DR-105/DR-114) with the naming join cited ("named by `permission-leftover-join.v12` (D-283)" / "named by `doctor-actor-leftover-join.v12` (D-285)"). | Neither page invents an owner; the corpus JSON already pins fixture bytes by digest, so a copy adds nothing. |
| 6.7 | Form of the sealing entry | **(c) both**: D-000 cycle on the measured-snapshot section, then the owner's verbatim seal. | The measurement is the last snapshot anyone will verify against live bytes; the seal itself is the owner's act (D-000/D-054/D-132 class). |
| 6.8 | Untracked dispatch texts; working files | **(a) commit the 31 untracked `_dispatch.*.txt` before the seal** (729 of 760 are already tracked, so (a) restores consistency — this is E4's decision, made once, at the seal), and **move `DECISION-PACKETS/` to `<ARCHIVE>/packets/` in the rename commit** so the packets that justified the seal travel with the record. The per-act rule "never `_dispatch.*` in commits" (HANDOFF) stays in force for ordinary acts; the seal is the one provenance commit that must be complete. | §7 R6: `git mv` does not move untracked files; they would be orphaned at the old path. |
| 6.9 | V1 prose | **(a) pointers only** (`v1/README.md`). | V1 authority is the claim-register/checkers and the byte-pinned freeze/blueprint (v1-authority-baseline.json 49/49 MATCH), not the narrative; rewriting V1 doubles the sizing. |
| 6.10 | Post-seal decision register | **(a) `docs/decisions/` ADRs continue the numbering**, each new ADR carrying the D-000 review verdict digests. One register; the D-NNN series ends at D-SEAL. | Two registers (b) would split history from new decisions. The ADR template's verbatim "Does not" and "Reversibility / overturn" sections keep the record's discipline. |

## Amendments Claude adds to the packet's plan

- **A. Pilot before the seal (no `docs/` change).** Author three pilot pages under `DECISION-PACKETS/pilot/` — one contract page (DR-104 — a `SATISFIED` V2 row, D-236, whose contract lineage is unusually deep: policy D-012, integration contract v4, negative-test corpus v1), one gate page (DR-G21, the lineage most recently remasured at D-292), one ADR (the D-056 + D-133 SATISFIED-rule group) — and run one dual-review cycle each against the *live* `docs/` paths (pilot citations use `docs/…`, the mapping is applied at the seal). Purpose: calibrate the fidelity rule and the pre-check script, and replace the packet's estimate (§5.2: ~190 turns, 16–28 working days for 127 pages, explicitly "not measurements") with three measured cycles before committing to the cadence. The pilot pages are discarded or re-derived after the seal; they are never published as `docs/`.
- **B. Build the tooling now**: the pre-check script (§4.6), the `seal-manifest.v1.json` generator (§4.2 Subject), the review-ledger writer (§4.7), and the citation-mapping rewriter `docs/<p>` → `<ARCHIVE>/<p>` (§7 R4). All read-only over `docs/`; all testable today against HEAD.
- **C. Sizing is a ceiling, not a plan.** Accept the packet's page counts (127 baseline; §5.1) but treat the days estimate as unmeasured until amendment A lands.

## What changes if the owner adopts F1 today

Nothing in file 08, COORD, or `docs/`. The adoption fixes the target tree, the templates, the review standard and the ten option answers; it authorises amendments A and B (pilot + tooling, outside `docs/`). The first record change is D-SEAL itself, which cannot be drafted for adoption until the five conditions are MET (condition 2 is 5 of 32 at HEAD; condition 5 needs the authorising entry — 08:L414–L418).

## Risks the owner should weigh (packet §7)

R1 silent standing upgrade, R2 lost negations, R8 scope creep into reserved values, R14 reviewer fatigue on 127 near-identical pages — all mitigated by the citation rule, the verbatim "Does not" sections, the pre-checks, and batches of ≤8 pages with a named byte check per page. R9 (cross-lineage citation staleness inside sealed joins) is settled by the owner's A4 decision and is quoted, not re-decided, by the new register.

## Open questions the owner still has to answer for the rewrite (packet §8)

The packet's 21 questions are the complete list; the ones that change page content are §8 Q1 (which DR-117 lineage — B3's agreed path answers it if executed before the seal), Q2 (COORD-highest version as the candidate, cell version as history), Q4 (show `PROPOSED-CLOSED-FOR-REVIEW` for DR-107/DR-122 with the candidate standing beneath), Q8 (harness cells say "not authored; not QUALIFIED" while 28 occupancies exist — page shows both), Q13 (49 vs 70 ADRs; position: 49). Claude's default for each is the packet's stated template behaviour; none needs to be decided before the pilot.

Confidence: **high** on the sequence, tree, templates, review form and options 6.2/6.3/6.4/6.6/6.7/6.9/6.10; **medium** on 6.1 (name is taste; the reason given is real but small), 6.5 (5-C costs a cycle the owner may not want before sealing), and 6.8 (overrides the per-act dispatch-text rule once); **low** on the packet's day estimates until amendment A measures them.
