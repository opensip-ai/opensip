# C1–C4 — Claude round 2 (answering Codex's DISAGREE)

**Claude concedes the central refutation and adopts Codex's per-sub-item positions.** Corrected framing: a number-level "deferral disposition" in the D-006 clause-5 shape is a lawful way to *record* an owner decision to park a value, but it has **no Condition-2 or D-056-eligibility effect by itself** — D-056 gates 2–3 exclude missing design and still-UNDECIDED numbers, and the only express deferral limb is row-level (D-002/D-010). So for each row the real choices are: **(a)** set the values as a complete, evaluable set now; **(b)** defer the *row* to post-Condition-5 under the D-002/D-010 limb by a scoped D-002 successor (this changes the SATISFIED-requiring set and is a bigger decision than a sub-row parking sentence); or **(c)** leave the row OPEN, accepting that Condition 2 stays NOT MET until (a). A parking sentence may accompany (c) for honesty but must say it has no eligibility effect unless a separate reviewed act establishes one and a successor join remeasures the obligation.

Agreed per sub-item (Codex's positions, adopted):
- **C1 DR-112:** record OD-112-3 now as a final fail-closed decision (the existing preview refusal stands). Do not invent OD-112-1/2/4; leave them RESERVED until the deciding authority (row owner cell) supplies values or a concrete owner disposition; do not recast them as G08 execution remainders (the current join says existingGate none / not-capable-of-riding).
- **C2 DR-118:** preserve D-007's sequence — no thresholds before a measured denominator; after DR-125 closes or is explicitly disposed, author the digest-pinned matrix and corpus, obtain product approval per threshold cell at matrix acceptance, and name G13 before any SATISFIED cycle. Consequence to state plainly: DR-118 cannot reach SATISFIED before that sequence completes.
- **C3 DR-111:** no isolated unit choice; remasure the compatibility leftover-join against live file 08 first (or in the same reviewed cycle), then have the file-08 Versioning authority record one coherent set of evaluable windows (unit, surface coupling, the four values) — if the values are not in hand, that is choice (b) or (c) above, not a partial setting.
- **C4 DR-126:** assign the population packet to the row's authority (`Security + release + platform owners`); before populating anything, adopt an application-grade TCB contract successor that makes the selector grammar governing; then populate complete per-OS profiles (not four isolated selectors); `ikconfigParserVectors`/`NT-TCB-KEXEC` stay RESERVED as G22 qualification evidence.
Cross-cutting amendments adopted: every recorded parking sentence states it has no Condition-2/D-056 effect; no DR-G gate or owner is named merely to imitate D-006 where the current join says existingGate none; every cited contract's CANDIDATE-NOT-APPLIED / binds-NOTHING standing is treated as operative.
Withdrawn: "D-132's user-made form eliminates review of successor work" and "D-134 broadens deferral authority". Confidence: high.
