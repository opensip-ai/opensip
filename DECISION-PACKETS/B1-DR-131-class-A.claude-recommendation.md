# B1 — DR-131 Class A opening — Claude recommendation (round 1)
Packet: `B1-DR-131-class-A.md` (§8 options).

**Recommendation: Option 3(a) + 3(b), then Option 1 — as part of one common Class A programme for DR-131, DR-133 and DR-117.**
1. One D-000 entry ruling the gate-2 question for all three rows at once: whether `OBL-G2x-FX-AUTHORING leftoverDesign: true` on the preview gate joins blocks D-056 gate 2 (clause-5 reading, D-249..D-253) or D-154's "hold" governs (packet §5). This is the single unreconciled pair of ADOPTED bytes on which every SATISFIED-GRADE cycle for these rows turns; ruling it once avoids three contested cycles.
2. A fresh application-grade dual review of `preview-analyze-contract.v2` (`081ff7fb…`) with the NEUTRAL question, so the opening cites verdicts with zero candidate-only language (removes the D-001 T2-02 EIR-v6 objection, packet §6 item 3(ii)).
3. Then the T2-02 opening entry (Appendix A draft) — zero Condition-2 effect until the SATISFIED-GRADE cycle, which follows fixture authoring for G24–G28 (decision D1).
Why not Option 1 directly: the opening's only value is the SATISFIED-GRADE cycle it unlocks, and that cycle is currently exposed on gate 2 either way; spending the opening first buys nothing. Why not Option 2/3(d): DR-131 is in the SATISFIED-requiring set (D-134); the `joins.dr117` pin to v5 is a candidate-grade sibling pin the record does not address — open per row, not jointly. Confidence: medium-high.
