# G20 — OBL-G20-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

Path: `docs/coop/artifacts/g20-leftover-join.v6.json`
sha256: `d666a4492ef3c598b53606fff453cb14a968822b9c29b25b0b535ebde01b2d97`

| JSON path | Value (verbatim) |
|---|---|
| `$.artifact` | `"g20-leftover-join.v6"` |
| `$.date` | `"2026-08-24"` |
| `$.registerRow` | `"DR-G20"` |
| `$.status` | `"CANDIDATE-NOT-APPLIED"` |
| `$.reviewStatus` | `"AWAITING-INDEPENDENT-REVIEW"` |
| `$.binds` | `"NOTHING"` |
| `$.head` | `"7df63a61cdb6f75a642a54ad29667301244924fe"` |
| `$.requiredNowUnchanged` | `28` |
| `$.file08StatusToken` | `"OPEN"` |

`$.sealRecommendation` = `"DO-NOT-SEAL"`. `$.liveGateOwners.DR-G20` =
`"Component architecture + CLI/operability"`.

COORD heading that recorded it (`docs/coop/COORDINATOR-DECISIONS.md`, sha256
`1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 13708), verbatim:

> `## D-269 — Record g20 leftover-join.v6 as G20 leftover remasurement`

`$.registerRowNote`, verbatim:

> registerRow is the already-named gate DR-G20 because this join remasures leftover-design of G20
> after occupancy v2 (D-217) and after sdk leftover-join.v6 (D-267). file08StatusToken is DR-G20's
> own live token (OPEN). leftover-join.v4 remains frozen and is not current after this successor is
> recorded. leftover-join.v5 is CANDIDATE-NOT-APPLIED (Dual REJECT MUST-FIX-1 / SHOULD-FIX-1 /
> CODEX-G20LJ-V5-SF1) and is not current. D-086 named DR-G20 as required-now. sdk leftover-join.v6
> remains the current DR-125 ROW leftover-join (D-267; registerRow DR-125). This join does not
> retarget DR-125 leftover, does not steal OBL-SDK-API-RESERVED, and does not SATISFY DR-125.

`$.summary.leftoverDesign` = `["OBL-G20-FX-AUTHORING"]` — the only leftover-design obligation on the
GATE join.

`$.namedCorpusNotAuthored` (join top level) = `["component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus"]`.

### 1.2 The obligation object (verbatim)

JSON path: `$.obligations[3]` of `docs/coop/artifacts/g20-leftover-join.v6.json`.

```json
{
  "id": "OBL-G20-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "Occupancy v2 namedCorpusNotAuthored carries one live harness-cell corpus class. Fixtures are unauthored. g20-named-corpus-catalog.v2 named that class; that naming is OBL-G20-NAMED-CATALOG, leftoverDesign false. D-056 Decision clause 5: authoring fixtures remains design work. This join does not invent those fixture bytes and does not invent reserved SDK APIs."
}
```

Field call-outs:

- `$.obligations[3].reason` — quoted in full above. It confirms the brief's expectation: the reason
  says "Occupancy v2 namedCorpusNotAuthored carries one live harness-cell corpus class", which
  matches the obligation's own citation of Occupancy v2 (see 1.3 for the confirmation from
  `$.basedOn`).
- `$.obligations[3].namedCorpusNotAuthored` — exactly one member:
  `"component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus"`.
- `namedCases` / `namedNotAuthored` / `remainingNotAuthored` — **absent** on this obligation. G20 uses
  `namedCorpusNotAuthored` only. (The occupancy carries a different field,
  `namedCorpusWhenFixturesExist`, quoted in 1.3 — that is where NT-4/NT-7 appear.)
- `$.obligations[3].existingGate` = `"none as authored implementations"`.
- `$.obligations[3].rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `$.obligations[3].executionObligationOwnerToday` = `"none"`.

Adjacent obligation carrying the SDK-API guard, `$.obligations[5]`:

```json
{
  "id": "OBL-SDK-API-NOT-STOLEN",
  "leftoverDesign": false,
  "existingGate": "none on G20. Remains on DR-125.",
  "executionObligationOwnerToday": "none on this row",
  "rideStanding": "rides-DR-125",
  "reason": "sdk leftover-join.v6 (D-267) still measures OBL-SDK-API-RESERVED leftoverDesign true on DR-125. File 02 and D-110 reserve exact SDK APIs and frameworks as implementation design. This join does not steal that leftover, does not invent an SDK language, framework, API surface, secret-handle store, doctor schema, or D9/exit map, and does not close OBL-DR125-ACTIVATION on DR-118."
}
```

`$.obligations[1].id` `"OBL-G20-NAMED-CATALOG"` and `$.obligations[2].id` `"OBL-G20-INPUT-CORPUS"`
are both `leftoverDesign: false` (`$.summary.specifiedNotLeftover`), so the naming and INPUT layers
are measured closed.

### 1.3 Occupancy file and the fields that name the fixture cells

**Version confirmed.** The obligation reason says "Occupancy v2". The join's `$.basedOn.occupancyV2`
names the file and its role, verbatim: "Current G20 occupancy remasurement. leftover-join.v3 cited
occupancy v1 as the specification. Occupancy v2 remasured that already-named identifier after D-195.
Occupancy v2 stays unmoved. This join does not occupy the identifier and does not execute G20." No
occupancy above v2 is named anywhere in `$.basedOn`; `$.doesNot[17]` = `"Does not record occupancy v1
as current occupancy."`

Path: `docs/coop/artifacts/harness.DR-G20.component-operability.v2.json`
sha256: `2c4823b7c5feb04afb739602397f81dc34333617c284bff21e82657fa289bb37` (matches
`g20-leftover-join.v6.json#$.basedOn.occupancyV2.sha256`)
`$.artifact` = `"harness.DR-G20.component-operability.v2"`, `$.date` = `"2026-08-22"`,
`$.harnessId` = `"harness.DR-G20.component-operability"`, `$.registerRow` = `"DR-G20"`,
`$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.binds` = `"NOTHING"`, recorded at D-217, dual ACCEPT 0/0.

**Present fields that name the fixture cells** (verbatim, with JSON paths):

`$.liveRowVerbatim`:
> DR-G20 COMPONENT-OPERABILITY | Components use common host behavior rather than reimplementing
> product UX/operations | named: harness.DR-G20.component-operability (D-086; not authored; not
> QUALIFIED). component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus | structured
> envelope and diagnostic parity; redaction/bounds/audit correlation; broker/cancellation/resource
> behavior; no direct UI/unstructured logs/final authority | Component architecture + CLI/operability
> | PROPOSED; not QUALIFIED | all external roles; SDK implementation may vary behind contract | OPEN

`$.claimVerbatim`:
> Components use common host behavior rather than reimplementing product UX/operations

`$.liveHarnessCellVerbatim`:
> named: harness.DR-G20.component-operability (D-086; not authored; not QUALIFIED). component-role ×
> human/JSON/SARIF/doctor/fault/config/progress/log corpus

`$.liveCellVerbatim`:
> structured envelope and diagnostic parity; redaction/bounds/audit correlation;
> broker/cancellation/resource behavior; no direct UI/unstructured logs/final authority

`$.waiverRuleVerbatim` = `"all external roles; SDK implementation may vary behind contract"`.
`$.liveAssuranceStageVerbatim` = `"PROPOSED; not QUALIFIED"`. `$.liveStatusVerbatim` = `"OPEN"`.
`$.sliceStanding` = `"REQUIRED"` (note: G20 is REQUIRED, not REQUIRED-SCOPED as G19 is).

`$.executesVerbatim`:
> First-party TypeScript provider uses common host envelopes/logs/progress/config/doctor/exit. No
> direct rendering, unstructured host logs, or authority escape. DR-133 NT-4, NT-7 execute here
> (provider-only-output-contract.v3 / provider-only-nt-gate-join.v6).

`$.namingDoesNotClaimVerbatim`:
> QUALIFIED. Does not execute DR-133 NT-4/NT-7 by existing. Does not execute DR-133 NT-3 or NT-5.
> Does not SATISFY DR-133.

`$.namedExecutesLimbs[0..2]`:
1. `"First-party TypeScript provider uses common host envelopes/logs/progress/config/doctor/exit"`
2. `"No direct rendering, unstructured host logs, or authority escape"`
3. `"DR-133 NT-4, NT-7 execute here (provider-only-output-contract.v3 / provider-only-nt-gate-join.v6)"`

`$.namedExecutesLimbNote`:
> Three naming-v6 executes limbs. None may be silently dropped. Live file 08 corpus surfaces
> human/JSON/SARIF/doctor/fault/config/progress/log are not optional. Naming-v6 'exit' is host-owned
> final-authority mapping on EV-4, not a ninth corpus surface. Each limb has exactByteIntent and
> passProperty on at least one retained-evidence member. Naming is not execution.

`$.matrix`:
> component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus — admitted external roles
> only × the four D-002 platforms

`$.platforms` = `["macos/arm64", "macos/x86_64", "linux/x86_64", "linux/arm64"]`.
`$.windowsStanding` = `"D-002 defers Windows with explicit disposition. Windows is not a G20 platform."`

**G20's occupancy DOES carry a `platforms` array.** A corpus quotes the four tokens from
`$.platforms` of this file and declares it does not invent a D-002 platform list.

#### `componentRoleAxis` — the component-role axis IS enumerated

`$.componentRoleAxis.waiverVerbatim` = `"all external roles; SDK implementation may vary behind
contract"`.

`$.componentRoleAxis.rule`:
> The corpus must include every external role admitted in the then-current D-002 slice. A role
> identity absent from the corpus fails. Skipping a live surface for a role that is in the corpus
> also fails. This axis is bound to D-002 admitted identities, not to a corpus already chosen.

`$.componentRoleAxis.slice1`:
> The TypeScript provider. This specification does not mint a second language role and does not mint
> Rust-as-core.

`$.componentRoleAxis.laterRoles`:
> When D-002 admits another external role, that identity enters this axis by that admission. This
> artifact does not admit it.

`$.componentRoleAxis.sdkMayVary`:
> SDK implementation may vary behind contract. Variance of SDK API or framework is not a skipped
> role, not a skipped surface, and not a skipped platform. Exact SDK APIs and frameworks remain
> implementation design.

The same enumeration is repeated inside the corpus class's `exactByteIntent`: "one cell per admitted
external role (D-002 slice-1: the TypeScript provider only)". `$.basedOn.d002.role` corroborates:
"Components: first-party/explicitly-trusted only, under the one host lifecycle/control model. G20 is
in the required-now set. Waiver 'all external roles' is every D-002-admitted external role, not a
second slice-1 language and not Rust-as-core." **The record therefore enumerates the component-role
axis as exactly one member for slice 1.** It is not an open question. But see **OQ-G20-1** on the
`then-current` wording.

#### `sarifAxisStanding` — the SARIF sub-axis and the D-077/G17/G26 tension

`$.sarifAxisStanding.liveToken` = `"SARIF"`; `$.sarifAxisStanding.isLiveCorpusAxis` = `true`;
`$.sarifAxisStanding.previewAnalyzeAdvertisesSarif` = `false`.

`$.sarifAxisStanding.g26`:
> Preview analyze refuses or does not offer SARIF 2.1.0. That advertisement-refusal measurement is
> harness.DR-G26.preview-analyze-sarif-not-advertised. This specification names the SARIF axis and
> records that preview analyze does not advertise SARIF. It does not execute G26.

`$.sarifAxisStanding.g17`:
> DR-G17 is dropped / inapplicable (D-077). Historical identifier retained, not required-now, zero C4
> progress. This specification does not restore G17, does not invent advertised-SARIF parity
> fixtures, and does not name G17 into required-now.

`$.sarifAxisStanding.dr122`:
> DR-122 remains the architecture row for optional host-owned SARIF when a command or capability
> advertises it. Preview analyze does not advertise. This specification does not SATISFY DR-122.

`$.sarifAxisStanding.d077`:
> D-077 owner-recorded Route B: SARIF drops for preview analyze. This specification does not overturn
> D-077.

`$.sarifAxisStanding.file01HistoricalG17Mention`:
> File 01 V2 readiness extension still names DR-G17. That mention is historical. It is not a restore
> and is not a required-now add.

**The record does resolve the tension the brief flags.** SARIF is a live G20 corpus axis
(`isLiveCorpusAxis: true`) because the file 08 harness cell names the token; what a SARIF cell
retains is the *non-advertisement* record, not an advertised projection. The corpus-class
`exactByteIntent` states it directly: "SARIF cells retain the live axis and record that preview
analyze does not advertise SARIF 2.1.0; they do not restore G17 advertised-parity fixtures and do not
execute harness.DR-G26.preview-analyze-sarif-not-advertised." What the record does **not** settle is
the byte content of such a cell — see **OQ-G20-2**.

#### `namedCorpusClasses[]` — one class, `exactByteIntent` in full

`$.namedCorpusClasses[0].id` = `"component-role-surface-matrix"`,
`$.namedCorpusClasses[0].memberVerbatim` =
`"component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus"`.

`$.namedCorpusClasses[0].slashJoiner`:
> human/JSON/SARIF/doctor/fault/config/progress/log stays one slash-joined surface set. The eight
> surfaces are cells of this one corpus class, not eight live-cell members and not eight invented
> corpus-class ids.

`$.namedCorpusClasses[0].surfacesInOrder` =
`["human", "JSON", "SARIF", "doctor", "fault", "config", "progress", "log"]`.

`$.namedCorpusClasses[0].exactByteIntent`:
> Once authored: one cell per admitted external role (D-002 slice-1: the TypeScript provider only)
> and per surface in the slash-joined set {human, JSON, SARIF, doctor, fault, config, progress, log},
> on each D-002 platform. Each cell retains the four live-cell members as they apply to that role ×
> surface. Human and JSON cells retain host-owned projections of the same structured envelope. SARIF
> cells retain the live axis and record that preview analyze does not advertise SARIF 2.1.0; they do
> not restore G17 advertised-parity fixtures and do not execute
> harness.DR-G26.preview-analyze-sarif-not-advertised. Doctor cells retain host-owned doctor
> integration; they do not author G12 FC implementations, doctor schema, D9 mapping, or purge. Fault
> cells retain structured fault-envelope / diagnostic host projection; they do not execute G21
> crash/panic/timeout/resource/malformed/truncated/duplicate/EOF/process-tree/recovery. Config cells
> retain schema-checked configuration with provenance and brokered secret handles; they do not invent
> DR-108 storage mechanics and do not score G09 requested/granted/denied tables. Progress cells retain
> host-owned progress/status projection of component-emitted progress. Log cells retain structured
> logging and audit correlation; they do not admit unstructured host logs. A second slice-1 language
> role is not a cell. Windows is not a cell. Fixtures are unauthored.

`$.namedCorpusClasses[0].passProperty`:
> Every admitted-role × {human, JSON, SARIF, doctor, fault, config, progress, log} cell is present on
> all four D-002 platforms. A skipped role, surface, or platform is not a waiver. SARIF cells record
> preview non-advertisement and do not restore G17. SDK-implementation variance is not a skipped
> cell. G12, G21, G15, G22, G09, G17, and G26 are not scored as this class. Pass all four D-002
> platforms.

#### `namedCorpusWhenFixturesExist` — where NT-4 and NT-7 live

`$.namedCorpusWhenFixturesExist.liveCellSurfaces` =
`["human", "JSON", "SARIF", "doctor", "fault", "config", "progress", "log"]`.
`$.namedCorpusWhenFixturesExist.dr133` = `["NT-4", "NT-7"]`.
`$.namedCorpusWhenFixturesExist.doesNotExecute` =
`["NT-1", "NT-2", "NT-3", "NT-5", "NT-6", "CC-1", "CC-2", "CC-3", "CC-4", "CC-5", "CC-6", "CC-7",
"CC-8", "CC-9", "CC-10", "CC-11"]`.

**NT-4 and NT-7 are inside the G20 coverage set.** The 16 identifiers in `doesNotExecute` bound the
coverage set from the other side: NT-1, NT-2, NT-3, NT-5, NT-6 and CC-1..CC-11 are not G20 members.

`$.namedCorpusNote`:
> Live harness-cell punctuation after the named-identifier sentence. This is one corpus class, not
> authored fixtures and not invented FX/SF/NT/T-1 identifiers. The eight slash-joined surfaces are
> cells of that one class. A later fixture-authoring cycle may pin bytes. This artifact does not. T-1
> cancellation traces, naming-v6 exit, and DR-133 NT-4/NT-7 are not live harness-cell corpus classes
> and are not invented as extra corpus-class ids.

#### `liveCellMemberSplit` — the four live-cell members

`$.liveCellMemberSplit.membersInOrder[0..3]`:
`"structured envelope and diagnostic parity"`, `"redaction/bounds/audit correlation"`,
`"broker/cancellation/resource behavior"`, `"no direct UI/unstructured logs/final authority"`.

`$.liveCellMemberSplit.notCollapsed`:
> structured envelope and diagnostic parity is not collapsed into redaction/bounds/audit
> correlation. redaction/bounds/audit correlation is not collapsed into broker/cancellation/resource
> behavior. broker/cancellation/resource behavior is not collapsed into G21 containment or G18
> journals. no direct UI/unstructured logs/final authority is not collapsed into G27, G28, or G21
> NT-6.

`$.liveCellMemberSplit.notInventedExtras` (28 entries) includes `"SARIF as a live-cell member"`,
`"doctor as a live-cell member"`, `"human or JSON as a live-cell member"`,
`"fault, config, progress, or log as a live-cell member"`, `"exit as a live-cell member"`,
`"T-1 as a live-cell member"`, `"v4 SF-* identifiers as applied live members"`, `"a sealed-Run
class"`, `"Rust-as-core"`.

#### `retainedEvidence[]` — four members, `exactByteIntent` in full

`$.retainedEvidence[0].id` = `"EV-1"`, `memberVerbatim` = `"structured envelope and diagnostic parity"`.
`$.retainedEvidence[0].exactByteIntent`:
> Once authored: per D-002 platform, per admitted role, digest-pinned envelope records on the human
> and JSON surfaces showing the same host-owned structured candidate/result/fault envelope and
> diagnostic taxonomy, plus a SARIF-axis record that preview analyze does not advertise SARIF 2.1.0.
> A component-emitted SARIF body is not admitted as a host-owned advertised projection. Human and
> JSON bytes remain host-owned; the component does not render them. This member does not restore G17
> advertised-parity fixtures, does not execute G26, and does not invent a SARIF schema. Fixtures are
> unauthored.

`$.retainedEvidence[0].passProperty`:
> Human and JSON describe the same host-owned envelope. A component-rendered human or JSON surface
> fails. An advertised preview-analyze SARIF projection fails this specification and does not restore
> G17. G26 is not scored. Pass all four D-002 platforms × every admitted role. Waiver of a role or
> surface is not a skipped cell.

`$.retainedEvidence[1].id` = `"EV-2"`, `memberVerbatim` = `"redaction/bounds/audit correlation"`.
`$.retainedEvidence[1].exactByteIntent`:
> Once authored: per D-002 platform, per admitted role, digest-pinned diagnostic/log/progress records
> on the log, human, JSON, progress, and fault surfaces showing (1) classified secrets and control
> sequences absent, (2) a recorded bound not exceeded, and (3) audit-correlation identifiers joining
> the structured envelope to the host log. Unstructured host logs fail this member on EV-4 as well.
> This member does not author G12 FC-REDACT, does not invent a doctor schema, and does not score G21
> EV-5 containment diagnostics. Fixtures are unauthored.

`$.retainedEvidence[1].passProperty`:
> Bytes stay within the bound. Classified secrets and control sequences are absent. Audit correlation
> joins envelope to log. Unstructured host logs fail. G12 and G21 are not scored as this member. Pass
> all four D-002 platforms × every admitted role.

`$.retainedEvidence[2].id` = `"EV-3"`, `memberVerbatim` = `"broker/cancellation/resource behavior"`.
`$.retainedEvidence[2].exactByteIntent`:
> Once authored: per D-002 platform, per admitted role, digest-pinned records on the config, fault,
> progress, and doctor surfaces showing host-brokered artifacts/state/secrets, host-owned
> cancellation and deadlines, and host-owned resource reporting. Components do not open raw host
> resources and do not bypass brokers. Cited expected cancellation traces from the D-110 candidate
> (not applied; APIs remain reserved): terminal-wins, Cancel-wins, write-fails,
> already-terminal/already-cancelled, no-in-band. D-110 honesty-work O-1 traces named here as the
> same live cancellation limb, not as extra corpus classes: drain-then-death-last,
> death-completes-T-5-without-restart. Observed process death is not a Cancel trigger. This
> specification does not author a control-plane provider Cancel, does not execute G21 process-tree
> kill/reap or core survival, does not invent DR-108 secret storage, and does not score G09 grants.
> Fixtures are unauthored.

`$.retainedEvidence[2].passProperty`:
> Host brokers are used. A raw-resource or broker-bypass fails. Host-owned
> cancellation/deadline/resource behavior holds on the named traces. A control-plane-authored
> provider Cancel fails. G21, G18, G09, and G08 are not scored as this member. Pass all four D-002
> platforms × every admitted role.

`$.retainedEvidence[3].id` = `"EV-4"`,
`memberVerbatim` = `"no direct UI/unstructured logs/final authority"`.
`$.retainedEvidence[3].exactByteIntent`:
> Once authored: per D-002 platform, per admitted role, digest-pinned refusal records for (a) a
> component attempting to render product UI, (b) a component writing unstructured host logs, and (c)
> a component attempting final authority: verdict/exit choice, evidence sealing,
> policyOutcome/verdict/threshold/waiver/gate (NT-4), or planAdmission/RequestId/ExecutionId
> allocation (NT-7). Product law for NT-4 and NT-7 stays on provider-only-output-contract.v3. Naming
> is not execution; fixtures are unauthored. This member does not invent a D9 code, exit number, or
> HostTermination. NT-6 (component-minted D9/exit/HostTermination) remains G21. G27 is
> preview-result-not-sealed-Run, not this member. G28 is host-must-not-mint from pack
> threshold/policyOutcome, not this member.

`$.retainedEvidence[3].passProperty`:
> All three slash-joined limbs refuse. Direct UI fails. Unstructured host logs fail. Component-set
> final authority fails. NT-4 and NT-7 refuse before admission once independently pinned fixtures
> exist. Splitting this member into three live-cell members fails this specification's punctuation.
> G21 NT-6, G27, and G28 are not scored. Pass all four D-002 platforms × every admitted role.

`$.retainedEvidence[3].suppliedBy`:
> component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus class. NT-4 and NT-7 when
> independently pinned fixtures exist. Not authored.

`$.retainedEvidence[0..2].suppliedBy` are all
`"component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus class. Not authored."`

#### Whole-harness gate

`$.passProperty`:
> Once independently pinned fixture implementations exist for every named corpus class, every named
> executes limb, every live retained-evidence member, every admitted external role, every live
> surface, and every D-002 platform, each retained-evidence member's passProperty holds. Until those
> bytes exist, this harness cannot be executed and cannot be QUALIFIED. File 08 remains PROPOSED; not
> QUALIFIED. File 08 carries G20 (D-086). The live G20 row is OPEN and not QUALIFIED. Live
> required-now is 28. This file existing is not execution and is not a three-limb recording. NT-4 and
> NT-7 are not executed by this file existing.

`$.namesRemaindersOf` = `["DR-133"]`. `$.namesRemaindersOfNote`:
> Naming is not execution. DR-133 NT-4 and NT-7 execute here only when independently pinned fixtures
> exist. This artifact does not execute them. NT-3 and NT-5 do not execute here.

`$.liveGateOwner.DR-G20` = `$.executionObligationOwnerIfRecorded` =
`"Component architecture + CLI/operability"`.

#### Fields from the brief's checklist that are ABSENT

`liveHarnessCellVerbatim` is present; `liveCellVerbatim` is present; `liveRowVerbatim` is present.
Absent: `namedCorpusNotAuthored` per-class `id`/`class` pairing beyond the single class;
`executionObligationOwner` (the file uses `executionObligationOwnerIfRecorded`). `doesNot` and
`failsIf` are present and quoted in section 3.

### 1.4 ROW twin join

Path: `docs/coop/artifacts/sdk-leftover-join.v6.json`
sha256: `e91d6e926830833d563bb89f3693d65328173af6f0d42275ad5339ef73880341`
`$.artifact` = `"sdk-leftover-join.v6"`, `$.registerRow` = `"DR-125"`, `$.date` = `"2026-08-24"`,
`$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.binds` = `"NOTHING"`,
`$.head` = `"96b5249e5ea0e89ddeb42db2abaca8ad1825bc02"`, `$.requiredNowUnchanged` = `28`,
`$.file08StatusToken` = `"OPEN"`. `$.liveGateOwners.DR-G20` =
`"Component architecture + CLI/operability"`.

Its object for the same id, JSON path `$.obligations[3]`:

```json
{
  "id": "OBL-G20-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "Occupancy v2 namedCorpusNotAuthored carries one live harness-cell corpus class. Fixtures are unauthored. g20-named-corpus-catalog.v2 named that class; that naming is OBL-G20-NAMED-CATALOG, leftoverDesign false. D-056 Decision clause 5: authoring fixtures remains design work. This join does not invent those fixture bytes and does not invent reserved SDK APIs."
}
```

The two objects are string-identical.

**Cross-custody sentence.** Unlike `state-class-leftover-join.v4` (G19's ROW twin), this ROW join
carries **no** "g20 leftover-join.vN remains the current G20 GATE leftover-join … does not steal that
leftover as a closure" sentence. Verified: the string `g20 leftover-join` does not occur anywhere in
`sdk-leftover-join.v6.json`, nor does the token `GATE`. What it carries instead is a
same-row-scoped guard, `$.registerRowNote`, verbatim:

> registerRow is DR-125 because this join remasures leftover-design of DR-125 after occupancy v2
> (D-217). file08StatusToken is DR-125's own live token (OPEN). leftover-join.v5 remains frozen and
> is not current after this successor is recorded. This join does not steal OBL-SDK-API-RESERVED,
> does not invent reserved SDK APIs, does not close OBL-DR125-ACTIVATION, and does not SATISFY
> DR-125.

The D1 packet's §3.2 table (`DECISION-PACKETS/D1-fixture-authoring-delegation.md` line 173) gives the
cross-custody sentence for this row as `"does not invent reserved SDK APIs"`, not a
does-not-steal-the-GATE-closure sentence — consistent with what is in the bytes. See **OQ-G20-3**.

**Closing OBL-G20-FX-AUTHORING requires a successor on both joins.** The same id is measured
`leftoverDesign: true` on `g20-leftover-join.v6` `$.obligations[3]` (GATE, DR-G20) and on
`sdk-leftover-join.v6` `$.obligations[3]` (ROW, DR-125). D1 packet line 175, verbatim: "Closing a
fixture obligation would require a successor on *both* the GATE join and its ROW twin, since the same
id is measured true on both." The packet says so again for G20 specifically at line 264: "G20 [via
its ROW twin DR-125; closing `OBL-G20-FX-AUTHORING` needs a successor on both joins, section 3.2
note]".

The ROW twin also carries a second leftover-design obligation that is **not** on the GATE join,
`$.obligations[5]`:

```json
{
  "id": "OBL-SDK-API-RESERVED",
  "leftoverDesign": true,
  "existingGate": "none",
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding",
  "reason": "D-110 records exact SDK APIs/frameworks remain reserved. v4 reserved first member is 'exact SDK APIs and frameworks'. v4 whatThisDoesNotDo: 'Does not choose an SDK language, framework, or API surface.' Undecided APIs are leftover-design (D-056). This join does not invent an envelope or an API surface."
}
```

The ROW join's reserved list, `$.reservedVerbatim`:
`["exact SDK APIs and frameworks", "argument-vector policy token list (already reserved from
DR-105)", "secret-handle storage mechanics (DR-108)", "D9 codes and exits (DR-007)", "doctor schema
(DR-114)", "control transport/framing/handshake (DR-102)"]`.

### 1.5 Governing contract/spec the occupancy pins

The occupancy pins **one contract as applied product law** for the NT classes and **one as adopted
control-plane law**, and explicitly refuses to apply the SDK contract candidate.

**`provider-only-output-contract.v3.json`** — path
`docs/coop/artifacts/provider-only-output-contract.v3.json`, sha256
`ef2a7416700cc8197486c6e29450673c60e3b94512be3ad278d1995f7d281309`,
`$.basedOn.providerOnlyV3.recording` = `"D-136"`,
`registerRowOfThatCandidate` = `"DR-133"`. `role`, verbatim:
> Consumed product law only for NT-4 (policy-verdict-threshold-waiver) and NT-7
> (planAdmission-refused). Product law stays on v3. This specification does not replace v3, does not
> SATISFY DR-133, and does not execute NT-4 or NT-7 by existing.

The two classes it defines, verbatim, with JSON paths in that file:

`$.negativeTests["NT-4"]`:
```json
{
  "name": "policy-verdict-threshold-waiver",
  "input": "Any constructible extra member or frame attempting to set policyOutcome, verdict, threshold, waiver, or gate.",
  "pass": "Refused before admission. Host policy is not taken from the component."
}
```

`$.negativeTests["NT-7"]`:
```json
{
  "name": "planAdmission-refused",
  "input": "A constructible extra member or frame attempting planAdmission or RequestId/ExecutionId allocation.",
  "pass": "Rejected before admission. Admission remains host-owned."
}
```

`$.negativeTests.status` = `"specified-classes-not-executed"`; `$.negativeTests.protocolJoin` =
`"Classes are constructible against the pinned TypeScript provider wire below. This artifact does not
change that wire."` The wire pin, `$.negativeTests.wirePin`:
```json
{
  "path": "docs/coop/artifacts/delivery.v2.json",
  "sha256": "47b6cfd17338fafd407c554afe1951ab23d2896aac99bcfd272fc0894e3cabf3",
  "namedMajor": "TypeScript provider major 1 as stated in those bytes",
  "note": "delivery.v4 is a derivation over v2. Classes are constructible against these v2 bytes. DR-011-R04 contest on later checkers does not move this pin."
}
```
This is the constructibility warrant for the NT-4/NT-7 fixture bytes: the classes are constructible
against `delivery.v2.json`.

For completeness, the file also defines the NT classes G20 explicitly does **not** execute:
`$.negativeTests["NT-1"]` (`"unknown-Finding-frame"`), `["NT-2"]`
(`"extra-finding-member-on-closed-payload"`), `["NT-3"]` (`"fact-candidate-masquerade"`), `["NT-5"]`
(`"coverage-domain-mutation"`), `["NT-6"]` (`"d9-exit-hosttermination-refused"`). There is **no NT-8**
key in this file (see **OQ-G20-4**).

**`provider-only-nt-gate-join.v6.json`** — path
`docs/coop/artifacts/provider-only-nt-gate-join.v6.json`, sha256
`93bc62d43751d8037aa2a696209eccbdee0ae3b3f11292d9a05be2bc245082a3`,
`$.basedOn.ntJoinV6.recording` = `"D-144 / D-145"`. `role`, verbatim:
> Routing only. NT-4 and NT-7 are capable of riding this already-named identifier. NT-1, NT-2, NT-6
> ride G21. NT-3 and NT-5 do not ride this gate. Naming is not execution.

**`control-protocol-contract.v2.json`** — path
`docs/coop/artifacts/control-protocol-contract.v2.json`, sha256
`c50a79fef566ecccbd8913a3d309b0cf7332f7d77f892474a548ef3d7b4ebdca`,
`$.basedOn.controlV2.recording` = `"D-015 / D-085"`, `registerRowOfThatCandidate` = `"DR-102"`.
`role`, verbatim:
> Consumed adopted control-plane non-translation law only. CC-1..CC-11 execution remains at DR-G21.
> This specification does not rewrite DR-102, does not execute CC-1..CC-11, and does not author a
> control-plane provider Cancel.

**`component-sdk-contract.v4.json` — cited, NOT applied.** Path
`docs/coop/artifacts/component-sdk-contract.v4.json`, sha256
`c53d541f12258eb96e86f0f5dbd3924a5f2e189d19c8f8672bae9037532461c3`, D-110, DR-125,
`statusOnThatCandidate` = `"CANDIDATE-NOT-APPLIED"`, `binds` = `"NOTHING"`. `role` ends: "Live
exact-byte intent is taken from live file 02, live file 08, and naming v6. v4's g20 block names this
already-named identifier and remains NAMED-NOT-AUTHORED on those bytes." `$.v4Standing` repeats:
"Applying v4 is a later D-000 act. This specification is not that act." `$.failsIf[32]` fails the
spec if "component-sdk-contract.v4 is treated as applied".

**Live architecture bytes, the actual exact-byte source.**

`$.basedOn.file02DeveloperContract.path` = `"docs/v2/architecture/02-distribution-and-components.md"`,
`sha256` `"1811c682cf293e1e0b255be82c62f7ed3c439f0873eb7922bfb0ad965b43f7db"` (recomputed at HEAD:
identical), `sourceHeadings` = `["Exact control/data-plane demarcation", "Common component developer
and operability contract"]`, `controlPlaneLines` = `"155-173"`, `developerContractLines` =
`"175-193"`. Verified: line 175 is `## Common component developer and operability contract`. The
operative bytes, verbatim (file 02 lines 177-193):

> Language analyzers and tools do not reimplement cross-cutting product behavior.
> The host plus common component SDK/control contract provides standardized:
>
> - structured candidate/result/fault envelopes and host-owned human, JSON, and
>   applicable SARIF projection;
> - diagnostic taxonomy, redaction, bounds, structured logging/audit correlation,
>   progress, and status;
> - schema-checked configuration with provenance and brokered secret handles;
> - artifact/state brokers, cancellation, deadlines, resource reporting, trust,
>   lifecycle, and doctor integration; and
> - consistent host verdict/finalization/D9/exit mapping.
>
> Components may emit domain candidates, events, progress, and bounded structured
> diagnostics. They must not render product UI directly, choose verdicts/exits,
> seal evidence, write unstructured host logs, or bypass host brokers. Common
> look-and-feel and operability across components are DR-125/DR-G20 acceptance
> properties; exact SDK APIs and frameworks remain implementation design.

`$.basedOn.file01OutputProjections.path` =
`"docs/v2/architecture/01-semantic-model-and-host-authority.md"`, `sha256`
`"40ab9a3e5713c9294a2c90a4b0e7c46065277b1e7013ed5023027a90c10193e1"` (recomputed at HEAD:
identical), `sourceHeading` = `"Output projections, including SARIF"`, `sourceLines` = `"108-133"`.
Verified: line 108 is `## Output projections, including SARIF`. The operative bytes for the SARIF
axis, verbatim (file 01 lines 110-114):

> **Preserved V1 output-surface contract.** Output surfaces are host-owned
> projections over the same canonical result; they do not participate in analysis,
> policy, evidence authority, finalization, or D9. For applicable findings/results,
> SARIF 2.1.0 remains an optional projection. This does not require every command
> to offer SARIF, and SARIF is not OpenSIP's native result or evidence format.

and the historical G17 mention (file 01 lines 128-133), verbatim:

> **V2 readiness extension.** DR-122 and DR-G17 require the V1 projection contract
> to be carried into the component architecture through a stable host-owned machine
> surface, explicit command/capability applicability, schema/version behavior, and
> parity/loss conformance goldens. This is readiness evidence for the V2 packaging
> and host boundary, not a new semantic authority or a universal SARIF command
> requirement.

**Supporting named/INPUT artifacts.**

- `docs/coop/artifacts/g20-named-corpus-catalog.v2.json`, sha256
  `9b9a23637dfaeebb0831dc10651a144c8bb446ad95a3397feb20d8c9f13a8997`. `$.classes[0].id` =
  `"component-role-surface-matrix"`; `exactByteIntentVerbatim`, `passPropertyVerbatim`,
  `slashJoinerVerbatim` and `surfacesInOrder` are string-identical to the occupancy's;
  `"fixtureBytes": "NOT-AUTHORED"`, `"sdkApiInvented": false`. `$.findingDisposition[0].id` =
  `"CLAUDE-G20CAT-V1-SF1"` (SHOULD-FIX, ACCEPTED, landed at `classes[0].slashJoinerVerbatim` and
  `classes[0].surfacesInOrder`) — that is why v2 supersedes v1.
- `docs/coop/artifacts/g20-named-corpus-catalog.v1.json`, sha256
  `e1311c458791c5f58425749c07fc2a3382f8244bbbb68fd7656dbe372adedb68` — the predecessor whose
  SHOULD-FIX v2 landed. Not current.
- `docs/coop/artifacts/g20-input-corpus.v1.json`, sha256
  `45b3e2d9acf897cd2c25a5b9fd8dc0f5b5769f6f54df97ba0881004831b40109`. `$.initialStates` has **one**
  entry: `{"id": "G20.component-role-surface-corpus", "memberVerbatim": "component-role ×
  human/JSON/SARIF/doctor/fault/config/progress/log corpus", "fixtureBytes": "NOT-AUTHORED",
  "assertsNamedNotAuthored": ["component-role × human/JSON/SARIF/doctor/fault/config/progress/log
  corpus"]}`. `$.whatIsAuthored`, verbatim: "One named initial state quoting live G20 v1
  namedCorpusNotAuthored. No fixture bytes. No reserved SDK APIs invented."

**Note the asymmetry with G19.** G19's INPUT corpus enumerates 12 named initial states; G20's
enumerates one, the corpus class itself. G20's fixture authoring therefore has no per-cell
`inputCorpusId` skeleton to cite — the cell enumeration must be derived from the occupancy's
`exactByteIntent` directly. See **OQ-G20-5**.

## 2. Coverage set

Axis A — platforms (4). Source `harness.DR-G20.component-operability.v2.json#$.platforms`.

1. `"macos/arm64"` — `#$.platforms[0]`
2. `"macos/x86_64"` — `#$.platforms[1]`
3. `"linux/x86_64"` — `#$.platforms[2]`
4. `"linux/arm64"` — `#$.platforms[3]`

Excluded, verbatim: `"D-002 defers Windows with explicit disposition. Windows is not a G20
platform."` — `#$.windowsStanding`; `#$.failsIf[45]` = `"Windows is treated as a G20 platform"`.

Axis B — admitted external component roles (1 in D-002 slice 1). Source
`#$.namedCorpusClasses[0].exactByteIntent` ("one cell per admitted external role (D-002 slice-1: the
TypeScript provider only)") and `#$.componentRoleAxis.slice1` ("The TypeScript provider.").

5. The TypeScript provider.

Bound, verbatim, `#$.componentRoleAxis.rule`: "The corpus must include every external role admitted
in the then-current D-002 slice. A role identity absent from the corpus fails. Skipping a live
surface for a role that is in the corpus also fails. This axis is bound to D-002 admitted identities,
not to a corpus already chosen." Excluded: a second slice-1 language role and Rust-as-core
(`#$.failsIf[44]`).

Axis C — corpus surfaces (8). Source `#$.namedCorpusClasses[0].surfacesInOrder`, identical to
`#$.namedCorpusWhenFixturesExist.liveCellSurfaces` and
`sdk-leftover-join.v6.json#$.surfacesInOrder`.

6. `"human"`
7. `"JSON"`
8. `"SARIF"`
9. `"doctor"`
10. `"fault"`
11. `"config"`
12. `"progress"`
13. `"log"`

Bound, verbatim, `#$.failsIf[26]`: "a live surface (human, JSON, SARIF, doctor, fault, config,
progress, or log) is dropped". And `#$.namedExecutesLimbNote`: "Live file 08 corpus surfaces
human/JSON/SARIF/doctor/fault/config/progress/log are not optional."

Axis D — live-cell retained-evidence members (4). Source
`#$.liveCellMemberSplit.membersInOrder[0..3]` and `#$.retainedEvidence[0..3]`.

14. EV-1 `"structured envelope and diagnostic parity"`
15. EV-2 `"redaction/bounds/audit correlation"`
16. EV-3 `"broker/cancellation/resource behavior"`
17. EV-4 `"no direct UI/unstructured logs/final authority"`

Bound, verbatim, `#$.failsIf[24]`: "live-cell punctuation is collapsed or extras are invented
(members must be structured envelope and diagnostic parity, redaction/bounds/audit correlation,
broker/cancellation/resource behavior, no direct UI/unstructured logs/final authority)".

Axis E — DR-133 negative-test classes riding G20 (2). Source
`#$.namedCorpusWhenFixturesExist.dr133` = `["NT-4", "NT-7"]`, corroborated by
`#$.namedExecutesLimbs[2]` and `#$.retainedEvidence[3].suppliedBy`.

18. `NT-4` — `provider-only-output-contract.v3.json#$.negativeTests["NT-4"]`,
    `"policy-verdict-threshold-waiver"`.
19. `NT-7` — `provider-only-output-contract.v3.json#$.negativeTests["NT-7"]`,
    `"planAdmission-refused"`.

Excluded by name, verbatim, `#$.namedCorpusWhenFixturesExist.doesNotExecute`: `["NT-1", "NT-2",
"NT-3", "NT-5", "NT-6", "CC-1", "CC-2", "CC-3", "CC-4", "CC-5", "CC-6", "CC-7", "CC-8", "CC-9",
"CC-10", "CC-11"]`; reinforced by `#$.failsIf[35]` and `#$.failsIf[36]`.

Axis F — EV-4 refusal limbs (3). Source `#$.retainedEvidence[3].exactByteIntent` clauses (a), (b),
(c); `#$.retainedEvidence[3].passProperty` "All three slash-joined limbs refuse."

20. (a) a component attempting to render product UI
21. (b) a component writing unstructured host logs
22. (c) a component attempting final authority: verdict/exit choice, evidence sealing,
    policyOutcome/verdict/threshold/waiver/gate (NT-4), or planAdmission/RequestId/ExecutionId
    allocation (NT-7)

Axis G — EV-3 named cancellation traces (7). Source `#$.retainedEvidence[2].exactByteIntent`. The
record states these are **not** extra corpus classes ("D-110 honesty-work O-1 traces named here as
the same live cancellation limb, not as extra corpus classes"; `#$.namedCorpusNote`: "T-1
cancellation traces … are not live harness-cell corpus classes and are not invented as extra
corpus-class ids"). They are nonetheless explicitly enumerated quantifiers the D-293 default policy
requires be preserved.

23. `terminal-wins`
24. `Cancel-wins`
25. `write-fails`
26. `already-terminal/already-cancelled`
27. `no-in-band`
28. `drain-then-death-last` (D-110 honesty-work O-1)
29. `death-completes-T-5-without-restart` (D-110 honesty-work O-1)

Plus the fate constraint, verbatim: "Observed process death is not a Cancel trigger."

Axis H — executes limbs (3). Source `#$.namedExecutesLimbs`, with `#$.failsIf[27]` = "a naming-v6
executes limb is omitted".

30. `"First-party TypeScript provider uses common host envelopes/logs/progress/config/doctor/exit"`
31. `"No direct rendering, unstructured host logs, or authority escape"`
32. `"DR-133 NT-4, NT-7 execute here (provider-only-output-contract.v3 / provider-only-nt-gate-join.v6)"`

**Arithmetic.**

- Surface-matrix corpus class: 1 admitted role (axis B) × 8 surfaces (axis C) × 4 platforms (axis A)
  = **32 cells**. Quantifier verbatim, `#$.namedCorpusClasses[0].passProperty`: "Every admitted-role
  × {human, JSON, SARIF, doctor, fault, config, progress, log} cell is present on all four D-002
  platforms. A skipped role, surface, or platform is not a waiver."
- NT-4/NT-7 refusal fixtures, which `#$.retainedEvidence[3].suppliedBy` names as an addition to the
  corpus class ("NT-4 and NT-7 when independently pinned fixtures exist"): 2 classes (axis E) × 1
  role × 4 platforms = **8 cells**. Quantifier verbatim, `#$.retainedEvidence[3].passProperty`: "NT-4
  and NT-7 refuse before admission once independently pinned fixtures exist. … Pass all four D-002
  platforms × every admitted role."

**Total coverage members: 40** (32 + 8).

Overlays, not extra axes: each of the 32 surface cells "retains the four live-cell members as they
apply to that role × surface" (`#$.namedCorpusClasses[0].exactByteIntent`), giving 128
member-instances; the 7 EV-3 cancellation traces (axis G) are witness requirements riding the config,
fault, progress and doctor cells, and the 3 EV-4 refusal limbs (axis F) ride the same 32 cells plus
the 8 NT cells. Whether the 7 traces require 7 × 4 = 28 additional payloads is not settled —
see **OQ-G20-6**.

## 3. Prohibitions bounding the authoring

From the GATE join `docs/coop/artifacts/g20-leftover-join.v6.json`:

- `$.obligations[3].reason`: "This join does not invent those fixture bytes and does not invent
  reserved SDK APIs."
- `$.doesNot[1]` = `"Does not steal OBL-SDK-API-RESERVED."`
- `$.doesNot[2]` = `"Does not invent reserved SDK APIs."` — **this is the prohibition the brief
  names.**
- `$.doesNot[3]` = `"Does not close OBL-DR125-ACTIVATION."`
- `$.doesNot[4]` = `"Does not invent a D9 code, exit number, or HostTermination."`
- `$.doesNot[5]` = `"Does not invent a section 7.1 recipe."`
- `$.doesNot[9]`, `[10]`, `[11]`, `[12]`, `[13]`, `[14]`, `[15]` = the row/required-now/file-08
  guards.
- `$.obligations[5].reason`: "This join does not steal that leftover, does not invent an SDK
  language, framework, API surface, secret-handle store, doctor schema, or D9/exit map, and does not
  close OBL-DR125-ACTIVATION on DR-118."

From the occupancy `harness.DR-G20.component-operability.v2.json`:

- `$.discipline`: "SPECIFICATION OF EXACT-BYTE INTENT. Occupancy remasurement of the already-named
  identifier. No harness runs here. No fixture bytes authored. No SDK APIs invented. NT-4/NT-7 are
  not executed by this file existing."
- `$.sdkHonesty`: "File 02 and D-110 reserve exact SDK APIs and frameworks as implementation design.
  This specification does not invent an SDK language, framework, API surface, secret-handle store,
  doctor schema, or D9/exit map."
- `$.d9Standing`: "This specification does not invent a D9 code, exit number, HostTermination, or
  section 7.1 recipe. Doctor D9 mapping remains DR-007-open / DR-114. Component-minted
  D9/exit/HostTermination refusal is NT-6 on G21, not this gate."
- `$.v4Standing`: "component-sdk-contract.v4.json sha256
  c53d541f12258eb96e86f0f5dbd3924a5f2e189d19c8f8672bae9037532461c3 is cited. Binds NOTHING. Not
  applied. D-110 recorded it as DR-125's accepted design-contract successor candidate. Applying v4 is
  a later D-000 act. This specification is not that act."
- `$.namedCorpusClasses[0].exactByteIntent` — the per-surface fences: "they do not restore G17
  advertised-parity fixtures and do not execute harness.DR-G26.preview-analyze-sarif-not-advertised";
  "they do not author G12 FC implementations, doctor schema, D9 mapping, or purge"; "they do not
  execute G21 crash/panic/timeout/resource/malformed/truncated/duplicate/EOF/process-tree/recovery";
  "they do not invent DR-108 storage mechanics and do not score G09 requested/granted/denied tables";
  "they do not admit unstructured host logs"; "A second slice-1 language role is not a cell. Windows
  is not a cell."
- `$.retainedEvidence[0].exactByteIntent`: "A component-emitted SARIF body is not admitted as a
  host-owned advertised projection. … This member does not restore G17 advertised-parity fixtures,
  does not execute G26, and does not invent a SARIF schema."
- `$.retainedEvidence[1].exactByteIntent`: "This member does not author G12 FC-REDACT, does not
  invent a doctor schema, and does not score G21 EV-5 containment diagnostics."
- `$.retainedEvidence[2].exactByteIntent`: "This specification does not author a control-plane
  provider Cancel, does not execute G21 process-tree kill/reap or core survival, does not invent
  DR-108 secret storage, and does not score G09 grants."
- `$.retainedEvidence[3].exactByteIntent`: "Product law for NT-4 and NT-7 stays on
  provider-only-output-contract.v3. … This member does not invent a D9 code, exit number, or
  HostTermination. NT-6 (component-minted D9/exit/HostTermination) remains G21. G27 is
  preview-result-not-sealed-Run, not this member. G28 is host-must-not-mint from pack
  threshold/policyOutcome, not this member."
- `$.g17Standing`, `$.g26Split`, `$.dr122Split`, `$.dr133Split`, `$.dr125Split`, `$.dr108Split`,
  `$.dr102Split`, `$.dr114Split`, `$.dr007Split`, `$.g12Split`, `$.g21Split`, `$.g09Split`,
  `$.g10Split`, `$.g15Split`, `$.g18Split`, `$.g22Split`, `$.g23Split`, `$.g27Split`, `$.g28Split`
  — each fences an adjacent row or gate. `$.g21Split`, verbatim: "G21 is component-failure
  containment: crash/panic/timeout/resource/malformed/truncated/duplicate/EOF/process-tree/recovery;
  CC-1..CC-11; NT-1, NT-2, NT-6; FC-NC consented CA-1 process-tree. EV-3 death-ordering traces are
  host cancellation composition, not G21 core survival or kill/reap. NT-6 remains G21. This
  specification does not take over harness.DR-G21.component-failure-containment and does not execute
  CC-1..CC-11."
- `$.failsIf[]` — the operative subset for authoring: `[11]` "this specification is read as authoring
  fixture bytes"; `[12]` "this specification is read as inventing SDK APIs"; `[13]` "NT-4 or NT-7 is
  treated as executed by this file existing"; `[23]` "a live G20 retained-evidence member is omitted
  or has no exactByteIntent / passProperty"; `[24]` (live-cell punctuation, quoted in section 2);
  `[25]` "component-role × human/JSON/SARIF/doctor/fault/config/progress/log corpus is omitted from
  exact-byte intent"; `[26]` (surface dropped, quoted in section 2); `[27]` "a naming-v6 executes
  limb is omitted"; `[28]` "SARIF is invented as advertised"; `[29]` "G17 is restored or named into
  required-now"; `[30]` "preview analyze is treated as advertising SARIF"; `[31]` "G26 is stolen";
  `[32]` "component-sdk-contract.v4 is treated as applied"; `[33]` "v4 SF-* identifiers are treated
  as this harness's applied property set"; `[34]` "an SDK API, language, or framework is invented";
  `[35]` "NT-3 or NT-5 is treated as executing here"; `[36]` "NT-1, NT-2, or NT-6 is treated as
  executing here"; `[37]`-`[41]` the G12/G21/G15/G22/G09 steal guards; `[42]` "a D9 code, exit
  number, or HostTermination is invented"; `[43]` "a section 7.1 recipe is invented"; `[44]`
  "Rust-as-core is minted or a second slice-1 language role is invented"; `[45]` "Windows is treated
  as a G20 platform"; `[46]` "componentRoleAxis is bound to a corpus already chosen rather than to
  D-002 admitted identities"; `[47]` "OBL-DR125-ACTIVATION is treated as closed".
- `$.doesNot[6]` = `"Does not author fixture bytes."`; `[7]` = `"Does not apply
  component-sdk-contract.v4."`; `[8]` = `"Does not invent an SDK API, language, or framework."`;
  `[9]` = `"Does not invent SARIF as advertised."`; `[10]` = `"Does not restore G17 or name G17 into
  required-now."`; `[12]` = `"Does not execute DR-133 NT-4 or NT-7 by existing."`; `[13]` = `"Does
  not execute NT-3 or NT-5."`; `[14]` = `"Does not execute NT-1, NT-2, or NT-6."`.

From the ROW twin `sdk-leftover-join.v6.json`: `$.doesNot[5]` = `"Does not invent an SDK API."`;
`$.doesNot[9]` = `"Does not choose an SDK language, framework, or API surface."`; `$.doesNot[10]` =
`"Does not invent an envelope."`; `$.doesNot[11]` = `"Does not mint D9/exit values."`; `$.doesNot[12]`
= `"Does not rewrite DR-102 bytes."`; `$.doesNot[13]` = `"Does not advertise SARIF in preview."`;
`$.doesNot[14]` = `"Does not invent secret-storage mechanics."`; `$.doesNot[22]` = `"Does not invent
fixture bytes."` Its `$.reservedVerbatim` (six members, quoted in 1.4) is the reserved list the
corpus may not populate.

From `g20-named-corpus-catalog.v2.json#$.whatIsNotAuthored`, verbatim:
`["fixture bytes", "an SDK language, framework, or API surface", "an envelope", "a D9 code"]`.

From `g20-input-corpus.v1.json#$.whatIsNotAuthored`, verbatim:
`["fixture bytes", "reserved SDK APIs", "an SDK language, framework, or API surface", "a D9 code,
exit number, or HostTermination"]`.

**D-293 constraint clause applying to G20**, from
`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md` line 3, verbatim:

> **Delegated to the orchestrator under separate D-000 review:** fixture choices for G15, G16, G18,
> G19, G20, G21 and G24–G30; `OBL-HOSTILE-GOLDENS`; the two DR-122 SARIF fixture obligations.
> Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new
> D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep
> their recorded prohibitions (adapter, CI encoding, journal).

and line 5, verbatim:

> **Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit platform,
> matrix, mutation, transition and fate quantifier in the governing bytes is preserved; for delegated
> byte sets, concrete witness bytes may be selected only within already-recorded schemas and fates,
> and any choice that would create a new semantic member, identifier, value, list or implementation
> stays outside the grant (recorded as a named open decision instead).

The "no … SDK API … reserved list … is authorised" limb is the operative one for G20: it matches
`sdk-leftover-join.v6.json#$.reservedVerbatim` and `harness…v2.json#$.sdkHonesty` exactly.

## 4. Dependencies

**OBL-SDK-API-RESERVED is NOT a hard dependency.** The brief asks whether the reserved-SDK-API
obligation on the ROW twin blocks the fixture. It does not: the record explicitly holds SDK-API
variance harmless to the coverage set. `harness.DR-G20.component-operability.v2.json`
`$.componentRoleAxis.sdkMayVary`, verbatim:

> SDK implementation may vary behind contract. Variance of SDK API or framework is not a skipped
> role, not a skipped surface, and not a skipped platform. Exact SDK APIs and frameworks remain
> implementation design.

repeated as a pass condition at `$.namedCorpusClasses[0].passProperty`: "SDK-implementation variance
is not a skipped cell." And `$.waiverRuleVerbatim` carries it into the file 08 waiver itself: "all
external roles; SDK implementation may vary behind contract". The obligation's own reason,
`sdk-leftover-join.v6.json#$.obligations[5].reason`, verbatim, states it as a reservation over a
*later implementation*, not over the corpus: "D-110 records exact SDK APIs/frameworks remain
reserved. v4 reserved first member is 'exact SDK APIs and frameworks'. v4 whatThisDoesNotDo: 'Does
not choose an SDK language, framework, or API surface.' Undecided APIs are leftover-design (D-056).
This join does not invent an envelope or an API surface." The corresponding constraint on the fixture
is a **prohibition** (section 3), not a precondition.

**NT-4/NT-7 fixture standing at G20 — a recorded sequencing item that is NOT a byte dependency.**
`DECISIONS-RECOMMENDED.md` (repo root, sha256
`42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`), heading
`## B2 — DR-133 Class A opening (provider-only-output-contract.v3)` (line 139),
`### Recommendation (Claude, round 2)` (line 146), bullet at line 152, verbatim:

> - NT-6 authoring treated as established work, and the NT-4/NT-7 fixture standing at G20
>   byte-resolved before deciding what authoring is required there;

and `### Codex (round 2): AGREE — confidence medium` (line 157), line 159, verbatim:

> Before the later per-row cycle, separately record the proposed file-01 preview-role delta or remove
> reliance on it, author NT-6, byte-resolve NT-4/NT-7 fixture standing at G20 and author what remains
> unauthored, and make the shared Gate-2 entry reconcile D-147 with the current G20/G21 joins through
> a distinct DR-133 finding.

That programme was adopted by the owner at COORD `## D-293 — User decisions: adopt the agreed
recommendations on DECISIONS-NEEDED A–F` (line 16169), Decision 5, verbatim (COORD lines 16228-16237):

> 5. **B1 (DR-131), B2 (DR-133), B3 (DR-117).** The agreed
>    programmes are authorized in the agreed order — for DR-117:
>    `preview-product-boundary-successor.v9` re-citing the twelve
>    current joins and stating its relationship to
>    `product-boundary-successor-contract.v8` (D-116); a fresh
>    application-grade dual review bound to v9's final digest; then
>    the owner-controlled opening entry; then G29/G30 fixture
>    authoring; then a separate SATISFIED-GRADE + MF-6 cycle. The
>    D-056 Class A openings themselves are separate owner-controlled
>    entries; this entry opens none of them.

**Byte-resolution of the NT-4/NT-7 standing at G20, performed here from the bytes:** NT-4 and NT-7
*are* G20 fixture members. Three independent statements say so:

1. `harness.DR-G20.component-operability.v2.json#$.namedCorpusWhenFixturesExist.dr133` = `["NT-4",
   "NT-7"]`, against a `doesNotExecute` list of 16 identifiers that excludes NT-1, NT-2, NT-3, NT-5,
   NT-6 and CC-1..CC-11.
2. `#$.retainedEvidence[3].suppliedBy`, verbatim: "component-role ×
   human/JSON/SARIF/doctor/fault/config/progress/log corpus class. NT-4 and NT-7 when independently
   pinned fixtures exist. Not authored."
3. `#$.namedExecutesLimbs[2]`, verbatim: "DR-133 NT-4, NT-7 execute here
   (provider-only-output-contract.v3 / provider-only-nt-gate-join.v6)", with `#$.passProperty` ending
   "NT-4 and NT-7 are not executed by this file existing."

Their product law is already pinned and constructible:
`provider-only-output-contract.v3.json#$.negativeTests["NT-4"]` and `["NT-7"]` give `input` and
`pass` strings, and `$.negativeTests.wirePin` pins `delivery.v2.json` sha256
`47b6cfd17338fafd407c554afe1951ab23d2896aac99bcfd272fc0894e3cabf3` with "Classes are constructible
against these v2 bytes." Nothing unauthored stands between the corpus and an NT-4 or NT-7 witness.

**This is a sequencing dependency, not a byte dependency, and it is distinct from DR-133's Class A
opening.** DR-133's own Class A opening is **unrecorded and owner-controlled**: D-293 Decision 5 says
"The D-056 Class A openings themselves are separate owner-controlled entries; this entry opens none
of them." If G20 fixture authoring were read as sequenced behind that opening, that would be a
sequencing dependency on an owner-controlled recording act — still not a byte dependency, because
NT-4 and NT-7 are fully defined at `provider-only-output-contract.v3.json#$.negativeTests` and
constructible against the pinned `delivery.v2.json` wire. The B2 programme places the G20 NT-4/NT-7
byte-resolution
*before* the later per-row / SATISFIED-GRADE cycle; it does not place it behind the Class A opening,
and it does not condition G20 fixture authoring on that opening. G20's own occupancy carries the
same separation: `#$.dr133Split`, verbatim: "provider-only-output-contract.v3 remains DR-133's
accepted design-contract candidate (D-136). DR-133 stays OPEN. NT-4 and NT-7 product law stays on v3.
This specification names those classes as riding here. Naming is not execution. It does not SATISFY
DR-133." A G20 fixture corpus authored now would satisfy the B2 item "byte-resolve NT-4/NT-7 fixture
standing at G20 and author what remains unauthored" rather than wait on it. See **OQ-G20-7** for the
one thing that is genuinely unsettled: whether the shared Gate-2 D-147 reconciliation must be
recorded first.

**Other candidate dependencies, checked and cleared:**

- *Platform list.* Not a dependency: `#$.platforms` and `#$.windowsStanding` are present on G20's own
  occupancy.
- *Component-role enumeration.* Not a dependency and not an open question: the record enumerates it
  as "the TypeScript provider only" for D-002 slice 1 (section 1.3).
- *Named corpus class.* Already named and accepted: `g20-leftover-join.v6.json#$.obligations[1]`
  measures `OBL-G20-NAMED-CATALOG` `leftoverDesign: false`.
- *INPUT states.* Named and accepted at the class level:
  `g20-leftover-join.v6.json#$.obligations[2]` measures `OBL-G20-INPUT-CORPUS` `leftoverDesign:
  false` — but only one state is named (section 1.5), so there is no per-cell input skeleton
  (**OQ-G20-5**). That is a shape question, not a block.
- *`component-sdk-contract.v4`.* Not a dependency, and applying it is forbidden (`#$.failsIf[32]`).
  Live exact-byte intent comes from file 02, file 08 and naming v6.
- *D9 / exit mapping (DR-007 / DR-114).* Not needed: NT-6, the D9-minting refusal, is explicitly
  **not** a G20 member (`#$.namedCorpusWhenFixturesExist.doesNotExecute` includes `"NT-6"`;
  `#$.d9Standing`: "Component-minted D9/exit/HostTermination refusal is NT-6 on G21, not this gate.").
- *DR-108 secret-handle storage.* Not needed: `#$.dr108Split`, verbatim: "Secret-handle storage
  mechanics remain DR-108 (deferred). This specification names brokered secret handles as host-owned
  config behavior and does not invent storage."
- *Owner reservation.* D-293 Decision 8 (COORD line 16264) delegates G20 and reserves the gate
  obligations at G07, G08, G09, G12, G14 and G22 "(with every current same-id ROW twin)". G20's ROW
  twin is DR-125, whose file 08 owner cell contains `security` — the D1 packet flags this at line 264
  as putting G20 in a *candidate* reserve set — but D-293's adopted reservation list is by gate id
  and does not name G20 or DR-125. G20 is delegated.
- *Recording cost, not a blocker.* Closing the obligation needs successors on both joins (section
  1.4).

**Standing: NOT BLOCKED**

## 5. Recommended artifact shape

Closest precedent: **`docs/coop/artifacts/g23-fixture-corpus.v4.json`**, sha256
`b3fce9f5bab6764919f5dc43c28a43f3d9c3b6be310e45c2c1bd08a617c755c5` (D-239) — same reason as G19: G20's
occupancy carries its own `platforms` array, so the corpus quotes the four tokens out of
`harness.DR-G20.component-operability.v2.json#$.platforms` and declares it does not invent a D-002
platform list.

G20 has no authored fixture bytes (`$.obligations[3].existingGate` = `"none as authored
implementations"`), so the first cycle is a **first-authoring** in the
`docs/coop/artifacts/g21-fixture-corpus.v1.json` shape (sha256
`861bb4e7d26a80158cc1cc3a0518c5e8e95311bee4d8c8ce63acd1e60d6c906d`, D-241): payloads under
`fixtures/<gate>.v1/` with **no platform subdirectory**; `authoredCatalog` keys `standing,
doesNotMutate, executionRemains, members`; member keys `id, class, inputCorpusId, path, sha256,
mutation, expected`. The per-platform copy layer is a separate later version
(`g21-fixture-corpus.v8.json`, sha256
`e8149a865e49bdcda9eda923e9918f332a83078f43ab6a3af9a10d6d31ef6359`, D-247, adding `payloadCount,
platformCount, copyCount` and `platformCopies[]`).

Fields the successor would carry, from `g23-fixture-corpus.v4.json`'s verified key list: `artifact,
version, date, documentClass, registerRow, registerRowNote, namedGate, status, reviewStatus,
sealRecommendation, binds, authorityClaim, purpose, basedOn, parentReview, file08Pin, head,
requiredNowUnchanged, file08StatusToken, platformsQuotedFromOccupancyV2,
windowsStandingQuotedFromOccupancyV2, cborConstruction, pinnedHostContext, whatIsAuthored,
whatIsNotAuthored, authoredCatalog, leftoverDesignClosedIfAcceptedAndRecorded,
leftoverDesignRemainingOnG23, remainderAfterThisCorpus, summary, doesNot, proposedLaterWork,
recordedInputs, remeasurementClause`, plus `failsIf` from the G21 lineage. G20-specific fields the
successor should add, by analogy with the G21 lineage's gate-specific fields: a
`sarifCellStanding` reciting `harness…v2.json#$.sarifAxisStanding`, and an
`sdkVarianceStanding` reciting `#$.componentRoleAxis.sdkMayVary`.

Proposed names and paths (proposals only; this record authors nothing):

- Artifact: `docs/coop/artifacts/g20-fixture-corpus.v1.json`.
- Fixture directory, first-authoring: `docs/coop/artifacts/fixtures/g20.v1/<id>.json`, mode 0444.
- Per-platform copy version, later:
  `docs/coop/artifacts/fixtures/g20.v2/<platform-with-slash-replaced-by-dash>/<id>.json` over the
  four tokens quoted from `harness.DR-G20.component-operability.v2.json#$.platforms`.
- `authoredCatalog.members[i].class` cites `"component-role-surface-matrix"` from
  `harness…v2.json#$.namedCorpusClasses[0].id` — never a new class id, and never an invented per-surface
  class id (`#$.namedCorpusClasses[0].slashJoiner`: "not eight invented corpus-class ids").
- NT-4 and NT-7 members cite `provider-only-output-contract.v3.json#$.negativeTests["NT-4"]` /
  `["NT-7"]` and the `wirePin` `delivery.v2.json` digest, as the g21 lineage cites its CC classes.
- `authoredCatalog.members[i].inputCorpusId` — G20's INPUT corpus offers only
  `"G20.component-role-surface-corpus"` (one id), so every member would cite the same id unless a
  successor INPUT corpus enumerates cells first (**OQ-G20-5**).

It is a **first-authoring** (no platform subdirectories). The per-platform copy is a separate later
version.

## 6. Effort and risk

**Number of cases.** 40 coverage members (section 2). As a first-authoring in the
`g21-fixture-corpus.v1` shape the unit is the platform-free cell: 1 role × 8 surfaces = 8 surface
cells, plus 2 NT refusal cells = **10 authored payloads**, before per-platform copying. The
per-platform copy version multiplies to 40 copies (10 × 4). By precedent scale: g23 v4 recorded 4
payloads × 4 platforms = 16 files; a G20 copy version is 2.5× that. Recorded cycle cost, from
`DECISION-PACKETS/D1-fixture-authoring-delegation.md` line 254, verbatim: "Each authored fixture
obligation needs a successor on the GATE join and, where a ROW twin exists (9 ids), on the ROW join
too; each such successor is itself a dual-review act (STATUS 3A: '~30–45 min per act when reviews
pass first time' for remasurement acts; fixture-corpus acts cost more, see section 4)." G20 has a ROW
twin, so: fixture corpus + `g20-leftover-join.v7` + `sdk-leftover-join.v7`, three recorded acts
minimum. If the per-cell INPUT skeleton is authored first (**OQ-G20-5**), add a
`g20-input-corpus.v2` act.

**(a) Witness-byte selections inside recorded schemas — choices available under the grant.**

1. The concrete envelope payload bytes for the human and JSON cells, drawn from file 02's already-recorded
   "structured candidate/result/fault envelopes" family (file 02 lines 180-181) — provided nothing is
   named that is not already in that phrase.
2. The `expected` fate string per member, drawn verbatim from the recorded passProperties: "Refused
   before admission" (NT-4), "Rejected before admission" (NT-7), "fails" for the EV-1..EV-4 negative
   forms.
3. Which of the 7 named EV-3 cancellation traces (axis G) a given config/fault/progress/doctor cell
   instantiates — the trace names are already recorded, so selecting among them is a witness choice.
4. The SARIF cell's non-advertisement record form, provided it records non-advertisement and nothing
   else (see risk).
5. The serialization and digest of each payload, provided the construction is declared.
6. Platform copy ordering and directory naming, since `$.platforms` is quoted rather than invented.
7. Member ids within `authoredCatalog.members[]`, which are catalog-local.

**(b) Choices that would create new semantics — record as a named open decision, do not choose.**

1. *Any concrete SDK API, language, framework, or envelope type name.* Violates
   `harness…v2.json#$.sdkHonesty`, `#$.failsIf[34]`, `g20-leftover-join.v6.json#$.doesNot[2]`,
   `sdk-leftover-join.v6.json#$.doesNot[9]`/`[10]`, and the D-293 "no … SDK API … is authorised"
   limb. **Record as a named open decision, do not choose.**
2. *An advertised SARIF projection, a SARIF schema, or a G17 advertised-parity fixture.* Violates
   `#$.failsIf[28]`/`[29]`/`[30]`, `#$.retainedEvidence[0].exactByteIntent` ("does not invent a SARIF
   schema"), and `#$.sarifAxisStanding.d077`. **Record as a named open decision, do not choose.**
3. *Applying `component-sdk-contract.v4` SF-* identifiers as fixture class or member ids.* Violates
   `#$.failsIf[32]`/`[33]` and `#$.liveCellMemberSplit.notInventedExtras` ("v4 SF-* identifiers as
   applied live members"). **Record as a named open decision, do not choose.**
4. *A D9 code, exit number, HostTermination, or section 7.1 recipe anywhere in the corpus.* Violates
   `#$.d9Standing`, `#$.failsIf[42]`/`[43]`, and the D-293 "no … new D9/HostTermination/pack
   semantics" limb. **Record as a named open decision, do not choose.**
5. *An NT-6 witness on G20.* Violates `#$.namedCorpusWhenFixturesExist.doesNotExecute` (which lists
   `"NT-6"`), `#$.failsIf[36]`, and `#$.g21Split` ("NT-6 remains G21"). **Record as a named open
   decision, do not choose.** (NT-6 authoring is B2 programme work at G21, not G20 — see G21.md.)
6. *An NT-1, NT-2, NT-3, NT-5 or CC-1..CC-11 witness on G20.* Same list, same `failsIf` entries.
   **Record as a named open decision, do not choose.**
7. *A doctor schema, doctor FC implementation, D9 mapping, or purge byte in a doctor cell.* Violates
   `#$.namedCorpusClasses[0].exactByteIntent` doctor clause and `#$.g12Split`; also collides with the
   D-293 owner reservation of the G12 gate obligations. **Record as a named open decision, do not
   choose.**
8. *DR-108 secret storage mechanics in a config cell.* Violates `#$.dr108Split` and the corpus class's
   config clause. **Record as a named open decision, do not choose.**
9. *A second slice-1 language role, Rust-as-core, or a second component role identity.* Violates
   `#$.componentRoleAxis.slice1`, `#$.failsIf[44]`, `#$.basedOn.d002.role`. **Record as a named open
   decision, do not choose.**
10. *Windows as a fifth platform copy.* Violates `#$.windowsStanding` and `#$.failsIf[45]`. **Record
    as a named open decision, do not choose.**
11. *Splitting EV-4 into three live-cell members, or promoting a surface (SARIF, doctor, human, JSON,
    fault, config, progress, log) to a live-cell member.* Violates `#$.failsIf[24]` and
    `#$.liveCellMemberSplit.notInventedExtras`. **Record as a named open decision, do not choose.**
12. *A control-plane provider Cancel, or treating observed process death as a Cancel trigger.*
    Violates `#$.retainedEvidence[2].exactByteIntent` ("Observed process death is not a Cancel
    trigger. This specification does not author a control-plane provider Cancel") and
    `#$.retainedEvidence[2].passProperty` ("A control-plane-authored provider Cancel fails").
    **Record as a named open decision, do not choose.**

**Risk.** Against the recorded reviewer-attack pattern: (a) *Inventing a vocabulary the record does
not carry* — this is G20's dominant risk, because the four live-cell members are all about
*structured* artifacts (envelope, log/audit-correlation identifier, broker handle, refusal record)
whose schemas are precisely what `$.sdkHonesty` and `$.reservedVerbatim` reserve. g23 v1 M2/M3 were
rejected for exactly this shape of over-reach (undeclared digest construction; an invented
Coverage-domain encoding vocabulary). Any concrete envelope field name a G20 fixture writes down is a
candidate SDK-API invention. See **OQ-G20-8**. (b) *Collapsing two classes the occupancy keeps
separate* — g23 v2 M1 rejected an EV-2/EV-3 collapse; G20's analogues are collapsing EV-2
redaction/bounds/audit into EV-4's unstructured-logs limb (the occupancy anticipates the overlap:
"Unstructured host logs fail this member on EV-4 as well"), and collapsing the human and JSON
surfaces into one "structured output" cell. (c) *Classifying a witness under a closed identifier it
does not belong to* — g21 v3 M1 rejected an RF-2 case filed under CC-5; G20's analogue is filing an
NT-6 or CC-* witness under G20, or filing a fault-surface crash witness that is actually G21's
`crash/panic/timeout/...` corpus (`#$.failsIf[38]`). (d) *Over-claiming closure* — g23 v1 S4;
on G20 `leftoverDesignClosedIfAcceptedAndRecorded` must not claim DR-125 closure, must not claim
`OBL-SDK-API-RESERVED` or `OBL-DR125-ACTIVATION` closure (`#$.failsIf[47]`), must not claim DR-133
SATISFIED (`#$.failsIf[15]`), and must not claim GATE closure without the ROW successor (section 1.4).

## 7. Open questions

**OQ-G20-1:** `$.componentRoleAxis.rule` binds the axis to "every external role admitted in the
*then-current* D-002 slice", and `$.componentRoleAxis.laterRoles` says a newly admitted role "enters
this axis by that admission". A fixture corpus authored today enumerates one role. The record does not
say whether a corpus is invalidated, or merely extended, when D-002 later admits a second role, nor
whether the corpus must carry a remeasurement clause naming that trigger. `#$.failsIf[46]` ("componentRoleAxis
is bound to a corpus already chosen rather than to D-002 admitted identities") makes the wrong answer
a failure, so this needs settling before authoring.

**OQ-G20-2:** What bytes does a SARIF cell contain? `$.sarifAxisStanding.isLiveCorpusAxis` is `true`
and `$.failsIf[26]` fails the spec if the SARIF surface is dropped, but
`$.sarifAxisStanding.previewAnalyzeAdvertisesSarif` is `false`,
`$.retainedEvidence[0].exactByteIntent` requires only "a SARIF-axis record that preview analyze does
not advertise SARIF 2.1.0", and `$.failsIf[28]` fails the spec if "SARIF is invented as advertised".
A cell that is a record of absence has no recorded byte schema. Whether it is a negative assertion
document, a refusal record in the EV-4 form, or something else is not settled by any pinned artifact.
This is the SARIF question the brief asks about: the *standing* is resolved (section 1.3), the *cell
content* is not.

**OQ-G20-3:** The ROW twin `sdk-leftover-join.v6.json` carries no cross-custody sentence naming the
G20 GATE join (verified: neither `g20 leftover-join` nor `GATE` occurs in the file), unlike
`state-class-leftover-join.v4` for G19 and unlike `monorepo-leftover-join.v4` / `lifecycle-leftover-join.v4`
per the D1 packet §3.2 table. Whether a `sdk-leftover-join.v7` successor recording the G20 fixture
closure must therefore *add* such a sentence, or whether the existing `$.registerRowNote` scope is
sufficient, is not settled by the record.

**OQ-G20-4:** `harness.DR-G20.component-operability.v2.json#$.retainedEvidence[3].exactByteIntent`
mentions "G28 is host-must-not-mint from pack threshold/policyOutcome, not this member", and the D1
packet's G28 row names `"host-must-not-mint corpus (DR-131 NT-7, NT-8)"` — but
`provider-only-output-contract.v3.json#$.negativeTests` has no `NT-8` key (it defines NT-1 through
NT-7 only). Whether NT-8 is a DR-131 identifier from a different contract, or a gap, is not settled by
the bytes read here. It does not affect the G20 coverage set (NT-8 is not in
`$.namedCorpusWhenFixturesExist`), but it affects any G20 corpus sentence that recites the G28 fence.

**OQ-G20-5:** G20's INPUT corpus (`g20-input-corpus.v1.json#$.initialStates`) names exactly one
initial state, the corpus class itself, whereas G19's names twelve per-cell states. The precedent
corpora (`g23-fixture-corpus.v4`, `g21-fixture-corpus.v1`) give every authored member an
`inputCorpusId`. The record does not say whether a G20 fixture corpus may cite the single class-level
id for all ten members, or whether a `g20-input-corpus.v2` enumerating the 8 surface cells and the 2
NT cells must be authored and reviewed first.

**OQ-G20-6:** Do the 7 named EV-3 cancellation traces require their own payloads?
`$.retainedEvidence[2].exactByteIntent` names them explicitly ("terminal-wins, Cancel-wins,
write-fails, already-terminal/already-cancelled, no-in-band … drain-then-death-last,
death-completes-T-5-without-restart") and `$.retainedEvidence[2].passProperty` requires "Host-owned
cancellation/deadline/resource behavior holds on the named traces", which under the D-293 default
policy ("every explicit … transition and fate quantifier in the governing bytes is preserved") reads
as 7 preserved quantifiers. But `$.namedCorpusNote` says "T-1 cancellation traces … are not live
harness-cell corpus classes and are not invented as extra corpus-class ids". Whether 7 traces × 4
platforms = 28 payloads are required, or whether they are witnessed inside the 4 config/fault/progress/doctor
cells, is not settled.

**OQ-G20-7:** Must the shared Gate-2 D-147 reconciliation be recorded before G20 fixture authoring?
`DECISIONS-RECOMMENDED.md` B2, Codex round-2 AGREE (line 159) lists four pre-cycle items in one
sentence — "separately record the proposed file-01 preview-role delta or remove reliance on it,
author NT-6, byte-resolve NT-4/NT-7 fixture standing at G20 and author what remains unauthored, and
make the shared Gate-2 entry reconcile D-147 with the current G20/G21 joins through a distinct DR-133
finding" — without stating an order among them. Section 4 reads the G20 item as satisfiable by
authoring; whether the D-147 reconciliation is a predecessor of that authoring or a sibling is not
settled. This is a **sequencing** question about recording order, not a byte dependency: no G20
fixture cell is unconstructible without it.

**OQ-G20-8:** How concrete may an envelope, log record, or refusal record be before it becomes an
invented SDK API? `$.retainedEvidence[0..3].exactByteIntent` require digest-pinned records with named
content (a diagnostic taxonomy, "audit-correlation identifiers joining the structured envelope to the
host log", broker handles, refusal records naming `policyOutcome`, `verdict`, `threshold`, `waiver`,
`gate`, `planAdmission`, `RequestId`, `ExecutionId`), while `$.sdkHonesty` and
`sdk-leftover-join.v6.json#$.reservedVerbatim` reserve "exact SDK APIs and frameworks" and
`$.failsIf[34]` fails on "an SDK API, language, or framework is invented". The NT-4/NT-7 field names
are safe because `provider-only-output-contract.v3.json#$.negativeTests` recites them; the EV-1/EV-2/EV-3
record field names are not recited anywhere pinned. The line between a witness selection and an
invented API surface is not drawn by the record.

**OQ-G20-9:** Should the fixture corpus be a first-authoring (`fixtures/g20.v1/`, no platform
subdirectory, per `g21-fixture-corpus.v1`) or go straight to per-platform copies (per
`g23-fixture-corpus.v4`)? Same unsettled point as on G19: G20's occupancy carries `$.platforms` and
every passProperty ends "Pass all four D-002 platforms × every admitted role", but the recorded
first-authoring precedent puts the copy layer in a separate later version.
