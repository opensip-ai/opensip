# G24 — OBL-G24-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

Path: `docs/coop/artifacts/g24-leftover-join.v4.json`
sha256: `c451f7ce20e93442172322ff2fd29a029a9a0ca209538ece7c590d32c72e43d7`

| field | value (verbatim) |
|---|---|
| `$.artifact` | `g24-leftover-join.v4` |
| `$.date` | `2026-08-23` |
| `$.registerRow` | `DR-G24` |
| `$.status` | `CANDIDATE-NOT-APPLIED` |
| `$.reviewStatus` | `AWAITING-INDEPENDENT-REVIEW` |
| `$.sealRecommendation` | `DO-NOT-SEAL` |
| `$.binds` | `NOTHING` |
| `$.head` | `1e939c5643fea73a6581f45f1d4067854747271c` |
| `$.requiredNowUnchanged` | `28` |
| `$.file08StatusToken` | `OPEN` |
| `$.liveGateOwners.DR-G24` | `Product + CLI / output` |

`$.file08Pin` = `{"path": "docs/v2/architecture/08-decision-and-readiness-register.md", "sha256":
"e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e"}` — recomputed at this HEAD and
equal.

COORD heading that recorded this join (`docs/coop/COORDINATOR-DECISIONS.md` sha256
`1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 12208, verbatim):

> `## D-250 — Record g24 leftover-join.v4 as G24 leftover remasurement`

`$.summary.leftoverDesign` = `["OBL-G24-FX-AUTHORING"]`. It is the only obligation on this join with
`leftoverDesign: true`.

### 1.2 The obligation object (verbatim)

JSON path `$.obligations[3]`:

```json
{
  "id": "OBL-G24-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2)"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "G24 v1 namedCorpusNotAuthored carries one live harness-cell corpus phrase. Fixtures are unauthored. D-056 Decision clause 5: authoring fixtures remains design work, distinct from the harness specification, distinct from live-cell naming, and distinct from execution. This join does not invent those fixture bytes and does not invent NT-3 through NT-8 as G24 classes. The INPUT-state corpus exists and is Claude ACCEPT 0/0; that INPUT layer is OBL-G24-INPUT-CORPUS, leftoverDesign false. This join does not invent fixture bytes."
}
```

Called out explicitly:

- `$.obligations[3].reason` — quoted in full above. Its operative clauses for authoring are
  "Fixtures are unauthored.", the D-056 clause-5 recital, and the two prohibitions
  "does not invent those fixture bytes" and "does not invent NT-3 through NT-8 as G24 classes".
- `$.obligations[3].namedCorpusNotAuthored` = `["hostile-but-well-formed admission corpus (DR-131
  NT-1, NT-2)"]` — one phrase. This is the sole corpus phrase the obligation names.
- `remainingNotAuthored` / `namedCases` / `namedNotAuthored` — **absent** on this obligation. The
  join uses `namedCorpusNotAuthored` only.
- `$.obligations[3].existingGate` = `"none as authored implementations"`. Note this is not "no gate":
  the gate DR-G24 exists (file 08 line 360); what does not exist is authored implementations.
- `$.obligations[3].rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `$.obligations[3].executionObligationOwnerToday` = `"none"`. Contrast
  `$.obligations[4]` (`OBL-G24-EXECUTION`) whose owner is `"Product + CLI / output"`, and
  `$.liveGateOwners.DR-G24` = `"Product + CLI / output"`.

### 1.3 Occupancy file and the fields that name the fixture cells

Path: `docs/coop/artifacts/harness.DR-G24.preview-analyze-well-formed-admission.preview.v3.json`
sha256: `ee41d14c7896ce97ebbf6611054991688ef1755499fbdc9d7f274498ebf9fdd4`
`$.artifact` = `harness.DR-G24.preview-analyze-well-formed-admission.preview.v3`

The join names it current at `$.basedOn.occupancyV3.role`: "Current G24 occupancy remasurement."

**`$.liveRowVerbatim`**

> `| DR-G24 PREVIEW-ANALYZE-WELL-FORMED-ADMISSION | Host admission of preview analyze requests refuses a non-bundled pack identity and a non-declarative pack or contribution | named: harness.DR-G24.preview-analyze-well-formed-admission.preview (D-150; not authored; not QUALIFIED). hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2) | pack-identity refusal before evaluation; imperative-pack refusal; no user or third-party pack | Product + CLI / output | PROPOSED; not QUALIFIED | pass all; no waiver for silent admission | OPEN |`

Byte-identical to file 08 line 360.

**`$.liveHarnessCellVerbatim`**

> `named: harness.DR-G24.preview-analyze-well-formed-admission.preview (D-150; not authored; not QUALIFIED). hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2)`

**`$.liveCellVerbatim`**

> `pack-identity refusal before evaluation; imperative-pack refusal; no user or third-party pack`

**`$.claimVerbatim`**

> `Host admission of preview analyze requests refuses a non-bundled pack identity and a non-declarative pack or contribution`

**`$.waiverRuleVerbatim`**

> `pass all; no waiver for silent admission`

**`$.namedCorpusNotAuthored`** = `["hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2)"]`

**`$.namedCorpusNote`**

> `Live harness-cell punctuation after the named-identifier sentence. The corpus is hostile-but-well-formed admission. The live cell names are NT-1 and NT-2 only. These are corpus classes, not authored fixtures and not invented NT numbers. A later fixture-authoring cycle may pin bytes. This artifact does not.`

**`$.namedCorpusClasses[0]` (NT-1)**

- `.nameVerbatim` = `user-or-third-party-pack-refused`
- `.memberVerbatim` = `hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2)`
- `.inputVerbatim` = `An analyze request that names a pack other than opensip.preview.typescript.pack version 1, or supplies a user/third-party pack.`
- `.passVerbatim` = `Request refused at admission. No evaluation.`
- `.suppliesMembers` = `["pack-identity refusal before evaluation", "no user or third-party pack"]`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned well-formed analyze-request bytes for each NT-1 limb, paired with pinned admission-refusal bytes showing no evaluation. Limb one names a pack other than opensip.preview.typescript.pack version 1 (wrong name, wrong version, or a non-bundled identity). Limb two supplies a user pack or a third-party pack. Each record retains the presented identity or supplied-pack provenance, the refusal, and a record that no core evaluation, no policyOutcome, no facts/Coverage consumption, and no semantic-universe construction occurred. Hostile-but-well-formed means the request is constructible as an analyze request; crash/malformed/truncated remains G21. This class does not score G29 publisher-form refusal, G30 useful-install advertisement, or G23 FactCandidate/Coverage admission. Fixtures are unauthored.`

- `.passProperty`:

> `NT-1 passVerbatim holds for both limbs. Silent admission fails. Evaluation does not run. Waiver of silent admission is forbidden. G23, G29, and G30 are not scored.`

**`$.namedCorpusClasses[1]` (NT-2)**

- `.nameVerbatim` = `imperative-rule-refused`
- `.memberVerbatim` = `hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2)`
- `.inputVerbatim` = `A pack or contribution that is not declarative-only.`
- `.passVerbatim` = `Refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded.`
- `.suppliesMembers` = `["imperative-pack refusal"]`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned well-formed pack or contribution bytes that are not declarative-only, paired with pinned admission-refusal bytes showing ARCH.PROBE-CONTRACT dependents remain excluded. Each record retains the non-declarative input, the refusal, and a record that no evaluation ran. This class is pack IR at analyze admission. It does not score G29 EE-5a contribution-hook/root/probe refusal, G29 EE-5b represented post-admission pre-stage substitution, or G23 provider FactCandidate/Coverage. This specification does not invent pack IR and does not discharge ARCH.PROBE-CONTRACT. Fixtures are unauthored.`

- `.passProperty`:

> `NT-2 passVerbatim holds. A non-declarative pack or contribution is refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded. Silent admission fails. G23 and G29 are not scored.`

**`$.retainedEvidence[0]` (EV-1)**

- `.memberVerbatim` = `pack-identity refusal before evaluation`
- `.measures` = `A well-formed analyze request that names a pack other than opensip.preview.typescript.pack version 1 is refused at admission. No evaluation. Before evaluation means the core does not evaluate policyOutcome, does not consume facts or Coverage, and does not construct a semantic universe.`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned analyze-request bytes that name a pack other than opensip.preview.typescript.pack version 1 (wrong name, wrong version, or a non-bundled identity), paired with pinned admission-refusal bytes showing no evaluation. The fixture pins the request identity, the refusal, and a record that no core evaluation, no policyOutcome, no facts/Coverage consumption, and no semantic-universe construction occurred. A first-party publisher presenting a different pack name is refused here. This member does not score G29 EE-1 publisher-form refusal, G30 useful-install advertisement of the future DR-131 pack, or G23 FactCandidate/Coverage admission. This specification does not invent a section 7.1 PlanId producing rule; admission refusal happens before identity derivation. Fixtures are unauthored.`

- `.passProperty`:

> `A named pack identity other than opensip.preview.typescript.pack version 1 is refused at admission. Evaluation does not run. Silent admission fails. Waiver of silent admission is forbidden. G23, G29, and G30 are not scored. A PlanId recipe is not invented.`

- `.suppliedBy` = `NT-1 first limb. Not authored.`

**`$.retainedEvidence[1]` (EV-2)**

- `.memberVerbatim` = `imperative-pack refusal`
- `.measures` = `A pack or contribution that is not declarative-only is refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded.`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned pack or contribution bytes that are not declarative-only, paired with pinned admission-refusal bytes showing ARCH.PROBE-CONTRACT dependents remain excluded. The fixture pins the non-declarative input, the refusal, and a record that no evaluation ran. This member is pack IR at analyze admission. It does not score G29 EE-5a contribution-owned project hook/root/probe refusal or G29 EE-5b represented post-admission pre-stage imperative substitution. It does not score G23 provider FactCandidate/Coverage. This specification does not invent pack IR, an evaluation algorithm, or a discharge of ARCH.PROBE-CONTRACT. Fixtures are unauthored.`

- `.passProperty`:

> `A non-declarative pack or contribution is refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded. Silent admission fails. G23 and G29 are not scored.`

- `.suppliedBy` = `NT-2. Not authored.`

**`$.retainedEvidence[2]` (EV-3)**

- `.memberVerbatim` = `no user or third-party pack`
- `.measures` = `A well-formed analyze request that supplies a user pack or a third-party pack is refused at admission. No evaluation. No user or third-party pack is admitted.`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned analyze-request bytes that supply a user pack or a third-party pack, paired with pinned admission-refusal bytes showing no evaluation. The fixture pins the supplied-pack provenance (user or third-party), the refusal, and a record that no evaluation ran. Distinct from EV-1: EV-1 is the named identity (name/version other than the bundled pack); EV-3 is a supplied user or third-party pack object. Distinct from G29 EE-1: EE-1 is a component publisher neither first-party nor explicitly trusted. Distinct from G30: G30 is useful-install advertisement of the future DR-131 pack, not analyze-request admission of a supplied pack. Fixtures are unauthored.`

- `.passProperty`:

> `A supplied user or third-party pack is refused at admission. Evaluation does not run. Silent admission fails. No user or third-party pack is admitted. G29 and G30 are not scored.`

- `.suppliedBy` = `NT-1 second limb. Not authored.`

**`$.liveCellMemberSplit`** (`.delimiter` = `semicolon`)

- `.membersInOrder` = `["pack-identity refusal before evaluation", "imperative-pack refusal", "no user or third-party pack"]`
- `.mapsFromLiveCellNames` = `{"NT-1": ["pack-identity refusal before evaluation", "no user or third-party pack"], "NT-2": ["imperative-pack refusal"]}`
- `.notCollapsed`:

> `pack-identity refusal before evaluation is not collapsed into no user or third-party pack. imperative-pack refusal is not collapsed into pack-identity refusal. no user or third-party pack is not collapsed into pack-identity refusal before evaluation.`

**`$.passProperty`**

> `Once independently pinned fixture implementations exist for NT-1, NT-2, and every live retained-evidence member, each retained-evidence member's passProperty holds. Until those bytes exist, this harness cannot be executed and cannot be QUALIFIED. File 08 remains PROPOSED; not QUALIFIED. Waiver of silent admission is forbidden.`

**`$.liveGateOwner`** = `{"DR-G24": "Product + CLI / output"}`
**`$.executionObligationOwner`** = `Product + CLI / output`
**`$.productLawOwner`** = `DR-131 / preview-analyze-contract.v2`

**`$.windowsStanding`**

> `D-002 defers Windows with explicit disposition. Windows is not a G24 preview-runner platform and is not a live-cell member.`

**`$.d002Standing`**

> `D-002 TypeScript alone; platforms macOS (arm64, x86_64) and Linux (x86_64, arm64); Windows deferred. This specification does not extend the role list, does not mint Rust-as-core, and does not name G13 into required-now. The live G24 cell has no platform matrix; platforms are not live-cell members.`

**`$.proposedLaterWork[2]`**

> `A later D-000 cycle may author and independently pin fixture implementations for NT-1 and NT-2 and the three live-cell members, on the four D-002 platforms. This v3 occupancy does not invent those bytes.`

**Fields checked for and ABSENT on this occupancy:** `platforms` (there is no `platforms` array).
`$.d002Standing` recites the four D-002 tokens in prose but the occupancy does not carry them as an
array. `namedCorpusNotAuthored`, `namedCorpusClasses`, `retainedEvidence`, `passProperty`, `failsIf`,
`doesNot`, `liveGateOwner`, `executionObligationOwner`, `liveRowVerbatim`, `liveHarnessCellVerbatim`,
`liveCellVerbatim`, `claimVerbatim`, `windowsStanding` are all present and quoted above.

**INPUT-state corpus** (consumed by the join at `$.basedOn.inputCorpus`, and by
`$.obligations[2]` `OBL-G24-INPUT-CORPUS`, `leftoverDesign: false`):
`docs/coop/artifacts/g24-input-corpus.v1.json`
sha256 `b279a615be2754e13621a73e37dcc9441ec5a1337716e84137c2ec0f61b33095`.
`$.status` = `CANDIDATE-NOT-APPLIED`; `$.binds` = `NOTHING`; `$.head` =
`5d5d77819ae3019d9e6e02f1e66de3d93c060402`; `$.requiredNowUnchanged` = `26` (stale against live 28 —
the join measures it as an INPUT-layer naming, not as a live remeasurement).

`$.whatIsAuthored`:

> `Named initial states drawn from live G24 v1 exactByteIntent: NT-1 wrong pack identity; NT-1 user pack; NT-1 third-party pack; NT-2 non-declarative pack. No fixture bytes. No pack IR invented.`

`$.initialStates[*].id` and `.stateNamed`, with `.fixtureBytes` = `NOT-AUTHORED` on every one:

1. `G24.nt1.wrong-pack-identity` — `NT-1 names a pack other than opensip.preview.typescript.pack version 1 (wrong name, wrong version, or a non-bundled identity)` (`.source` = `G24 v1 NT-1 / EV-1 exactByteIntent`)
2. `G24.nt1.user-pack` — `NT-1 supplies a user pack` (`.source` = `G24 v1 NT-1 / EV-3 exactByteIntent`)
3. `G24.nt1.third-party-pack` — `NT-1 supplies a third-party pack` (`.source` = `G24 v1 NT-1 / EV-3 exactByteIntent`)
4. `G24.nt2.non-declarative-pack` — `NT-2 pack or contribution bytes that are not declarative-only` (`.source` = `G24 v1 NT-2 / EV-2 exactByteIntent`)

`$.whatIsNotAuthored` = `["fixture bytes", "pack IR", "a section 7.1 recipe", "the future DR-131 pack"]`.

### 1.4 ROW twin join (or: none)

**None.** No ROW-side leftover-join measures `OBL-G24-FX-AUTHORING`.

Measured: a search of `docs/coop/artifacts/*.json` (excluding `*.review-*`) for the literal
`OBL-G24-FX-AUTHORING` returns exactly five files, all in the G24 lineage:
`g24-leftover-join.v1.json`, `g24-leftover-join.v2.json`, `g24-leftover-join.v3.json`,
`g24-leftover-join.v4.json`, `g24-input-corpus.v1.json`.

The D1 packet's own table agrees. `DECISION-PACKETS/D1-fixture-authoring-delegation.md` sha256
`bc8484cc7159af26a142b97a55b1095049f7ea2ac10c283cd1e2428ba2569ea9`, section 3.2 ("ROW-side
obligations that are the same id as a GATE row above (9 measurements — the duplicates)", line 161)
lists nine ROW twins and none of them is a G24–G28 id. Line 155 of the same packet's section 3.1
table carries the G24 row with a final column of `none`.

The cross-custody rule that would otherwise apply is stated at the end of that section 3.2 (verbatim):

> Closing a fixture obligation would require a successor on *both* the GATE join and its ROW twin, since the same id is measured true on both.

Because there is no ROW twin for `OBL-G24-FX-AUTHORING`, closing it needs a successor on
`g24-leftover-join` alone.

### 1.5 Governing contract/spec the occupancy pins

**Shared DR-131 contract, common to G24, G25, G26, G27 and G28.**

Path: `docs/coop/artifacts/preview-analyze-contract.v2.json`
sha256: `081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970`

Pinned by this occupancy at `$.basedOn.v2Contract.path` and at
`$.recordedInputs["docs/coop/artifacts/preview-analyze-contract.v2.json"]` (same digest). The role
string, verbatim (`$.basedOn.v2Contract.role`):

> `Accepted DR-131 design-contract candidate. Product law and NT-1 / NT-2 name/input/pass remain here. This specification does not replace v2 and does not apply v2 as executed fixtures.`

Contract standing in its own bytes: `$.registerRow` = `DR-131`; `$.status` =
`CANDIDATE-NOT-APPLIED`; `$.reviewStatus` = `AWAITING-INDEPENDENT-REVIEW`; `$.sealRecommendation` =
`DO-NOT-SEAL`; `$.binds` = `NOTHING`. `$.authorityClaim`:

> `This artifact PROPOSES the DR-131 preview non-authoritative analyze product contract. It applies nothing, edits no register row, seals nothing, marks nothing SATISFIED, and does not authorize docs/v2/implementation/. D-056 Class A is not opened. File 12 has no authority.`

`$.negativeTests.status` = `specified-classes-not-executed`.

**The NT-1..NT-8 definitions, verbatim, at `$.negativeTests`.** All eight are quoted here once
because the five preview-analyze gates share this one contract lineage; each gate's own file cites
the subset it owns.

| path | name | input | pass |
|---|---|---|---|
| `$.negativeTests.NT-1` | `user-or-third-party-pack-refused` | `An analyze request that names a pack other than opensip.preview.typescript.pack version 1, or supplies a user/third-party pack.` | `Request refused at admission. No evaluation.` |
| `$.negativeTests.NT-2` | `imperative-rule-refused` | `A pack or contribution that is not declarative-only.` | `Refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded.` |
| `$.negativeTests.NT-3` | `missing-rung-not-success` | `Required TypeScript semantic universe cannot be constructed.` | `Typed Coverage-indeterminate. Not authoritative success. No silent syntax fallback.` |
| `$.negativeTests.NT-4` | `component-finding-not-analyze-finding` | `Provider payload that would be a finding under file 01's general may-return.` | `Refused under DR-133. Analyze findings, if any, come only from host-owned rules over admitted facts.` |
| `$.negativeTests.NT-5` | `sarif-not-advertised` | `A request or flag that advertises SARIF 2.1.0 for preview analyze.` | `Refused or not offered. D-077 drop stands.` |
| `$.negativeTests.NT-6` | `preview-result-not-sealed-run` | `A consumer treats the preview terminal result as a sealed authoritative Run.` | `The contract forbids that label. No silent promotion.` |
| `$.negativeTests.NT-7` | `host-must-not-mint-verdict` | `A host path that derives fail or warn other than from the core policyOutcome in CoreCompletion.` | `Refused. The only policy result is core-evaluated policyOutcome. Host does not mint fail/warn.` |
| `$.negativeTests.NT-8` | `host-must-not-mint-d9-or-termination-from-policy` | `A host path that mints a D9 class, exit number, or HostTermination from the pack threshold or from policyOutcome.` | `Refused. Host maps only existing D9 v1.14 classes. policyOutcome is not HostTermination.` |

**The classes this gate owns: `$.negativeTests.NT-1` and `$.negativeTests.NT-2`.** The occupancy's
`$.namedCorpusClasses[0]` and `[1]` reproduce those `input`/`pass` strings byte-for-byte (verified by
string comparison against the contract bytes).

**Contract sections that supply the semantics NT-1 and NT-2 test.**

`$.pack` (verbatim):

- `.name` = `opensip.preview.typescript.pack`
- `.version` = `1`
- `.rule` = `Exactly one pack. Host-owned. Bundled. First-party. Declarative-only. Over TypeScript facts admitted under DR-133. No user packs. No third-party packs.`
- `.notFrozenHere` = `["rule IR", "evaluation algorithm", "packaging adapters", "default numeric constants"]`

`$.registerRowQuoted.obligationDecomposition[0]` (`OBL-1`):
`.cellFragmentVerbatim` = `single first-party bundled declarative pack name/version`;
`.readAs` = `Exactly one host-owned bundled first-party declarative pack, named and versioned here. No user or third-party packs.`

`$.planIdMembership.reason` (verbatim):

> `Freeze §7.1 parks PlanId-affecting producing rules. This contract freezes membership, not the digest recipe. Preview analyze must not invent a PlanId producing rule.`

The occupancy re-states the pack law at `$.packIdentityLaw` with
`.source` = `preview-analyze-contract.v2 pack` and `.ruleVerbatim` byte-identical to `$.pack.rule`,
and adds to `.notFrozenHere` the extra item `a section 7.1 PlanId producing rule`.

**The two other artifacts this join's `basedOn` names** (both historical DR-131 leftover-design
measurements; neither names `OBL-G24-FX-AUTHORING`):

1. `docs/coop/artifacts/preview-analyze-nt-gate-join.v2.json`
   sha256 `4081c7400b3b9eae61089bb807140b4f75f5dd512b664c1f6657553a7da03813` (recording D-148).
   Join role at `$.basedOn.ntGateJoinV2.role`: "Historical leftover-design measurement of DR-131
   NT-to-gate join. Recorded at D-148. Does not name OBL-G24-HARNESS-SPEC."
   Its `$.classes[0]` (NT-1) and `$.classes[1]` (NT-2) each carry `"executionObligationOwnerToday":
   "none"`, `"existingGate": "none"`, `"existingHarness": "none"`, `"rideStanding":
   "not-capable-of-riding"`, `"leftoverDesign": true`, and:
   - NT-1 `.leftoverReason` = `Hostile-but-well-formed pack-identity admission. G23 owns FactCandidate/Coverage only. G20/G21 do not own pack-name admission. DR-117 EE-1 is candidate-owned, not a live DR-G*.`
   - NT-2 `.leftoverReason` = `Hostile-but-well-formed imperative-pack admission. G23 does not own pack IR. DR-117 EE-5 is candidate-owned, not a live DR-G*.`
   `$.summary.leftoverDesign` = `["NT-1","NT-2","NT-3","NT-5","NT-6","NT-7","NT-8"]`;
   `$.summary.dischargedByNamedDR133Classes` = `["NT-4"]`; `$.summary.requiredNowUnchanged` = `19`
   (that measurement's own HEAD, not live).
2. `docs/coop/artifacts/preview-analyze-admission-leftover.v1.json`
   sha256 `1222501032917790832a3ffa8f3953ceb7a73907942a5ea30442346bf59935a5` (recording D-149).
   `$.classes[0]` (NT-1) `.group` = `A-admission`, `.residual` = `leftover-design:
   hostile-but-well-formed pack-identity admission`; `$.classes[1]` (NT-2) `.group` = `A-admission`,
   `.residual` = `leftover-design: hostile-but-well-formed imperative-pack admission`.
   `$.proposedObligations[0]` proposed the kind `PREVIEW-ANALYZE-WELL-FORMED-ADMISSION` owning
   `["NT-1","NT-2"]` with `"status": "candidate-not-adopted"` and
   `.numberNote` = `A later MF-6 assigns or remints any DR-G* number. This artifact does not add a
   row and does not take G24 or any later identifier.` D-150 later named G24.

**DR-131's own D-056 Class A standing (shared across all five gates).**
`DECISION-PACKETS/B1-DR-131-class-A.md` line 116, verbatim:

> **Gate 1 (Class A) — does NOT hold today.**

and line 118, verbatim (excerpt):

> Fails: "application-grade acceptance and no express reservation". The reviews grant candidate-grade acceptance in terms (Claude `acceptanceGrade`: `"ACCEPT as an independently reviewed DR-131 design-contract candidate … does not open D-056 Class A"`; Codex `opensD056ClassA: false`, `appliesOrSealsCandidate: false`).

This join records the same thing at `$.proposedLaterWork[2]`, verbatim:

> `A later dedicated SATISFIED-GRADE cycle may test DR-131 only when D-056 five gates hold. Gate 1 Class A remains unopened. This join is not that cycle.`

Whether that is a *dependency for the fixtures* is answered in section 4.

## 2. Coverage set

Every explicit quantifier the governing bytes name for this gate, with its source.

**Axis A — named corpus classes (the live cell names).**

1. `NT-1` — `harness.DR-G24…preview.v3.json#$.namedCorpusClasses[0].id`; also
   `g24-leftover-join.v4.json#$.namedClassesThisGate[0]`; also
   `…preview.v3.json#$.namedExecutesLimbs[0]`.
2. `NT-2` — `…preview.v3.json#$.namedCorpusClasses[1].id`; `g24-leftover-join.v4.json#$.namedClassesThisGate[1]`;
   `…preview.v3.json#$.namedExecutesLimbs[1]`.

Closed at two, `…preview.v3.json#$.namedExecutesLimbNote` (full, untruncated):

> `Two live-cell class names only. Use only NT-1 and NT-2 as the live cell names. NT-3, NT-4, NT-5, NT-6, NT-7, and NT-8 are not this gate. No NT number is invented. Leftover-design of NT-1 and NT-2 already closed at D-150 as remainder G24 execution. This specification authors the runner; it does not execute those classes by existing.`

**Axis B — NT-1 limbs (the only sub-class quantifier named).**

3. NT-1 limb one — `…preview.v3.json#$.namedCorpusClasses[0].exactByteIntent`: `Limb one names a
   pack other than opensip.preview.typescript.pack version 1 (wrong name, wrong version, or a
   non-bundled identity).`
4. NT-1 limb two — same path: `Limb two supplies a user pack or a third-party pack.`

`…preview.v3.json#$.namedCorpusClasses[0].passProperty` requires `NT-1 passVerbatim holds for both
limbs.` NT-2 has no limb split.

**Axis C — live retained-evidence members (the file 08 "Required retained evidence" cell, split on
semicolons at `…preview.v3.json#$.liveCellMemberSplit.membersInOrder`).**

5. `pack-identity refusal before evaluation` — `…preview.v3.json#$.retainedEvidence[0].memberVerbatim`
6. `imperative-pack refusal` — `…preview.v3.json#$.retainedEvidence[1].memberVerbatim`
7. `no user or third-party pack` — `…preview.v3.json#$.retainedEvidence[2].memberVerbatim`

Non-collapse is mandatory: `…preview.v3.json#$.liveCellMemberSplit.notCollapsed` (quoted in §1.3) and
`…preview.v3.json#$.failsIf[28]` = `live-cell punctuation is collapsed or extras are invented
(members must be pack-identity refusal before evaluation, imperative-pack refusal, no user or third-party pack)`.

**Axis D — named initial states (the INPUT layer, already accepted).**

8. `G24.nt1.wrong-pack-identity` — `g24-input-corpus.v1.json#$.initialStates[0].id`
9. `G24.nt1.user-pack` — `#$.initialStates[1].id`
10. `G24.nt1.third-party-pack` — `#$.initialStates[2].id`
11. `G24.nt2.non-declarative-pack` — `#$.initialStates[3].id`

These four are the payload set. The INPUT corpus splits NT-1 limb two into two states (user pack;
third-party pack) because `…preview.v3.json#$.retainedEvidence[2].exactByteIntent` requires the
fixture to pin "the supplied-pack provenance (user or third-party)".

**Axis E — mutations / fates.** No mutation axis is named for this gate. The fate is a single one
per class, `passVerbatim`, quoted at items 1–2 above. The negative fate is named at
`…preview.v3.json#$.passProperty`: `Waiver of silent admission is forbidden.` and at
`$.waiverRuleVerbatim` = `pass all; no waiver for silent admission`.

**Axis F — platforms.** The occupancy has **no `platforms` array**. Two bytes speak to platforms and
they point in different directions:

12. `…preview.v3.json#$.d002Standing`: `The live G24 cell has no platform matrix; platforms are not
    live-cell members.`
13. `…preview.v3.json#$.proposedLaterWork[2]`: `A later D-000 cycle may author and independently pin
    fixture implementations for NT-1 and NT-2 and the three live-cell members, **on the four D-002
    platforms**.` (emphasis added; the string is verbatim)

The four platform tokens are **not enumerated as an array anywhere in the G24 record**. The recorded
practice for a gate whose occupancy lacks a `platforms` array is `g21-fixture-corpus.v8.json`
sha256 `e8149a865e49bdcda9eda923e9918f332a83078f43ab6a3af9a10d6d31ef6359`, whose
`$.g21OccupancyV4HasNoPlatformsArray` is `true`, whose `$.platformsQuotedFromG10OccupancyV2` =
`["macos/arm64", "macos/x86_64", "linux/x86_64", "linux/arm64"]`, whose
`$.g23OccupancyV2PlatformsOrderedEqual` is `true`, and whose `$.failsIf[1]` reads `a D-002 platform
list is invented rather than quoted from G10 occupancy v2`. Those two source arrays are live and
equal: `harness.DR-G10.provider-conformance.ts-major-1.v2.json`
sha256 `b0cbce06487b96bbe7f6af1dae62ba3b3ca55aaa41305cb96f531099e86bf7c9` `$.platforms` and
`harness.DR-G23.provider-well-formed-admission.preview.v2.json`
sha256 `f48ba637bdf193785c05906a1686ce268b27b6ce7355de07fa5effefdd84fb0b` `$.platforms` are both
`["macos/arm64", "macos/x86_64", "linux/x86_64", "linux/arm64"]`.

**Total coverage members: 4 first-authoring payloads; 12 (class-cell × evidence-member) obligations;
16 per-platform copies in a later copy version.**

Arithmetic, shown three ways because the bytes name three different quantifier sets:

- **Payloads (first-authoring, no platform subdirectory):** 4 named initial states = 4 payload files.
  Axis D items 8–11.
- **Evidence obligations:** 3 live retained-evidence members (axis C) each with its own
  `exactByteIntent` and `passProperty`, supplied by 4 payloads via
  `$.retainedEvidence[*].suppliedBy` (`NT-1 first limb` → EV-1; `NT-2` → EV-2; `NT-1 second limb` →
  EV-3, which the INPUT corpus splits in two). Counting the (class, limb, evidence-member) triples the
  occupancy actually names: NT-1 limb one × EV-1, NT-1 limb two × EV-3 (× 2 provenances),
  NT-2 × EV-2 = 4. Adding the per-class `passProperty` obligations (2) and the per-evidence-member
  `passProperty` obligations (3) gives 4 + 2 + 3 = 9 distinct pass predicates over 4 payloads.
- **Per-platform copy version:** 4 payloads × 4 D-002 platforms = **16** copy files, following
  `g23-fixture-corpus.v4` (4 payloads × 4 platforms = 16 files) exactly.

The 4 is the number the record determines. The 16 depends on resolving the tension between axis F
items 12 and 13 — see OQ-G24-1.

## 3. Prohibitions bounding the authoring

**From the GATE join `g24-leftover-join.v4.json`.**

`$.obligations[3].reason` (the obligation's own two clauses):

> `This join does not invent those fixture bytes and does not invent NT-3 through NT-8 as G24 classes.`

`$.doesNot` (all 20 items, verbatim):

> `Does not SATISFY DR-131.` · `Does not reopen leftover-design of NT-1 or NT-2.` · `Does not open D-056 Class A.` · `Does not close leftover-design of fixture implementations.` · `Does not close OBL-G24-FX-AUTHORING.` · `Does not invent NT-3 through NT-8 as G24 classes.` · `Does not mint leftover codes for G23, G25, G26, G27, G28, G29, or G30.` · `Does not add a DR-G* row.` · `Does not change live required-now 28.` · `Does not execute fixtures.` · `Does not claim QUALIFIED.` · `Does not edit file 08.` · `Does not invent a D9 code.` · `Does not invent a section 7.1 recipe.` · `Does not invent a pack IR.` · `Does not invent fixture bytes.` · `Does not authorize docs/v2/implementation/.` · `Does not record leftover-join.v3 as current after this successor is recorded.` · `Does not record occupancy v1 as current occupancy.` · `Does not occupy the identifier.`

**From the occupancy `harness.DR-G24…preview.v3.json`.**

`$.doesNot` — 40 items. The ones that bound *fixture content* rather than this-file custody:

> `Does not author fixture bytes.` · `Does not invent an NT number.` · `Does not take over G23 provider admission.` · `Does not take over G29 excluded-form admission.` · `Does not take over G26 SARIF.` · `Does not take over G27 sealed-Run.` · `Does not take over G28 host-must-not-mint.` · `Does not take over G30 install-shape.` · `Does not take over G21 crash/malformed containment.` · `Does not take over G25 missing-rung.` · `Does not restore G17.` · `Does not name G13 into required-now.` · `Does not mint Rust-as-core.` · `Does not invent a second slice-1 language role.` · `Does not invent a D9 code, exit number, or HostTermination.` · `Does not invent a section 7.1 recipe.` · `Does not discharge ARCH.PROBE-CONTRACT.` · `Does not settle DR-007.`

`$.failsIf` — 48 items. The fixture-content ones, verbatim:

> `this specification is read as authoring fixture bytes` · `NT-3, NT-4, NT-5, NT-6, NT-7, or NT-8 is treated as a G24 class` · `DR-133 NT-3 or DR-133 NT-5 is treated as executing here` · `a live G24 retained-evidence member is omitted or has no exactByteIntent / passProperty` · `live-cell punctuation is collapsed or extras are invented (members must be pack-identity refusal before evaluation, imperative-pack refusal, no user or third-party pack)` · `an NT number other than NT-1 or NT-2 is invented or treated as this gate's live cell name` · `hostile-but-well-formed admission corpus (DR-131 NT-1, NT-2) is omitted from exact-byte intent` · `G23 provider FactCandidate/Coverage admission is stolen` · `G29 excluded-form admission is stolen` · `G26 SARIF is stolen` · `G27 sealed-Run is stolen` · `G28 host-must-not-mint is stolen` · `G30 install-shape is stolen` · `G21 crash/malformed containment is stolen` · `G25 missing-rung is stolen` · `G17 is restored` · `G13 is named into required-now` · `Rust-as-core is minted or a second slice-1 language role is invented` · `a D9 code, exit number, or HostTermination is invented` · `a section 7.1 recipe is invented` · `ARCH.PROBE-CONTRACT is treated as discharged` · `silent admission is waived`

`$.liveCellMemberSplit.notInventedExtras` — 24 items the corpus may not add as live-cell members,
verbatim: `pack-identity as a member without refusal`, `before evaluation as a second member`,
`evaluation as a member`, `user and third-party as two members`, `pack and contribution as two
live-cell members`, `ARCH.PROBE-CONTRACT as a live-cell member`, `NT-3`, `NT-4`, `NT-5`, `NT-6`,
`NT-7`, `NT-8`, `an invented NT-1a or NT-1b`, `G23 FactCandidate or Coverage members`, `G29
excluded-form members`, `G26 SARIF`, `G27 sealed-Run`, `G28 host-must-not-mint`, `G30 install-shape`,
`a PlanId producing rule`, `a D9 code`, `an exit number`, `Rust-as-core`, `G13 as a live member`.

**From the contract `preview-analyze-contract.v2.json`.**

`$.doesNot` (verbatim, all nine):

> `Does not SATISFY DR-131.` · `Does not open D-056 Class A.` · `Does not invent §7.1 identity recipes.` · `Does not advertise SARIF.` · `Does not freeze public rule IR.` · `Does not rewrite DR-123.` · `Does not authorize docs/v2/implementation/.` · `Does not rewrite D-002 commands, platforms, or independent-release machinery.` · `Does not SATISFY DR-117 or DR-133.`

`$.pack.notFrozenHere` = `["rule IR", "evaluation algorithm", "packaging adapters", "default numeric
constants"]` — the corpus may not supply any of those four.

**From the INPUT corpus `g24-input-corpus.v1.json`.**

`$.whatIsNotAuthored` = `["fixture bytes", "pack IR", "a section 7.1 recipe", "the future DR-131
pack"]`. `$.doesNot` includes `Does not invent pack IR.`, `Does not invent the future DR-131 pack.`,
`Does not invent a D9 code.`

**The D-293 constraint clause that applies to this gate.**

`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md`
sha256 `f530cedca1c799097ed0fc30cf8ec6f0480abe9a56d495236909a3a23b84fc33`, verbatim:

> Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep their recorded prohibitions (adapter, CI encoding, journal).

> **Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved; for delegated byte sets, concrete witness bytes may be selected only within already-recorded schemas and fates, and any choice that would create a new semantic member, identifier, value, list or implementation stays outside the grant (recorded as a named open decision instead).

The clause **"new … pack semantics"** binds this gate directly: NT-1 and NT-2 are both pack-admission
classes and the occupancy's own `$.packIdentityLaw.notFrozenHere` names `rule IR`, `evaluation
algorithm`, `packaging adapters`, `default numeric constants`, `a section 7.1 PlanId producing rule`.

Adopted by the owner at COORD `## D-293 — User decisions: adopt the agreed recommendations on
DECISIONS-NEEDED A–F` Decision 8, verbatim:

> **D1 (fixture authoring).** Delegated to the orchestrator under D-000 review for the enumerated G15, G16, G18, G19, G20, G21, G24–G30, `OBL-HOSTILE-GOLDENS` and the two DR-122 SARIF fixture obligations, under the agreed semantic, coverage, dependency and D-000 constraints.

## 4. Dependencies

**(a) Sequencing dependency — the DR-131 Class A opening. Not a byte gap.**

This is an ordering constraint on *when the authoring act may be recorded*, not a missing input to
the fixture. Every byte the fixture needs (schemas, class names, inputs, passes, evidence members,
initial states) exists and is quoted in §1.3 and §1.5.

`DECISIONS-RECOMMENDED.md` (repo root) sha256
`42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`, heading
`## B1 — DR-131 Class A opening (preview-analyze-contract.v2)` (line 114), subsection
`### Recommendation (Claude, round 2)`, numbered item 4, verbatim:

> 4. G24–G28 fixture authoring (D1) before the separate DR-131 SATISFIED-GRADE + MF-6 cycle — as conservative sequencing, not as a precedent-proven automatic reopening of gate 2.

Items 1–3 of the same numbered list, verbatim:

> 1. One shared gate-2 entry that states a common interpretation of D-056 gate 2 (declaring whether it interprets D-056 or is a scoped successor amendment) and then measures each of DR-131, DR-133, DR-117 separately from bytes; it reconciles the row-level findings (D-154; D-147; D-207) with the current gate joins' FX-AUTHORING flags expressly, and does not describe D-249..D-253 as silently overturning D-154.
> 2. A fresh application-grade dual review with the NEUTRAL question of the exact final DR-131 contract bytes the opening entry will cite (v2 `081ff7fb…` unless a successor is authored, in which case the successor).
> 3. The T2-02 opening entry per row, citing those verdicts and the owner's product word.

The Codex round-2 concurrence in the same section states the order as a sequence, verbatim:

> Then use a separate DR-131 opening entry, author the G24-G28 fixtures as conservative sequencing, and conduct a later separate DR-131 SATISFIED-GRADE plus MF-6 cycle.

Adopted at COORD `## D-293 — …` Decision 5, verbatim (the two operative sentences):

> **B1 (DR-131), B2 (DR-133), B3 (DR-117).** The agreed programmes are authorized in the agreed order — for DR-117: `preview-product-boundary-successor.v9` re-citing the twelve current joins and stating its relationship to `product-boundary-successor-contract.v8` (D-116); a fresh application-grade dual review bound to v9's final digest; then the owner-controlled opening entry; then G29/G30 fixture authoring; then a separate SATISFIED-GRADE + MF-6 cycle. The D-056 Class A openings themselves are separate owner-controlled entries; this entry opens none of them.

Confirmed: `preview-analyze-contract.v2` at digest prefix `081ff7fb` **is** the DR-131 contract
pinned in §1.5 of this file (full digest
`081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970`). It is the same artifact.

**My reading of these bytes.** D-293 Decision 5 authorizes the programmes "in the agreed order". The
agreed DR-131 programme is the four-item list above, in which fixture authoring is item 4 and the
Class A opening entry is item 3. Decision 5's spelled-out DR-117 sequence puts fixture authoring
after the opening entry ("then the owner-controlled opening entry; then G29/G30 fixture authoring"),
and Codex's round-2 sentence states the same order for DR-131. Decision 5 then says the openings
"themselves are separate owner-controlled entries; this entry opens none of them." So on the bytes as
written the opening has not happened and the agreed order places it before fixture authoring.

I read that as blocking the *recording act*, not the *drafting*. Item 4's own text constrains
authoring only relative to "the separate DR-131 SATISFIED-GRADE + MF-6 cycle", and D-293 Decision 8
delegates D1 for G24–G30 with no express Class A precondition of its own. The two readings are not
reconciled anywhere in the record. Recorded as OQ-G24-4; the standing line below takes the
conservative reading.

**(b) Byte dependencies: none.**

There is no reserved list, reserved number, reserved axis, unauthored schema, other gate's corpus, or
undecided threshold that this fixture cannot be completed without. Measured:

- The pack identity is closed: `preview-analyze-contract.v2.json#$.pack.name` =
  `opensip.preview.typescript.pack`, `.version` = `1`.
- The class names, inputs and passes are closed: `#$.negativeTests.NT-1`, `#$.negativeTests.NT-2`.
- The initial states are already named and independently accepted:
  `g24-input-corpus.v1.json#$.initialStates` (4 items), measured `leftoverDesign: false` at
  `g24-leftover-join.v4.json#$.obligations[2]`.
- No `RESERVED` token appears anywhere in `g24-leftover-join.v4.json`,
  `harness.DR-G24…preview.v3.json`, or `g24-input-corpus.v1.json`.

The one thing the record does *not* determine is the platform copy axis (§2 axis F, OQ-G24-1). That
is a scope question about which artifact version to author, not a missing byte: the first-authoring
version (`g23-fixture-corpus.v3` / `g21-fixture-corpus.v1` shape, no platform subdirectory) is
constructible today without it.

**Standing: BLOCKED-ON-DR-131-CLASS-A-OPENING (sequencing; no byte dependency)**

## 5. Recommended artifact shape

**Closest precedent: `docs/coop/artifacts/g23-fixture-corpus.v3.json`**
sha256 `3576e2e606b3eed68feced5b83a34247263d3b563274ef3fd9054c8b2a2ba6a7` (recorded D-237) — the
**first-authoring** version of the G23 lineage, which is the same shape this gate needs. Its
per-platform successor `g23-fixture-corpus.v4.json` sha256
`b3fce9f5bab6764919f5dc43c28a43f3d9c3b6be310e45c2c1bd08a617c755c5` (D-239) is the model for a later
copy version, not for the first act.

`g23-fixture-corpus.v3.json` top-level fields the successor would carry:

`artifact, version, date, documentClass ("DESIGN-CONTRACT-CANDIDATE"), registerRow, registerRowNote,
namedGate, status ("CANDIDATE-NOT-APPLIED"), reviewStatus ("AWAITING-INDEPENDENT-REVIEW"),
sealRecommendation ("DO-NOT-SEAL"), binds ("NOTHING"), authorityClaim, purpose, basedOn, parentReview,
file08Pin, head, requiredNowUnchanged, file08StatusToken, appliesFindingsOf, cborConstruction,
pinnedHostContext, whatIsAuthored, whatIsNotAuthored, authoredCatalog, 
leftoverDesignClosedIfAcceptedAndRecorded, leftoverDesignRemainingOnG23, remainderAfterThisCorpus,
summary, doesNot, proposedLaterWork, recordedInputs, remeasurementClause`

`authoredCatalog` keys: `standing, doesNotMutate, executionRemains, members`.
Each `authoredCatalog.members[i]` carries: `id, class, inputCorpusId, path, sha256,
snapshotMembership, mutation, expected`.

For G24 the per-member fields map as: `id` and `inputCorpusId` = the four
`g24-input-corpus.v1.json#$.initialStates[*].id` values; `class` = `NT-1` or `NT-2`;
`path`/`sha256` = the payload; `mutation` = the limb description quoted from the occupancy's
`exactByteIntent`; `expected` = the class `passVerbatim`. `cborConstruction` is probably not needed
(G23's payloads are Coverage/FactCandidate CBOR-commitment vectors; G24's are analyze-request JSON),
but `pinnedHostContext` is: the pack name/version must be cited to
`preview-analyze-contract.v2.json#$.pack`, not restated as a free value.

**Proposed artifact name:** `docs/coop/artifacts/g24-fixture-corpus.v1.json`
**Proposed fixture directory:** `docs/coop/artifacts/fixtures/g24.v1/`
with four files named after the INPUT ids:
`G24.nt1.wrong-pack-identity.json`, `G24.nt1.user-pack.json`, `G24.nt1.third-party-pack.json`,
`G24.nt2.non-declarative-pack.json`, mode `0444` (measured: `docs/coop/artifacts/fixtures/g23.v3/*`
are `-r--r--r--`).

**It is a first-authoring: no platform subdirectories.** The per-platform copy version is a separate
later artifact (`g24-fixture-corpus.v2` under `fixtures/g24.v2/<platform-with-slash-replaced-by-dash>/`),
following D-239 for G23 and D-243/D-247 for G21, and would have to quote its four platform tokens
from `harness.DR-G10.provider-conformance.ts-major-1.v2.json#$.platforms` ORDERED-EQUAL to
`harness.DR-G23.provider-well-formed-admission.preview.v2.json#$.platforms`, as
`g21-fixture-corpus.v8.json#$.platformsQuotedFromG10OccupancyV2` /
`#$.g23OccupancyV2PlatformsOrderedEqual` did — never inventing the list.

## 6. Effort and risk

**Number of cases: 4** payload files in the first-authoring version; **16** in the later per-platform
copy version. Two review cycles (Stage A dual ACCEPT, Stage B dual CONSENT) per version, plus one
`g24-leftover-join.v5` remasurement successor per version. No ROW twin successor is needed (§1.4).

**(a) Witness-byte selections inside recorded schemas — choices allowed under the grant.**

1. The concrete wrong pack **name** string for `G24.nt1.wrong-pack-identity` — any string that is not
   `opensip.preview.typescript.pack` (`preview-analyze-contract.v2.json#$.pack.name`). The occupancy
   names the three sub-shapes ("wrong name, wrong version, or a non-bundled identity") and the
   fixture must pick one or pin all three inside the one payload.
2. The concrete wrong **version** integer — any integer other than `1`
   (`preview-analyze-contract.v2.json#$.pack.version`).
3. The concrete user-pack and third-party-pack provenance markers, provided they are expressed only
   with vocabulary already in `$.pack.rule` (`No user packs. No third-party packs.`).
4. The concrete non-declarative construct for NT-2, provided it is described in the terms
   `$.negativeTests.NT-2.input` already uses (`not declarative-only`) and does not name a pack-IR
   construct.
5. File names, ordering, and the `mutation`/`expected` prose, as long as `expected` reproduces the
   class `passVerbatim` byte-exactly.
6. Whether NT-1's two limbs are two payloads or one payload with two records — the occupancy says
   "digest-pinned well-formed analyze-request bytes for each NT-1 limb", which the INPUT corpus
   already resolved into three NT-1 states.

**(b) Choices that would create new semantics — record as a named open decision, do not choose.**

1. **A pack IR** — any concrete syntax for what a declarative pack rule *is*. Violates
   `harness.DR-G24…preview.v3.json#$.namedCorpusClasses[1].exactByteIntent`: `This specification does
   not invent pack IR and does not discharge ARCH.PROBE-CONTRACT.`; `g24-leftover-join.v4.json#$.doesNot`
   `Does not invent a pack IR.`; D-293 constraint `new … pack semantics`.
   **Record as a named open decision, do not choose.**
2. **An evaluation algorithm or a default numeric constant** — violates
   `preview-analyze-contract.v2.json#$.pack.notFrozenHere`.
   **Record as a named open decision, do not choose.**
3. **A section 7.1 PlanId producing rule / any identity recipe** — violates
   `#$.retainedEvidence[0].exactByteIntent` (`This specification does not invent a section 7.1 PlanId
   producing rule; admission refusal happens before identity derivation.`), `#$.failsIf` `a section
   7.1 recipe is invented`, contract `$.doesNot` `Does not invent §7.1 identity recipes.`
   **Record as a named open decision, do not choose.**
4. **A D9 code, exit number, or HostTermination in a refusal payload** — violates `#$.failsIf` `a D9
   code, exit number, or HostTermination is invented` and D-293's `new D9/HostTermination … semantics`.
   The refusal must be expressed without minting a code.
   **Record as a named open decision, do not choose.**
5. **A D-002 platform list written out rather than quoted** — violates
   `preview-analyze-contract.v2.json#$.doesNot` `Does not rewrite D-002 commands, platforms, or
   independent-release machinery.` and the D-293 default policy's "new … list".
   **Record as a named open decision, do not choose.**
6. **A fourth live-cell member, or an NT-1a/NT-1b split, or NT-3..NT-8 as a G24 class** — violates
   `#$.liveCellMemberSplit.notInventedExtras` and `#$.failsIf` `an NT number other than NT-1 or NT-2
   is invented or treated as this gate's live cell name`.
   **Record as a named open decision, do not choose.**
7. **Collapsing EV-1 into EV-3** (treating the wrong-identity case and the supplied-pack case as one
   member) — violates `#$.liveCellMemberSplit.notCollapsed` and `#$.failsIf` `live-cell punctuation
   is collapsed or extras are invented`. This is precisely the shape that got
   `g23-fixture-corpus.v2` rejected.
   **Record as a named open decision, do not choose.**
8. **Discharging ARCH.PROBE-CONTRACT**, or asserting anything about what its dependents are —
   violates `#$.doesNot` `Does not discharge ARCH.PROBE-CONTRACT.` The NT-2 fixture must retain
   "ARCH.PROBE-CONTRACT dependents stay excluded" as a recorded outcome without defining the
   dependents.
   **Record as a named open decision, do not choose.**

**Risk.** The reviewer-attack pattern on the two recorded fixture-corpus lineages is: (a) inventing a
vocabulary or encoding the record does not carry, (b) collapsing two classes the occupancy keeps
separate, (c) classifying a witness under a closed identifier it does not belong to, (d) over-claiming
closure. G24's exposure is heaviest on (b) and (a). On (b),
`g23-fixture-corpus.v1.review-independent.claude2.json` returned `REJECT` with five MUST-FIX and four
SHOULD-FIX including `M3 — hostRequestedDomainBaseline invents a Coverage-domain encoding vocabulary
and under-determines the requested domain`, and
`g23-fixture-corpus.v2.review-independent.claude2.json` returned `REJECT` on a single MUST-FIX,
`M1 — the EV-2 narrows fixture also converts unknown to covered, collapsing EV-2 into EV-3 against
the governing occupancy's explicit non-collapse requirement`. G24 has the identical hazard: EV-1
(named identity) and EV-3 (supplied pack object) are distinguished only by
`#$.retainedEvidence[2].exactByteIntent`'s sentence `Distinct from EV-1: EV-1 is the named identity
(name/version other than the bundled pack); EV-3 is a supplied user or third-party pack object.` A
payload that both names a wrong pack and supplies a user pack collapses them and will be rejected.
On (a), any attempt to give the non-declarative NT-2 payload a concrete rule syntax invents pack IR.
On (d), `g23-fixture-corpus.v1`'s `S4 — leftoverDesignClosedIfAcceptedAndRecorded over-claims closure
of OBL-G23-FX-AUTHORING` is the template: the G24 successor's
`leftoverDesignClosedIfAcceptedAndRecorded` should be `[]`, as `g23-fixture-corpus.v3.json` has it,
because four payloads do not exhaust the corpus phrase.

## 7. Open questions

**OQ-G24-1:** Does the first G24 fixture corpus carry per-platform copies, or is it a
platform-free first authoring? `harness.DR-G24…preview.v3.json#$.d002Standing` says `The live G24
cell has no platform matrix; platforms are not live-cell members.` while
`#$.proposedLaterWork[2]` says the later cycle authors implementations `on the four D-002
platforms`. The G23 lineage resolved the same tension by splitting first-authoring (v3, D-237) from
per-platform copies (v4, D-239), but no G24 byte records that split as this gate's answer.

**OQ-G24-2:** If a per-platform copy version is authored, which occupancy supplies the four platform
tokens? The G24 occupancy has no `platforms` array. `g21-fixture-corpus.v8.json` quoted
`harness.DR-G10.provider-conformance.ts-major-1.v2.json#$.platforms` and checked ORDERED-EQUAL
against `harness.DR-G23.provider-well-formed-admission.preview.v2.json#$.platforms`. Nothing in the
G24 record names either as G24's source, and `#$.d002Standing`'s prose recital is not an array.

**OQ-G24-3:** `harness.DR-G24…preview.v3.json#$.namedCorpusClasses[0].exactByteIntent` requires each
record to retain "a record that no core evaluation, no policyOutcome, no facts/Coverage consumption,
and no semantic-universe construction occurred." No schema for such a negative-observation record
exists in the pinned inputs. Whether that record is a field of the refusal payload, a sibling file,
or prose in the catalog member is not settled by any byte cited here.

**OQ-G24-4:** Does D-293 Decision 5's "authorized in the agreed order" make the DR-131 Class A
opening entry a precondition for *recording* the G24 fixture corpus, or only for the SATISFIED-GRADE
cycle? `DECISIONS-RECOMMENDED.md` §B1 item 4 constrains fixture authoring only relative to "the
separate DR-131 SATISFIED-GRADE + MF-6 cycle", and D-293 Decision 8 delegates D1 for G24–G30 with no
express Class A precondition; but the item is numbered 4 in a list whose item 3 is the opening entry,
and Codex's round-2 sentence orders opening-then-fixtures. The record does not reconcile the two.

**OQ-G24-5:** `harness.DR-G24…preview.v3.json#$.retainedEvidence[1].exactByteIntent` requires the
NT-2 fixture to show "ARCH.PROBE-CONTRACT dependents remain excluded", while `#$.doesNot` forbids
discharging ARCH.PROBE-CONTRACT and no artifact cited by this occupancy enumerates those dependents.
How a fixture demonstrates the exclusion of an unenumerated dependent set is not in the record.
