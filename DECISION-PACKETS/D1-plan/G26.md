# G26 — OBL-G26-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

Path: `docs/coop/artifacts/g26-leftover-join.v4.json`
sha256: `aba91c5a43f77ccb9244977c746ca8238b54a4e3af5f431b37b74ce6e5e68591`

| field | value (verbatim) |
|---|---|
| `$.artifact` | `g26-leftover-join.v4` |
| `$.date` | `2026-08-23` |
| `$.registerRow` | `DR-G26` |
| `$.status` | `CANDIDATE-NOT-APPLIED` |
| `$.reviewStatus` | `AWAITING-INDEPENDENT-REVIEW` |
| `$.sealRecommendation` | `DO-NOT-SEAL` |
| `$.binds` | `NOTHING` |
| `$.head` | `c7bb5ee97ddb1a789fa109ca70cc9fc1b7bb6c55` |
| `$.requiredNowUnchanged` | `28` |
| `$.file08StatusToken` | `OPEN` |
| `$.liveGateOwners.DR-G26` | `Output/operability + CLI/product owners` |

`$.file08Pin` = `{"path": "docs/v2/architecture/08-decision-and-readiness-register.md", "sha256":
"e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e"}` — recomputed at this HEAD and
equal.

COORD heading that recorded this join (`docs/coop/COORDINATOR-DECISIONS.md` sha256
`1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 12295, verbatim):

> `## D-251 — Record g26 leftover-join.v4 as G26 leftover remasurement`

`$.summary.leftoverDesign` = `["OBL-G26-FX-AUTHORING"]`. `$.summary.g17Restored` = `false`;
`$.summary.sarifAdvertisementInvented` = `false`.

This join carries seven obligations (one more than G24/G25/G27/G28), the extra one being
`$.obligations[6]` `OBL-G17-REMAINS-INAPPLICABLE`, `leftoverDesign: false`, whose `reason` is:

> `D-077 dropped G17. G26 v1 EV-2 passProperty: G17 remains inapplicable. Restoring G17 or naming it into required-now fails. This join does not restore G17.`

That obligation is not the fixture obligation, but it is the reason EV-2 exists as a retained
evidence member — see §1.3.

### 1.2 The obligation object (verbatim)

JSON path `$.obligations[3]`:

```json
{
  "id": "OBL-G26-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "refuse-or-not-offer corpus (DR-131 NT-5)"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "G26 v1 retained evidence EV-1 and EV-2 are unauthored fixture cells of the refuse-or-not-offer corpus. D-056 Decision clause 5: authoring fixtures remains design work. This join does not invent those fixture bytes, does not invent a SARIF advertisement, and does not restore G17. The INPUT-state corpus exists and is Claude ACCEPT 0/0; that INPUT layer is OBL-G26-INPUT-CORPUS, leftoverDesign false. This join does not invent fixture bytes."
}
```

Called out explicitly:

- `$.obligations[3].reason` — quoted in full above. **Its first sentence names both retained-evidence
  members by id:** `G26 v1 retained evidence EV-1 and EV-2 are unauthored fixture cells of the
  refuse-or-not-offer corpus.` This is the only one of the five preview-analyze FX-AUTHORING reasons
  that enumerates EV ids in the reason text; G24, G27 and G28 name only "unauthored fixture cells" or
  the corpus phrase, and G25 names two NT-3 cells. Its operative prohibitions are the three clauses
  `does not invent those fixture bytes`, `does not invent a SARIF advertisement`, and `does not
  restore G17`.
- `$.obligations[3].namedCorpusNotAuthored` = `["refuse-or-not-offer corpus (DR-131 NT-5)"]`.
- `remainingNotAuthored` / `namedCases` / `namedNotAuthored` / `namedNt3Readings` — **absent** on
  this obligation.
- `$.obligations[3].existingGate` = `"none as authored implementations"` (the gate DR-G26 exists at
  file 08 line 362; the authored implementations do not).
- `$.obligations[3].rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `$.obligations[3].executionObligationOwnerToday` = `"none"`. Contrast `$.obligations[4]`
  (`OBL-G26-EXECUTION`) whose owner is `"Output/operability + CLI/product owners"`.

### 1.3 Occupancy file and the fields that name the fixture cells

Path: `docs/coop/artifacts/harness.DR-G26.preview-analyze-sarif-not-advertised.preview.v2.json`
sha256: `3a6f13799ef960170370a2a74930d62778a9671b2065ac3e83ca485c21721ffb`
`$.artifact` = `harness.DR-G26.preview-analyze-sarif-not-advertised.preview.v2`

**`$.liveRowVerbatim`**

> `| DR-G26 PREVIEW-ANALYZE-SARIF-NOT-ADVERTISED | Preview analyze refuses or does not offer SARIF 2.1.0; D-077 drop stands | named: harness.DR-G26.preview-analyze-sarif-not-advertised.preview (D-152; not authored; not QUALIFIED). refuse-or-not-offer corpus (DR-131 NT-5) | request or flag advertising SARIF is refused or not offered; G17 remains inapplicable | Output/operability + CLI/product owners | PROPOSED; not QUALIFIED | pass all; no waiver by silent advertisement | OPEN |`

Byte-identical to file 08 line 362.

**`$.liveHarnessCellVerbatim`**

> `named: harness.DR-G26.preview-analyze-sarif-not-advertised.preview (D-152; not authored; not QUALIFIED). refuse-or-not-offer corpus (DR-131 NT-5)`

**`$.liveCellVerbatim`**

> `request or flag advertising SARIF is refused or not offered; G17 remains inapplicable`

**`$.claimVerbatim`**

> `Preview analyze refuses or does not offer SARIF 2.1.0; D-077 drop stands`

**`$.waiverRuleVerbatim`**

> `pass all; no waiver by silent advertisement`

**`$.namedCorpusNotAuthored`** = `["refuse-or-not-offer corpus (DR-131 NT-5)"]`

**`$.namedCorpusClasses` — ABSENT.** This is the structural difference between G26 and G24/G25/G28.
The G26 occupancy carries no `namedCorpusClasses` array, so NT-5's `name`, `input` and `pass` are
**not restated on the occupancy at all**. They live only on the contract
(`preview-analyze-contract.v2.json#$.negativeTests.NT-5`, quoted in §1.5). The occupancy's only
class-level bytes are `$.namedExecutesLimbs` = `["NT-5"]` and `$.namedExecutesLimbNote`:

> `Use only NT-5 as the live cell name. This is DR-131 NT-5, not DR-133 NT-5.`

Consequence for authoring: a G26 corpus that wants to recite the class name/input/pass must cite the
contract by path + sha256 + JSON path, not the occupancy. See OQ-G26-3.

**`$.retainedEvidence[0]` (EV-1)** — the first of the two the obligation's reason names.

- `.memberVerbatim` = `request or flag advertising SARIF is refused or not offered`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned preview-analyze invocations that request or flag SARIF 2.1.0, paired with pinned refusal-or-not-offered bytes showing no SARIF document emitted and no advertised SARIF surface. Fixtures are unauthored.`

- `.passProperty`:

> `A request or flag advertising SARIF is refused or not offered. Silent advertisement fails.`

- `.suppliedBy` = `refuse-or-not-offer corpus class. Not authored.`

**`$.retainedEvidence[1]` (EV-2)** — the second of the two.

- `.memberVerbatim` = `G17 remains inapplicable`
- `.exactByteIntent` (full, untruncated):

> `Once authored: a record that G17 stays INAPPLICABLE-DROPPED-BY-D077 and is not named into required-now, and that no preview-analyze SARIF projection harness ran. Fixtures are unauthored.`

- `.passProperty`:

> `G17 remains inapplicable. Restoring G17 or naming it into required-now fails this specification.`

- `.suppliedBy` = `refuse-or-not-offer corpus class. Not authored.`

`$.retainedEvidence` has exactly two entries. Both `.suppliedBy` strings name the same supplier (the
corpus class), not an NT limb — unlike G24 (`NT-1 first limb` / `NT-2` / `NT-1 second limb`), G25
(`NT-3 both cells`) and G28 (`NT-8` / `NT-7`).

**`$.discipline`**

> `SPECIFICATION OF EXACT-BYTE INTENT. Occupancy remasurement of the already-named identifier. No harness runs here. Naming is not execution. No fixture bytes authored. DR-131 NT-5 is not executed by this file existing. DR-133 NT-5 is G23, not this gate. G17 remains inapplicable.`

**`$.liveGateOwner`** = `{"DR-G26": "Output/operability + CLI/product owners"}`
**`$.executionObligationOwner`** = `Output/operability + CLI/product owners`
**`$.productLawOwner`** = `DR-131 / preview-analyze-contract.v2`
**`$.palV1A1`** = `PAL-V1-A1: Output/operability + CLI/product owners is the execution-obligation owner. Product-law ownership remains DR-131 / preview-analyze-contract.v2. This specification does not transfer product law onto the gate owner.`

**`$.eligibilityEffect`**

> `D-152 named G26 live. leftover-join.v3 recorded leftover-design remaining on G26 as OBL-G26-FX-AUTHORING. Remainder is G26 execution once fixture implementations exist. leftover-design of DR-131 NT-5 as an unnamed remainder stays closed at D-152. G17 remains inapplicable. Class A is not opened. DR-131 is not SATISFIED. This remasurement occupies the named identifier after those recordings. This file existing is not a SATISFIED-GRADE cycle and does not mark SATISFIED.`

**`$.proposedLaterWork[2]`**

> `A later D-000 cycle may author and independently pin fixture implementations for DR-131 NT-5 and the two live-cell members, on the four D-002 platforms. This v2 occupancy does not invent those bytes.`

Note it says **two** live-cell members, matching `$.retainedEvidence` length 2 and the obligation
reason's `EV-1 and EV-2`.

**Fields checked for and ABSENT on this occupancy:** `namedCorpusClasses`, `platforms`,
`windowsStanding`, `passProperty` (the gate-level roll-up), `liveCellMemberSplit`, `d002Standing`.
The only platform byte in the whole file is `$.proposedLaterWork[2]` above.

**INPUT-state corpus** (`$.basedOn.inputCorpusV1`; measured `leftoverDesign: false` at
`g26-leftover-join.v4.json#$.obligations[2]`): `docs/coop/artifacts/g26-input-corpus.v1.json`
sha256 `d511883acdc7029bf3f193b859165e1b88172c0659c7f60049647da03b775291`.
`$.status` = `CANDIDATE-NOT-APPLIED`; `$.binds` = `NOTHING`; `$.head` =
`5d5d77819ae3019d9e6e02f1e66de3d93c060402`; `$.requiredNowUnchanged` = `26` (stale against live 28).

`$.whatIsAuthored`:

> `Named initial states drawn from live G26 v1 exactByteIntent: request-SARIF and flag-SARIF refuse-or-not-offer cells, plus the G17-inapplicable standing record. No fixture bytes. No SARIF advertisement invented.`

`$.initialStates` (3 items, all `.fixtureBytes` = `NOT-AUTHORED`):

1. `G26.request-sarif-refused-or-not-offered` — `.stateNamed` = `preview-analyze invocation that
   requests SARIF 2.1.0`; `.source` = `G26 v1 exactByteIntent: digest-pinned preview-analyze
   invocations that request or flag SARIF 2.1.0`
2. `G26.flag-sarif-refused-or-not-offered` — `.stateNamed` = `preview-analyze invocation that flags
   SARIF 2.1.0`; `.source` = same string
3. `G26.g17-remains-inapplicable` — `.stateNamed` = `G17 stays INAPPLICABLE-DROPPED-BY-D077 and is
   not named into required-now`; `.source` = `G26 v1 exactByteIntent: a record that G17 stays
   INAPPLICABLE-DROPPED-BY-D077`

The INPUT corpus therefore splits EV-1's `request or flag` into two states and gives EV-2 one. Every
one carries `.classMemberVerbatim` = `refuse-or-not-offer corpus (DR-131 NT-5)`.

`$.whatIsNotAuthored` = `["fixture bytes", "a SARIF advertisement table", "G17 restored or named into
required-now"]`.

### 1.4 ROW twin join (or: none)

**None.** No ROW-side leftover-join measures `OBL-G26-FX-AUTHORING`.

Measured: a search of `docs/coop/artifacts/*.json` (excluding `*.review-*`) for the literal
`OBL-G26-FX-AUTHORING` returns exactly five files, all in the G26 lineage:
`g26-leftover-join.v1.json` through `.v4.json` and `g26-input-corpus.v1.json`.

`DECISION-PACKETS/D1-fixture-authoring-delegation.md` sha256
`bc8484cc7159af26a142b97a55b1095049f7ea2ac10c283cd1e2428ba2569ea9`, section 3.2 (line 161) lists the
nine ROW twins and none is a G24–G28 id; line 153 of section 3.1 carries the G26 row with a final
column of `none`.

The cross-custody rule that would otherwise apply, verbatim from the end of that section 3.2:

> Closing a fixture obligation would require a successor on *both* the GATE join and its ROW twin, since the same id is measured true on both.

Because there is no ROW twin, closing `OBL-G26-FX-AUTHORING` needs a successor on
`g26-leftover-join` alone.

**One adjacent standing worth naming, because it is a row that could be mistaken for a twin.** G17
(`DR-G17 SARIF-PROJECTION-PARITY`, file 08 line 353) is the other SARIF gate, but it is not a twin of
this obligation and carries no `OBL-G26-FX-AUTHORING`. Its live harness cell reads `dropped /
inapplicable (D-077 SARIF drop; D-086). not required-now.` (quoted at
`preview-analyze-nt-gate-join.v2.json#$.g17Quoted.harnessVerbatim`), and
`#$.g17Quoted.historicalStanding` says:

> `After D-077 the advertised-surface and drop/change/truncation corpus is historical. G17 is inapplicable and not required-now. It is not a current countable execution obligation for preview analyze NT-5.`

### 1.5 Governing contract/spec the occupancy pins

**Shared DR-131 contract, common to G24, G25, G26, G27 and G28.**

Path: `docs/coop/artifacts/preview-analyze-contract.v2.json`
sha256: `081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970`

Pinned by this occupancy at `$.basedOn.v2Contract.path` and at
`$.recordedInputs["docs/coop/artifacts/preview-analyze-contract.v2.json"]` (same digest). The role
string, verbatim (`$.basedOn.v2Contract.role`):

> `Accepted DR-131 design-contract candidate. This specification does not replace v2.`

Contract standing in its own bytes: `$.registerRow` = `DR-131`; `$.status` =
`CANDIDATE-NOT-APPLIED`; `$.reviewStatus` = `AWAITING-INDEPENDENT-REVIEW`; `$.sealRecommendation` =
`DO-NOT-SEAL`; `$.binds` = `NOTHING`; `$.negativeTests.status` = `specified-classes-not-executed`.
`$.authorityClaim`:

> `This artifact PROPOSES the DR-131 preview non-authoritative analyze product contract. It applies nothing, edits no register row, seals nothing, marks nothing SATISFIED, and does not authorize docs/v2/implementation/. D-056 Class A is not opened. File 12 has no authority.`

**The NT-1..NT-8 definitions, verbatim, at `$.negativeTests`.** All eight are quoted here once
because the five preview-analyze gates share this one contract lineage.

| path | name | input | pass |
|---|---|---|---|
| `$.negativeTests.NT-1` | `user-or-third-party-pack-refused` | `An analyze request that names a pack other than opensip.preview.typescript.pack version 1, or supplies a user/third-party pack.` | `Request refused at admission. No evaluation.` |
| `$.negativeTests.NT-2` | `imperative-rule-refused` | `A pack or contribution that is not declarative-only.` | `Refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded.` |
| `$.negativeTests.NT-3` | `missing-rung-not-success` | `Required TypeScript semantic universe cannot be constructed.` | `Typed Coverage-indeterminate. Not authoritative success. No silent syntax fallback.` |
| `$.negativeTests.NT-4` | `component-finding-not-analyze-finding` | `Provider payload that would be a finding under file 01's general may-return.` | `Refused under DR-133. Analyze findings, if any, come only from host-owned rules over admitted facts.` |
| `$.negativeTests.NT-5` | `sarif-not-advertised` | `A request or flag that advertises SARIF 2.1.0 for preview analyze.` | `Refused or not offered. D-077 drop stands.` |
| `$.negativeTests.NT-6` | `preview-result-not-sealed-run` | `A consumer treats the preview terminal result as a sealed authoritative Run.` | `The contract forbids that label. No silent promotion.` |
| `$.negativeTests.NT-7` | `host-must-not-mint-verdict` | `A host path that derives fail or warn other than from the core policyOutcome in CoreCompletion.` | `Refused. The only policy result is core-evaluated policyOutcome. Host does not mint fail/warn.` |
| `$.negativeTests.NT-8` | `host-must-not-mint-d9-or-termination-from-policy` | `A host path that mints a D9 class, exit number, or HostTermination from the pack threshold or from policyOutcome.` | `Refused. Host maps only existing D9 v1.14 classes. policyOutcome is not HostTermination.` |

**The class this gate owns: `$.negativeTests.NT-5`.** Because the occupancy carries no
`namedCorpusClasses`, this contract path is the **sole** source of NT-5's `name`, `input` and `pass`
in the whole G26 record. A G26 corpus must cite it directly.

**Contract sections that supply the semantics NT-5 tests.**

`$.unstableIdentities.notAdvertised` = `["SARIF 2.1.0"]` — a one-element list; this is the closed set
the fixture may reference.

`$.registerRowQuoted.obligationDecomposition[5]` (`OBL-6`):
`.cellFragmentVerbatim` = `which preview identities are unstable`;
`.answeredAt` = `["unstableIdentities", "joins.d077", "joins.d078"]`;
`.readAs` = `Preview output identities listed as unstable or conceptual. No durable finding identity. SARIF not advertised.`

`$.joins.d077` (verbatim, all three fields):

- `.entry` = `COORDINATOR-DECISIONS.md D-077`
- `.commit` = `d401ecd8494cd3e1b5f7b3553d9d9e6fed4dd9e5`
- `.citedLaw` = `SARIF drops; cache keys, Coverage, and PlanId stay conceptual. Binding recipes remain owed.`

`$.d002SarifReconciliation` (verbatim, all seven fields — this is the section that reconciles D-002's
adopted SARIF advertisement with D-077's drop, and is the only place the record explains *why* NT-5
is a refusal class rather than a parity corpus):

- `.d002Adopted` = `D-002 adopted "SARIF 2.1.0 advertised for analyze only" and G17 then applying to that surface.`
- `.d002Ride` = `SARIF for analyze rides DR-006. If DR-006 closes by scoped disposition rather than binding recipes, SARIF drops from slice 1 by that disposition's terms.`
- `.d077Disposition` = `D-077 owner-recorded Route B for DR-006: SARIF for analyze drops; cache keys, Coverage, and PlanId stay conceptual.`
- `.g17` = `G17 is inapplicable (D-077).`
- `.thisContract` = `Preview analyze does not advertise SARIF. NT-5 is the retained class. This is the D-002 / D-077 reconciliation, not a rewrite of D-002 commands or platforms.`
- `.d077Commit` = `d401ecd8494cd3e1b5f7b3553d9d9e6fed4dd9e5`
- `.d078Commit` = `17bbf202107e8f8fa78366ce5422fd53a1bf6363`

`$.doesNot` includes `Does not advertise SARIF.`

**The NT-5-to-gate assignment reasoning** (historical, `preview-analyze-nt-gate-join.v2.json`
sha256 `4081c7400b3b9eae61089bb807140b4f75f5dd512b664c1f6657553a7da03813`, recording D-148):
`$.classes[4].leftoverReason`, verbatim:

> `Retained refuse-or-not-offer class. G17 is inapplicable and owns advertised-surface parity, not this refusal. G20's SARIF token is component envelope corpus, not analyze advertisement.`

and `$.g17Quoted.whyNotAssigned`, verbatim:

> `G17 owns parity of advertised SARIF, including the advertised-capability and drop/change/truncation corpus. D-077 dropped advertisement, so that corpus is historical and not a current required-now ride. NT-5 is the retained refuse-or-not-offer class, not a parity corpus on an advertised surface.`

**DR-131 NT-5 is a different NT-5 from DR-133 NT-5.** `harness.DR-G26…preview.v2.json#$.namedExecutesLimbNote`
says so (`This is DR-131 NT-5, not DR-133 NT-5.`), `#$.discipline` repeats it (`DR-133 NT-5 is G23,
not this gate.`), and `#$.failsIf[14]` = `DR-133 NT-5 is treated as executing here`. For the record:
DR-133 NT-5 is at `docs/coop/artifacts/provider-only-output-contract.v3.json` sha256
`ef2a7416700cc8197486c6e29450673c60e3b94512be3ad278d1995f7d281309` `$.negativeTests.NT-5`, `name` =
`coverage-domain-mutation`, `input` = `A Coverage payload that narrows, widens, or converts unknown
to covered relative to the host-requested domain.`, `pass` = `Host refuses the domain mutation.
Sufficiency remains host-owned. No unknown-to-covered conversion.` — a Coverage class at G23, not a
SARIF class. Its payloads already exist at `docs/coop/artifacts/fixtures/g23.v3/G23.nt5.*.json` and
must not be reused as G26 witnesses.

**DR-131's own D-056 Class A standing (shared across all five gates).**
`DECISION-PACKETS/B1-DR-131-class-A.md` line 116, verbatim:

> **Gate 1 (Class A) — does NOT hold today.**

This join records the same at `$.proposedLaterWork[2]`, verbatim:

> `A later dedicated SATISFIED-GRADE cycle may test DR-131 only when D-056 five gates hold. Gate 1 Class A remains unopened. This join is not that cycle.`

Whether that is a *dependency for the fixtures* is answered in section 4.

## 2. Coverage set

**Axis A — named corpus classes.**

1. `NT-5` — `harness.DR-G26…preview.v2.json#$.namedExecutesLimbs[0]`;
   `g26-leftover-join.v4.json#$.namedClassesThisGate[0]`. Exactly one class. The occupancy has no
   `namedCorpusClasses` array, so the class's `name`/`input`/`pass` come from
   `preview-analyze-contract.v2.json#$.negativeTests.NT-5` (§1.5). No sub-cell or limb split is named
   at class level.

**Axis B — live retained-evidence members (file 08 "Required retained evidence" cell,
semicolon-split). Both are named by id in the obligation's own reason text.**

2. `request or flag advertising SARIF is refused or not offered` —
   `harness.DR-G26…preview.v2.json#$.retainedEvidence[0].memberVerbatim` (EV-1); named at
   `g26-leftover-join.v4.json#$.obligations[3].reason` (`retained evidence EV-1 and EV-2`).
3. `G17 remains inapplicable` — `harness.DR-G26…preview.v2.json#$.retainedEvidence[1].memberVerbatim`
   (EV-2); named in the same reason sentence.

Confirmed as exactly two by `harness.DR-G26…preview.v2.json#$.proposedLaterWork[2]`: `…for DR-131
NT-5 and the **two** live-cell members…` (emphasis added; string verbatim).

**Axis C — the request/flag disjunction inside EV-1.**

4. `request` — `harness.DR-G26…preview.v2.json#$.retainedEvidence[0].exactByteIntent`: `digest-pinned
   preview-analyze invocations that **request** or flag SARIF 2.1.0`; realised at
   `g26-input-corpus.v1.json#$.initialStates[0].stateNamed` = `preview-analyze invocation that
   requests SARIF 2.1.0`.
5. `flag` — same `exactByteIntent`; realised at
   `g26-input-corpus.v1.json#$.initialStates[1].stateNamed` = `preview-analyze invocation that flags
   SARIF 2.1.0`.

The disjunction also appears in the contract at `$.negativeTests.NT-5.input` (`A request or flag that
advertises SARIF 2.1.0 for preview analyze.`) and in the live cell itself
(`$.liveCellVerbatim` first member: `request or flag advertising SARIF is refused or not offered`).
Three independent bytes carry it, so it is a preserved quantifier under the D-293 default policy, not
an author's elaboration.

**Axis D — the refuse / not-offer disjunction inside the fate.**

6. `refused` and 7. `not offered` — `preview-analyze-contract.v2.json#$.negativeTests.NT-5.pass` =
   `Refused or not offered. D-077 drop stands.`; `harness.DR-G26…preview.v2.json#$.retainedEvidence[0].passProperty`
   = `A request or flag advertising SARIF is refused or not offered. Silent advertisement fails.`

The record does **not** say whether both fates must be witnessed or whether either satisfies the
member — see OQ-G26-1. Counted here as one axis with two named values, not two mandatory cases.

**Axis E — named initial states (INPUT layer, already accepted).**

8. `G26.request-sarif-refused-or-not-offered` — `g26-input-corpus.v1.json#$.initialStates[0].id`
9. `G26.flag-sarif-refused-or-not-offered` — `#$.initialStates[1].id`
10. `G26.g17-remains-inapplicable` — `#$.initialStates[2].id`

**Axis F — mutations / transitions.** None named. The negative fate is
`$.waiverRuleVerbatim` = `pass all; no waiver by silent advertisement`, restated as EV-1's `Silent
advertisement fails.`

**Axis G — platforms.** The occupancy has **no `platforms` array**, no `windowsStanding` and no
`d002Standing`. The one platform byte is `harness.DR-G26…preview.v2.json#$.proposedLaterWork[2]`:
`A later D-000 cycle may author and independently pin fixture implementations for DR-131 NT-5 and the
two live-cell members, **on the four D-002 platforms**.` (emphasis added; string verbatim). The four
tokens are **not enumerated anywhere in the G26 record**. The recorded practice for that situation is
`g21-fixture-corpus.v8.json` sha256 `e8149a865e49bdcda9eda923e9918f332a83078f43ab6a3af9a10d6d31ef6359`
(`$.g21OccupancyV4HasNoPlatformsArray` = `true`; `$.platformsQuotedFromG10OccupancyV2` =
`["macos/arm64", "macos/x86_64", "linux/x86_64", "linux/arm64"]`;
`$.g23OccupancyV2PlatformsOrderedEqual` = `true`; `$.failsIf[1]` = `a D-002 platform list is invented
rather than quoted from G10 occupancy v2`). Source arrays, both live and equal:
`harness.DR-G10.provider-conformance.ts-major-1.v2.json` sha256
`b0cbce06487b96bbe7f6af1dae62ba3b3ca55aaa41305cb96f531099e86bf7c9` `$.platforms`, and
`harness.DR-G23.provider-well-formed-admission.preview.v2.json` sha256
`f48ba637bdf193785c05906a1686ce268b27b6ce7355de07fa5effefdd84fb0b` `$.platforms`. See OQ-G26-2.

**Total coverage members: 3 first-authoring payloads; 3 (state × evidence-member) obligations;
12 per-platform copies in a later copy version.**

Arithmetic:

- **Payloads (first-authoring, no platform subdirectory):** 3 named initial states = 3 files (axis E).
  Two are invocation payloads (request, flag) supplying EV-1; one is the G17 standing record
  supplying EV-2.
- **Evidence obligations:** 2 live retained-evidence members (axis B), mapped from the 3 states as
  EV-1 ← states 1 and 2, EV-2 ← state 3. Counting (state, evidence-member) pairs the record names:
  2 + 1 = **3**. Adding the two member-level `passProperty` obligations gives 3 + 2 = 5 distinct pass
  predicates over 3 payloads.
- **Per-platform copy version:** 3 payloads × 4 D-002 platforms = **12** copy files, following the
  `g23-fixture-corpus.v4` pattern (4 payloads × 4 platforms = 16 files) at G26's smaller cardinality.

The 3 is determined by the record. The 12 depends on OQ-G26-2. Whether axis D's two fates each need a
witness — which would take the payload count to 4 or 5 — is OQ-G26-1 and is **not** a number this
record supplies.

## 3. Prohibitions bounding the authoring

**From the GATE join `g26-leftover-join.v4.json`.**

`$.obligations[3].reason`:

> `This join does not invent those fixture bytes, does not invent a SARIF advertisement, and does not restore G17.`

`$.doesNot` (all 19 items, verbatim):

> `Does not SATISFY DR-131.` · `Does not reopen leftover-design of DR-131 NT-5.` · `Does not restore G17 or name G17 into required-now.` · `Does not invent a SARIF advertisement.` · `Does not open D-056 Class A.` · `Does not close leftover-design of fixture implementations.` · `Does not close OBL-G26-FX-AUTHORING.` · `Does not invent a D9 code.` · `Does not add a DR-G* row.` · `Does not change live required-now 28.` · `Does not execute fixtures.` · `Does not claim QUALIFIED.` · `Does not edit file 08.` · `Does not invent a section 7.1 recipe.` · `Does not authorize docs/v2/implementation/.` · `Does not invent fixture bytes.` · `Does not occupy the identifier.` · `Does not record leftover-join.v3 as current after this successor is recorded.` · `Does not record occupancy v1 as current occupancy.`

`$.leftoverDesignOpenStanding` (verbatim):

> `The live DR-G26 token is OPEN. leftover-design of unnamed G26 initial states is stale as a naming claim after g26-input-corpus.v1. leftover-design of G26 fixture implementations remains. G17 remains inapplicable. DR-131 is not SATISFIED.`

`$.obligations[6].reason` (`OBL-G17-REMAINS-INAPPLICABLE`), verbatim:

> `D-077 dropped G17. G26 v1 EV-2 passProperty: G17 remains inapplicable. Restoring G17 or naming it into required-now fails. This join does not restore G17.`

**From the occupancy `harness.DR-G26…preview.v2.json`.**

`$.doesNot` — 21 items. The fixture-content ones, verbatim:

> `Does not author fixture bytes.` · `Does not restore G17 or name G17 into required-now.` · `Does not invent a D9 code or a section 7.1 recipe.` · `Does not mint Rust-as-core.` · `Does not treat DR-133 NT-5 as executing here.` · `Does not execute NT-5 by existing.`

`$.failsIf` — 28 items. The fixture-content ones, verbatim:

> `this specification is read as authoring fixture bytes` · `DR-131 NT-5 is treated as executed by this file existing` · `DR-133 NT-5 is treated as executing here` · `G17 is restored or named into required-now` · `live G26 harness cell is not quoted byte-exact` · `current INPUT basis is claimed other than g26-input-corpus.v1 as measured by leftover-join.v3 (D-201)`

`$.retainedEvidence[1].passProperty` is itself a prohibition on the corpus, verbatim:

> `G17 remains inapplicable. Restoring G17 or naming it into required-now fails this specification.`

**From the contract `preview-analyze-contract.v2.json`.**

`$.doesNot` (verbatim, all nine):

> `Does not SATISFY DR-131.` · `Does not open D-056 Class A.` · `Does not invent §7.1 identity recipes.` · `Does not advertise SARIF.` · `Does not freeze public rule IR.` · `Does not rewrite DR-123.` · `Does not authorize docs/v2/implementation/.` · `Does not rewrite D-002 commands, platforms, or independent-release machinery.` · `Does not SATISFY DR-117 or DR-133.`

`Does not advertise SARIF.` is the direct one. `$.d002SarifReconciliation.thisContract` bounds the
scope of the reconciliation, verbatim: `Preview analyze does not advertise SARIF. NT-5 is the
retained class. This is the D-002 / D-077 reconciliation, not a rewrite of D-002 commands or
platforms.`

**From the INPUT corpus `g26-input-corpus.v1.json`.**

`$.whatIsNotAuthored` = `["fixture bytes", "a SARIF advertisement table", "G17 restored or named into
required-now"]`. `$.doesNot` includes `Does not restore G17.`, `Does not name G17 into
required-now.`, `Does not invent a SARIF advertisement table.`, `Does not invent a D9 code.`

**The D-293 constraint clause that applies to this gate.**

`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md`
sha256 `f530cedca1c799097ed0fc30cf8ec6f0480abe9a56d495236909a3a23b84fc33`, verbatim:

> Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep their recorded prohibitions (adapter, CI encoding, journal).

> **Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved; for delegated byte sets, concrete witness bytes may be selected only within already-recorded schemas and fates, and any choice that would create a new semantic member, identifier, value, list or implementation stays outside the grant (recorded as a named open decision instead).

For G26 the binding half of the "no … reserved list … is authorised" clause is the SARIF surface
itself: a "SARIF advertisement table" is exactly the kind of list the INPUT corpus's
`$.whatIsNotAuthored` already forbids, and the D1 packet's own summary of this gate's adjacent
prohibition (section 3.1, line 153) reads `"does not invent a SARIF advertisement, and does not
restore G17"`.

Adopted by the owner at COORD `## D-293 — User decisions: adopt the agreed recommendations on
DECISIONS-NEEDED A–F` Decision 8, verbatim:

> **D1 (fixture authoring).** Delegated to the orchestrator under D-000 review for the enumerated G15, G16, G18, G19, G20, G21, G24–G30, `OBL-HOSTILE-GOLDENS` and the two DR-122 SARIF fixture obligations, under the agreed semantic, coverage, dependency and D-000 constraints.

Note that the same Decision 8 sentence separately delegates "the two DR-122 SARIF fixture
obligations" (`OBL-FC-NONAUTH-TERM-FX` and `OBL-FC-OUTFAIL-FX` on `sarif-leftover-join.v4`). Those
are a different row (DR-122, owner `Output/operability owner + CLI/product owner`) and a different
obligation set; G26 must not absorb or be absorbed by them.

## 4. Dependencies

**(a) Sequencing dependency — the DR-131 Class A opening. Not a byte gap.**

This is an ordering constraint on *when the authoring act may be recorded*, not a missing input to
the fixture. The class definition, both evidence members, and all three initial states exist and are
quoted in §1.3 and §1.5.

`DECISIONS-RECOMMENDED.md` (repo root) sha256
`42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`, heading
`## B1 — DR-131 Class A opening (preview-analyze-contract.v2)` (line 114), subsection
`### Recommendation (Claude, round 2)`, numbered item 4, verbatim:

> 4. G24–G28 fixture authoring (D1) before the separate DR-131 SATISFIED-GRADE + MF-6 cycle — as conservative sequencing, not as a precedent-proven automatic reopening of gate 2.

Items 1–3 of the same numbered list, verbatim:

> 1. One shared gate-2 entry that states a common interpretation of D-056 gate 2 (declaring whether it interprets D-056 or is a scoped successor amendment) and then measures each of DR-131, DR-133, DR-117 separately from bytes; it reconciles the row-level findings (D-154; D-147; D-207) with the current gate joins' FX-AUTHORING flags expressly, and does not describe D-249..D-253 as silently overturning D-154.
> 2. A fresh application-grade dual review with the NEUTRAL question of the exact final DR-131 contract bytes the opening entry will cite (v2 `081ff7fb…` unless a successor is authored, in which case the successor).
> 3. The T2-02 opening entry per row, citing those verdicts and the owner's product word.

The Codex round-2 concurrence in the same section, verbatim:

> Then use a separate DR-131 opening entry, author the G24-G28 fixtures as conservative sequencing, and conduct a later separate DR-131 SATISFIED-GRADE plus MF-6 cycle.

Adopted at COORD `## D-293 — …` Decision 5, verbatim:

> **B1 (DR-131), B2 (DR-133), B3 (DR-117).** The agreed programmes are authorized in the agreed order — for DR-117: `preview-product-boundary-successor.v9` re-citing the twelve current joins and stating its relationship to `product-boundary-successor-contract.v8` (D-116); a fresh application-grade dual review bound to v9's final digest; then the owner-controlled opening entry; then G29/G30 fixture authoring; then a separate SATISFIED-GRADE + MF-6 cycle. The D-056 Class A openings themselves are separate owner-controlled entries; this entry opens none of them.

Confirmed: `preview-analyze-contract.v2` at digest prefix `081ff7fb` **is** the DR-131 contract
pinned in §1.5 of this file (full digest
`081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970`). It is the same artifact. For G26
this matters more than for the other four gates, because the occupancy carries no
`namedCorpusClasses` and the contract is the *sole* source of NT-5's name/input/pass — a contract
successor would move the only definition the corpus can cite. See OQ-G26-3.

**My reading.** D-293 Decision 5 authorizes the programmes "in the agreed order"; the DR-131 list
places the opening entry at item 3 and fixture authoring at item 4; the spelled-out DR-117 sequence
and Codex's round-2 sentence both order opening-then-fixtures; and Decision 5 says the openings "are
separate owner-controlled entries; this entry opens none of them." So on the bytes the opening has
not happened and the agreed order places it first. I read that as blocking the *recording act*, not
the *drafting*: item 4's own text constrains fixture authoring only relative to the SATISFIED-GRADE
cycle, and D-293 Decision 8 delegates D1 for G24–G30 with no express Class A precondition. The record
does not reconcile the two readings. Recorded as OQ-G26-4; the standing line takes the conservative
reading.

**(b) Byte dependencies: none.**

No reserved list, reserved number, reserved axis, unauthored schema, other gate's corpus, or
undecided threshold is required. Measured:

- The class name, input and pass are closed:
  `preview-analyze-contract.v2.json#$.negativeTests.NT-5`.
- The advertised-format token is closed to one value:
  `preview-analyze-contract.v2.json#$.unstableIdentities.notAdvertised` = `["SARIF 2.1.0"]`.
- The G17 standing token is closed and quoted:
  `harness.DR-G26…preview.v2.json#$.retainedEvidence[1].exactByteIntent` supplies
  `INAPPLICABLE-DROPPED-BY-D077` verbatim, and `g26-input-corpus.v1.json#$.initialStates[2].stateNamed`
  repeats it.
- The three initial states are already named and independently accepted:
  `g26-input-corpus.v1.json#$.initialStates`, measured `leftoverDesign: false` at
  `g26-leftover-join.v4.json#$.obligations[2]`.
- No `RESERVED` token appears in `g26-leftover-join.v4.json`, `harness.DR-G26…preview.v2.json`, or
  `g26-input-corpus.v1.json`.

Two things the record does not determine, neither a missing byte: the refuse-vs-not-offer fate
requirement (OQ-G26-1) and the platform copy axis (OQ-G26-2). Both are scope questions, and the
first-authoring version is constructible today without settling either — with the caveat that the
first is a *coverage* question a reviewer may attack (§6).

**Standing: BLOCKED-ON-DR-131-CLASS-A-OPENING (sequencing; no byte dependency)**

## 5. Recommended artifact shape

**Closest precedent: `docs/coop/artifacts/g21-fixture-corpus.v1.json`**
sha256 `861bb4e7d26a80158cc1cc3a0518c5e8e95311bee4d8c8ce63acd1e60d6c906d` (recorded D-241) — the
leanest first-authoring in the record, two members, no `snapshotMembership`, no per-platform
subdirectory. G26's payloads are CLI invocation records and a standing record, not Coverage vectors,
so the heavier `g23-fixture-corpus.v3` machinery (`cborConstruction`, `pinnedHostContext.coverageVectorConstants`,
`snapshotMembership`) is not needed; carrying it would invite the "under-determined baseline" attack
for no coverage gain.

`g21-fixture-corpus.v1.json` top-level fields the successor would carry:

`artifact, version, date, documentClass ("DESIGN-CONTRACT-CANDIDATE"), registerRow, registerRowNote,
namedGate, status ("CANDIDATE-NOT-APPLIED"), reviewStatus ("AWAITING-INDEPENDENT-REVIEW"),
sealRecommendation ("DO-NOT-SEAL"), binds ("NOTHING"), authorityClaim, purpose, basedOn, parentReview,
file08Pin, head, requiredNowUnchanged, file08StatusToken, cborConstruction, pinnedHostContext,
whatIsAuthored, whatIsNotAuthored, authoredCatalog, leftoverDesignClosedIfAcceptedAndRecorded,
leftoverDesignRemainingOnG21, remainderAfterThisCorpus, summary, doesNot, failsIf, proposedLaterWork,
recordedInputs, remeasurementClause`

`authoredCatalog` keys: `standing, doesNotMutate, executionRemains, members`; each member carries
`id, class, inputCorpusId, path, sha256, mutation, expected` (no `snapshotMembership`, no
`platformCopies` — that is the first-authoring signature).

For G26 the member fields map as: `id`/`inputCorpusId` = the three
`g26-input-corpus.v1.json#$.initialStates[*].id` values; `class` = `NT-5` for all three;
`expected` = `preview-analyze-contract.v2.json#$.negativeTests.NT-5.pass` reproduced byte-exactly for
the two invocation members, and `harness.DR-G26…preview.v2.json#$.retainedEvidence[1].passProperty`
for the G17 standing member. A `retainedEvidenceMap` field naming which member supplies EV-1 and
which supplies EV-2 is advisable, since the obligation reason itself enumerates `EV-1 and EV-2` and a
reviewer will check the mapping. `pinnedHostContext` should carry the contract pin
(`preview-analyze-contract.v2.json` + digest + `$.negativeTests.NT-5`) because the occupancy does not
restate the class (§1.3).

**Proposed artifact name:** `docs/coop/artifacts/g26-fixture-corpus.v1.json`
**Proposed fixture directory:** `docs/coop/artifacts/fixtures/g26.v1/`
with three files named after the INPUT ids:
`G26.request-sarif-refused-or-not-offered.json`, `G26.flag-sarif-refused-or-not-offered.json`,
`G26.g17-remains-inapplicable.json`, mode `0444` (measured:
`docs/coop/artifacts/fixtures/g23.v3/*` are `-r--r--r--`).

**It is a first-authoring: no platform subdirectories.** The per-platform copy version is a separate
later artifact (`g26-fixture-corpus.v2` under
`fixtures/g26.v2/<platform-with-slash-replaced-by-dash>/`), following D-239 for G23 and D-243/D-247
for G21, and would have to quote its four platform tokens from
`harness.DR-G10.provider-conformance.ts-major-1.v2.json#$.platforms` ORDERED-EQUAL to
`harness.DR-G23.provider-well-formed-admission.preview.v2.json#$.platforms`, never inventing the
list.

## 6. Effort and risk

**Number of cases: 3** payload files in the first-authoring version, carrying **3** (state ×
evidence-member) obligations; **12** files in the later per-platform copy version. Two review cycles
(Stage A dual ACCEPT, Stage B dual CONSENT) per version, plus one `g26-leftover-join.v5`
remasurement successor per version. No ROW twin successor is needed (§1.4). This is the smallest and
lowest-risk of the five preview-analyze corpora.

**(a) Witness-byte selections inside recorded schemas — choices allowed under the grant.**

1. The concrete shape of "an invocation that requests SARIF" — a request field, a `--format` value, a
   capability query — provided it is expressed with the single closed token
   `SARIF 2.1.0` (`preview-analyze-contract.v2.json#$.unstableIdentities.notAdvertised[0]`).
2. The concrete shape of "an invocation that flags SARIF" — the flag spelling, provided the same
   token discipline holds and the spelling is not asserted to be a real CLI surface.
3. Which of `refused` and `not offered` each invocation payload witnesses — the fate is a disjunction
   in the governing bytes (`Refused or not offered.`). Selecting one per payload is a witness choice;
   asserting that only one is lawful is not. See OQ-G26-1.
4. The form of the G17 standing record for EV-2 — the occupancy asks for "a record that G17 stays
   INAPPLICABLE-DROPPED-BY-D077 and is not named into required-now, and that no preview-analyze SARIF
   projection harness ran", and both halves are quotable from already-recorded bytes.
5. File names, ordering, and the `mutation` prose.

**(b) Choices that would create new semantics — record as a named open decision, do not choose.**

1. **A SARIF advertisement table, capability list, or advertised-surface enumeration** — violates
   `g26-leftover-join.v4.json#$.doesNot` `Does not invent a SARIF advertisement.`,
   `g26-input-corpus.v1.json#$.whatIsNotAuthored` `a SARIF advertisement table`, and
   `preview-analyze-contract.v2.json#$.doesNot` `Does not advertise SARIF.` A fixture that shows
   *what would have been advertised* invents the surface D-077 dropped.
   **Record as a named open decision, do not choose.**
2. **Any SARIF document body, even as a negative** — the same three bytes. EV-1 requires "no SARIF
   document emitted"; a fixture that embeds one to prove it was not emitted authors the thing the
   record drops.
   **Record as a named open decision, do not choose.**
3. **Restoring G17, or naming G17 into required-now, or authoring a parity/truncation corpus** —
   violates `harness.DR-G26…preview.v2.json#$.retainedEvidence[1].passProperty`
   (`Restoring G17 or naming it into required-now fails this specification.`), `#$.failsIf` `G17 is
   restored or named into required-now`, and `g26-leftover-join.v4.json#$.obligations[6].reason`.
   The G17 harness cell's `drop/change/truncation mutations` corpus is historical, per
   `preview-analyze-nt-gate-join.v2.json#$.g17Quoted.historicalStanding`.
   **Record as a named open decision, do not choose.**
4. **A D9 code, exit number, or HostTermination in a refusal payload** — violates
   `harness.DR-G26…preview.v2.json#$.doesNot` `Does not invent a D9 code or a section 7.1 recipe.`
   and D-293's `new D9/HostTermination … semantics`. The `not offered` fate in particular tempts an
   exit-code witness; the refusal must be expressed without minting one.
   **Record as a named open decision, do not choose.**
5. **A real CLI flag spelling asserted as the product surface** — the record carries no CLI grammar
   for preview analyze, and `preview-analyze-contract.v2.json#$.doesNot` includes `Does not rewrite
   D-002 commands, platforms, or independent-release machinery.` A witness flag string must be
   labelled as a fixture witness, not as the surface.
   **Record as a named open decision, do not choose.**
6. **Reusing DR-133 NT-5's payloads** (`fixtures/g23.v3/G23.nt5.coverage-narrows.json`,
   `…coverage-widens.json`, `…unknown-to-covered.json`) or classifying a G26 witness under DR-133
   NT-5 — violates `#$.failsIf` `DR-133 NT-5 is treated as executing here` and
   `#$.namedExecutesLimbNote`. See §1.5.
   **Record as a named open decision, do not choose.**
7. **Absorbing the two DR-122 SARIF fixture obligations** (`OBL-FC-NONAUTH-TERM-FX`,
   `OBL-FC-OUTFAIL-FX` on `sarif-leftover-join.v4.json` sha256
   `a2ab59d79051337906ae610b4c34f8203dcac0d9038f2826b32f68630bd07640`) — those are a different row
   with a different owner cell, separately enumerated in D-293 Decision 8.
   **Record as a named open decision, do not choose.**
8. **A finding fingerprint or durable output identity** in a refusal record — violates
   `preview-analyze-contract.v2.json#$.unstableIdentities.notDurable` = `["finding fingerprint",
   "RunId as a sealed authoritative identity", "EvidenceDigest"]`.
   **Record as a named open decision, do not choose.**

**Risk.** The reviewer-attack pattern on the two recorded fixture-corpus lineages is: (a) inventing a
vocabulary or encoding the record does not carry, (b) collapsing two classes the occupancy keeps
separate, (c) classifying a witness under a closed identifier it does not belong to, (d) over-claiming
closure. G26's exposure is concentrated on (a) and (d), and is lower overall than the other four.

On (a), the hazard is peculiar to this gate: the fixture must witness the *absence* of a surface, and
the only way to make an absence legible is to describe what is absent — which is exactly how a SARIF
advertisement gets invented. `g23-fixture-corpus.v1.review-independent.claude2.json` returned
`REJECT` with five MUST-FIX including `M3 — hostRequestedDomainBaseline invents a Coverage-domain
encoding vocabulary and under-determines the requested domain`; the same reviewer applied to a G26
corpus that sketched an advertised-capability shape would land the same finding against
`g26-leftover-join.v4.json#$.doesNot` `Does not invent a SARIF advertisement.`

On (b), the two members are far apart semantically (an invocation outcome and a register standing),
so collapse is unlikely — but the mirror hazard exists: EV-2 is *not* a live-cell member of anything
else and must not be quietly dropped as "not a fixture". The obligation reason names it
(`EV-1 and EV-2`) and `$.proposedLaterWork[2]` counts `two live-cell members`; a two-payload corpus
covering only EV-1 is under-covered.

On (c), the DR-133 NT-5 name collision is a live trap and §1.5 exists so the corpus can recite the
distinction rather than trip it.

On (d), `g23-fixture-corpus.v1`'s `S4 — leftoverDesignClosedIfAcceptedAndRecorded over-claims closure
of OBL-G23-FX-AUTHORING` is the template, and G26 is the gate where over-claiming is most tempting:
one class, two members, three states, all authored in one act. The safe form remains
`g23-fixture-corpus.v3.json#$.leftoverDesignClosedIfAcceptedAndRecorded` = `[]`, leaving the closure
measurement to the successor leftover-join, whose own `$.doesNot` already says
`Does not close OBL-G26-FX-AUTHORING.`

## 7. Open questions

**OQ-G26-1:** Does the corpus need a witness for each of the two fates in `Refused or not offered.`
(`preview-analyze-contract.v2.json#$.negativeTests.NT-5.pass`), or does either fate satisfy EV-1?
`harness.DR-G26…preview.v2.json#$.retainedEvidence[0].passProperty` reproduces the disjunction
without resolving it, `g26-input-corpus.v1.json` names two initial states split on *request vs flag*
rather than on *refused vs not-offered*, and `$.waiverRuleVerbatim` (`pass all; no waiver by silent
advertisement`) forbids only the silent third case. Under the D-293 default policy ("every explicit
… fate quantifier in the governing bytes is preserved") a two-fate reading would raise the payload
count from 3 to 4 or 5. The record does not settle it.

**OQ-G26-2:** Does the first G26 fixture corpus carry per-platform copies, or is it a platform-free
first authoring? The occupancy has no `platforms` array, no `windowsStanding` and no `d002Standing`;
its only platform byte is `$.proposedLaterWork[2]`'s phrase `on the four D-002 platforms`, and the
four tokens are not enumerated in the G26 record. Nothing in the G26 record names which occupancy
supplies the tokens, nor records the first-authoring/copy split (D-237→D-239 at G23, D-245→D-247 at
G21) as this gate's answer.

**OQ-G26-3:** The G26 occupancy carries no `namedCorpusClasses`, so
`preview-analyze-contract.v2.json#$.negativeTests.NT-5` is the only place in the record that states
NT-5's `name`, `input` and `pass`. Every other preview-analyze gate whose occupancy names a class
restates them locally (G24 `$.namedCorpusClasses[0..1]`, G25 `$.namedCorpusClasses[0]`, G28
`$.namedCorpusClasses[0..1]`, each with a `.source` string). Whether a G26 corpus must cite the
contract directly, or whether the missing restatement is itself a defect a reviewer would raise
against the occupancy, is not settled — and `DECISIONS-RECOMMENDED.md` §B1 item 2 leaves the cited
contract conditional (`v2 081ff7fb… unless a successor is authored, in which case the successor`),
which would move the only definition available.

**OQ-G26-4:** Does D-293 Decision 5's "authorized in the agreed order" make the DR-131 Class A
opening entry a precondition for *recording* the G26 fixture corpus, or only for the SATISFIED-GRADE
cycle? `DECISIONS-RECOMMENDED.md` §B1 item 4 constrains fixture authoring only relative to "the
separate DR-131 SATISFIED-GRADE + MF-6 cycle", and D-293 Decision 8 delegates D1 for G24–G30 with no
express Class A precondition; but item 4 sits in a list whose item 3 is the opening entry, and
Codex's round-2 sentence orders opening-then-fixtures. The record does not reconcile the two.

**OQ-G26-5:** `harness.DR-G26…preview.v2.json#$.retainedEvidence[1].exactByteIntent` asks for "a
record that G17 stays INAPPLICABLE-DROPPED-BY-D077 and is not named into required-now, and that no
preview-analyze SARIF projection harness ran." The first two halves are quotable from file 08 line
353 and from the required-now count 28. The third — a record that *no harness ran* — has no schema in
the pinned inputs, and no other retained-evidence member in the five preview-analyze occupancies asks
for a negative-execution record of a *sibling gate's* harness. Whether that is a catalog assertion, a
payload field, or something the register itself already carries is not settled.
