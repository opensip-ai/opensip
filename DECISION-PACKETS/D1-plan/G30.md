# G30 — OBL-G30-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

- Path: `docs/coop/artifacts/g30-leftover-join.v4.json`
- sha256: `3f3d84e0e24a2aea0ba95c282f809c8343d1827d0577ca8396d4560c5e012c75`
- `$.artifact` = `"g30-leftover-join.v4"`
- `$.date` = `"2026-08-24"`
- `$.registerRow` = `"DR-G30"`
- `$.status` = `"CANDIDATE-NOT-APPLIED"`
- `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`
- `$.binds` = `"NOTHING"`
- `$.head` = `"acfead24ed452a4e85fb47c0fd00e76cea9dcc2a"`
- `$.requiredNowUnchanged` = `28`
- `$.file08StatusToken` = `"OPEN"`
- `$.sealRecommendation` = `"DO-NOT-SEAL"`
- `$.documentClass` = `"DESIGN-CONTRACT-CANDIDATE"`

COORD heading that recorded it (`docs/coop/COORDINATOR-DECISIONS.md`
`1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 12585), verbatim:

> `## D-255 — Record g30 leftover-join.v4 as G30 leftover remasurement`

`$.file08Pin` matches live file 08:

```json
{
  "path": "docs/v2/architecture/08-decision-and-readiness-register.md",
  "sha256": "e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e"
}
```

`$.registerRowNote`, verbatim:

> "registerRow is the already-named gate DR-G30 because this join remasures leftover-design of G30
> after occupancy v2 (D-230). file08StatusToken is DR-G30's own live token (OPEN). leftover-join.v3
> remains frozen and is not current after this successor is recorded. D-158 named DR-G30 as
> required-now. This join does not reopen leftover-design of EE-7a, EE-7b, or EE-7d, does not invent
> fixture bytes, does not take over DR-101, G13, G14, G16, G24, or G29, and does not SATISFY DR-117."

`$.leftoverDesignOpenStanding`, verbatim:

> "The live DR-G30 token is OPEN. leftover-design of unnamed G30 initial states is stale as a naming
> claim after g30-input-corpus.v1. leftover-design of G30 fixture implementations remains. A section
> 7.1 recipe is not invented. DR-117 is not SATISFIED."

`$.namedClassesThisGate` = `["EE-7a","EE-7b","EE-7d"]`.

`$.summary.leftoverDesign` = `["OBL-G30-FX-AUTHORING"]`; `$.summary.dr117Satisfied` = `false`;
`$.summary.fixturesExecuted` = `false`; `$.summary.packInvented` = `false`;
`$.summary.rustAsCoreMinted` = `false`; `$.summary.g13NamedIntoRequiredNow` = `false`;
`$.summary.requiredNowUnchanged` = `28`.

### 1.2 The obligation object (verbatim)

JSON path: `$.obligations[3]` of `docs/coop/artifacts/g30-leftover-join.v4.json` (verified: the
`obligations` array is `[OBL-G30-HARNESS-SPEC, OBL-G30-NAMED-CORPUS, OBL-G30-INPUT-CORPUS,
OBL-G30-FX-AUTHORING, OBL-G30-EXECUTION, OBL-EE7ABD-UNNAMED-REMAINDER-CLOSED]`).

```json
{
  "id": "OBL-G30-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "useful-install advertisement, role-list, and product-statement corpus (DR-117 EE-7a, EE-7b, EE-7d)"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "G30 occupancy v2 names unauthored fixture cells of the useful-install advertisement, role-list, and product-statement corpus. D-056 Decision clause 5: authoring fixtures remains design work. This join does not invent those fixture bytes, does not invent the DR-131 pack, and does not mint Rust-as-core. The INPUT-state corpus exists and is Claude ACCEPT 0/0; that INPUT layer is OBL-G30-INPUT-CORPUS, leftoverDesign false. This join does not invent fixture bytes."
}
```

Field-by-field:

- `$.obligations[3].reason` — quoted in full above. Unlike G29's obligation, this reason correctly
  names the current occupancy ("G30 occupancy v2"), matching `$.basedOn.occupancyV2.role`
  ("Current G30 occupancy remasurement").
- `$.obligations[3].namedCorpusNotAuthored` — one element; the string is byte-identical to the join
  top-level `$.namedCorpusNotAuthored[0]` and to the corpus phrase in live file 08 line 366,
  column 3.
- `$.obligations[3].existingGate` = `"none as authored implementations"`.
- `$.obligations[3].rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `$.obligations[3].executionObligationOwnerToday` = `"none"`.
- `remainingNotAuthored`, `namedCases`, `namedNotAuthored` — absent on this obligation object.

### 1.3 Occupancy file and the fields that name the fixture cells

- Path: `docs/coop/artifacts/harness.DR-G30.preview-boundary-install-shape.preview.v2.json`
- sha256: `371695b8fc7b5cf61e016508da69436fbe6146683979f0c2468f52757a16cfda`
- `$.artifact` = `"harness.DR-G30.preview-boundary-install-shape.preview.v2"`
- Named as current by `$.basedOn.occupancyV2` of the join, role verbatim: "Current G30 occupancy
  remasurement. leftover-join.v3 cited occupancy v1 as the specification. Occupancy v2 remasured
  that already-named identifier after D-205. Occupancy v2 stays unmoved. This join does not occupy
  the identifier and does not execute G30."
- Dual ACCEPT 0/0 (`$.basedOn.occupancyV2.reviews.claude.verdict` /
  `$.basedOn.occupancyV2.reviews.codex.verdict`).

**`$.liveRowVerbatim`** (identical to live file 08 line 366):

> "| DR-G30 PREVIEW-BOUNDARY-INSTALL-SHAPE | Preview useful-install selection is signed distribution
> core + semantic host + one TypeScript closure + future DR-131 pack; core-only is not the analysis
> product; a second preview language role is refused; the rustc_driver sidecar is deferred not
> abandoned. Corpus is useful-install advertisement, role-list, and product-statement cases. Not
> DR-101 signed-distribution-core inventory. Not G13 language-quality. Not DR-131 pack identity |
> named: harness.DR-G30.preview-boundary-install-shape.preview (D-158; not authored; not QUALIFIED).
> useful-install advertisement, role-list, and product-statement corpus (DR-117 EE-7a, EE-7b, EE-7d)
> | default useful install includes selected TypeScript closure; core-only is not advertised as the
> analysis product; role list remains TypeScript-only; sidecar remains a deferred supported role;
> not DR-101 core inventory; not G13 quality green; not G24 pack-identity admission | Product owner
> | PROPOSED; not QUALIFIED | pass all; no waiver by core-only advertisement or second-role
> admission | OPEN |"

**`$.liveHarnessCellVerbatim`**:

> "named: harness.DR-G30.preview-boundary-install-shape.preview (D-158; not authored; not
> QUALIFIED). useful-install advertisement, role-list, and product-statement corpus (DR-117 EE-7a,
> EE-7b, EE-7d)"

**`$.liveCellVerbatim`**:

> "default useful install includes selected TypeScript closure; core-only is not advertised as the
> analysis product; role list remains TypeScript-only; sidecar remains a deferred supported role;
> not DR-101 core inventory; not G13 quality green; not G24 pack-identity admission"

**`$.claimVerbatim`**:

> "Preview useful-install selection is signed distribution core + semantic host + one TypeScript
> closure + future DR-131 pack; core-only is not the analysis product; a second preview language
> role is refused; the rustc_driver sidecar is deferred not abandoned. Corpus is useful-install
> advertisement, role-list, and product-statement cases. Not DR-101 signed-distribution-core
> inventory. Not G13 language-quality. Not DR-131 pack identity"

**`$.waiverRuleVerbatim`** = `"pass all; no waiver by core-only advertisement or second-role admission"`.
**`$.liveAssuranceStageVerbatim`** = `"PROPOSED; not QUALIFIED"`.
**`$.liveStatusVerbatim`** = `"OPEN"`.
**`$.sliceStanding`** = `"REQUIRED"`.
**`$.liveGateOwner`** = `{"DR-G30": "Product owner"}`.
**`$.executionObligationOwner`** = `"Product owner"`.
**`$.productLawOwner`** = `"DR-117 / preview-product-boundary-successor.v5"`.
**`$.namedGateTitle`** = `"PREVIEW-BOUNDARY-INSTALL-SHAPE"`.

**`$.namedCorpusNotAuthored`** = `["useful-install advertisement","role-list","product-statement"]`.
**`$.namedCorpusNote`**:

> "Live harness-cell punctuation after the named-identifier sentence. These are corpus classes, not
> authored fixtures and not invented FX/AT/NT/G30-* identifiers. A later fixture-authoring cycle may
> pin bytes. This artifact does not."

**`$.namedExecutesLimbs`** = `["useful-install advertisement","role-list","product-statement"]`.
**`$.namedExecutesLimbNote`**:

> "Live harness-cell punctuation after the named-identifier sentence, comma-and. Three corpus
> classes. The parenthetical (DR-117 EE-7a, EE-7b, EE-7d) names those classes; it is not a fourth
> corpus class and not three extra live-cell members. None may be silently dropped. Each class has
> exactByteIntent and passProperty on at least one retained-evidence member."

**`$.ownsOnly`** = `["EE-7a","EE-7b","EE-7d"]`.
**`$.doesNotOwnEe`** = `["EE-1","EE-2","EE-3a","EE-3b","EE-4","EE-5a","EE-5b","EE-6a","EE-6b","EE-7c","EE-7e"]`.
**`$.doesNotInventEeNumbers`** = `true`.

**`$.namedCorpusClasses[]`** — three classes, each with `exactByteIntent` and `passProperty`.
Quoted in full.

`$.namedCorpusClasses[0]`:

```json
{
  "id": "useful-install-advertisement",
  "memberVerbatim": "useful-install advertisement",
  "ee": "EE-7a",
  "sourceInvariantVerbatim": "Default useful install is signed distribution core + semantic host + one selected TypeScript closure + future DR-131 pack. Core-only is recovery/management.",
  "sourceInputVerbatim": "A release that advertises core-only as the analysis product, or that omits the selected TypeScript closure from the default useful install.",
  "sourcePassVerbatim": "Fails this class.",
  "exactByteIntent": "Once authored: digest-pinned release-advertisement records covering both v5 EE-7a input limbs: (1) a release that omits the selected TypeScript closure from the default useful install, and (2) a release that advertises core-only as the analysis product. Limb (1) is EV-1. Limb (2) is EV-2. The advertised default useful-install selection is signed distribution core + semantic host + one selected TypeScript closure + future DR-131 pack. The pack is a future selected limb, not a present shipped component and not a pack identity invented here. File 02's signed-distribution-core default install is not this class. This class is not DR-101 inventory, not G13 quality, and not G24 pack-identity admission. Fixtures are unauthored.",
  "passProperty": "Omitting the selected TypeScript closure fails. Advertising core-only as the analysis product fails. There is no waiver by core-only advertisement. Inventing pack identity or requiring a present shipped pack fails. DR-101, G13, and G24 are not scored here."
}
```

`$.namedCorpusClasses[1]`:

```json
{
  "id": "role-list",
  "memberVerbatim": "role-list",
  "ee": "EE-7b",
  "sourceInvariantVerbatim": "Preview admits one language role: TypeScript. A second preview language role is refused.",
  "sourceInputVerbatim": "A preview PlanIntent or release that admits a second language role.",
  "sourcePassVerbatim": "Refused. Role list remains TypeScript-only.",
  "exactByteIntent": "Once authored: digest-pinned role-list records of a preview PlanIntent or release that admits a second language role. Pass is refusal; role list remains TypeScript-only. The rustc_driver sidecar is not a second preview language role and is not Rust-as-core. This class is the D-002 / DR-118 role-list decision, not G13 language-quality green (EV-6). This specification does not invent a PlanIntent schema, a D9 code, or a section 7.1 recipe. Fixtures are unauthored.",
  "passProperty": "A second preview language role is refused. Role list remains TypeScript-only. There is no waiver by second-role admission. Rust-as-core is not minted. G13 is not named into required-now and is not scored here."
}
```

`$.namedCorpusClasses[2]`:

```json
{
  "id": "product-statement",
  "memberVerbatim": "product-statement",
  "ee": "EE-7d",
  "sourceInvariantVerbatim": "The sidecar remains the substrate and a deferred supported analysis role.",
  "sourceInputVerbatim": "A preview product statement that abandons or deletes the rustc_driver sidecar as a future supported role.",
  "sourcePassVerbatim": "Fails this class.",
  "exactByteIntent": "Once authored: digest-pinned preview product-statement records that abandon or delete the rustc_driver sidecar as a future supported role. Those records fail this class. The sidecar remains a deferred supported role, not abandoned. Deferral is not abandonment, not a second preview language role, and not Rust-as-core. This class does not execute rustc_driver and does not mint a slice-1 Rust role. Fixtures are unauthored.",
  "passProperty": "A preview product statement that abandons or deletes the rustc_driver sidecar as a future supported role fails. Sidecar remains a deferred supported role. Rust-as-core is not minted. Role-list refusal is the role-list class, not this class."
}
```

**`$.retainedEvidence[]`** — seven members. Four positive (EV-1..EV-4), three not-that-gate
(EV-5..EV-7). Each carries `exactByteIntent` and `passProperty`; quoted in full.

`$.retainedEvidence[0]`:

```json
{
  "id": "EV-1",
  "memberVerbatim": "default useful install includes selected TypeScript closure",
  "ee": "EE-7a",
  "measures": "The TypeScript-closure inclusion limb of v5 useful-install selection. Default useful install is signed distribution core + semantic host + one selected TypeScript closure + future DR-131 pack.",
  "exactByteIntent": "Once authored: digest-pinned useful-install advertisement records whose advertised default useful-install selection is signed distribution core + semantic host + one selected TypeScript closure + future DR-131 pack. The live-cell member is the TypeScript-closure inclusion limb of that selection: the default useful install includes the selected TypeScript closure. A record that omits the selected TypeScript closure from the default useful install fails. The future DR-131 pack is a selected limb, not a present shipped component and not a pack identity invented here. Pack identity remains DR-131; pack-identity admission remains G24 (EV-7). This member does not author DR-101 signed-distribution-core inventory (EV-5) and does not score G14 self-contained-closure quality (EE-7c). Fixtures are unauthored.",
  "passProperty": "Default useful install includes the selected TypeScript closure. Omitting that closure fails. Requiring a present shipped pack or inventing pack identity fails. Core-only advertisement is EV-2. DR-101 inventory, G13 quality, and G24 pack-identity admission are not scored here.",
  "suppliedBy": "useful-install advertisement corpus class. Not authored."
}
```

`$.retainedEvidence[1]`:

```json
{
  "id": "EV-2",
  "memberVerbatim": "core-only is not advertised as the analysis product",
  "ee": "EE-7a",
  "measures": "The core-only-is-not-the-analysis-product limb of v5 EE-7a. Core-only remains recovery/management.",
  "exactByteIntent": "Once authored: digest-pinned useful-install advertisement records in which core-only is advertised as the analysis product fail this member. Core-only remains recovery/management (file 02; v5 EE-7a). This member does not execute DR-101 inventory (EV-5) and does not convert file 02's signed-distribution-core default install into the analysis product. Fixtures are unauthored.",
  "passProperty": "A release that advertises core-only as the analysis product fails. There is no waiver by core-only advertisement. DR-101 inventory is not scored here. TypeScript-closure omission is EV-1.",
  "suppliedBy": "useful-install advertisement corpus class. Not authored."
}
```

`$.retainedEvidence[2]`:

```json
{
  "id": "EV-3",
  "memberVerbatim": "role list remains TypeScript-only",
  "ee": "EE-7b",
  "measures": "Preview admits one language role: TypeScript. A second preview language role is refused.",
  "exactByteIntent": "Once authored: digest-pinned role-list records for a preview PlanIntent or release that admits a second language role. Pass is refusal; role list remains TypeScript-only (v5 EE-7b). The rustc_driver sidecar is not a second preview language role and is not Rust-as-core (EV-4). This member does not name G13 into required-now and does not score G13 quality green (EV-6). This specification does not invent a PlanIntent schema, a D9 code, or a section 7.1 recipe. Fixtures are unauthored.",
  "passProperty": "A second preview language role is refused. Role list remains TypeScript-only. There is no waiver by second-role admission. Rust-as-core is not minted. G13 is not named into required-now and is not scored here.",
  "suppliedBy": "role-list corpus class. Not authored."
}
```

`$.retainedEvidence[3]`:

```json
{
  "id": "EV-4",
  "memberVerbatim": "sidecar remains a deferred supported role",
  "ee": "EE-7d",
  "measures": "The rustc_driver sidecar remains a deferred supported analysis role, not abandoned.",
  "exactByteIntent": "Once authored: digest-pinned product-statement records that abandon or delete the rustc_driver sidecar as a future supported role fail this member (v5 EE-7d). The sidecar remains a deferred supported role, not abandoned. Deferral is not abandonment and is not a second preview language role (EV-3). This member does not mint Rust-as-core and does not execute rustc_driver. Fixtures are unauthored.",
  "passProperty": "A preview product statement that abandons or deletes the rustc_driver sidecar as a future supported role fails. Sidecar remains a deferred supported role. Rust-as-core is not minted. Role-list refusal is EV-3.",
  "suppliedBy": "product-statement corpus class. Not authored."
}
```

`$.retainedEvidence[4]`:

```json
{
  "id": "EV-5",
  "memberVerbatim": "not DR-101 core inventory",
  "ee": null,
  "notThatGate": true,
  "measures": "Live-cell not-that-gate distinction. G30 does not own or execute signed-distribution-core inventory.",
  "exactByteIntent": "Once authored: this member remains a not-that-gate distinction, not a fourth corpus class. Digest-pinned G30 records do not include a signed-distribution-core inventory census as this gate's analysis-product evidence. DR-101 leftover remains on DR-101 / D-160. This member does not author DR-101 inventory bytes and does not execute G01-G05. File 02's signed-distribution-core default install stays DR-101. Fixtures are unauthored.",
  "passProperty": "Scoring DR-101 signed-distribution-core inventory as a G30 pass fails this member. Core-only advertised as the analysis product is EV-2, not this member. G01-G05 are not scored here.",
  "suppliedBy": "not a corpus class. Live-cell not-that-gate distinction. Not authored."
}
```

`$.retainedEvidence[5]`:

```json
{
  "id": "EV-6",
  "memberVerbatim": "not G13 quality green",
  "ee": null,
  "notThatGate": true,
  "measures": "Live-cell not-that-gate distinction. Second-role refusal is the role-list decision, not G13 language-quality.",
  "exactByteIntent": "Once authored: this member remains a not-that-gate distinction, not a fourth corpus class. Digest-pinned G30 records do not score G13 capability/parity matrix, corpus, or thresholds as this gate's pass. DR-G13 remains reserved, not named, not required-now, blocked on DR-118, IDENTIFIER-RESERVED-ZERO-C4-PROGRESS. This member does not name G13 into required-now and does not author harness.DR-G13.language-quality.typescript. Role-list refusal is EV-3. Fixtures are unauthored.",
  "passProperty": "Scoring G13 language-quality green as a G30 pass fails this member. Second-role refusal is EV-3, not G13 quality. G13 is not named into required-now.",
  "suppliedBy": "not a corpus class. Live-cell not-that-gate distinction. Not authored."
}
```

`$.retainedEvidence[6]`:

```json
{
  "id": "EV-7",
  "memberVerbatim": "not G24 pack-identity admission",
  "ee": null,
  "notThatGate": true,
  "measures": "Live-cell not-that-gate distinction. G30 does not own pack-identity admission. Distinct from claim Not DR-131 pack identity.",
  "exactByteIntent": "Once authored: this member remains a not-that-gate distinction, not a fourth corpus class. Digest-pinned G30 records do not execute G24 pack-identity admission (DR-131 NT-1, NT-2). The future DR-131 pack is a selected limb of EV-1, not a present shipped identity invented here. Pack identity remains DR-131. Pack-identity admission remains G24. Claim 'Not DR-131 pack identity' is not substituted for this member. Fixtures are unauthored.",
  "passProperty": "Scoring G24 pack-identity admission as a G30 pass fails this member. Inventing pack identity fails. EV-1 may name the future pack as a selected limb; it may not execute G24.",
  "suppliedBy": "not a corpus class. Live-cell not-that-gate distinction. Not authored."
}
```

**`$.liveCellMemberSplit`** — the seven live-cell members and the non-collapse rule:

```json
{
  "liveCellVerbatim": "default useful install includes selected TypeScript closure; core-only is not advertised as the analysis product; role list remains TypeScript-only; sidecar remains a deferred supported role; not DR-101 core inventory; not G13 quality green; not G24 pack-identity admission",
  "delimiter": "semicolon",
  "membersInOrder": [
    "default useful install includes selected TypeScript closure",
    "core-only is not advertised as the analysis product",
    "role list remains TypeScript-only",
    "sidecar remains a deferred supported role",
    "not DR-101 core inventory",
    "not G13 quality green",
    "not G24 pack-identity admission"
  ],
  "positiveMembers": [
    "default useful install includes selected TypeScript closure",
    "core-only is not advertised as the analysis product",
    "role list remains TypeScript-only",
    "sidecar remains a deferred supported role"
  ],
  "notThatGateMembers": [
    "not DR-101 core inventory",
    "not G13 quality green",
    "not G24 pack-identity admission"
  ],
  "notInventedExtras": [
    "default useful install as a member without the TypeScript-closure limb",
    "selected TypeScript closure as a member without default useful install",
    "signed distribution core, semantic host, TypeScript closure, and future pack as four live-cell members",
    "analysis product as a member",
    "TypeScript-only as a member without role list",
    "rustc_driver as a live-cell member distinct from sidecar remains a deferred supported role",
    "substrate as a live-cell member",
    "EE-7a, EE-7b, or EE-7d as live-cell members",
    "EE-7c, EE-7e, or EE-6b as live-cell members",
    "G13 harness as a live member",
    "G24 pack-identity admission as an executed corpus of this file",
    "DR-101 inventory census as a live member to execute",
    "Rust-as-core",
    "a second preview language role",
    "a present shipped DR-131 pack",
    "v1-slice section 7 item 8 as a G30 live member",
    "G29 excluded-form admission as a live member"
  ],
  "notCollapsed": "default useful install includes selected TypeScript closure is not collapsed into core-only is not advertised as the analysis product. role list remains TypeScript-only is not collapsed into not G13 quality green. sidecar remains a deferred supported role is not collapsed into a second-role admission. not DR-101 core inventory is not collapsed into the core-only advertisement member. not G13 quality green is not collapsed into the role-list member. not G24 pack-identity admission is not collapsed into the TypeScript-closure inclusion limb or into claim Not DR-131 pack identity."
}
```

**`$.platforms`** = `["macos/arm64","macos/x86_64","linux/x86_64","linux/arm64"]` — **present** on
this occupancy (unlike G29's, which has none).

**`$.windowsStanding`**:

> "D-002 defers Windows with explicit disposition. Windows is not a G30 preview platform. Platform
> is not a live-cell axis of this gate."

**`$.platformAxis`**:

> "Not a live-cell member. The three corpus classes are advertisement, role-list, and
> product-statement cases. A later fixture-authoring cycle may pin those cases on D-002 platforms.
> This artifact does not invent a four-platform matrix as a live-cell member and does not invent
> those bytes."

**`$.passProperty`** (top-level, present on this occupancy):

> "Once independently pinned fixture implementations exist for every named corpus class and every
> live retained-evidence member, each retained-evidence member's passProperty holds. Until those
> bytes exist, this harness cannot be executed and cannot be QUALIFIED. File 08 remains PROPOSED;
> not QUALIFIED. There is no waiver by core-only advertisement or second-role admission. Platform is
> not a live-cell axis. The future DR-131 pack is not invented here."

**`$.discipline`**:

> "SPECIFICATION OF EXACT-BYTE INTENT. Occupancy remasurement of the already-named identifier. No
> harness runs here. Naming is not execution. No fixture bytes authored. EE-7a, EE-7b, and EE-7d are
> not executed by this file existing. A section 7.1 recipe is not invented. DR-101, G13, G14, G16,
> G24, and G29 are not taken over."

**`$.languageRoleAxis`**:

```json
{
  "axisName": "preview language role",
  "slice1Domain": ["TypeScript"],
  "soleSlice1Role": "TypeScript",
  "doesNotMint": ["Rust-as-core","any second slice-1 language role","a future-role catalog","a present shipped DR-131 pack identity"],
  "sidecarIsNotASecondRole": "The rustc_driver sidecar remains a deferred supported role. Deferral is not a second preview language role and is not Rust-as-core.",
  "futureRoles": "D-002 activates TypeScript alone. A later role's admission is a later product-role-list successor, not this specification inventing a catalog. This specification does not name G13 into required-now."
}
```

**`$.claimNotThatGate`** — the claim-cell tail, kept distinct from the evidence-cell tail:

```json
{
  "liveClaimVerbatimTail": "Not DR-101 signed-distribution-core inventory. Not G13 language-quality. Not DR-131 pack identity",
  "membersInOrder": ["Not DR-101 signed-distribution-core inventory","Not G13 language-quality","Not DR-131 pack identity"],
  "note": "Claim-cell not-that-gate from CLAUDE-PPBAL-V1-ADV1 / D-158. Distinct from the last three live retained-evidence members. Do not substitute claim 'Not DR-131 pack identity' for evidence 'not G24 pack-identity admission'. Pack identity remains DR-131; pack-identity admission remains G24."
}
```

**The three per-EE blocks.** `$.ee7a`:

```json
{"id":"EE-7a","namedBy":"D-158","leftoverDesign":false,"existingGate":"DR-G30","rides":"useful-install advertisement","notALiveCellMember":true,"limbs":["default useful install includes selected TypeScript closure","core-only is not advertised as the analysis product"],"invariantVerbatim":"Default useful install is signed distribution core + semantic host + one selected TypeScript closure + future DR-131 pack. Core-only is recovery/management.","doesNotSatisfy":["DR-117","DR-101","DR-131"]}
```

`$.ee7b`:

```json
{"id":"EE-7b","namedBy":"D-158","leftoverDesign":false,"existingGate":"DR-G30","rides":"role-list","notALiveCellMember":true,"invariantVerbatim":"Preview admits one language role: TypeScript. A second preview language role is refused.","doesNotSatisfy":["DR-117","DR-118"]}
```

`$.ee7d`:

```json
{"id":"EE-7d","namedBy":"D-158","leftoverDesign":false,"existingGate":"DR-G30","rides":"product-statement","notALiveCellMember":true,"invariantVerbatim":"The sidecar remains the substrate and a deferred supported analysis role.","doesNotSatisfy":["DR-117"]}
```

**Fields ABSENT from this occupancy** (checked against the brief's minimum list):
`liveGateOwner` is present but there is no `namedCorpusNotAuthored` *parenthetical* class list beyond
the three strings; there is no `section7Item8Standing` (that is G29's); no `failsIf` entry naming a
platform matrix as required. Everything else on the brief's list is present and quoted above:
`liveRowVerbatim`, `liveHarnessCellVerbatim`, `liveCellVerbatim`, `claimVerbatim`,
`namedCorpusNotAuthored`, `namedCorpusClasses[]` (with `exactByteIntent` / `passProperty`),
`retainedEvidence[]` (same), `platforms`, `windowsStanding`, `passProperty`, `failsIf` (§3),
`doesNot` (§3), `liveGateOwner`, `executionObligationOwner`.

**The INPUT-state corpus.** `docs/coop/artifacts/g30-input-corpus.v1.json`
sha256 `5b23f3a6228a84a2945e21786f6a9b1549345c8aa6d851458dc2915a48e174e1`,
`$.artifact` = `"g30-input-corpus.v1"`, `$.registerRow` = `"DR-117"`, `$.namedGate` = `"DR-G30"`,
`$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`,
`$.binds` = `"NOTHING"`, `$.head` = `"5d5d77819ae3019d9e6e02f1e66de3d93c060402"`,
`$.requiredNowUnchanged` = `26`. Pinned by the join at `$.basedOn.inputCorpusV1`, role verbatim:

> "Named initial states from live G30 exactByteIntent. fixtureBytes NOT-AUTHORED. This join consumes
> that naming. It does not close leftover-design of fixture implementations."

`$.whatIsAuthored`:

> "Named initial states drawn from live G30 v1 exactByteIntent: omit TypeScript closure from default
> useful install; advertise core-only as analysis product; admit a second language role; abandon
> rustc_driver sidecar. No fixture bytes. No DR-131 pack invented. No Rust-as-core minted."

`$.assertsNamedNotAuthored` = `["useful-install advertisement","role-list","product-statement"]`.

Its four `$.initialStates[i]`, each carrying `"fixtureBytes": "NOT-AUTHORED"`:

| i | `id` | `classMemberVerbatim` | `stateNamed` | `source` |
|---|---|---|---|---|
| 0 | `G30.ee7a.omit-typescript-closure` | "useful-install advertisement" | "a release that omits the selected TypeScript closure from the default useful install" | "G30 v1 exactByteIntent EE-7a limb (1) / EV-1" |
| 1 | `G30.ee7a.core-only-as-analysis-product` | "useful-install advertisement" | "a release that advertises core-only as the analysis product" | "G30 v1 exactByteIntent EE-7a limb (2) / EV-2" |
| 2 | `G30.ee7b.second-language-role` | "role-list" | "a preview PlanIntent or release that admits a second language role" | "G30 v1 exactByteIntent EE-7b / EV-3. rustc_driver sidecar is not a second preview language role." |
| 3 | `G30.ee7d.abandon-sidecar` | "product-statement" | "preview product-statement that abandons or deletes the rustc_driver sidecar as a future supported role" | "G30 v1 exactByteIntent EE-7d / EV-4. Sidecar remains a deferred supported role, not abandoned." |

Note the `source` strings cite **occupancy v1**, not v2: the input corpus was authored at HEAD
`5d5d778` / required-now 26, before occupancy v2 (D-230). See OQ-G30-4.

### 1.4 ROW twin join (or: none)

**None.** `DECISION-PACKETS/D1-fixture-authoring-delegation.md`
sha256 `bc8484cc7159af26a142b97a55b1095049f7ea2ac10c283cd1e2428ba2569ea9`, §3.1 GATE-side table,
DR-G30 row (file 08 l.366; owner cell `Product owner`), last column ("ROW twin") reads `none`.

The packet's §3.2 note records what a twin would have required:

> "Closing a fixture obligation would require a successor on *both* the GATE join and its ROW twin,
> since the same id is measured true on both."

Inapplicable here. `OBL-G30-FX-AUTHORING` is measured `leftoverDesign: true` on
`g30-leftover-join.v4` only; closing it needs a successor on that lineage alone.

### 1.5 Governing contract/spec the occupancy pins

**The class-defining contract — `preview-product-boundary-successor.v5`.**
Pinned by the occupancy at `$.productLawOwner` = `"DR-117 / preview-product-boundary-successor.v5"`
and `$.palV1A1`:

> "PAL-V1-A1: Product owner is the execution-obligation owner. Product-law ownership remains DR-117
> / preview-product-boundary-successor.v5. This specification does not transfer product law onto the
> gate owner."

- Path: `docs/coop/artifacts/preview-product-boundary-successor.v5.json`
- sha256: `5face6a97b311117569044c0214452571e6d3f051e1ab9b38f46abf442ce1262`
- `$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`,
  `$.binds` = `"NOTHING"`, `$.registerRow` = `"DR-117"`.

The three G30 classes are defined at `$.enforcementEvidence.classes[]` (a 14-element array ordered
`EE-1, EE-2, EE-3a, EE-3b, EE-4, EE-5a, EE-5b, EE-6a, EE-6b, EE-7a, EE-7b, EE-7c, EE-7d, EE-7e`).
Verbatim, with JSON paths:

`$.enforcementEvidence.classes[9]` (EE-7a):

```json
{"id":"EE-7a","item":7,"subLimb":"useful-install inventory and core-only is not the analysis product","invariant":"Default useful install is signed distribution core + semantic host + one selected TypeScript closure + future DR-131 pack. Core-only is recovery/management.","input":"A release that advertises core-only as the analysis product, or that omits the selected TypeScript closure from the default useful install.","pass":"Fails this class.","owner":"this-candidate for the aggregate default-useful-install *selection* (core + host + one TS closure + future DR-131 pack) and the core-only-is-not-analysis-product rule. DR-101 / D-114 distribution-core-inventory owns only the signed-distribution-core inventory and excludes analyzer components from that core closure.","existingGate":"DR-101 does not own the TypeScript-closure limb of the useful install. Pack identity remains DR-131.","laterExecution":"Selection class specified here, unexecuted. Core inventory leftover remains on DR-101."}
```

`$.enforcementEvidence.classes[10]` (EE-7b):

```json
{"id":"EE-7b","item":7,"subLimb":"TypeScript-only preview language role","invariant":"Preview admits one language role: TypeScript. A second preview language role is refused.","input":"A preview PlanIntent or release that admits a second language role.","pass":"Refused. Role list remains TypeScript-only.","owner":"D-002 / DR-118 product role-list decision (TypeScript sole preview role).","existingGate":"DR-G13 is the future per-supported-role language-quality corpus, not the second-role refusal. The discriminating evidence is the role-list decision, not G13 green.","laterExecution":"Role-list specified here / D-002. G13 leftover measurement remains condition 4 for quality, not role-count."}
```

`$.enforcementEvidence.classes[12]` (EE-7d):

```json
{"id":"EE-7d","item":7,"subLimb":"rustc_driver sidecar deferred, not abandoned","invariant":"The sidecar remains the substrate and a deferred supported analysis role.","input":"A preview product statement that abandons or deletes the rustc_driver sidecar as a future supported role.","pass":"Fails this class.","owner":"this-candidate / D-002 substrate posture","existingGate":"none","laterExecution":"Specified here. Unexecuted."}
```

The same contract's item-7 disposition at `$.sevenItems.dispositions[6]`:

```json
{"item":7,"name":"replacement of the full-default G3 physical substrate","preview":"PARTIAL, recorded: TypeScript is the only preview language role. Default useful install is signed distribution core + semantic host + one selected TypeScript closure + the future DR-131 pack (pack not invented here). Core-only is recovery/management, not the analysis product. rustc_driver sidecar remains the substrate and is deferred as a supported analysis role, not abandoned. D-002 independent-release surface is preserved, not narrowed."}
```

**EE-7c — does it exist, and is it assigned elsewhere? Yes to both.**
The assignment brief's EE list (EE-7a, EE-7b, EE-7d) skips EE-7c because the record assigns EE-7c to
a different gate. Three independent bytes say so:

1. `preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[11]`, verbatim:

```json
{"id":"EE-7c","item":7,"subLimb":"TypeScript self-contained closure","invariant":"The selected TypeScript closure is self-contained / offline-capable.","input":"A TypeScript closure that requires ambient/implicit download.","pass":"Fails DR-G14 / DR-119.","owner":"DR-119 / DR-G14","existingGate":"DR-G14 only. Does not own role-set, lockstep, or useful-install inventory.","laterExecution":"DR-G14 leftover measurement remains condition 4. Not QUALIFIED."}
```

2. `harness.DR-G30.preview-boundary-install-shape.preview.v2.json#$.ee7cSplit`, verbatim:

> "EE-7c leftoverDesign is already false and rides G14 (D-159). Self-contained offline TypeScript
> closure is G14 / DR-119. This specification does not take over G14 and does not own EE-7c."

3. `harness.DR-G30…preview.v2.json#$.doesNotOwnEe` includes `"EE-7c"`, and `$.failsIf[21]` bites if
   "G14 language-runtime-UX or EE-7c is treated as executing here".

The sibling exclusions in the same file, for completeness: `$.ee7eSplit` — "EE-7e leftoverDesign is
already false and rides G16 (D-159). Independent-release / anti-lockstep is G16. D-002
independent-release surface is preserved, not narrowed, and is not executed here." — and
`$.ee6bSplit` — "EE-6b leftoverDesign is already false and rides G09 (D-159) as DISCLOSURE-ONLY
honesty. This specification does not take over G09 and does not convert honesty into confinement."

So EE-7c is **not** an omission or a gap in the G30 obligation: it is a named class whose
leftoverDesign is already false and whose gate is DR-G14. Nothing about EE-7c is owed by a G30
fixture corpus, and authoring an EE-7c witness here would fire `$.failsIf[21]`. What the record does
**not** carry is the D-159 heading text itself as a quotable sentence inside the G30 lineage — the
three bytes above all cite "D-159" without reproducing it. See OQ-G30-5.

**The gate-split statements the occupancy carries** (each bounds what the corpus may claim):

- `$.dr117Split`: "D-158 closed leftover-design of EE-7a, EE-7b, and EE-7d; remainder is G30
  execution. D-159 names EE-6b / EE-7c / EE-7e at G09 / G14 / G16. DR-117 stays OPEN. Class A
  remains reserved on v5 (D-137). This specification does not SATISFY DR-117 and does not open D-056
  Class A."
- `$.dr131Split`: "DR-131 owns preview-analyze pack identity and remains OPEN. The future pack is a
  selected limb of EV-1, not a present shipped identity invented here. Pack-identity admission
  remains G24. This specification does not SATISFY DR-131."
- `$.dr101Split`: "DR-101 owns signed-distribution-core inventory. leftover-join v3 remains DR-101
  leftover-design measurement (D-160). This specification does not SATISFY DR-101 and does not
  author inventory bytes."
- `$.g13Split`: "DR-G13 remains reserved, not named, not required-now, blocked on DR-118,
  IDENTIFIER-RESERVED-ZERO-C4-PROGRESS. Second-role refusal is EV-3, not G13 quality green (EV-6).
  This specification does not name G13 into required-now and does not author
  harness.DR-G13.language-quality.typescript."

**"Section 7.1" — what it refers to, pinned.** `harness.DR-G30…preview.v2.json#$.doesNot[24]` and
`$.failsIf[27]` prohibit inventing "a section 7.1 recipe". The referent is not `v1-slice.md` (which
contains zero occurrences of the string `7.1`); it is `docs/coop/IMPLEMENTATION-FREEZE.md`
sha256 `e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd`, line 1675:

> `### 7.1 Parked identity recipes — named for escalation, NOT non-blocking`

Its operative sentences, verbatim: line 1681 — "Naming them makes escalation compliant. It does not
make them optional and it does not authorise anyone to invent one."; line 1711 — "Every row above
must be closed by a binding artifact before signature. None may be closed by this record, by the
blueprint, by a checker, or by an implementer."; and the governing property blockquoted at line
1728 — "**Any identity-, digest-, commitment- or ref-typed field for which no binding artifact
states a rule producing its value from real inputs is parked under this section and is escalable,
whether or not it appears as a row above.**" (Cross-checked: COORD line 383 "the Finding fingerprint
(§7.1 parks both)"; `IMPLEMENTATION-FREEZE.md` line 4449 is `### 7.10 …`, so §7.1 and §7.10 are
sections of the same document, matching COORD line 1354's "§7.10 rule 2".)

**The D9 class list** (for `$.doesNot[23]`, "Does not invent a D9 code, exit number, or
HostTermination"): `docs/coop/artifacts/d9-exit-contract.v1.14.json`
sha256 `8dd3303855f49bfdbb2751ee65f54a906405f0654159ebe815472f73cdf7da31`, pinned live at file 08
line 40 (DR-007 source-pin cell). `$.codeVocabulary.closed` = `true`; `$.codeVocabulary.errorCodes`
includes `"EXTENSION.INSTALL_IO_FAILED"` and `"EXTENSION.ADMISSION_REJECTED"`;
`$.classToExitCode` = `{"success":0,"policy-failed":1,"request-rejected":2,"indeterminate":3,
"operational-failed":4,"interrupted":130}`. No G30 retained-evidence member names a D9 code, so the
corpus never needs one.

## 2. Coverage set

Every explicit quantifier the governing bytes name, verbatim, grouped by axis.

**Axis A — corpus classes.** Three.

1. `harness.DR-G30…preview.v2#$.namedCorpusNotAuthored` = `["useful-install advertisement",
   "role-list","product-statement"]` — **3 classes**.
2. `harness.DR-G30…preview.v2#$.namedExecutesLimbs` = the same three strings.
3. `harness.DR-G30…preview.v2#$.namedExecutesLimbNote`: "Live harness-cell punctuation after the
   named-identifier sentence, comma-and. Three corpus classes. The parenthetical (DR-117 EE-7a,
   EE-7b, EE-7d) names those classes; it is not a fourth corpus class and not three extra live-cell
   members. None may be silently dropped. Each class has exactByteIntent and passProperty on at
   least one retained-evidence member."
4. `g30-leftover-join.v4#$.obligations[3].namedCorpusNotAuthored[0]` = "useful-install
   advertisement, role-list, and product-statement corpus (DR-117 EE-7a, EE-7b, EE-7d)" — the same
   three, as one file-08 phrase.

**Axis B — EE classes.** Three, and only three.

5. `harness.DR-G30…preview.v2#$.ownsOnly` = `["EE-7a","EE-7b","EE-7d"]`.
6. `harness.DR-G30…preview.v2#$.doesNotOwnEe` = `["EE-1","EE-2","EE-3a","EE-3b","EE-4","EE-5a",
   "EE-5b","EE-6a","EE-6b","EE-7c","EE-7e"]` — **11 classes explicitly disowned**, EE-7c among them.
7. `harness.DR-G30…preview.v2#$.doesNotInventEeNumbers` = `true`.

**Axis C — live-cell retained-evidence members (the fate axis).** Seven, in order, none collapsible.

8. `…preview.v2#$.liveCellMemberSplit.membersInOrder[0]` = "default useful install includes selected
   TypeScript closure" (EV-1, EE-7a).
9. `…[1]` = "core-only is not advertised as the analysis product" (EV-2, EE-7a).
10. `…[2]` = "role list remains TypeScript-only" (EV-3, EE-7b).
11. `…[3]` = "sidecar remains a deferred supported role" (EV-4, EE-7d).
12. `…[4]` = "not DR-101 core inventory" (EV-5, not-that-gate).
13. `…[5]` = "not G13 quality green" (EV-6, not-that-gate).
14. `…[6]` = "not G24 pack-identity admission" (EV-7, not-that-gate).

Split recorded at `$.liveCellMemberSplit.positiveMembers` (4) and
`$.liveCellMemberSplit.notThatGateMembers` (3). Non-collapse rule at
`$.liveCellMemberSplit.notCollapsed`, quoted in full in §1.3.

**Axis D — EE-7a limbs (the mutation axis inside one class).** Two.

15. `…preview.v2#$.ee7a.limbs` = `["default useful install includes selected TypeScript closure",
    "core-only is not advertised as the analysis product"]`.
16. `…preview.v2#$.namedCorpusClasses[0].exactByteIntent`: "…covering both v5 EE-7a input limbs: (1)
    a release that omits the selected TypeScript closure from the default useful install, and (2) a
    release that advertises core-only as the analysis product. Limb (1) is EV-1. Limb (2) is EV-2."

EE-7b and EE-7d carry no `limbs` array; each maps to exactly one retained-evidence member.

**Axis E — named initial states (already fixed by the INPUT layer).** Four.

17. `g30-input-corpus.v1#$.initialStates[0].id` = `"G30.ee7a.omit-typescript-closure"`.
18. `g30-input-corpus.v1#$.initialStates[1].id` = `"G30.ee7a.core-only-as-analysis-product"`.
19. `g30-input-corpus.v1#$.initialStates[2].id` = `"G30.ee7b.second-language-role"`.
20. `g30-input-corpus.v1#$.initialStates[3].id` = `"G30.ee7d.abandon-sidecar"`.

These map onto EV-1..EV-4 exactly. **EV-5, EV-6 and EV-7 have no named initial state** —
`g30-input-corpus.v1#$.whatIsAuthored` names only the four, and each of the three not-that-gate
members is `"suppliedBy": "not a corpus class. Live-cell not-that-gate distinction. Not authored."`
See OQ-G30-3.

**Axis F — fates.** Two per class, from `$.waiverRuleVerbatim` = "pass all; no waiver by core-only
advertisement or second-role admission" and the seven passProperties. Recorded fates: *fails this
class* (EE-7a limb 1 and 2, EE-7d) and *refused* (EE-7b). Source strings:
`$.namedCorpusClasses[0].sourcePassVerbatim` = "Fails this class.";
`$.namedCorpusClasses[1].sourcePassVerbatim` = "Refused. Role list remains TypeScript-only.";
`$.namedCorpusClasses[2].sourcePassVerbatim` = "Fails this class."

**Axis G — platforms.** Enumerated on this occupancy, but expressly **not a live-cell axis**.

21. `…preview.v2#$.platforms` = `["macos/arm64","macos/x86_64","linux/x86_64","linux/arm64"]` —
    **4 tokens**.
22. `…preview.v2#$.windowsStanding` = "D-002 defers Windows with explicit disposition. Windows is
    not a G30 preview platform. Platform is not a live-cell axis of this gate."
23. `…preview.v2#$.platformAxis` = "Not a live-cell member. The three corpus classes are
    advertisement, role-list, and product-statement cases. A later fixture-authoring cycle may pin
    those cases on D-002 platforms. This artifact does not invent a four-platform matrix as a
    live-cell member and does not invent those bytes."
24. `…preview.v2#$.proposedLaterWork[2]` = "A later D-000 cycle may author and independently pin
    fixture implementations for EE-7a, EE-7b, EE-7d and the seven live-cell members, on the four
    D-002 platforms. This v2 occupancy does not invent those bytes."
25. `…preview.v2#$.failsIf[29]` = "Windows is treated as a G30 preview platform";
    `$.failsIf[30]` = "a four-platform matrix is invented as a live-cell member".

Unlike G29, a G30 corpus does not have to hunt for the four tokens: they are on this gate's own
occupancy and may be quoted directly (the recorded practice named in the brief). But `$.failsIf[30]`
makes the *live-cell* reading of that matrix a defect, so a per-platform copy version must state
that platform is a copy axis, not a live-cell member.

**Arithmetic.**

- 4 named initial states (Axis E) → 4 payloads covering EV-1, EV-2, EV-3, EV-4.
- 3 not-that-gate records (EV-5, EV-6, EV-7), each required by its own `exactByteIntent` ("Once
  authored: this member remains a not-that-gate distinction, not a fourth corpus class. Digest-pinned
  G30 records do not …"), with no named initial state.
- 4 + 3 = **7 corpus cells** at first authoring, no platform multiplier applied.
- Per-platform copy version: 7 x 4 = 28 copies, the multiplier taken from `$.platforms` (4 tokens,
  quotable from this gate's own occupancy) per `$.proposedLaterWork[2]`.

**Total coverage members: 7** (4 named initial states + 3 not-that-gate records), first authoring,
no platform subdirectories. **28** in a later per-platform copy version.

## 3. Prohibitions bounding the authoring

**From the GATE join** `g30-leftover-join.v4.json#$.doesNot` (21 entries), verbatim:

```
[0]  Does not SATISFY DR-117.
[1]  Does not reopen leftover-design of EE-7a, EE-7b, or EE-7d.
[2]  Does not invent the DR-131 pack.
[3]  Does not mint Rust-as-core.
[4]  Does not name G13 into required-now.
[5]  Does not take over DR-101, G14, G16, G24, or G29.
[6]  Does not open D-056 Class A.
[7]  Does not close leftover-design of fixture implementations.
[8]  Does not close OBL-G30-FX-AUTHORING.
[9]  Does not invent a D9 code.
[10] Does not invent a section 7.1 recipe.
[11] Does not add a DR-G* row.
[12] Does not change live required-now 28.
[13] Does not execute fixtures.
[14] Does not claim QUALIFIED.
[15] Does not edit file 08.
[16] Does not authorize docs/v2/implementation/.
[17] Does not invent fixture bytes.
[18] Does not occupy the identifier.
[19] Does not record leftover-join.v3 as current after this successor is recorded.
[20] Does not record occupancy v1 as current occupancy.
```

And `g30-leftover-join.v4.json#$.obligations[3].reason`, third sentence: "This join does not invent
those fixture bytes, does not invent the DR-131 pack, and does not mint Rust-as-core."

**From the occupancy** `harness.DR-G30…preview.v2.json#$.doesNot` (43 entries), verbatim:

```
[0]  Does not SATISFY DR-117 or any other architecture row.
[1]  Does not treat leftover-design of EE-7a, EE-7b, or EE-7d as reopened or as closed by this file existing.
[2]  Does not treat leftover-design of OBL-G30-HARNESS-SPEC as closed by this file existing.
[3]  Does not open D-056 Class A.
[4]  Does not claim QUALIFIED.
[5]  Does not execute fixtures.
[6]  Does not author fixture bytes.
[7]  Does not invent EE numbers.
[8]  Does not invent FX/AT/NT/G30-* fixture identifiers.
[9]  Does not name G13 into required-now.
[10] Does not mint Rust-as-core.
[11] Does not invent a second slice-1 language role.
[12] Does not abandon the rustc_driver sidecar.
[13] Does not treat the sidecar as a second preview language role.
[14] Does not take over DR-101 signed-distribution-core inventory.
[15] Does not take over G24 pack-identity admission.
[16] Does not invent the DR-131 pack or require it as a present shipped component.
[17] Does not take over G29 excluded-form admission.
[18] Does not take over G14 language-runtime-UX.
[19] Does not take over G16 CI isolation.
[20] Does not take over G09 permissions.
[21] Does not take over G01-G05 core measurement.
[22] Does not take over G08 trust-recovery install surfaces.
[23] Does not invent a D9 code, exit number, or HostTermination.
[24] Does not invent a section 7.1 recipe.
[25] Does not invent a PlanIntent schema.
[26] Does not change live required-now 28.
[27] Does not add a DR-G* row.
[28] Does not edit file 08.
[29] Does not rewrite G29 or any sibling harness file.
[30] Does not authorize docs/v2/implementation/.
[31] Does not SATISFY DR-131.
[32] Does not SATISFY DR-133.
[33] Does not close leftover-design of OBL-G30-FX-AUTHORING.
[34] Does not execute EE-7a, EE-7b, or EE-7d by existing.
[35] Does not treat naming v6 as naming G30.
[36] Does not rewrite naming-v6 bytes.
[37] Does not record frozen v1 as a current occupancy remasurement.
[38] Does not unwrite D-158, D-205, or D-229.
[39] Does not claim that no leftover-join exists for this gate.
[40] Does not add a COORD heading.
[41] Does not assign a D-* number.
[42] Does not treat D-158 turn-1 Claude OBJECT as the accepted naming parent.
```

**From the occupancy** `harness.DR-G30…preview.v2.json#$.failsIf` — 55 entries. The ones that bound
fixture authoring directly, verbatim with index:

```
[1]  leftover-design of EE-7a, EE-7b, or EE-7d is treated as reopened or as closed by this file existing
[2]  a live G30 retained-evidence member is omitted or has no exactByteIntent / passProperty
[3]  live-cell punctuation is collapsed or extras are invented (members must be default useful install includes selected TypeScript closure, core-only is not advertised as the analysis product, role list remains TypeScript-only, sidecar remains a deferred supported role, not DR-101 core inventory, not G13 quality green, not G24 pack-identity admission)
[4]  useful-install advertisement, role-list, or product-statement is omitted from exact-byte intent
[5]  EE-7a, EE-7b, or EE-7d is treated as a live-cell member rather than a corpus-class name
[6]  an EE number other than EE-7a, EE-7b, or EE-7d is invented or owned here
[7]  this specification is read as executing fixtures
[8]  this specification is read as authoring fixture bytes
[11] EE-7a, EE-7b, or EE-7d is treated as executed by this file existing
[12] Rust-as-core is minted or a second slice-1 language role is invented
[13] the rustc_driver sidecar is abandoned or treated as a second preview language role
[14] G13 is named into required-now
[15] DR-101 signed-distribution-core inventory is treated as executing here
[16] G24 pack-identity admission is treated as executing here
[17] the DR-131 pack is invented or required as a present shipped component
[18] claim Not DR-131 pack identity is substituted for evidence not G24 pack-identity admission
[19] file 02's signed-distribution-core default install is collapsed into v5 useful-install selection
[20] G29 excluded-form admission or v1-slice section 7 item 8 is treated as executing here
[21] G14 language-runtime-UX or EE-7c is treated as executing here
[22] G16 CI isolation or EE-7e is treated as executing here
[23] G09 permissions or EE-6b is treated as executing here
[26] a D9 code, exit number, or HostTermination is invented
[27] a section 7.1 recipe is invented
[28] a PlanIntent schema is invented
[29] Windows is treated as a G30 preview platform
[30] a four-platform matrix is invented as a live-cell member
[46] live G30 harness cell is not quoted byte-exact
[47] current INPUT basis is claimed other than g30-input-corpus.v1 as measured by leftover-join.v3 (D-205)
[48] EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, or EE-6a is treated as a G30 class
[49] DR-101, G13, G14, G16, G24, or G29 is taken over
```

**From the class passProperties** (each a prohibition when negated):
`$.namedCorpusClasses[0].passProperty` — "Inventing pack identity or requiring a present shipped
pack fails."; `$.retainedEvidence[6].passProperty` — "Inventing pack identity fails. EV-1 may name
the future pack as a selected limb; it may not execute G24."; `$.namedCorpusClasses[1].passProperty`
— "There is no waiver by second-role admission. Rust-as-core is not minted."

**From the INPUT corpus** `g30-input-corpus.v1.json#$.whatIsNotAuthored` (7 entries):
`["fixture bytes","the DR-131 pack","Rust-as-core","G13 named into required-now","a PlanIntent
schema","a D9 code","a section 7.1 recipe"]`, and `#$.doesNot` (15 entries) repeating "Does not
invent the DR-131 pack.", "Does not mint Rust-as-core.", "Does not name G13 into required-now.",
"Does not invent a PlanIntent schema.", "Does not invent a D9 code.", "Does not invent a section 7.1
recipe.", "Does not author fixture bytes."

**From `IMPLEMENTATION-FREEZE.md` §7.1**
(`e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd`), line 1681 and line 1711,
verbatim:

> "Naming them makes escalation compliant. It does not make them optional and
> it does not authorise anyone to invent one."

> "Every row above must be closed by a binding artifact before signature. None may
> be closed by this record, by the blueprint, by a checker, or by an implementer."

**From the D9 class list** `d9-exit-contract.v1.14.json#$.codeVocabulary.rule`:

> "A code outside this vocabulary is a contract violation. Codes are grouped by remedy: two codes
> with the same remedy are a smell, two remedies behind one code is a defect."

**The D-293 constraint clause that applies to this gate.**
`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md`
sha256 `f530cedca1c799097ed0fc30cf8ec6f0480abe9a56d495236909a3a23b84fc33`, line 3, verbatim:

> "Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new
> D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep
> their recorded prohibitions (adapter, CI encoding, journal)."

The clause "**no … new … pack … semantics … is authorised**" is the one that bites hardest on G30:
it is the D-293-level restatement of `$.doesNot[16]` and `$.failsIf[17]`.

And line 6, the default policy, verbatim:

> "**Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit
> platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved;
> for delegated byte sets, concrete witness bytes may be selected only within already-recorded
> schemas and fates, and any choice that would create a new semantic member, identifier, value, list
> or implementation stays outside the grant (recorded as a named open decision instead)."

## 4. Dependencies

### 4.1 The DR-131 pack question — settled, and settled in the corpus's favour

The assignment asks whether the useful-install advertisement corpus can be authored while the DR-131
pack is unauthored. The record **settles it: yes.** The sentence that settles it is
`harness.DR-G30.preview-boundary-install-shape.preview.v2.json#$.retainedEvidence[0].exactByteIntent`,
this clause verbatim:

> "The future DR-131 pack is a selected limb, not a present shipped component and not a pack
> identity invented here. Pack identity remains DR-131; pack-identity admission remains G24 (EV-7)."

Four corroborating bytes, all verbatim:

1. `…preview.v2#$.namedCorpusClasses[0].exactByteIntent`: "The pack is a future selected limb, not a
   present shipped component and not a pack identity invented here."
2. `…preview.v2#$.retainedEvidence[0].passProperty`: "**Requiring a present shipped pack or
   inventing pack identity fails.**" — the pack's absence is not merely tolerated; *requiring its
   presence is itself a failure of EV-1*.
3. `…preview.v2#$.failsIf[17]`: "the DR-131 pack is invented **or required as a present shipped
   component**". Same two-sided bite.
4. `…preview.v2#$.retainedEvidence[6].passProperty`: "EV-1 may name the future pack as a selected
   limb; it may not execute G24."

The claim string in file 08 line 366 that prompted the question — "future DR-131 pack" — is
therefore load-bearing in the *advertisement text* only. What a fixture must contain is a release
advertisement whose advertised default useful-install selection **names** "signed distribution core
+ semantic host + one selected TypeScript closure + future DR-131 pack". Naming a future limb needs
no pack bytes. `$.passProperty` (top-level) closes the point: "The future DR-131 pack is not
invented here." And `$.dr131Split`: "DR-131 owns preview-analyze pack identity and remains OPEN. The
future pack is a selected limb of EV-1, not a present shipped identity invented here."

**Not a dependency. The DR-131 pack being unauthored does not block OBL-G30-FX-AUTHORING.**

The same paragraph disposes of the second half of the file-08 line-366 claim, "the rustc_driver
sidecar is deferred not abandoned": `$.namedCorpusClasses[2].exactByteIntent` requires only
"digest-pinned preview product-statement records that abandon or delete the rustc_driver sidecar as
a future supported role", whose fate is *fails this class*. No sidecar implementation, no
rustc_driver execution: "This class does not execute rustc_driver and does not mint a slice-1 Rust
role."

### 4.2 Sequencing dependency — DR-117 Class A opening precedes G30 fixture authoring

COORD `docs/coop/COORDINATOR-DECISIONS.md`
sha256 `1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, heading
`## D-293 — User decisions: adopt the agreed recommendations on DECISIONS-NEEDED A–F` (line 16169),
Decision 5 at line 16228, verbatim:

> "5. **B1 (DR-131), B2 (DR-133), B3 (DR-117).** The agreed
>    programmes are authorized in the agreed order — for DR-117:
>    `preview-product-boundary-successor.v9` re-citing the twelve
>    current joins and stating its relationship to
>    `product-boundary-successor-contract.v8` (D-116); a fresh
>    application-grade dual review bound to v9's final digest; then
>    the owner-controlled opening entry; then G29/G30 fixture
>    authoring; then a separate SATISFIED-GRADE + MF-6 cycle. The
>    D-056 Class A openings themselves are separate owner-controlled
>    entries; this entry opens none of them."

Same programme at `DECISIONS-RECOMMENDED.md`
sha256 `42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`, heading
`## B3 — DR-117 Class A opening (preview-product-boundary-successor.v8)` (line 164),
`### Recommendation (Claude, round 2)` (line 171), line 174, verbatim:

> "**All four amendments adopted. Agreed recommendation:** Option C with **both C1 and C2 on the
> current record, C1 first**: author `preview-product-boundary-successor.v9` re-citing the twelve
> current joins and stating its relationship to `product-boundary-successor-contract.v8` (D-116);
> then a fresh application-grade dual review bound to v9's final digest; then the owner-controlled
> opening entry (which lifts D-137's express reservation with the owner's word and restates the
> v9/contract.v8 relationship); then G29/G30 fixture authoring (D1); then a separate DR-117
> SATISFIED-GRADE + MF-6 cycle. The shared gate-2 entry carries a distinct DR-117 finding."

Measured state of that chain at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`:

- `docs/coop/artifacts/preview-product-boundary-successor.v9.json` **does not exist** (zero matches
  on the glob). The lineage head on disk is
  `docs/coop/artifacts/preview-product-boundary-successor.v8.json`
  sha256 `f2e788e51c347e1033073f0718e701d164affe51e7f667da9bcd49a08837144c`,
  `$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`,
  `$.binds` = `"NOTHING"`.

  > **Correction, remeasured 2026-08-28 13:14 (same HEAD `8bc9963`).** The bullet above was true
  > when measured. It is now stale: a concurrent session authored
  > `docs/coop/artifacts/preview-product-boundary-successor.v9.json` at 12:58 on 2026-08-28,
  > sha256 `e0221a1c095f688dcd5b127bce9f712543165599c71dc8415b94fb7bfdea4dd5`, untracked, with
  > `$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`,
  > `$.binds` = `"NOTHING"`. **Both independent reviews returned REJECT**
  > (`preview-product-boundary-successor.v9.review-independent.claude2.json` and
  > `.review-independent.codex.json`), and there is no
  > `^## D-.*preview-product-boundary-successor.v9` heading in COORD, so v9 is neither accepted nor
  > recorded. The dependency and the standing below are unchanged, and on these bytes are
  > strengthened rather than weakened: the first step of D-293 Decision 5's DR-117 order has been
  > attempted and has not yet passed review.
- The related contract the v9 must state its relationship to,
  `docs/coop/artifacts/product-boundary-successor-contract.v8.json`, exists at
  sha256 `52c70f7715fb869bae70bc588043dc5b4d731b73408d2d451e868b8de963f362`.
- The owner-controlled opening entry is unrecorded. D-293's own closing words, COORD line 16300,
  verbatim: "It does not open D-056 Class A."
- `harness.DR-G30…preview.v2.json#$.dr117Split` records the reservation still standing: "Class A
  remains reserved on v5 (D-137)."

This is an **ordering** dependency, not a byte dependency. Nothing in the seven G30 cells needs a v9
byte to exist; the classes are already defined at
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[9]`, `[10]`, `[12]`.

### 4.3 Prohibition-only, not dependency: section 7.1 and D9

- **Section 7.1.** Prohibited by `$.doesNot[24]` / `$.failsIf[27]`. The dependency sentence, from
  `IMPLEMENTATION-FREEZE.md` line 1711, verbatim: "Every row above must be closed by a binding
  artifact before signature. None may be closed by this record, by the blueprint, by a checker, or
  by an implementer." Measured effect on this corpus: each of the seven exactByteIntents asks only
  for **"digest-pinned"** *advertisement*, *role-list* or *product-statement* records — text
  artifacts about what a release advertises. None requires producing a RunId, EvidenceDigest,
  Finding fingerprint, FactViewId, cache key, `executionPlanCommitment` or `subjectScopeCommitment`
  value. "Digest-pinned" is satisfied by a SHA-256 over the payload bytes, the recorded practice at
  `g23-fixture-corpus.v4.json#$.authoredCatalog.members[i].payloadSha256` and
  `g21-fixture-corpus.v1.json#$.authoredCatalog.members[i].sha256`. Live prohibition, **not** an
  unmet byte dependency.
- **A D9 code.** Prohibited by `$.doesNot[23]` and `g30-leftover-join.v4#$.doesNot[9]`. The closed
  class list exists (`d9-exit-contract.v1.14.json`
  sha256 `8dd3303855f49bfdbb2751ee65f54a906405f0654159ebe815472f73cdf7da31`, pinned live at file 08
  line 40, `$.codeVocabulary.closed` = `true`). No G30 retained-evidence member names a D9 code, an
  exit number or a HostTermination. Not blocking.
- **A PlanIntent schema.** Prohibited by `$.doesNot[25]` / `$.failsIf[28]`, and repeated inside
  `$.namedCorpusClasses[1].exactByteIntent` ("This specification does not invent a PlanIntent
  schema, a D9 code, or a section 7.1 recipe."). This one is sharper than it looks: EE-7b's
  `sourceInputVerbatim` is "A preview PlanIntent **or release** that admits a second language role."
  The disjunction lets a witness be a *release* record rather than a PlanIntent, which avoids the
  schema question entirely. See OQ-G30-1.

### 4.4 Not dependencies

No reserved list, reserved number, reserved axis, undecided threshold, or other gate's corpus is
required by the seven G30 cells. Specifically:

- **DR-101 inventory** is not required: `$.retainedEvidence[4].exactByteIntent` — "Digest-pinned G30
  records do not include a signed-distribution-core inventory census as this gate's analysis-product
  evidence. DR-101 leftover remains on DR-101 / D-160."
- **G13 / DR-G13** is not required and is reserved: `$.g13Split` — "DR-G13 remains reserved, not
  named, not required-now, blocked on DR-118, IDENTIFIER-RESERVED-ZERO-C4-PROGRESS." A G30 corpus
  neither needs nor may produce G13 bytes.
- **G24 pack-identity admission** is not required: `$.retainedEvidence[6].exactByteIntent` —
  "Digest-pinned G30 records do not execute G24 pack-identity admission (DR-131 NT-1, NT-2)."
- **The platform token list** is not a dependency here (unlike G29): `$.platforms` is on this gate's
  own occupancy.
- **The INPUT layer** is already measured closed: `g30-leftover-join.v4#$.summary.specifiedNotLeftover`
  contains `"OBL-G30-INPUT-CORPUS"`, and `$.doesNotCloseLeftoverAlone` — "OBL-G30-INPUT-CORPUS
  initial-state naming is measured closed."

**Standing: BLOCKED-ON-DR-117-CLASS-A-OPENING (sequencing)**

## 5. Recommended artifact shape

**Closest precedent: `docs/coop/artifacts/g21-fixture-corpus.v1.json`**
sha256 `861bb4e7d26a80158cc1cc3a0518c5e8e95311bee4d8c8ce63acd1e60d6c906d` (D-241) — the
**first-authoring** shape, payloads under `docs/coop/artifacts/fixtures/g21.v1/` with **no platform
subdirectory**, carrying a `failsIf` roster next to `doesNot`. The per-platform successor
`g21-fixture-corpus.v8.json` sha256 `e8149a865e49bdcda9eda923e9918f332a83078f43ab6a3af9a10d6d31ef6359`
(D-247) and the four-platform `g23-fixture-corpus.v4.json`
sha256 `b3fce9f5bab6764919f5dc43c28a43f3d9c3b6be310e45c2c1bd08a617c755c5` (D-239) are the copy-version
shapes.

G30 is a **first authoring**: no G30 fixture corpus of any version exists in
`docs/coop/artifacts/`.

Top-level fields carried by `g21-fixture-corpus.v1.json` (verified key list):

`artifact, version, date, documentClass, registerRow, registerRowNote, namedGate, status,
reviewStatus, sealRecommendation, binds, authorityClaim, purpose, basedOn, parentReview, file08Pin,
head, requiredNowUnchanged, file08StatusToken, cborConstruction, pinnedHostContext, whatIsAuthored,
whatIsNotAuthored, authoredCatalog, leftoverDesignClosedIfAcceptedAndRecorded,
leftoverDesignRemainingOnG21, remainderAfterThisCorpus, summary, doesNot, failsIf, proposedLaterWork,
recordedInputs, remeasurementClause`

`$.authoredCatalog` keys: `standing, doesNotMutate, executionRemains, members`.
`$.authoredCatalog.members[i]` keys: `id, class, inputCorpusId, path, sha256, mutation, expected`.

The four-platform variant `g23-fixture-corpus.v4.json` additionally carries
`platformsQuotedFromOccupancyV2` and `windowsStandingQuotedFromOccupancyV2` at top level, and its
`$.authoredCatalog` keys are `standing, doesNotMutate, executionRemains, payloadCount (4),
platformCount (4), copyCount (16), members`, with `$.authoredCatalog.members[i]` keys
`id, class, inputCorpusId, payloadSha256, snapshotMembership, mutation, expected, platformCopies`.

Unlike G29, a G30 successor **could** fill `platformsQuotedFromOccupancyV2`-style fields from its own
gate (`harness.DR-G30…preview.v2.json#$.platforms` and `#$.windowsStanding`). The recommendation is
still to keep the first authoring platform-free, because `$.failsIf[30]` bites on "a four-platform
matrix is invented as a live-cell member" and `$.platformAxis` says platform "Not a live-cell
member" — a first authoring that carries no platform structure cannot fire that finding.

**Proposed artifact name:** `docs/coop/artifacts/g30-fixture-corpus.v1.json`
**Proposed fixture directory:** `docs/coop/artifacts/fixtures/g30.v1/<id>.json`, mode 0444, no
platform subdirectory (first-authoring shape).
**Per-platform successor, if a later cycle wants one:**
`docs/coop/artifacts/g30-fixture-corpus.v2.json` with payloads at
`docs/coop/artifacts/fixtures/g30.v2/<platform-with-slash-replaced-by-dash>/<id>.json`, quoting
`$.platforms` and `$.windowsStanding` from this gate's own occupancy v2 and stating that platform is
a copy axis, not a live-cell member.

**G30-specific fields the successor should carry beyond the v1 skeleton**, each traceable to a
governing byte rather than invented:

- `liveCellMemberCoverage` — a member-by-member map over the seven `$.liveCellMemberSplit.
  membersInOrder` strings, because `$.failsIf[2]` bites on any omitted retained-evidence member and
  `$.failsIf[3]` bites on collapsed punctuation.
- `namedCorpusClassCoverage` — the three class ids, because `$.failsIf[4]` bites if "useful-install
  advertisement, role-list, or product-statement is omitted from exact-byte intent".
- `ee7aLimbSplit` — limb (1) → EV-1, limb (2) → EV-2, because
  `$.namedCorpusClasses[0].exactByteIntent` states that mapping and `$.failsIf[3]` bites on
  collapse.
- `packIsFutureSelectedLimb` — an explicit statement that the advertisement text names the future
  DR-131 pack as a selected limb and that no pack byte is authored, because `$.failsIf[17]` is
  two-sided (inventing the pack *and* requiring it as present both fail).
- `claimNotThatGateNotSubstituted` — because `$.failsIf[18]` bites if claim "Not DR-131 pack
  identity" is substituted for evidence "not G24 pack-identity admission".
- `platformAxisNotALiveCellMember` — because `$.failsIf[30]` exists.
- `doesNotOwnEe` — restating `["EE-1","EE-2","EE-3a","EE-3b","EE-4","EE-5a","EE-5b","EE-6a","EE-6b",
  "EE-7c","EE-7e"]`, because `$.failsIf[6]`, `[21]`, `[22]`, `[23]`, `[48]` all bite on a witness
  classified under a non-G30 EE number.
- `leftoverDesignRemainingOnG30` — must remain non-empty; see the over-claim risk in §6.

## 6. Effort and risk

**Number of cases:** 7 payload cells at first authoring (4 named initial states + 3 not-that-gate
records). Zero platform copies in v1. A per-platform version is 28 copies.

**(a) Witness-byte selections inside recorded schemas — permitted under the grant.**

1. The concrete release-advertisement text for limb (1) — a release that omits the selected
   TypeScript closure from the default useful install — within
   `preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[9].input`.
2. The concrete release-advertisement text for limb (2) — a release that advertises core-only as the
   analysis product — same source.
3. The concrete role-list record for a second language role, within `classes[10].input` ("A preview
   PlanIntent or release that admits a second language role."), choosing the *release* form to stay
   clear of the PlanIntent-schema prohibition (see OQ-G30-1).
4. The concrete product-statement text that abandons or deletes the rustc_driver sidecar, within
   `classes[12].input`.
5. The three not-that-gate records, whose content is fixed by their own exactByteIntent strings
   (EV-5, EV-6, EV-7 in §1.3).
6. The serialization of each payload and its `sha256` over those bytes.
7. Fixture ids following the already-recorded `g30-input-corpus.v1.json#$.initialStates[i].id`
   names (`G30.ee7a.omit-typescript-closure`, `G30.ee7a.core-only-as-analysis-product`,
   `G30.ee7b.second-language-role`, `G30.ee7d.abandon-sidecar`) rather than minting a scheme —
   `$.doesNot[8]` forbids inventing "FX/AT/NT/G30-* fixture identifiers".
8. `mutation` and `expected` values drawn from the recorded passProperties and the recorded fates
   ("Fails this class." / "Refused. Role list remains TypeScript-only.").

**(b) Choices that would create new semantics — record as a named open decision, do not choose.**

1. **Authoring, naming, or versioning a DR-131 pack identity**, or writing a fixture whose pass
   requires a present shipped pack. Violates `…preview.v2#$.doesNot[16]`, `$.failsIf[17]`,
   `$.retainedEvidence[0].passProperty` ("Requiring a present shipped pack or inventing pack
   identity fails."), and the D-293 constraint "no … new D9/HostTermination/pack semantics …
   is authorised". Record as a named open decision, do not choose.
2. **Minting Rust-as-core, a second slice-1 language role, or a future-role catalog.** Violates
   `$.doesNot[10]`, `$.doesNot[11]`, `$.failsIf[12]`, and `$.languageRoleAxis.doesNotMint` =
   `["Rust-as-core","any second slice-1 language role","a future-role catalog","a present shipped
   DR-131 pack identity"]`. Record as a named open decision, do not choose.
3. **Inventing a PlanIntent schema** to express the second-role witness. Violates `$.doesNot[25]`,
   `$.failsIf[28]`. Record as a named open decision, do not choose. (The `or release` disjunction in
   `classes[10].input` is the recorded escape; see OQ-G30-1.)
4. **Minting a D9 code, exit number, or HostTermination** for a refusal fate. Violates
   `$.doesNot[23]`, `$.failsIf[26]`, `g30-leftover-join.v4#$.doesNot[9]`, and
   `d9-exit-contract.v1.14.json#$.codeVocabulary.rule`. Record as a named open decision, do not
   choose.
5. **Supplying any parked identity/digest/commitment/ref derivation.** Violates `$.doesNot[24]`,
   `$.failsIf[27]`, and `IMPLEMENTATION-FREEZE.md` §7.1 line 1711. Record as a named open decision,
   do not choose.
6. **Collapsing any of the seven live-cell members**, in either direction listed in
   `$.liveCellMemberSplit.notCollapsed`, or adding any of the seventeen strings in
   `$.liveCellMemberSplit.notInventedExtras` as a member. Violates `$.failsIf[3]`. Record as a named
   open decision, do not choose.
7. **Classifying a witness under EE-7c, EE-7e, EE-6b, or any EE-1..EE-6a class.** Violates
   `$.failsIf[6]`, `[21]`, `[22]`, `[23]`, `[48]` and `$.doesNotOwnEe`. Record as a named open
   decision, do not choose.
8. **Authoring a four-platform matrix as a live-cell member**, or treating Windows as a G30 preview
   platform. Violates `$.failsIf[29]`, `$.failsIf[30]`, and `$.platformAxis`. Quoting `$.platforms`
   as a *copy* axis in a later version is permitted; asserting it as a live-cell axis is not. Record
   as a named open decision, do not choose.
9. **Substituting the claim-cell string "Not DR-131 pack identity" for the evidence-cell member
   "not G24 pack-identity admission".** Violates `$.failsIf[18]` and `$.claimNotThatGate.note`.
   Record as a named open decision, do not choose.
10. **Claiming `leftoverDesignClosedIfAcceptedAndRecorded: ["OBL-G30-FX-AUTHORING"]`.** Seven cells
    at first authoring do not exhaust the corpus while EV-5..EV-7 have no named initial state and
    the platform copy version is unwritten. Record as a named open decision, do not choose.

**Risk.** The reviewer-attack pattern on the two recorded corpus lineages was (a) inventing a
vocabulary or encoding the record does not carry, (b) collapsing two classes the occupancy keeps
separate, (c) classifying a witness under a closed identifier it does not belong to, (d)
over-claiming closure. G30's exposure is unusually concentrated on (b): this occupancy is the one
that spells out its non-collapse rule in prose (`$.liveCellMemberSplit.notCollapsed`, six named
collapses) and lists seventeen `notInventedExtras`, which is exactly the material a reviewer will
check line by line. The `g23-fixture-corpus.v2.review-independent.claude2.json` REJECT — "M1 — the
EV-2 narrows fixture also converts unknown to covered, collapsing EV-2 into EV-3 against the
governing occupancy's explicit non-collapse requirement" — is the same defect one gate over, and
G30's two EE-7a limbs (EV-1 vs EV-2) sit in precisely that position: a single advertisement record
that both omits the TypeScript closure *and* advertises core-only as the analysis product would
collapse EV-1 into EV-2 and reproduce M1 verbatim.

On (a), the trap is the digest construction: `g23-fixture-corpus.v1.review-independent.claude2.json`
was REJECTed on "M2 — coverageCommitment is a SHA-256 over compact JSON, not deterministic-CBOR, and
its construction is undeclared", so a G30 successor must declare how it computes each
`sha256`/`payloadSha256`, the way `g21-fixture-corpus.v1.json#$.cborConstruction` does. A second (a)
trap is inventing an advertisement *vocabulary*: the record gives the advertised selection as one
prose string ("signed distribution core + semantic host + one selected TypeScript closure + future
DR-131 pack") and nowhere gives it a field schema; a fixture that turns it into a structured object
with invented key names would draw the M3-class finding ("hostRequestedDomainBaseline invents a
Coverage-domain encoding vocabulary"). See OQ-G30-2.

On (c), the closed identifier is the EE set: `$.doesNotOwnEe` lists eleven classes that exist at
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[]` and none of which is a
G30 class — putting a witness under EE-7c (self-contained closure) is the exact `G21FXV3-M1` shape
("The non-object-top-level payload is an RF-2 case but is not authorized as a member of the closed
CC-5 corpus"). On (d), `$.failsIf[37]` already names the over-claim: "leftover-design of
OBL-G30-FX-AUTHORING is claimed closed by this file existing".

One further structural risk, specific to this gate: three of the seven cells (EV-5, EV-6, EV-7) are
**not-that-gate records**, whose exactByteIntent opens "Once authored: this member remains a
not-that-gate distinction, not a fourth corpus class." Authoring a *payload* for a negative
distinction is not a shape either precedent corpus demonstrates — `g23-fixture-corpus.v4` and
`g21-fixture-corpus.v1` members are all positive payloads with a `mutation` and an `expected`. A
successor must decide whether these three are payload files or catalog assertions, and a reviewer
will ask. That is OQ-G30-3.

## 7. Open questions

**OQ-G30-1:** In what form is the EE-7b second-role witness authored?
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[10].input` and
`harness.DR-G30…preview.v2.json#$.namedCorpusClasses[1].sourceInputVerbatim` both read "A preview
PlanIntent **or release** that admits a second language role", and
`g30-input-corpus.v1.json#$.initialStates[2].stateNamed` repeats the disjunction. But
`…preview.v2#$.doesNot[25]` and `$.failsIf[28]` forbid inventing a PlanIntent schema, and no
PlanIntent schema is pinned anywhere in the G30 lineage. The record therefore permits a witness form
it also cannot supply the schema for. Whether the corpus must take the *release* branch (which needs
no schema) or may take the PlanIntent branch (which would need one that does not exist) is not
settled.

**OQ-G30-2:** What is the byte form of a "release-advertisement record"?
Every EE-7a exactByteIntent asks for "digest-pinned release-advertisement records" whose "advertised
default useful-install selection is signed distribution core + semantic host + one selected
TypeScript closure + future DR-131 pack". The record gives that selection only as a prose string
(identically in `…preview.v2#$.claimVerbatim`, `$.ee7a.invariantVerbatim`,
`$.namedCorpusClasses[0].sourceInvariantVerbatim` and
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[9].invariant`). No schema,
key set, or serialization is pinned for an advertisement record anywhere in the G30 lineage. A
corpus must choose one, and any structured choice risks the M3-class "invents a … vocabulary"
finding. Whether the intended form is the prose string verbatim or a structured record is unsettled.

**OQ-G30-3:** Are EV-5, EV-6 and EV-7 payload cells or catalog assertions?
Each is `"suppliedBy": "not a corpus class. Live-cell not-that-gate distinction. Not authored."`, yet
each carries an `exactByteIntent` beginning "Once authored:" and
`…preview.v2#$.failsIf[2]` bites if "a live G30 retained-evidence member is omitted or has no
exactByteIntent / passProperty". `g30-input-corpus.v1.json#$.whatIsAuthored` names initial states
for only four of the seven. So the record simultaneously requires all seven members to be covered
and declines to name initial states for three of them. Whether the three not-that-gate members
become fixture payloads, catalog-level assertions, or nothing at all is not settled.

**OQ-G30-4:** Does the INPUT corpus's citation of occupancy v1 need refreshing before it can be
consumed? `g30-input-corpus.v1.json#$.initialStates[i].source` cites "G30 v1 exactByteIntent"
throughout, and `$.head` is `5d5d77819ae3019d9e6e02f1e66de3d93c060402` with
`$.requiredNowUnchanged` = `26`. The current occupancy is v2 (D-230) at required-now 28, and
`…preview.v2#$.failsIf[47]` requires the "current INPUT basis" to be "g30-input-corpus.v1 as
measured by leftover-join.v3 (D-205)". The four state names are byte-identical between v1 and v2's
`namedCorpusClasses` source strings, so nothing is contradicted — but whether a fixture corpus cites
the input corpus as-is or must first record a v2-based remasurement of it is not stated.

**OQ-G30-5:** Is D-159's assignment of EE-7c quotable inside the G30 lineage?
`…preview.v2#$.ee7cSplit`, `$.ee7eSplit`, `$.ee6bSplit` and `$.dr117Split` all attribute the
EE-6b / EE-7c / EE-7e assignments to "D-159" without reproducing any sentence from that heading, and
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[11]` assigns EE-7c to
"DR-119 / DR-G14" without citing D-159 at all. The assignment is stated in three places and
contradicted in none, so this is a citation gap rather than a substantive doubt: EE-7c exists, its
`leftoverDesign` is already false, and it rides G14. What a G30 corpus cannot do is quote D-159's
own words for that, because the G30 lineage does not carry them.

**OQ-G30-6:** Is `preview-product-boundary-successor.v5` still the governing class definition after
v9 lands? `…preview.v2#$.productLawOwner` pins v5. The lineage head on disk is v8
(sha256 `f2e788e51c347e1033073f0718e701d164affe51e7f667da9bcd49a08837144c`), and D-293 Decision 5
orders a **v9** before the opening entry and before G29/G30 fixture authoring. Whether a G30 corpus
authored after v9 lands must re-cite v5 (the occupancy's pin) or v9 (the then-current contract) —
and whether v9's re-citation of "the twelve current joins" would move the EE-7a/7b/7d class
definitions — is not stated anywhere in the G30 record.
