# G16 — OBL-G16-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

- Path: `docs/coop/artifacts/g16-leftover-join.v5.json`
- sha256: `7ce75ea514322a6e17546ec8e9b91c4fb2f66128271d6c6d757e3f627e05ab78`
- `$.artifact` = `"g16-leftover-join.v5"`
- `$.date` = `"2026-08-24"`
- `$.registerRow` = `"DR-G16"`
- `$.status` = `"CANDIDATE-NOT-APPLIED"`
- `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`
- `$.binds` = `"NOTHING"`
- `$.head` = `"af870f41c6af4f4fa5c41e3f0fd302231e727233"` (the join's own recorded HEAD, not this record's)
- `$.requiredNowUnchanged` = `28`
- `$.file08StatusToken` = `"OPEN"`
- `$.sealRecommendation` = `"DO-NOT-SEAL"`, `$.documentClass` = `"DESIGN-CONTRACT-CANDIDATE"`

COORD heading that recorded it (`docs/coop/COORDINATOR-DECISIONS.md`
sha256 `1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 14502), verbatim:

> ## D-278 — Record g16 leftover-join.v5 as G16 leftover remasurement

### 1.2 The obligation object (verbatim)

`docs/coop/artifacts/g16-leftover-join.v5.json` `$.obligations[3]`:

```json
{
  "id": "OBL-G16-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "change-impact corpus × component/language/platform matrix",
    "forced dependency/ownership mutations",
    "aggregate release selection"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "Occupancy v5 namedCorpusNotAuthored carries three live harness-cell corpus classes. Fixtures are unauthored. g16-named-corpus-catalog.v1 named those classes; that naming is OBL-G16-NAMED-CATALOG, leftoverDesign false. D-056 Decision clause 5: authoring fixtures remains design work. This join does not invent those fixture bytes, does not apply v16, and does not invent reserved CI encodings."
}
```

(The `×` in the first class name is the multiplication sign U+00D7, stored as `×`.)

Called out explicitly:

- `reason` — quoted in full above. Its operative prohibitions are the closing sentence:
  "This join does not invent those fixture bytes, does not apply v16, and does not invent reserved
  CI encodings."
- `namedCorpusNotAuthored` — the three corpus classes, quoted above; identical strings at the join's
  top level `$.namedCorpusNotAuthored`, at `monorepo-leftover-join.v4.json`
  `$.obligations[3].namedCorpusNotAuthored`, at `harness.DR-G16.ci-isolation-integration.v5.json`
  `$.namedCorpusNotAuthored` and `$.namedExecutesLimbs`, and at
  `g16-named-corpus-catalog.v1.json` `$.namedCorpusNotAuthored`.
- `remainingNotAuthored` / `namedCases` / `namedNotAuthored` — **absent** on this obligation and on
  this join.
- `existingGate` = `"none as authored implementations"`.
- `rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `executionObligationOwnerToday` = `"none"`.

Join-level standing: `$.summary.leftoverDesign` = `["OBL-G16-FX-AUTHORING"]`;
`$.summary.v16Applied` = `false`; `$.summary.rustAsCoreMinted` = `false`;
`$.summary.g13NamedIntoRequiredNow` = `false`;
`$.liveGateOwners` = `{"DR-G16": "Release engineering + component/core/protocol/integration owners (owner cell made concrete 2026-08-13, C4)"}`.

### 1.3 Occupancy file and the fields that name the fixture cells

- Path: `docs/coop/artifacts/harness.DR-G16.ci-isolation-integration.v5.json`
- sha256: `3e3107499ffb576c11b3d4c290470921062066f518cbd80b6a563b446ebc918e`
- `$.artifact` = `"harness.DR-G16.ci-isolation-integration.v5"`
- Confirmed as the occupancy the join names as current: `g16-leftover-join.v5.json`
  `$.basedOn.occupancyV5` and `$.obligations[0].reason` ("occupancy remasurement v5 exists at
  harness.DR-G16.ci-isolation-integration.v5 (D-215; dual ACCEPT 0/0; CANDIDATE-NOT-APPLIED; not
  QUALIFIED)").
- Recorded at COORD line 9557: "## D-215 — Record harness.DR-G16.ci-isolation-integration.v5 as G16
  occupancy remasurement"

**Present**, verbatim:

`$.claimVerbatim`:

> Monorepo changes run all and only required component/core/integration qualification lanes from declared ownership/dependencies

`$.liveHarnessCellVerbatim`:

> named: harness.DR-G16.ci-isolation-integration (D-086; not authored; not QUALIFIED). change-impact corpus × component/language/platform matrix; forced dependency/ownership mutations; aggregate release selection

`$.liveCellVerbatim`:

> selected-lane explanation, per-lane signed closure evidence, skipped-lane proof, shared-core and cross-component protocol/semantic/lock/offline/bundle results

`$.waiverRuleVerbatim`:

> no waiver for an affected omitted lane; platform scope follows component manifest

`$.liveAssuranceStageVerbatim` = `"PROPOSED; not QUALIFIED"`; `$.liveStatusVerbatim` = `"OPEN"`;
`$.sliceStanding` = `"REQUIRED"`.

`$.platforms`:

```json
["macos/arm64", "macos/x86_64", "linux/x86_64", "linux/arm64"]
```

`$.windowsStanding`:

> D-002 defers Windows with explicit disposition. Windows is not a G16 preview-runner platform. Windows is not a skipped-lane. Skipped-lane proof is for lanes that exist in the declared comparison-basis and were not selected. A deferred platform is outside the preview domain.

`$.matrix`:

> change-impact corpus × component/language/platform matrix; forced dependency/ownership mutations; aggregate release selection — TypeScript only × the four D-002 platforms × declared components, with platform scope following each component manifest

`$.discipline`:

> SPECIFICATION OF EXACT-BYTE INTENT. Occupancy remasurement of the already-named identifier. No harness runs here. No fixture bytes authored. monorepo-ci-contract.v16 is not applied.

`$.namedCorpusNotAuthored`:

```json
["change-impact corpus × component/language/platform matrix", "forced dependency/ownership mutations", "aggregate release selection"]
```

`$.namedCorpusNote`:

> Live harness-cell punctuation after the named-identifier sentence, semicolon-separated. These are corpus classes, not authored fixtures and not invented G16-*/IR-*/C-*/SL-*/FX/AT identifiers. A later fixture-authoring cycle may pin bytes. This artifact does not. Citing v16's leftover T2-02 names for the same live-cell obligations is not authoring those fixtures and is not applying v16. leftover-join.v3 measured OBL-G16-NAMED-CATALOG leftoverDesign false. leftover-design of OBL-G16-FX-AUTHORING remains. This occupancy does not author fixture bytes.

`$.passProperty`:

> Once independently pinned TypeScript fixture implementations exist for every named corpus class, every live retained-evidence member, and every D-002 platform in each component's manifest-declared scope, each retained-evidence member's passProperty holds. Until those bytes exist, this harness cannot be executed and cannot be QUALIFIED. File 08 remains PROPOSED; not QUALIFIED. File 08 carries G16 (D-086). The live G16 row is OPEN and not QUALIFIED. Live required-now is 28. Waiver of an affected omitted lane is not a skipped cell. Platform scope follows the component manifest. monorepo-ci-contract.v16 remains unapplied. This file existing is not execution and is not a three-limb recording.

`$.languageRoleAxis`:

```json
{
  "axisName": "language",
  "slice1Domain": ["TypeScript"],
  "soleSlice1Role": "TypeScript",
  "doesNotMint": ["Rust-as-core", "any second slice-1 language role", "a future-role catalog"],
  "futureRoles": "D-002 activates TypeScript alone. File 02 isolation (an unrelated change does not force every language closure to build) is scored on the TypeScript slice-1 role. A later role's isolated-lane evidence is a later G16 qualification of that role. This specification does not invent those roles and does not name G13 into required-now."
}
```

`$.componentAxis`:

```json
{
  "axisName": "component",
  "source": "Declared component ownership and dependency metadata (file 02). Comparison-basis identities are not invented here.",
  "catalogAuthoredHere": false,
  "note": "This specification does not author an ownership record, a component catalog, or DR-103 dependency-edge bytes. Once fixtures exist, those identities are digest-pinned inputs. Fixtures are unauthored."
}
```

`$.selectorLawFromFile02`:

```json
{
  "selects": "Declared component ownership and dependency metadata select isolated language/component CI lanes.",
  "isolation": "An unrelated change does not force every language closure to build.",
  "failSafe": "The selector must fail safe when ownership or dependency impact is ambiguous. Fail-safe is over-selection or typed refusal, never under-selection.",
  "waiver": "no waiver for an affected omitted lane; platform scope follows component manifest",
  "notAuthority": ["CI provider", "YAML", "repository path filters", "caches", "commands", "implementation tooling"],
  "doesNotApplyV16Algorithm": true
}
```

**The `exactByteIntent` strings, in full.** Three sit on `$.namedCorpusClasses[*]`, four on
`$.retainedEvidence[*]`.

`$.namedCorpusClasses[0]` — `id` `"change-impact-matrix"`, `memberVerbatim` "change-impact corpus ×
component/language/platform matrix". `exactByteIntent`:

> Once authored: one cell per declared comparison-basis component identity, per slice-1 language-role value (TypeScript only), and per that identity's manifest-declared D-002 platforms (macos/arm64, macos/x86_64, linux/x86_64, linux/arm64). Each cell retains a digest-pinned change set of add/delete/rename/byte-change units, the ownership/dependency inputs that selected or skipped lanes, and the four live-cell members for that component × role × platform. An unrelated change does not force every language closure to build (file 02). A skipped platform that the component manifest does not declare is not a waiver of an affected lane; platform scope follows the component manifest. Windows is not a cell. A second language role is not a cell. Path filters, CI vendor config, and cache keys are not the authority that decides impact (file 02 reserved). Fixtures are unauthored.

`passProperty`:

> Every TypeScript × declared-component × manifest-declared-D-002-platform cell is present. A skipped affected lane is not a waiver. Windows is not a cell. A second language role is not a cell. CI vendor/YAML/path-filter authority fails this class.

`$.namedCorpusClasses[1]` — `id` `"forced-mutations"`, `memberVerbatim` "forced dependency/ownership
mutations". `exactByteIntent`:

> Once authored: digest-pinned hostile mutations of declared ownership and dependency metadata, including omitted dependency, omitted owner, missing component declaration, owner outside the comparison-basis universe, and incomplete or invalid universe construction (file 02 omitted dependency/owner detection). Each record retains the presented mutation, the selector's fail-safe outcome (over-selection of every lane that the mutation could affect, or a typed refusal), and proof that the mutation did not silently omit an affected lane. Fail-safe is over-selection or typed refusal, never under-selection (file 02; live waiver cell). This class is CI ownership/dependency integrity. It is not G07 hostile archive/path/loader/TOCTOU, not G10 provider-frame goldens, and not G15 adapter hostile-path emission. Fixtures are unauthored.

`passProperty`:

> A forced ownership or dependency mutation either selects every affected lane or refuses. Silent omit of an affected lane fails. There is no waiver for an affected omitted lane. G07, G10, and G15 are not scored here.

`$.namedCorpusClasses[2]` — `id` `"aggregate-release"`, `memberVerbatim` "aggregate release
selection". `exactByteIntent`:

> Once authored: digest-pinned records that a release selects independently qualified compatible bytes through signed metadata and per-surface matrices (file 02). Component release identity and core release identity are independently versioned. Independent releases do not require separate repositories and do not create lockstep versions. A same-version train that cannot emit a component-new/core-unchanged selection and a core-new/component-unchanged selection fails. A bundle may qualify a selection; it cannot be the only release route. DR-117 EE-7e rides this class: the selected TypeScript closure is independently releasable and not lockstepped to the core version train. Window evaluation rides DR-111 and is not minted here. Hostile dual-channel anti-lockstep goldens ride DR-127 / G21 and are not this class. v16 independently spells discriminating cases as IR-1..IR-4 and lockstep discrimination as G16-12; those identifiers are cited-not-applied and are not authored here. Fixtures are unauthored.

`passProperty`:

> Independent component and core identities can release without a required same-version train. Lockstep of the selected TypeScript closure to the core version train fails. Bundle-only release fails. Separate repositories are not required. DR-111 windows and DR-127 dual-channel goldens are not scored here.

`$.retainedEvidence[0]` — EV-1, `memberVerbatim` "selected-lane explanation". `exactByteIntent`:

> Once authored: per change-impact-matrix cell and per forced-mutations cell, a digest-pinned per-selected-lane explanation derived only from declared ownership, reverse-dependency, shared-surface consumers, and the file-02 fail-safe-on-ambiguous rule. Path filters, CI vendor YAML, caches, and commands cannot be the explanation authority (file 02 reserved). Each explanation names the change units, the owner or dependency edge that selected the lane, and the lane identity. Ambiguous ownership or dependency impact produces either over-selection with an explanation for every selected lane or a typed refusal; it does not produce a silent empty selection. This member does not score G15 adapter emission or G10 provider-frame opacity. Fixtures are unauthored.

`passProperty`:

> Every selected lane has an explanation bound to declared ownership/dependencies. An unexplained selected lane fails. A path-filter-only explanation fails. Under-selection of an affected lane fails. Pass TypeScript × every manifest-declared D-002 platform of every declared comparison-basis component. Waiver of an affected omitted lane is not a skipped explanation.

`suppliedBy`: "change-impact corpus × component/language/platform matrix corpus class and forced
dependency/ownership mutations corpus class. Not authored."

`$.retainedEvidence[1]` — EV-2, "per-lane signed closure evidence". `exactByteIntent`:

> Once authored: per selected component lane and per that lane's manifest-declared D-002 platforms, digest-pinned signed closure bytes/declarations joined to the EV-1 explanation of that lane. File 02 also says the lane runs language-native capability/parity, behavior, performance, runtime-UX, packaging-adapter, and conformance gates. Those rides remain at their owning gates: capability/parity/behavior/performance ride DR-118 leftover / reserved G13 (not named into required-now); runtime-UX rides G14; packaging-adapter/conformance rides G15. This member records that the selected CI lane emitted signed closure evidence. It does not execute AT-1..AT-8, does not author the DR-103 fixture corpus, and does not score G14 Node/runtime substitution. Shared-core and cross-component results are EV-4, not this member. Fixtures are unauthored.

`passProperty`:

> Every selected preview-required component lane retains signed closure evidence on each manifest-declared D-002 platform. A selected lane without signed closure fails. Platform scope follows the component manifest. G15 adapter identity and G14 runtime-UX are not scored. G13 is not named into required-now. Pass TypeScript only.

`suppliedBy`: "change-impact corpus × component/language/platform matrix corpus class. Not authored."

`$.retainedEvidence[2]` — EV-3, "skipped-lane proof". `exactByteIntent`:

> Once authored: per change-impact-matrix cell and per forced-mutations cell, a digest-pinned proof for every unselected lane in the closed comparison-basis lane universe. Proof is membership in the unselected set plus the explanation that no ownership, reverse-dependency, or shared-surface consumer rule selected it, and that fail-safe-on-ambiguous did not require it. The word trigger is not a rule (file 02 has no trigger table). A hostile omitted-dependency or omitted-owner mutation cannot convert an affected lane into a skipped-lane proof; that case is EV-1 fail-safe or typed refusal (forced-mutations class). Windows deferred is not a skipped-lane proof. This member does not score G07 skipped extraction or G12 skipped doctor probes. Fixtures are unauthored.

`passProperty`:

> Every unselected comparison-basis lane has a skipped-lane proof. An affected omitted lane presented as skipped fails. There is no waiver for an affected omitted lane. Windows is not proved skipped. Pass TypeScript × the four D-002 platforms' declared component scopes.

`$.retainedEvidence[3]` — EV-4, "shared-core and cross-component protocol/semantic/lock/offline/bundle
results". `exactByteIntent`:

> Once authored: per change-impact-matrix cell, a digest-pinned result or skipped-lane proof for each of the six file02SharedLaneLimbs. Shared-core is required whenever those contracts change or fail-safe selects them. Protocol is the common control protocol and unchanged opaque provider data-plane integration slot; it does not run G10 goldens and does not translate provider frames. Semantic is file 02's semantic-authority and D9/fate integration slot; this specification does not mint a D9 code, exit number, or HostTermination. Lock is deterministic lock/dependency/compatibility resolution; window evaluation rides DR-111. Offline is the named clean-machine authoritative offline-closure slot; D-002 defers DR-106 / G06 and this specification does not execute G06 or delete the slot. Bundle is aggregate convenience/air-gap bundle qualification; it cannot be the only release route (aggregate-release class; DR-127 ride). v16 independently labels these limbs SL-1..SL-6; those identifiers are not applied here. Fixtures are unauthored.

`passProperty`:

> Every file-02 shared-lane limb has either a result or a skipped-lane proof. An omitted shared-core, protocol, semantic, lock, offline, or bundle slot fails. Bundle-only release fails. An invented D9 code fails this specification. G10, G06, G15, and DR-111 window numbers are not scored here. Pass TypeScript × the four D-002 platforms where the comparison-basis requires those slots.

`$.liveCellMemberSplit.membersInOrder`:

```json
["selected-lane explanation", "per-lane signed closure evidence", "skipped-lane proof", "shared-core and cross-component protocol/semantic/lock/offline/bundle results"]
```

`$.liveCellMemberSplit.notCollapsed`:

> selected-lane explanation is not collapsed into skipped-lane proof. per-lane signed closure evidence is not collapsed into selected-lane explanation. skipped-lane proof is not collapsed into selected-lane explanation. shared-core and cross-component protocol/semantic/lock/offline/bundle results is not collapsed into per-lane signed closure evidence and is not split into six live-cell members. DR-121's acceptance-evidence cell (protocol/authority/…) is not substituted for this G16 cell (protocol/semantic/…).

`$.file02SharedLaneLimbs.limbsInOrder` — six `{g16Token, file02Verbatim}` pairs:
`shared-core` / "the shared distribution core/semantic host/pure-core contracts";
`protocol` / "common control protocol and unchanged opaque provider data planes";
`semantic` / "semantic-authority and D9/fate integration";
`lock` / "deterministic lock/dependency/compatibility resolution";
`offline` / "clean-machine authoritative offline closure";
`bundle` / "aggregate convenience/air-gap bundle qualification".

`$.ee7e.invariantVerbatim`:

> The selected TypeScript closure is independently releasable and not lockstepped to the core version train.

with `$.ee7e.rides` = `"aggregate release selection"` and `$.ee7e.notALiveCellMember` = `true`.

`$.liveGateOwner` and `$.liveGateOwnerIfRecorded` both =
`{"DR-G16": "Release engineering + component/core/protocol/integration owners (owner cell made concrete 2026-08-13, C4)"}`;
`$.executionObligationOwnerIfRecorded` = the same owner string.

**Absent** from occupancy v5, of the fields the brief lists: `liveRowVerbatim`, `retainedEvidence[*]`
has no `exactByteIntent`-less member (all four carry both `exactByteIntent` and `passProperty`),
`failsIf` is present (52 members), `doesNot` is present (44 members). There is no `reportUnit` field
and no `closedMap` — G16's cell count is not closed by the occupancy (see §2).

### 1.4 ROW twin join

A ROW twin exists.

- Path: `docs/coop/artifacts/monorepo-leftover-join.v4.json`
- sha256: `03d4478c3ce6ea843f8a4ee3ea1dcc6d8c06bd661f71970fe836ce107b611481`
- `$.artifact` = `"monorepo-leftover-join.v4"`, `$.registerRow` = `"DR-121"`,
  `$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.binds` = `"NOTHING"`,
  `$.requiredNowUnchanged` = `28`, `$.file08StatusToken` = `"OPEN"`,
  `$.head` = `"456a5285fd44a4fed720c39fe3dd82ce0e6ccadb"`
- COORD heading (line 14414): "## D-277 — Record monorepo leftover-join.v4 as DR-121 leftover remasurement"

Its object for the same obligation id, `$.obligations[3]`:

```json
{
  "id": "OBL-G16-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "change-impact corpus × component/language/platform matrix",
    "forced dependency/ownership mutations",
    "aggregate release selection"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "G16 occupancy namedCorpusNotAuthored carries three live harness-cell corpus classes. Fixtures are unauthored. D-056 Decision clause 5: authoring fixtures remains design work, distinct from the G16 harness specification, distinct from the named catalog, and distinct from G16 execution. This join does not invent those fixture bytes and does not invent G16-*/IR-*/C-*/SL-*/FX/AT identifiers. g16-named-corpus-catalog.v1.json named those classes; that naming is OBL-G16-NAMED-CATALOG, leftoverDesign false. g16 leftover-join.v4 (D-262) remains the current G16 GATE leftover-join and also measures OBL-G16-FX-AUTHORING leftoverDesign true. This DR-121 leftover-join does not steal that leftover as a closure and does not close it."
}
```

Cross-custody sentence, verbatim (last sentence of that reason):

> g16 leftover-join.v4 (D-262) remains the current G16 GATE leftover-join and also measures OBL-G16-FX-AUTHORING leftoverDesign true. This DR-121 leftover-join does not steal that leftover as a closure and does not close it.

(The ROW twin names GATE version v4/D-262 because that was current at its own recording; the GATE
lineage has since moved to v5/D-278. The D1 packet records that this staleness does not unseat the
ROW twin: "the ROW twins cite the GATE join version current at *their* recording … COORD precedent
D-276/D-278/D-281 keeps such ROW joins current".)

The reciprocal custody sentence on the GATE side is `g16-leftover-join.v5.json`
`$.obligations[5].reason`:

> monorepo leftover-join.v4 (D-277) still measures OBL-CI-ENCODING-RESERVED leftoverDesign true on DR-121. reservedForBlueprint remains CI provider, YAML, repository path filters, caches, commands, implementation tooling. This join does not steal that leftover, does not invent those encodings, and does not apply v16.

**Closing OBL-G16-FX-AUTHORING therefore needs a successor on both joins.**
`DECISION-PACKETS/D1-fixture-authoring-delegation.md`
sha256 `bc8484cc7159af26a142b97a55b1095049f7ea2ac10c283cd1e2428ba2569ea9`, section 3.2 closing
sentence, verbatim:

> Closing a fixture obligation would require a successor on *both* the GATE join and its ROW twin, since the same id is measured true on both.

The ROW twin's second live obligation is the reserved-encoding one, `$.obligations[5]`:

```json
{
  "id": "OBL-CI-ENCODING-RESERVED",
  "leftoverDesign": true,
  "existingGate": "none",
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding",
  "reason": "v16 reservedForBlueprint is CI provider, YAML, repository path filters, caches, commands, implementation tooling. G16 occupancy does not choose those encodings. Undecided encodings are leftover-design (D-056). This join does not invent them. g16 leftover-join.v4 does not steal OBL-CI-ENCODING-RESERVED. This join does not close it."
}
```

and `$.reservedForBlueprintVerbatim` on the ROW twin is the same six-member list:

```json
["CI provider", "YAML", "repository path filters", "caches", "commands", "implementation tooling"]
```

### 1.5 Governing contract/spec the occupancy pins

Occupancy v5 pins `docs/coop/artifacts/monorepo-ci-contract.v16.json` sha256
`67ca501660a2ba515ce37adc799c5418e4ffd156308189662245e5a5e45a2ddb` at `$.basedOn.monorepoCiV16`
(recording D-124, registerRow DR-121) — **and expressly declines to apply it.** `$.doesNotApplyV16`
= `true`. `$.v16Standing`:

> CANDIDATE-NOT-APPLIED. binds NOTHING. D-124 leftover T2-02 for DR-121. Cited. Not applied. Not this harness's fixture catalog.

`$.basedOn.monorepoCiV16.role`:

> Cited only. D-124 recorded v16 as DR-121 leftover T2-02 successor candidate. status CANDIDATE-NOT-APPLIED, binds NOTHING. Dual ACCEPT 0/0. DR-121 stays OPEN. G16 is not QUALIFIED. This v5 remasures occupancy; it does not apply v16. It does not import v16's G16-1..G16-14 classes, C-* change classes, SL-* lane identifiers, IR-1..IR-4 discriminating cases, selector algorithm, fixtureDomainBasis, or tests[] as this harness's authored fixtures or applied law. Live-cell punctuation used here is derived from the live file 08 DR-G16 row. v16 independently decomposed that same live cell into G16-OBL-1..G16-OBL-7; agreement of punctuation is not application. v16's g16RowQuoted.sourceSha256 is the older whole-document pin 1cdcf9d4071a6c5eaa7cd7d28fac288eaced796c9ee906b6f1db0f7a4b1f95b3; this specification pins the live file 08 digest.

**Consequence for this plan record: the governing spec for the classes is the occupancy itself plus
live file 02, not v16.** Class definitions are `$.namedCorpusClasses[0..2]` and
`$.retainedEvidence[0..3]`, quoted in full in §1.3.

Live file 02: `docs/v2/architecture/02-distribution-and-components.md` sha256
`1811c682cf293e1e0b255be82c62f7ed3c439f0873eb7922bfb0ad965b43f7db`, pinned at
`$.basedOn.file02MonorepoCi` with `sourceHeadings` `["Monorepo CI and independent component
releases"]` and `monorepoCiLines` `"123-152"`. Its six separate-lane bullets are at lines 138–143:

> - the shared distribution core/semantic host/pure-core contracts;
> - common control protocol and unchanged opaque provider data planes;
> - semantic-authority and D9/fate integration;
> - deterministic lock/dependency/compatibility resolution;
> - clean-machine authoritative offline closure; and
> - aggregate convenience/air-gap bundle qualification.

The `basedOn.file02MonorepoCi.role` closes with: "CI provider, YAML, repository path filters, caches,
commands, and implementation tooling remain reserved for the later blueprint. This specification does
not rewrite those bytes."

The naming catalog that quotes the three classes verbatim is
`docs/coop/artifacts/g16-named-corpus-catalog.v1.json` sha256
`bceb4d16daebe3059debdd93913d20a45b999b5c69dd04a73ff3d09a50ce140d`, `$.classes[0..2]`, each with
`id`, `memberVerbatim`, `exactByteIntentVerbatim`, `passPropertyVerbatim`, `fixtureBytes`
(`"NOT-AUTHORED"` on all three), `inventedIdentifiers` (`false` on all three). Its
`$.namedCorpusNoteVerbatim` and `$.v16StandingVerbatim` carry the same non-application language.

The INPUT layer is `docs/coop/artifacts/g16-input-corpus.v2.json` sha256
`9f07459d703f3194bedaa5afae5bce4b9cce9fc4d8e934783da9002e64eec8c6`, `$.initialStates` (12 objects,
all `"fixtureBytes": "NOT-AUTHORED"`), `$.whatIsAuthored`:

> Named initial states drawn from live G16 v2 exactByteIntent: four change-set units plus the unrelated-change case; five forced-mutation kinds; two independent-release selections. No fixture bytes. No reserved CI encodings.

Live file 08 row (`docs/v2/architecture/08-decision-and-readiness-register.md` sha256
`e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e`, line 352) owner cell:
`Release engineering + component/core/protocol/integration owners (owner cell made concrete
2026-08-13, C4)`; status cell: `OPEN`.

## 2. Coverage set

**Platform axis (4)** — occupancy `$.platforms`. Occupancy v5 **does** carry a `platforms` array, so
a corpus quotes these four tokens and does not invent a D-002 platform list. It also carries
`$.windowsStanding` (quoted in §1.3), so the Windows recital has a source too.

1. `macos/arm64`
2. `macos/x86_64`
3. `linux/x86_64`
4. `linux/arm64`

Qualified by `$.namedCorpusClasses[0].exactByteIntent`: "per that identity's manifest-declared D-002
platforms", and by `$.waiverRuleVerbatim`: "platform scope follows component manifest". So the
per-cell platform count is at most 4 and is set by each component's manifest.

**Language-role axis (1)** — occupancy `$.languageRoleAxis.slice1Domain` = `["TypeScript"]`,
`.soleSlice1Role` = `"TypeScript"`.

5. `TypeScript`

**Component axis (unenumerated)** — occupancy `$.componentAxis.source`: "Declared component ownership
and dependency metadata (file 02). Comparison-basis identities are not invented here.";
`.catalogAuthoredHere` = `false`.

6. `<declared comparison-basis component identity>` — **the record enumerates no members.** See
   OQ-G16-1 and §4(a).

**Corpus-class axis (3)** — join `$.obligations[3].namedCorpusNotAuthored`, occupancy
`$.namedCorpusNotAuthored`, occupancy `$.namedExecutesLimbs`:

7. `change-impact corpus × component/language/platform matrix`
8. `forced dependency/ownership mutations`
9. `aggregate release selection`

**Retained-evidence axis (4)** — occupancy `$.liveCellMemberSplit.membersInOrder` and
`$.retainedEvidence[0..3]`:

10. `selected-lane explanation` (EV-1)
11. `per-lane signed closure evidence` (EV-2)
12. `skipped-lane proof` (EV-3)
13. `shared-core and cross-component protocol/semantic/lock/offline/bundle results` (EV-4)

**Shared-lane-limb axis inside EV-4 (6)** — occupancy `$.file02SharedLaneLimbs.limbsInOrder`, sourced
to `docs/v2/architecture/02-distribution-and-components.md` lines 136–143:

14. `shared-core`
15. `protocol`
16. `semantic`
17. `lock`
18. `offline`
19. `bundle`

**Change-set-unit / mutation axis** — `g16-input-corpus.v2.json` `$.initialStates`, sourced to
occupancy `$.namedCorpusClasses[*].exactByteIntent`:

20. `add change-set unit` (`$.initialStates[0]`, `platforms` = the four)
21. `delete change-set unit` (`$.initialStates[1]`, `platforms` = the four)
22. `rename change-set unit` (`$.initialStates[2]`, `platforms` = the four)
23. `byte-change change-set unit` (`$.initialStates[3]`, `platforms` = the four)
24. `unrelated change does not force every language closure to build` (`$.initialStates[4]`, no
    `platforms` field; `languageRole` "TypeScript only")
25. `omitted dependency` (`$.initialStates[5]`)
26. `omitted owner` (`$.initialStates[6]`)
27. `missing component declaration` (`$.initialStates[7]`)
28. `owner outside the comparison-basis universe` (`$.initialStates[8]`)
29. `incomplete or invalid universe construction` (`$.initialStates[9]`)
30. `component-new/core-unchanged selection` (`$.initialStates[10]`)
31. `core-new/component-unchanged selection` (`$.initialStates[11]`)

**Fate axis** — occupancy `$.selectorLawFromFile02.failSafe`: "Fail-safe is over-selection or typed
refusal, never under-selection." and `$.namedCorpusClasses[1].passProperty`: "A forced ownership or
dependency mutation either selects every affected lane or refuses."

32. `over-selection of every lane that the mutation could affect`
33. `a typed refusal`
34. (for EV-3) `skipped-lane proof` for every unselected lane in the closed comparison-basis lane
    universe — `$.retainedEvidence[2].exactByteIntent`.

Arithmetic on the enumerated axes: 4 change-set units × 4 platforms = 16 platform-scoped
change-impact states; + 1 unrelated-change state (no platform field) + 5 forced-mutation states + 2
aggregate-release states = 24 class-level cells. Occupancy `$.namedCorpusClasses[0].exactByteIntent`
then multiplies the change-impact limb by "one cell per declared comparison-basis component
identity", and that identity set is not enumerated anywhere in the record.

**Total coverage members: 24 class-level cells; the declared-comparison-basis component multiplier is
not enumerated in the record (OQ-G16-1), so 24 is a floor and not a closed total**

(Rider: each change-impact cell additionally bears EV-1, EV-2, EV-3 and EV-4; EV-4 itself bears six
limb results or skipped-lane proofs. Those ride on the same cells rather than adding cells.)

## 3. Prohibitions bounding the authoring

**The recorded prohibition D-293 singles out for this gate.**
`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md`
sha256 `f530cedca1c799097ed0fc30cf8ec6f0480abe9a56d495236909a3a23b84fc33`, first bullet, verbatim:

> Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep their recorded prohibitions (adapter, CI encoding, journal).

For G16 the kept prohibition is **reserved CI encodings**. It is recorded in the join in four places:

- `g16-leftover-join.v5.json` `$.obligations[3].reason` (last sentence): "This join does not invent
  those fixture bytes, does not apply v16, and does not invent reserved CI encodings."
- `$.doesNot[3]`: "Does not invent reserved CI encodings."
- `$.authorityClaim`: "It does not invent reserved CI encodings. It does not mint Rust-as-core. It
  does not name G13 into required-now. … It does not steal OBL-CI-ENCODING-RESERVED."
- `$.purpose`: "Do not steal OBL-CI-ENCODING-RESERVED. Do not invent reserved CI encodings. Do not
  name G13 into required-now."

The six reserved members, verbatim, from `docs/coop/artifacts/monorepo-ci-contract.v16.json`
`$.reservedForBlueprint` (sha256 `67ca501660a2ba515ce37adc799c5418e4ffd156308189662245e5a5e45a2ddb`),
identical to `monorepo-leftover-join.v4.json` `$.reservedForBlueprintVerbatim` and to occupancy
`$.selectorLawFromFile02.notAuthority`:

```json
["CI provider", "YAML", "repository path filters", "caches", "commands", "implementation tooling"]
```

The whole of `g16-leftover-join.v5.json` `$.doesNot` (27 members), verbatim:

> "Does not SATISFY DR-121.", "Does not apply monorepo-ci-contract.v16.", "Does not steal OBL-CI-ENCODING-RESERVED.", "Does not invent reserved CI encodings.", "Does not mint Rust-as-core.", "Does not name G13 into required-now.", "Does not close DR-117 leftover-design.", "Does not flatten DR-118 DECIDED-V1-NOT-INTEGRATED to OPEN.", "Does not flatten DR-107 PROPOSED-CLOSED-FOR-REVIEW to OPEN.", "Does not open D-056 Class A.", "Does not close leftover-design of fixture implementations.", "Does not close OBL-G16-FX-AUTHORING.", "Does not invent a D9 code.", "Does not invent a section 7.1 recipe.", "Does not add a DR-G* row.", "Does not change live required-now 28.", "Does not execute fixtures.", "Does not claim QUALIFIED.", "Does not edit file 08.", "Does not authorize docs/v2/implementation/.", "Does not invent fixture bytes.", "Does not occupy the identifier.", "Does not record leftover-join.v4 as current after this successor is recorded.", "Does not record leftover-join.v3 as current G16 leftover-join.", "Does not record occupancy v2 as current occupancy.", "Does not record monorepo leftover-join.v3 as current DR-121 leftover-join.", "Does not record monorepo leftover-join.v2 as current DR-121 leftover-join."

From occupancy v5 `$.doesNot` (44 members), the authoring-bounding half, verbatim:

> "Does not apply monorepo-ci-contract.v16.", "Does not steal OBL-CI-ENCODING-RESERVED.", "Does not invent reserved CI encodings.", "Does not close leftover-design of OBL-G16-FX-AUTHORING.", "Does not execute fixtures.", "Does not author fixture bytes.", "Does not invent G16-*/IR-*/C-*/SL-* fixture identifiers.", "Does not name G13 into required-now.", "Does not mint Rust-as-core.", "Does not invent a second slice-1 language role.", "Does not take over G15 packaging-adapter conformance.", "Does not take over G10 provider-conformance.", "Does not take over G14 language-runtime-UX.", "Does not take over G22 platform TCB loader allowlist.", "Does not take over G07 exact-bytes.", "Does not execute G06.", "Does not take over G12 doctor-purge.", "Does not take over G30 useful-install shape.", "Does not invent a D9 code, exit number, or HostTermination.", "Does not invent a section 7.1 recipe.", "Does not settle DR-007.", "Does not mint DR-111 windows.", "Does not choose a CI vendor, YAML, path filter, cache, or command."

From occupancy v5 `$.failsIf` (52 members), the authoring constraints, verbatim:

> "a live G16 retained-evidence member is omitted or has no exactByteIntent / passProperty", "live-cell punctuation is collapsed or extras are invented (members must be selected-lane explanation, per-lane signed closure evidence, skipped-lane proof, shared-core and cross-component protocol/semantic/lock/offline/bundle results)", "change-impact corpus × component/language/platform matrix, forced dependency/ownership mutations, or aggregate release selection is omitted from exact-byte intent", "shared-core and cross-component protocol/semantic/lock/offline/bundle results is split into six live-cell members or collapsed into per-lane signed closure evidence", "DR-121's protocol/authority wording is substituted for this G16 cell's protocol/semantic wording", "a naming-v6 / live-harness-cell corpus class is omitted", "this specification is read as executing fixtures", "this specification is read as authoring fixture bytes", "this specification is read as applying monorepo-ci-contract.v16", "v16 G16-1..G16-14, C-*, SL-*, or IR-* identifiers are treated as authored fixtures of this file", "reserved CI encodings are invented", "EE-7e is treated as a fifth live-cell member", "Rust-as-core is minted or a second slice-1 language role is invented", "G13 is named into required-now", "a D9 code, exit number, or HostTermination is invented", "a section 7.1 recipe is invented", "Windows is treated as a G16 preview platform or as a skipped-lane proof", "an affected omitted lane is waived", "CI vendor, YAML, or path filters are treated as impact authority"

From `g16-named-corpus-catalog.v1.json` `$.whatIsNotAuthored`, verbatim:

> "fixture bytes", "a CI provider, YAML, path filter, cache, command, or implementation tooling", "G16-*/IR-*/C-*/SL-*/FX/AT identifiers as this catalog's own", "numeric windows", "hostile dual-channel goldens"

From `g16-input-corpus.v2.json` `$.whatIsNotAuthored`, verbatim:

> "fixture bytes", "reserved CI encodings", "monorepo-ci-contract.v16 application", "IR-1..IR-4", "G16-12", "Rust-as-core", "a second slice-1 language role"

From `monorepo-leftover-join.v4.json` `$.doesNot`, the authoring-bounding members, verbatim:

> "Does not close OBL-CI-ENCODING-RESERVED.", "Does not close OBL-G16-FX-AUTHORING.", "Does not invent a CI encoding.", "Does not apply monorepo-ci-contract.v16.", "Does not author fixture bytes.", "Does not invent a CI provider, YAML, repository path filter, cache, command, or implementation tooling.", "Does not invent numeric windows.", "Does not execute EE-7e.", "Does not author hostile dual-channel goldens.", "Does not name G13 into required-now.", "Does not steal GATE leftover as a closure."

Three class-level prohibitions inside the corpus classes themselves, which bound corpus *content*
rather than the successor's claims:

- `$.namedCorpusClasses[0].exactByteIntent`: "Windows is not a cell. A second language role is not a
  cell. Path filters, CI vendor config, and cache keys are not the authority that decides impact
  (file 02 reserved)."
- `$.namedCorpusClasses[1].exactByteIntent`: "This class is CI ownership/dependency integrity. It is
  not G07 hostile archive/path/loader/TOCTOU, not G10 provider-frame goldens, and not G15 adapter
  hostile-path emission."
- `$.namedCorpusClasses[2].exactByteIntent`: "Window evaluation rides DR-111 and is not minted here.
  Hostile dual-channel anti-lockstep goldens ride DR-127 / G21 and are not this class. v16
  independently spells discriminating cases as IR-1..IR-4 and lockstep discrimination as G16-12;
  those identifiers are cited-not-applied and are not authored here."

## 4. Dependencies

**(a) The declared comparison-basis component identity set — byte dependency, and the blocking one.**

The corpus cannot be written without it. Occupancy `$.namedCorpusClasses[0].exactByteIntent` opens:

> Once authored: one cell per declared comparison-basis component identity, per slice-1 language-role value (TypeScript only), and per that identity's manifest-declared D-002 platforms (macos/arm64, macos/x86_64, linux/x86_64, linux/arm64).

and `$.namedCorpusClasses[0].passProperty` requires: "Every TypeScript × declared-component ×
manifest-declared-D-002-platform cell is present." EV-3 needs "every unselected lane in the closed
comparison-basis lane universe" (`$.retainedEvidence[2].exactByteIntent`). The forced-mutations class
needs "owner outside the comparison-basis universe" and "incomplete or invalid universe construction"
(`$.namedCorpusClasses[1].exactByteIntent`).

The record does not enumerate that set, and says so. Occupancy `$.componentAxis`, verbatim:

> Declared component ownership and dependency metadata (file 02). Comparison-basis identities are not invented here.

> This specification does not author an ownership record, a component catalog, or DR-103 dependency-edge bytes. Once fixtures exist, those identities are digest-pinned inputs. Fixtures are unauthored.

The unapplied DR-121 contract confirms the shape without supplying members —
`monorepo-ci-contract.v16.json` `$.selector.closedLaneUniverse.componentLanes`:

> Exactly one component lane per component identity in the comparison-basis union. A current-declaration-only enumeration is never authoritative.

and `$.selector.ownershipRecord.declaration.ownerKinds[0]` gives `ownerId` as "a declared component
identity" — a shape, not a list. Live file 02 (sha256
`1811c682cf293e1e0b255be82c62f7ed3c439f0873eb7922bfb0ad965b43f7db`, heading "Monorepo CI and
independent component releases", lines 123–152) enumerates no component identities either; its only
enumerated list under that heading is the six separate-lane bullets at lines 138–143.

Choosing component identities would create a new identifier and a new list. That is expressly outside
the D-293 grant: `D1-fixture-authoring-delegation.claude-recommendation.r2.md`, default-policy bullet,
verbatim:

> **Default policy:** coverage, not one-case-per-member — every explicit platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved; for delegated byte sets, concrete witness bytes may be selected only within already-recorded schemas and fates, and any choice that would create a new semantic member, identifier, value, list or implementation stays outside the grant (recorded as a named open decision instead).

Classification: **byte** dependency (a missing list). It is not an obligation id on either join; no
`leftoverDesign` measurement in the record names the ownership record or component catalog.

**(b) OBL-CI-ENCODING-RESERVED — NOT a dependency for this corpus.**

The six reserved members are `CI provider`, `YAML`, `repository path filters`, `caches`, `commands`,
`implementation tooling` (`monorepo-ci-contract.v16.json` `$.reservedForBlueprint`). The bytes state
positively that none of them may be expressed as an authority in any of the three classes, and that
expressing one **fails** the class:

- `$.namedCorpusClasses[0].exactByteIntent`: "Path filters, CI vendor config, and cache keys are not
  the authority that decides impact (file 02 reserved)."
- `$.namedCorpusClasses[0].passProperty`: "CI vendor/YAML/path-filter authority fails this class."
- `$.retainedEvidence[0].exactByteIntent`: "a digest-pinned per-selected-lane explanation derived
  only from declared ownership, reverse-dependency, shared-surface consumers, and the file-02
  fail-safe-on-ambiguous rule. Path filters, CI vendor YAML, caches, and commands cannot be the
  explanation authority (file 02 reserved)."
- `$.retainedEvidence[0].passProperty`: "A path-filter-only explanation fails."
- `$.selectorLawFromFile02.notAuthority` lists exactly the six.
- occupancy `$.failsIf[44]`: "CI vendor, YAML, or path filters are treated as impact authority".

So every member of every class is expressible without any of the six, and expressing any of them as
authority is a recorded failure. **This corpus is not blocked on `OBL-CI-ENCODING-RESERVED`.**

The C5 disposition adopted at D-293 Decision 7 keeps that obligation open regardless.
`DECISIONS-RECOMMENDED.md` sha256 `42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`,
heading `## C5–C9 — Reserved encodings/owners/units: DR-121, DR-107, DR-103 OD-1/OD-2, DR-101
OD-101-1/2, DR-115/D-006` (line 209), `### Recommendation (Claude, round 2)`, C5 bullet, verbatim:

> - **C5 DR-121:** a reviewed DR-121 architecture-scope act classifies the six `reservedForBlueprint` members as implementation encodings (ownership record stays the impact authority); concrete encodings move after Condition 5 only if a scoped D-056/Condition-2 successor expressly accepts that classification and a successor join remeasures; until then `OBL-CI-ENCODING-RESERVED` stays leftoverDesign true.

Adopted at COORD `## D-293 — User decisions: adopt the agreed recommendations on DECISIONS-NEEDED
A–F` (line 16169), Decision 7, which reads in part: "C5/C6: reviewed architecture-scope acts classify
the reserved encodings; no concrete encoding becomes post-Condition-5 work without an express
scope/eligibility successor and a successor-join remeasurement". That is a **sequencing** constraint
on the encoding obligation, not a byte dependency of this corpus.

**(c) OBL-G16-FX-AUTHORING is measured true on two joins — sequencing dependency.** Closing it needs a
successor on `g16-leftover-join.v5.json` and on `monorepo-leftover-join.v4.json` both (D1 packet §3.2
closing sentence, quoted in §1.4).

**(d) Rides that are recorded standings, not blockers.** DR-111 window evaluation, DR-127 hostile
dual-channel goldens, DR-118/G13 capability-parity, G14 runtime-UX, G15 packaging-adapter and G06
offline all ride away from this corpus by express sentence
(`$.retainedEvidence[1].exactByteIntent`, `$.retainedEvidence[3].exactByteIntent`,
`$.namedCorpusClasses[2].exactByteIntent`). The offline limb of EV-4 stays a named slot:
"D-002 defers DR-106 / G06 and this specification does not execute G06 or delete the slot."

**Standing: BLOCKED-ON-UNENUMERATED-COMPARISON-BASIS-COMPONENT-AXIS**

## 5. Recommended artifact shape

Closest precedent: `docs/coop/artifacts/g21-fixture-corpus.v1.json` sha256
`861bb4e7d26a80158cc1cc3a0518c5e8e95311bee4d8c8ce63acd1e60d6c906d` — the first-authoring shape, whose
payloads sit under `docs/coop/artifacts/fixtures/g21.v1/` with no platform subdirectory. Its
top-level fields, as read from the file:

`artifact, version, date, documentClass, registerRow, registerRowNote, namedGate, status,
reviewStatus, sealRecommendation, binds, authorityClaim, purpose, basedOn, parentReview, file08Pin,
head, requiredNowUnchanged, file08StatusToken, cborConstruction, pinnedHostContext, whatIsAuthored,
whatIsNotAuthored, authoredCatalog, leftoverDesignClosedIfAcceptedAndRecorded,
leftoverDesignRemainingOnG21, remainderAfterThisCorpus, summary, doesNot, failsIf, proposedLaterWork,
recordedInputs, remeasurementClause`

with `authoredCatalog` = `{standing, doesNotMutate, executionRemains, members}` and each member =
`{id, class, inputCorpusId, path, sha256, mutation, expected}`.

The per-platform-copy precedent, for the later version, is
`docs/coop/artifacts/g23-fixture-corpus.v4.json` sha256
`b3fce9f5bab6764919f5dc43c28a43f3d9c3b6be310e45c2c1bd08a617c755c5`, which adds
`platformsQuotedFromOccupancyV2`, `windowsStandingQuotedFromOccupancyV2`, and
`authoredCatalog.{payloadCount, platformCount, copyCount}` with each member carrying
`platformCopies[{platform, path, sha256}]`.

Proposed artifact name: `docs/coop/artifacts/g16-fixture-corpus.v1.json`.
Proposed fixture directory: `docs/coop/artifacts/fixtures/g16.v1/`.

Shape: **first-authoring, no platform subdirectories.** G16's payloads are ownership-record and
change-set documents, not per-platform binaries; occupancy `$.platforms` scopes the *evidence*
("per that identity's manifest-declared D-002 platforms"), so a per-platform copy version
(`fixtures/g16.v2/<platform>/`) is the natural later successor. Because occupancy v5 carries a
`platforms` array and a `windowsStanding` string, the successor should carry both g23.v4-style
recitals — `platformsQuotedFromOccupancyV5` and `windowsStandingQuotedFromOccupancyV5` — plus a
`v16NotApplied` recital quoting `$.v16Standing`, a `liveCellMembersQuotedFromOccupancyV5` recital of
the four members in order, and a `sharedLaneLimbsQuotedFromOccupancyV5` recital of the six file-02
limbs. Those five recitals are what stop a reviewer reading the corpus as applying v16 or as
inventing an axis.

## 6. Effort and risk

Cases: **24 class-level cells** as enumerated in §2 (16 platform-scoped change-impact states + 1
unrelated-change state + 5 forced-mutation states + 2 aggregate-release states), each change-impact
cell repeated per declared comparison-basis component identity — a multiplier the record does not
supply. Distinct payload families to author, before component/platform multiplication: 1 committed
ownership record (blocked, see §4(a)), 5 change-set documents, 5 mutated ownership/dependency
documents, 2 release-selection records, plus per-cell EV-1/EV-2/EV-3 explanations and EV-4's six limb
results = 13 payload families.

### (a) Witness-byte selections inside recorded schemas — permitted under the grant

1. The concrete bytes of an `add`, `delete`, `rename` and `byte-change` unit inside a change set,
   given a component identity — the four unit kinds are already recorded at
   `$.namedCorpusClasses[0].exactByteIntent` and at `g16-input-corpus.v2.json` `$.initialStates[0..3]`.
2. Which change-set units the `unrelated change` case touches, provided the case demonstrates the
   recorded property "An unrelated change does not force every language closure to build".
3. The concrete shape of each of the five recorded forced mutations (`omitted dependency`,
   `omitted owner`, `missing component declaration`, `owner outside the comparison-basis universe`,
   `incomplete or invalid universe construction`), at either recorded fate (over-selection or typed
   refusal).
4. The concrete bytes of the two recorded release selections (`component-new/core-unchanged`,
   `core-new/component-unchanged`).
5. Digest-pinning: which digest each payload is bound by, and the `{path, sha256}` join shape —
   the classes say "digest-pinned" throughout without naming a construction, so the construction must
   be *declared* (see the risk paragraph).
6. The four platform tokens, quoted from `$.platforms` rather than restated.
7. Binding each authored payload to one of the 12 `g16-input-corpus.v2` initial-state ids
   (`inputCorpusId`, per the g21.v1 member shape).

### (b) Choices that would create new semantics — record as a named open decision, do not choose

1. **Any of the six `reservedForBlueprint` members — a CI provider, YAML, repository path filter,
   cache, command, or implementation tooling — used as impact authority.** Violates the kept
   prohibition: join `$.doesNot[3]` "Does not invent reserved CI encodings.", occupancy
   `$.failsIf[27]` "reserved CI encodings are invented" and `$.failsIf[44]` "CI vendor, YAML, or path
   filters are treated as impact authority", and `$.namedCorpusClasses[0].passProperty` "CI
   vendor/YAML/path-filter authority fails this class." — **record as a named open decision, do not
   choose.**
2. **A component catalog or an ownership record with invented component identities.** Violates
   `$.componentAxis.note` and the D-293 default policy's "any choice that would create a new semantic
   member, identifier, value, list or implementation stays outside the grant". — **record as a named
   open decision, do not choose.** (This is §4(a); it is the reason the standing is BLOCKED.)
3. **Any `G16-*`, `IR-*`, `C-*`, or `SL-*` identifier from v16.** Violates occupancy
   `$.doesNot[12]` "Does not invent G16-*/IR-*/C-*/SL-* fixture identifiers." and `$.failsIf[26]`.
   The six shared-lane limbs must be named by their file-02 token (`shared-core`, `protocol`,
   `semantic`, `lock`, `offline`, `bundle`), not `SL-1..SL-6`. — **record as a named open decision,
   do not choose.**
4. **A second slice-1 language role, or Rust-as-core.** Violates `$.languageRoleAxis.doesNotMint` and
   `$.failsIf[32]`. — **record as a named open decision, do not choose.**
5. **A Windows cell, or a Windows skipped-lane proof.** Violates `$.windowsStanding` and
   `$.failsIf[42]`. — **record as a named open decision, do not choose.**
6. **A numeric compatibility window.** Violates join `$.doesNot` on the ROW twin ("Does not invent
   numeric windows.") and `$.namedCorpusClasses[2].exactByteIntent` "Window evaluation rides DR-111
   and is not minted here." — **record as a named open decision, do not choose.**
7. **A D9 code, exit number, or HostTermination inside the `semantic` limb of EV-4.** Violates
   occupancy `$.doesNot[24]` and `$.failsIf[40]`; `$.retainedEvidence[3].passProperty` says "An
   invented D9 code fails this specification." — **record as a named open decision, do not choose.**
8. **A hostile dual-channel anti-lockstep golden.** Those ride DR-127 / G21
   (`$.namedCorpusClasses[2].exactByteIntent`); `OBL-HOSTILE-GOLDENS` is a separate delegated
   obligation on `anti-lockstep-leftover-join.v3`. — **record as a named open decision, do not
   choose.**
9. **Naming G13 into required-now, or scoring DR-118 capability/parity in EV-2.** Violates
   `$.doesNot[13]` and `$.failsIf[33]`. — **record as a named open decision, do not choose.**

### Risk

The reviewer-attack pattern maps onto G16 in a specific and dangerous way. (a) *Inventing a
vocabulary the record does not carry* — this is the dominant G16 risk, because every naturally
available way to write a CI fixture (a workflow YAML, a path filter, a cache key, a command line) is
one of the six reserved members, and the g23.v1 finding M3 ("hostRequestedDomainBaseline invents a
Coverage-domain encoding vocabulary") is precisely what a reviewer would land on a corpus that
expressed lane selection as a YAML matrix. A second instance of the same risk is the digest
construction: the classes say "digest-pinned" seven times without naming a construction, and the
g23.v1 finding M2 ("coverageCommitment is a SHA-256 over compact JSON, not deterministic-CBOR, and
its construction is undeclared") is the same defect in advance. (b) *Collapsing two classes the
occupancy keeps separate* — occupancy `$.liveCellMemberSplit.notCollapsed` pre-names five collapses
that fail, and `$.failsIf[20]` names splitting EV-4 into six live-cell members as a failure in the
other direction; the g23.v2 finding M1 is the same defect. (c) *Classifying a witness under a closed
identifier it does not belong to* — the g21.v3/v4 finding `G21FXV3-M1` maps onto the forced-mutations
class: a mutation record filed as a skipped-lane proof is expressly named as a failure at
`$.retainedEvidence[2].exactByteIntent` ("A hostile omitted-dependency or omitted-owner mutation
cannot convert an affected lane into a skipped-lane proof"). (d) *Over-claiming closure* — the g23.v1
finding S4 maps onto `leftoverDesignClosedIfAcceptedAndRecorded`: `OBL-G16-FX-AUTHORING` is measured
true on two joins, and `OBL-CI-ENCODING-RESERVED` stays true on DR-121 under the adopted C5, so a
corpus that claims either closed is over-claiming on its face. The additional G16-specific risk is
that the component axis is open (§4(a)): a corpus that quietly picks component identities to make its
cell count finite has invented the list that C5 says the ownership record owns.

## 7. Open questions

**OQ-G16-1:** What is the declared comparison-basis component identity set? Occupancy
`$.namedCorpusClasses[0].exactByteIntent` requires "one cell per declared comparison-basis component
identity"; `$.componentAxis.source` says "Comparison-basis identities are not invented here" and
`.catalogAuthoredHere` is `false`; `monorepo-ci-contract.v16.json`
`$.selector.closedLaneUniverse.componentLanes` says "Exactly one component lane per component
identity in the comparison-basis union. A current-declaration-only enumeration is never
authoritative." and v16 is not applied; live file 02 lines 123–152 enumerate no component identities.
No obligation on either join names this gap. It is the reason for the §4 standing.

**OQ-G16-2:** Who owns the committed ownership record, and is authoring one part of
`OBL-G16-FX-AUTHORING` or a separate unmeasured obligation? `monorepo-ci-contract.v16.json`
`$.selector.ownershipRecord.standing` is "AUTHORITATIVE committed design record. Not CI YAML. Not a
path-filter table as authority.", and the adopted C5 says "ownership record stays the impact
authority". But v16 is CANDIDATE-NOT-APPLIED, occupancy `$.componentAxis.catalogAuthoredHere` is
`false`, and neither `g16-leftover-join.v5` nor `monorepo-leftover-join.v4` carries an
`OBL-OWNERSHIP-RECORD`-shaped measurement. The record does not say whether the ownership record is a
fixture input the corpus supplies or a separate design act.

**OQ-G16-3:** How is "digest-pinned" constructed? The string appears in all three
`namedCorpusClasses[*].exactByteIntent` and all four `retainedEvidence[*].exactByteIntent` with no
construction named anywhere in occupancy v5, the catalog, or the input corpus. The g23 lineage was
rejected once on exactly this (`g23-fixture-corpus.v1.review-independent.claude2.json` M2). A
successor must declare a construction; whether declaring one is a witness-byte selection inside a
recorded schema or the creation of a new value is not settled by the bytes.

**OQ-G16-4:** Does EV-2's "per-lane signed closure evidence" require signatures that do not yet
exist? `$.retainedEvidence[1].exactByteIntent` requires "digest-pinned signed closure
bytes/declarations", and signing material rides DR-112 elsewhere in the record (see the G15 record's
AT-1-TRUSTED-INSTALL / AT-5-TRUSTED-INSTALL rides). Occupancy v5 records no ride and no
blocked-on-ride fate for EV-2, unlike G15's `closedMap`, so the record supplies no lawful
"unavailable" standing for this member. Whether a corpus may author EV-2 with a placeholder signature
is not stated.

**OQ-G16-5:** The ROW twin `monorepo-leftover-join.v4` names the GATE join as "g16 leftover-join.v4
(D-262)" while the current GATE join is v5 (D-278), and its `$.registerRowNote` names monorepo
leftover-join.v3 as the predecessor. A successor pair would have to state which HEAD it measures at
(`g16-leftover-join.v5` `$.head` = `af870f41c6af4f4fa5c41e3f0fd302231e727233`;
`monorepo-leftover-join.v4` `$.head` = `456a5285fd44a4fed720c39fe3dd82ce0e6ccadb`; this record's HEAD
is `8bc9963f68784842de643d5dbb1269bd4cf4411a`).
