# G29 — OBL-G29-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

- Path: `docs/coop/artifacts/g29-leftover-join.v4.json`
- sha256: `9e1af4ba3b21e483154825fa2c6d275f7ee805d1fb455f01c9d35e48411c3f64`
- `$.artifact` = `"g29-leftover-join.v4"`
- `$.date` = `"2026-08-23"`
- `$.registerRow` = `"DR-G29"`
- `$.status` = `"CANDIDATE-NOT-APPLIED"`
- `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`
- `$.binds` = `"NOTHING"`
- `$.head` = `"cbe471bfc0de4b49791d40c9d795b57dbb2a298c"`
- `$.requiredNowUnchanged` = `28`
- `$.file08StatusToken` = `"OPEN"`
- `$.sealRecommendation` = `"DO-NOT-SEAL"`
- `$.documentClass` = `"DESIGN-CONTRACT-CANDIDATE"`

COORD heading that recorded it (`docs/coop/COORDINATOR-DECISIONS.md`
`1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 12500), verbatim:

> `## D-254 — Record g29 leftover-join.v4 as G29 leftover remasurement`

`$.file08Pin` matches live file 08:

```json
{
  "path": "docs/v2/architecture/08-decision-and-readiness-register.md",
  "sha256": "e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e"
}
```

`$.leftoverDesignOpenStanding`, verbatim:

> "The live DR-G29 token is OPEN. leftover-design of unnamed G29 initial states is stale as a naming
> claim after g29-input-corpus.v1. leftover-design of G29 fixture implementations remains. A section
> 7.1 recipe is not invented. DR-117 is not SATISFIED."

`$.namedClassesThisGate` = `["EE-1","EE-2","EE-3b","EE-4","EE-5a","EE-5b","EE-6a"]`.

`$.summary.leftoverDesign` = `["OBL-G29-FX-AUTHORING"]`; `$.summary.dr117Satisfied` = `false`;
`$.summary.fixturesExecuted` = `false`; `$.summary.requiredNowUnchanged` = `28`.

### 1.2 The obligation object (verbatim)

JSON path: `$.obligations[3]` of `docs/coop/artifacts/g29-leftover-join.v4.json`.

```json
{
  "id": "OBL-G29-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "hostile-but-well-formed excluded-form admission corpus and post-admission/pre-stage substitution-mutation corpus (DR-117 EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, EE-6a)"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "G29 v2 names unauthored fixture cells of the excluded-form admission and substitution-mutation corpus. D-056 Decision clause 5: authoring fixtures remains design work. This join does not invent those fixture bytes and does not invent a D9 code or a section 7.1 recipe. The INPUT-state corpus exists and is Claude ACCEPT 0/0; that INPUT layer is OBL-G29-INPUT-CORPUS, leftoverDesign false. This join does not invent fixture bytes."
}
```

Field-by-field:

- `$.obligations[3].reason` — quoted in full above. Note it attributes the naming to "G29 v2" while
  `$.basedOn.occupancyV3.role` records occupancy v3 as "Current G29 occupancy remasurement". See
  OQ-G29-1.
- `$.obligations[3].namedCorpusNotAuthored` — a single-element array; the string is the live file 08
  harness-cell corpus phrase, byte-identical to `$.namedCorpusNotAuthored` at the join top level and
  to the occupancy's `$.namedCorpusNotAuthored[0]`.
- `$.obligations[3].existingGate` = `"none as authored implementations"`.
- `$.obligations[3].rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `$.obligations[3].executionObligationOwnerToday` = `"none"`.
- `remainingNotAuthored`, `namedCases`, `namedNotAuthored` — absent on this obligation object.

### 1.3 Occupancy file and the fields that name the fixture cells

- Path: `docs/coop/artifacts/harness.DR-G29.preview-boundary-excluded-form-admission.preview.v3.json`
- sha256: `94a40de95097afbf51e50461bac54f5fc95326215cf94e89a2f3655c731be96d`
- `$.artifact` = `"harness.DR-G29.preview-boundary-excluded-form-admission.preview.v3"`
- Named as current by `$.basedOn.occupancyV3` of the join, with role
  `"Current G29 occupancy remasurement. leftover-join.v3 cited occupancy v2 as the specification.
  Occupancy v3 remasured that already-named identifier after D-204. Occupancy v3 stays unmoved. This
  join does not occupy the identifier and does not execute G29."`
- Dual ACCEPT 0/0 (`$.basedOn.occupancyV3.reviews.claude.verdict`,
  `$.basedOn.occupancyV3.reviews.codex.verdict`).

**`$.liveRowVerbatim`** (identical to live file 08 line 365):

> "| DR-G29 PREVIEW-BOUNDARY-EXCLUDED-FORM-ADMISSION | Admission and pre-stage validation refuse
> represented excluded forms: publisher neither first-party nor explicitly trusted,
> public-lifecycle/discovery, contributor authority claims, untrusted native/WASM, contribution
> hooks/roots/probes, represented pre-stage imperative substitution, and network-granted analysis |
> named: harness.DR-G29.preview-boundary-excluded-form-admission.preview (D-157; not authored; not
> QUALIFIED). hostile-but-well-formed excluded-form admission corpus and post-admission/pre-stage
> substitution-mutation corpus (DR-117 EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, EE-6a) |
> admission-time excluded forms refused with no ExecutionId; represented post-admission
> substitutions rejected before any stage; v1-slice §7 item 8; not G21 sandbox; not G23 provider
> FactCandidate/Coverage | Product owner | PROPOSED; not QUALIFIED | pass all; no waiver for silent
> admission | OPEN |"

**`$.liveHarnessCellVerbatim`**:

> "named: harness.DR-G29.preview-boundary-excluded-form-admission.preview (D-157; not authored; not
> QUALIFIED). hostile-but-well-formed excluded-form admission corpus and post-admission/pre-stage
> substitution-mutation corpus (DR-117 EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, EE-6a)"

**`$.liveCellVerbatim`**:

> "admission-time excluded forms refused with no ExecutionId; represented post-admission
> substitutions rejected before any stage; v1-slice §7 item 8; not G21 sandbox; not G23 provider
> FactCandidate/Coverage"

**`$.claimVerbatim`**:

> "Admission and pre-stage validation refuse represented excluded forms: publisher neither
> first-party nor explicitly trusted, public-lifecycle/discovery, contributor authority claims,
> untrusted native/WASM, contribution hooks/roots/probes, represented pre-stage imperative
> substitution, and network-granted analysis"

**`$.waiverRuleVerbatim`** = `"pass all; no waiver for silent admission"`.
**`$.liveAssuranceStageVerbatim`** = `"PROPOSED; not QUALIFIED"`.
**`$.liveStatusVerbatim`** = `"OPEN"`.
**`$.liveGateOwner`** = `{"DR-G29": "Product owner"}`.
**`$.executionObligationOwner`** = `"Product owner"`.
**`$.productLawOwner`** = `"DR-117 / preview-product-boundary-successor.v5"`.
**`$.namedCorpusNotAuthored`** = the single corpus string quoted above.
**`$.namedExecutesLimbs`** = `["EE-1","EE-2","EE-3b","EE-4","EE-5a","EE-5b","EE-6a"]`.
**`$.namedExecutesLimbNote`**:

> "Use only EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, and EE-6a as the live cell names. EE-7a, EE-7b,
> and EE-7d ride G30. This specification does not invent EE numbers."

**`$.retainedEvidence[]`** — five members, each with `exactByteIntent` and `passProperty`. These are
the single most important bytes for authoring; quoted in full.

`$.retainedEvidence[0]`:

```json
{
  "id": "EV-1",
  "memberVerbatim": "admission-time excluded forms refused with no ExecutionId",
  "exactByteIntent": "Once authored: digest-pinned well-formed representations of the excluded forms named in the live claim (publisher neither first-party nor explicitly trusted; public-lifecycle/discovery; contributor authority claims; untrusted native/WASM; contribution hooks/roots/probes; network-granted analysis), each paired with an admission refusal and a record that no ExecutionId was minted. Fixtures are unauthored.",
  "passProperty": "Admission-time excluded forms are refused with no ExecutionId. Silent admission fails.",
  "suppliedBy": "excluded-form admission corpus class. Not authored."
}
```

`$.retainedEvidence[1]`:

```json
{
  "id": "EV-2",
  "memberVerbatim": "represented post-admission substitutions rejected before any stage",
  "exactByteIntent": "Once authored: digest-pinned post-admission / pre-stage substitution-mutation records (EE-5b) showing rejection before any stage runs. Fixtures are unauthored.",
  "passProperty": "Represented post-admission substitutions are rejected before any stage.",
  "suppliedBy": "post-admission/pre-stage substitution-mutation corpus class. Not authored."
}
```

`$.retainedEvidence[2]`:

```json
{
  "id": "EV-3",
  "memberVerbatim": "v1-slice §7 item 8",
  "exactByteIntent": "Once authored: a record that the cited item is the existing v1-slice section 7 item 8 at docs/coop/v1-slice.md sha256 6b8717fef545fe73f0de5879a7389fbc0c7c499c70e06b344789e5150478bee3, verbatim: 'A PlanIntent containing an excluded form is rejected during request validation with the required typed outcome and creates no ExecutionId, AttemptRecord, ExecutionPlan, or authoritative Run; a substitution after admission is also rejected before any stage executes.' This specification does not invent a section 7.1 recipe. Fixtures are unauthored.",
  "passProperty": "The cited item equals the pinned v1-slice section 7 item 8 verbatim above. Inventing a new recipe, including a section 7.1 recipe, fails.",
  "suppliedBy": "excluded-form admission corpus class. Not authored."
}
```

`$.retainedEvidence[3]`:

```json
{
  "id": "EV-4",
  "memberVerbatim": "not G21 sandbox",
  "exactByteIntent": "Once authored: a not-that-gate record that this refusal is admission/pre-stage validation, not G21 process-tree containment. Fixtures are unauthored.",
  "passProperty": "This gate is not G21 sandbox.",
  "suppliedBy": "excluded-form admission corpus class. Not authored."
}
```

`$.retainedEvidence[4]`:

```json
{
  "id": "EV-5",
  "memberVerbatim": "not G23 provider FactCandidate/Coverage",
  "exactByteIntent": "Once authored: a not-that-gate record that this refusal is excluded-form admission, not G23 FactCandidate/Coverage admission. Fixtures are unauthored.",
  "passProperty": "This gate is not G23 provider FactCandidate/Coverage.",
  "suppliedBy": "excluded-form admission corpus class. Not authored."
}
```

**`$.section7Item8Standing`** — the pinned v1-slice item (see §1.5 and §3):

```json
{
  "present": true,
  "pin": {
    "path": "docs/coop/v1-slice.md",
    "sha256": "6b8717fef545fe73f0de5879a7389fbc0c7c499c70e06b344789e5150478bee3",
    "selector": "section 7 item 8",
    "verbatim": "A PlanIntent containing an excluded form is rejected during request validation with the required typed outcome and creates no ExecutionId, AttemptRecord, ExecutionPlan, or authoritative Run; a substitution after admission is also rejected before any stage executes."
  },
  "role": "Existing v1-slice section 7 item 8, already pinned inside preview-product-boundary-successor.v5 enforcementEvidence.v1SlicePin. This specification quotes that item. It does not invent a section 7.1 recipe."
}
```

`docs/coop/v1-slice.md` re-hashed at HEAD: `6b8717fef545fe73f0de5879a7389fbc0c7c499c70e06b344789e5150478bee3`
— matches the pin.

**`$.discipline`**:

> "SPECIFICATION OF EXACT-BYTE INTENT. Occupancy remasurement of the already-named identifier. No
> harness runs here. Naming is not execution. No fixture bytes authored. EE-1, EE-2, EE-3b, EE-4,
> EE-5a, EE-5b, and EE-6a are not executed by this file existing. A section 7.1 recipe is not
> invented. G21, G23, G24, and G30 are not taken over."

**Fields that are ABSENT from this occupancy** (checked against the brief's minimum list):
`platforms`, `windowsStanding`, `namedCorpusClasses`, `passProperty` (top-level). The only
platform-bearing string in the whole file is `$.proposedLaterWork[2]`:

> "A later D-000 cycle may author and independently pin fixture implementations for EE-1, EE-2,
> EE-3b, EE-4, EE-5a, EE-5b, EE-6a and the five live-cell members, on the four D-002 platforms. This
> v3 occupancy does not invent those bytes."

So the platform quantifier ("the four D-002 platforms") is named but the four tokens are not
enumerated anywhere in this occupancy. See OQ-G29-2.

Present and quoted above: `liveRowVerbatim`, `liveHarnessCellVerbatim`, `liveCellVerbatim`,
`claimVerbatim`, `namedCorpusNotAuthored`, `retainedEvidence[]` (with `exactByteIntent` /
`passProperty`), `liveGateOwner`, `executionObligationOwner`, `doesNot`, `failsIf` (§3).

### 1.4 ROW twin join (or: none)

**None.** `DECISION-PACKETS/D1-fixture-authoring-delegation.md`
sha256 `bc8484cc7159af26a142b97a55b1095049f7ea2ac10c283cd1e2428ba2569ea9`, §3.1 GATE-side table,
DR-G29 row, last column ("ROW twin") reads `none`. The same packet's §3.2 note records the
consequence where a twin does exist:

> "Closing a fixture obligation would require a successor on *both* the GATE join and its ROW twin,
> since the same id is measured true on both."

That clause is inapplicable here: `OBL-G29-FX-AUTHORING` is measured `leftoverDesign: true` on the
GATE join only, and closing it needs a successor on `g29-leftover-join` alone. Two independent
checks agree: the string `OBL-G29-FX-AUTHORING` appears in no other leftover-join, and
`$.registerRow` of the G29 join is the gate row `DR-G29`, not an architecture row.

### 1.5 Governing contract/spec the occupancy pins

Two artifacts, both pinned by the occupancy.

**(a) The class-defining contract — `preview-product-boundary-successor.v5`.**
The occupancy pins it at `$.productLawOwner` = `"DR-117 / preview-product-boundary-successor.v5"`
and `$.palV1A1`:

> "PAL-V1-A1: Product owner is the execution-obligation owner. Product-law ownership remains DR-117
> / preview-product-boundary-successor.v5. This specification does not transfer product law onto the
> gate owner."

- Path: `docs/coop/artifacts/preview-product-boundary-successor.v5.json`
- sha256: `5face6a97b311117569044c0214452571e6d3f051e1ab9b38f46abf442ce1262`
- `$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`,
  `$.binds` = `"NOTHING"`, `$.registerRow` = `"DR-117"`.

The seven G29 classes are defined at `$.enforcementEvidence.classes[]`. Verbatim, with JSON paths:

`$.enforcementEvidence.classes[0]` (EE-1):
```json
{"id":"EE-1","item":1,"invariant":"No marketplace, public catalog, or ecosystem governance. First-party / explicitly trusted only.","input":"An otherwise well-formed admission request whose component publisher is not first-party and not on the explicit-trust list.","pass":"Refused at admission. No ExecutionId. v1-slice §7 item 8.","owner":"this-candidate","existingGate":"none. DR-104 is identity/namespace, not marketplace admission.","laterExecution":"Specified here. Unexecuted. Not QUALIFIED. Not assigned to condition 4 by default."}
```

`$.enforcementEvidence.classes[1]` (EE-2):
```json
{"id":"EE-2","item":2,"invariant":"No public lifecycle parity or external discovery service.","input":"A PlanIntent or admission request that requires an external discovery or public-lifecycle endpoint.","pass":"Excluded form. Refused in request validation. No ExecutionId.","owner":"this-candidate","existingGate":"none","laterExecution":"Specified here. Unexecuted."}
```

`$.enforcementEvidence.classes[3]` (EE-3b):
```json
{"id":"EE-3b","item":3,"invariant":"Contributors do not own policy, persistence, rendering, termination, or host lifecycle.","input":"A manifest claiming policy, persistence, rendering, termination, or host-lifecycle authority.","pass":"Refused at admission. No ExecutionId.","owner":"this-candidate","existingGate":"none. Not DR-133 (output law only).","laterExecution":"Specified here. Unexecuted."}
```

`$.enforcementEvidence.classes[4]` (EE-4):
```json
{"id":"EE-4","item":4,"invariant":"Untrusted native or WASM is not admitted. DR-G21 is not a sandbox.","input":"A PlanIntent or manifest requesting untrusted native or WASM admission.","pass":"Excluded form. Rejected in request validation; no ExecutionId / AttemptRecord / ExecutionPlan / authoritative Run. Post-admission substitution rejected before any stage.","owner":"this-candidate for preview refusal. DR-128 for demonstrated OS/WASM enforcement (post-MVP, not condition 4).","existingGate":"DR-G21 does not own this property.","laterExecution":"Preview class specified here, unexecuted. DR-128 leftover-design remains on DR-128."}
```

`$.enforcementEvidence.classes[5]` (EE-5a):
```json
{"id":"EE-5a","item":5,"invariant":"Contribution-owned project hooks, root commands, and probes are excluded.","input":"A component manifest claiming a project hook, root command, or contribution-granted probe.","pass":"Refused at admission. No ExecutionId.","owner":"this-candidate","existingGate":"none for the contribution-role refusal. Host-owned doctor probes remain D-002 / D-032 / DR-114.","laterExecution":"Specified here. Unexecuted."}
```

`$.enforcementEvidence.classes[6]` (EE-5b):
```json
{"id":"EE-5b","item":5,"invariant":"Generic imperative contribution, if represented as a request or protocol substitution, is excluded before stage execution. Arbitrary ambient acts of an already-running trusted-TCB component are not admission-enforced and are not claimed confined.","input":"A PlanIntent, manifest, or protocol frame that substitutes an imperative contribution (not merely a named hook/root/probe) after admission but before any stage executes.","pass":"Detected substitution is rejected before any stage executes (v1-slice §7 item 8). If the act is only an ambient trusted-TCB effect with no request/protocol representation, this class does not apply; that effect is trusted-code behavior, not an admission exclusion.","owner":"this-candidate for the constructible pre-stage substitution. Ambient trusted-TCB acts have no preview confinement owner.","existingGate":"none","laterExecution":"Specified here. Unexecuted."}
```

`$.enforcementEvidence.classes[7]` (EE-6a):
```json
{"id":"EE-6a","item":6,"invariant":"No network-granted analysis default.","input":"A PlanIntent whose analysis branch requests network-granted analysis.","pass":"Excluded form. Refused in request validation. No ExecutionId.","owner":"this-candidate","existingGate":"none. DR-G01..G05 measure download bytes, installed closure, help/version, memory, and component cost. They do not own this property.","laterExecution":"Specified here. Unexecuted."}
```

The same contract carries the v1-slice pin the occupancy re-quotes,
`$.enforcementEvidence.v1SlicePin`:

```json
{"path":"docs/coop/v1-slice.md","sha256":"6b8717fef545fe73f0de5879a7389fbc0c7c499c70e06b344789e5150478bee3","selector":"§7 item 8","verbatim":"A `PlanIntent` containing an excluded form is rejected during request validation with the required typed outcome and creates no `ExecutionId`, `AttemptRecord`, `ExecutionPlan`, or authoritative Run; a substitution after admission is also rejected before any stage executes."}
```

Note the backtick difference: the v5 pin's `verbatim` carries markdown backticks around
`PlanIntent`, `ExecutionId`, `AttemptRecord`, `ExecutionPlan`; the occupancy's
`$.section7Item8Standing.pin.verbatim` and `$.retainedEvidence[2].exactByteIntent` carry the same
sentence without backticks. Both cite the same file at the same digest. See OQ-G29-3.

**(b) The pinned prose source — `docs/coop/v1-slice.md` §7 item 8.**
sha256 `6b8717fef545fe73f0de5879a7389fbc0c7c499c70e06b344789e5150478bee3` (re-hashed at HEAD).
Selector: `section 7 item 8`. This is the passProperty target of EV-3.

**"Section 7.1" — what it refers to, pinned.** The prohibition "does not invent … a section 7.1
recipe" does *not* point at `v1-slice.md`: that file contains zero occurrences of the string `7.1`.
The referent is `docs/coop/IMPLEMENTATION-FREEZE.md`
sha256 `e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd`, line 1675:

> `### 7.1 Parked identity recipes — named for escalation, NOT non-blocking`

Corroborating cross-references in the same lineage: COORD line 383 ("the Finding fingerprint (§7.1
parks both)"), COORD line 1354 ("§7.10 rule 2"), and `IMPLEMENTATION-FREEZE.md` line 4449
(`### 7.10 Structural: …`) — §7.1 and §7.10 are sections of the same document. The section's own
operative sentences, verbatim:

- line 1681: "Naming them makes escalation compliant. It does not make them optional and
  it does not authorise anyone to invent one."
- line 1711: "Every row above must be closed by a binding artifact before signature. None may
  be closed by this record, by the blueprint, by a checker, or by an implementer."
- the governing property, blockquoted at line 1728:
  > "**Any identity-, digest-, commitment- or ref-typed field for which no binding
  > artifact states a rule producing its value from real inputs is parked under
  > this section and is escalable, whether or not it appears as a row above.**"
  and line 1731–1733: "A value that appears only as a literal in a vector, fixture, golden or example
  is not a rule. A rule that exists only inside a checker's Python is not a
  binding artifact. Neither is closed by an implementer inferring one."

So "a section 7.1 recipe" is a *parked identity recipe* — a derivation rule for an identity-,
digest-, commitment- or ref-typed field that no binding artifact states. The parked rows named in
that section include `RunId` derivation, sealed-Run semantic manifest identity, `EvidenceDigest`
byte recipe, Finding fingerprint recipe, `FactViewId` derivation, cache/regeneration key recipes,
`executionPlanCommitment`, and `subjectScopeCommitment`. Dependency consequence: see §4.

**No `platforms` array on this occupancy.** Unlike G23 occupancy v2
(`docs/coop/artifacts/harness.DR-G23.provider-well-formed-admission.preview.v2.json`
sha256 `f48ba637bdf193785c05906a1686ce268b27b6ce7355de07fa5effefdd84fb0b`, whose `$.platforms` =
`["macos/arm64","macos/x86_64","linux/x86_64","linux/arm64"]` and `$.windowsStanding` = `"D-002
defers Windows with explicit disposition. Windows is not a G23 preview-runner platform."`), the G29
occupancy carries neither field. The record does not settle which occupancy a G29 corpus would quote
the four tokens from. See OQ-G29-2.

**The INPUT-state corpus.** `docs/coop/artifacts/g29-input-corpus.v1.json`
sha256 `7a176c542f2fb4db652671cfe1475bae4e22889efa0729a26f1338343317993b`, `$.artifact` =
`"g29-input-corpus.v1"`, `$.registerRow` = `"DR-117"`, `$.namedGate` = `"DR-G29"`,
`$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.head` = `"5d5d77819ae3019d9e6e02f1e66de3d93c060402"`,
`$.requiredNowUnchanged` = `26`. It is pinned by the join at `$.basedOn.inputCorpus` with role:

> "Named initial states from live exactByteIntent. fixtureBytes NOT-AUTHORED. This join consumes
> that naming. It does not close leftover-design of fixture implementations."

`$.whatIsAuthored`:

> "Named initial states drawn from live G29 v2 exactByteIntent: six excluded-form admission cells,
> EE-5b post-admission/pre-stage substitution, and the existing v1-slice section 7 item 8 record. No
> fixture bytes. No section 7.1 recipe invented."

Its eight `$.initialStates[i].id` values, each carrying `"fixtureBytes": "NOT-AUTHORED"`:

| i | `id` | `stateNamed` |
|---|---|---|
| 0 | `G29.ee.publisher-neither-first-party-nor-explicitly-trusted` | "publisher neither first-party nor explicitly trusted" |
| 1 | `G29.ee.public-lifecycle-discovery` | "public-lifecycle/discovery" |
| 2 | `G29.ee.contributor-authority-claims` | "contributor authority claims" |
| 3 | `G29.ee.untrusted-native-wasm` | "untrusted native/WASM" |
| 4 | `G29.ee.contribution-hooks-roots-probes` | "contribution hooks/roots/probes" |
| 5 | `G29.ee.network-granted-analysis` | "network-granted analysis" |
| 6 | `G29.ee5b.post-admission-pre-stage-substitution` | "EE-5b post-admission / pre-stage substitution-mutation rejected before any stage" |
| 7 | `G29.v1-slice-section-7-item-8` | "existing v1-slice section 7 item 8 (not a section 7.1 recipe)" |

## 2. Coverage set

Every explicit quantifier named in the governing bytes, verbatim, grouped by axis.

**Axis A — corpus classes (the live harness cell).** One corpus string, naming seven DR-117 EE
classes.

1. `harness.DR-G29…preview.v3#$.namedCorpusNotAuthored[0]` = "hostile-but-well-formed excluded-form
   admission corpus and post-admission/pre-stage substitution-mutation corpus (DR-117 EE-1, EE-2,
   EE-3b, EE-4, EE-5a, EE-5b, EE-6a)". Same string at
   `g29-leftover-join.v4.json#$.obligations[3].namedCorpusNotAuthored[0]` and at file 08 line 365,
   column 3.
2. `harness.DR-G29…preview.v3#$.namedExecutesLimbs` = `["EE-1","EE-2","EE-3b","EE-4","EE-5a",
   "EE-5b","EE-6a"]` — **7 EE classes**.
3. `harness.DR-G29…preview.v3#$.namedExecutesLimbNote`: "Use only EE-1, EE-2, EE-3b, EE-4, EE-5a,
   EE-5b, and EE-6a as the live cell names. EE-7a, EE-7b, and EE-7d ride G30. This specification
   does not invent EE numbers."

**Axis B — live-cell retained-evidence members (the fate/pass axis).** Five, none collapsible.

4. `…preview.v3#$.retainedEvidence[0].memberVerbatim` = "admission-time excluded forms refused with
   no ExecutionId" — passProperty "Admission-time excluded forms are refused with no ExecutionId.
   Silent admission fails."
5. `…preview.v3#$.retainedEvidence[1].memberVerbatim` = "represented post-admission substitutions
   rejected before any stage" — passProperty "Represented post-admission substitutions are rejected
   before any stage."
6. `…preview.v3#$.retainedEvidence[2].memberVerbatim` = "v1-slice §7 item 8" — passProperty "The
   cited item equals the pinned v1-slice section 7 item 8 verbatim above. Inventing a new recipe,
   including a section 7.1 recipe, fails."
7. `…preview.v3#$.retainedEvidence[3].memberVerbatim` = "not G21 sandbox" — passProperty "This gate
   is not G21 sandbox."
8. `…preview.v3#$.retainedEvidence[4].memberVerbatim` = "not G23 provider FactCandidate/Coverage" —
   passProperty "This gate is not G23 provider FactCandidate/Coverage."

**Axis C — excluded forms enumerated inside EV-1 (the mutation/input axis).** Six, enumerated
verbatim inside `…preview.v3#$.retainedEvidence[0].exactByteIntent`:

9. "publisher neither first-party nor explicitly trusted"
10. "public-lifecycle/discovery"
11. "contributor authority claims"
12. "untrusted native/WASM"
13. "contribution hooks/roots/probes"
14. "network-granted analysis"

The live claim (`$.claimVerbatim`) lists **seven** excluded forms; the seventh, "represented
pre-stage imperative substitution", is not in EV-1's six — it is EV-2 / EE-5b. That split is
explicit in `…preview.v3#$.retainedEvidence[1].exactByteIntent` ("post-admission / pre-stage
substitution-mutation records (EE-5b)").

**Axis D — named initial states (already fixed by the INPUT layer).** Eight, at
`g29-input-corpus.v1.json#$.initialStates[0..7]` (table in §1.5). Mapping to Axis B: six feed EV-1,
one feeds EV-2, one feeds EV-3. `EV-4` and `EV-5` have **no named initial state** in the input
corpus; `g29-input-corpus.v1.json#$.whatIsAuthored` names only "six excluded-form admission cells,
EE-5b post-admission/pre-stage substitution, and the existing v1-slice section 7 item 8 record".

**Axis E — fates.** Two, both from `$.waiverRuleVerbatim` = "pass all; no waiver for silent
admission" and from the passProperties: refusal-with-no-ExecutionId (EV-1) and
rejection-before-any-stage (EV-2). `$.failsIf[11]` adds "live G29 harness cell is not quoted
byte-exact" as a corpus-level fate.

**Axis F — platforms.** Named as a quantifier, not enumerated on this gate.

15. `…preview.v3#$.proposedLaterWork[2]`: "A later D-000 cycle may author and independently pin
    fixture implementations for EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, EE-6a and the five live-cell
    members, on the four D-002 platforms. This v3 occupancy does not invent those bytes."

The four tokens are absent from this occupancy (no `$.platforms`, no `$.windowsStanding`). This
axis's members are **not enumerated anywhere in the G29 record** — that is OQ-G29-2, not a number
supplied here.

**Arithmetic.**

- 8 named initial states (Axis D) → 8 payloads covering EV-1 (6), EV-2 (1), EV-3 (1).
- 2 not-that-gate records (EV-4, EV-5), each required by its own
  `…preview.v3#$.retainedEvidence[i].exactByteIntent` opening "Once authored: a not-that-gate record
  …", with no named initial state in the input corpus. (G29's `$.failsIf` has no
  omitted-retained-evidence-member entry; the sibling gate does, at
  `harness.DR-G30…preview.v2.json#$.failsIf[2]`: "a live G30 retained-evidence member is omitted or
  has no exactByteIntent / passProperty".)
- 8 + 2 = **10 corpus cells** at first authoring, with no platform multiplier applied.
- Per-platform copy version, if a later cycle applies Axis F: 10 x 4 = 40 copies. The platform token
  list is not enumerated on this gate, so the 4 is `$.proposedLaterWork[2]`'s own word "four", not a
  list this record can reproduce.

**Total coverage members: 10** (8 named initial states + 2 not-that-gate records), first authoring,
no platform subdirectories. **40** if and only if a later per-platform copy version resolves
OQ-G29-2.

## 3. Prohibitions bounding the authoring

**From the GATE join** `g29-leftover-join.v4.json#$.doesNot` (18 entries), verbatim:

```
[0]  Does not SATISFY DR-117.
[1]  Does not reopen leftover-design of EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, or EE-6a.
[2]  Does not take over G21, G23, G24, or G30.
[3]  Does not open D-056 Class A.
[4]  Does not close leftover-design of fixture implementations.
[5]  Does not close OBL-G29-FX-AUTHORING.
[6]  Does not invent a D9 code.
[7]  Does not invent a section 7.1 recipe.
[8]  Does not add a DR-G* row.
[9]  Does not change live required-now 28.
[10] Does not execute fixtures.
[11] Does not claim QUALIFIED.
[12] Does not edit file 08.
[13] Does not authorize docs/v2/implementation/.
[14] Does not invent fixture bytes.
[15] Does not occupy the identifier.
[16] Does not record leftover-join.v3 as current after this successor is recorded.
[17] Does not record occupancy v2 as current occupancy.
```

Also `g29-leftover-join.v4.json#$.obligations[3].reason`, second sentence-group: "This join does not
invent those fixture bytes and does not invent a D9 code or a section 7.1 recipe."

**From the occupancy** `harness.DR-G29…preview.v3.json#$.doesNot` (22 entries), verbatim:

```
[0]  Does not SATISFY DR-117.
[1]  Does not claim QUALIFIED.
[2]  Does not execute fixtures.
[3]  Does not invent a section 7.1 recipe.
[4]  Does not invent a D9 code.
[5]  Does not take over G21, G23, G24, or G30.
[6]  Does not mint Rust-as-core.
[7]  Does not change live required-now 28.
[8]  Does not authorize docs/v2/implementation/.
[9]  Does not SATISFY DR-131.
[10] Does not SATISFY DR-133.
[11] Does not close leftover-design of OBL-G29-FX-AUTHORING.
[12] Does not execute EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, or EE-6a by existing.
[13] Does not treat naming v6 as naming G29.
[14] Does not rewrite naming-v6 bytes.
[15] Does not record frozen v2 as a current occupancy remasurement.
[16] Does not unwrite D-157, D-204, or D-228.
[17] Does not claim that no leftover-join exists for this gate.
[18] Does not add a COORD heading.
[19] Does not assign a D-* number.
[20] Does not author fixture bytes.
[21] Does not treat D-157 turn-1 Codex OBJECT as the accepted naming parent.
```

**From the occupancy** `harness.DR-G29…preview.v3.json#$.failsIf` — the 32 entries that bound a
successor. The ones that bind fixture authoring directly, verbatim with index:

```
[4]  leftover-design of DR-117 EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, EE-6a as unnamed remainders is treated as reopened, or as closed by this file rather than by D-157
[11] live G29 harness cell is not quoted byte-exact
[13] this specification is read as executing fixtures
[14] this specification is read as authoring fixture bytes
[15] EE-1, EE-2, EE-3b, EE-4, EE-5a, EE-5b, or EE-6a is treated as executed by this file existing
[16] EE-7a, EE-7b, or EE-7d is treated as a G29 class
[17] a section 7.1 recipe is invented
[18] G21, G23, G24, or G30 is taken over
[19] QUALIFIED is claimed
[20] SATISFIED is claimed, including SATISFIED of DR-117
[31] CLAUDE-G29-V1-SF1 is not landed (v1-slice section 7 item 8 is not pinned with path, digest, and verbatim, or a section 7.1 recipe is invented)
```

`$.failsIf[31]` is the sharpest authoring constraint: the corpus must pin `docs/coop/v1-slice.md`
with **path, digest, and verbatim** and must not substitute a recipe of its own.

**From the retained-evidence passProperties** (each is a prohibition when negated):
`$.retainedEvidence[2].passProperty` — "Inventing a new recipe, including a section 7.1 recipe,
fails."; `$.retainedEvidence[0].passProperty` — "Silent admission fails."

**From the INPUT corpus** `g29-input-corpus.v1.json#$.whatIsNotAuthored` = `["fixture bytes","a
section 7.1 recipe","a D9 code","EE-7a, EE-7b, or EE-7d (G30)"]`, and `#$.doesNot` (11 entries)
which repeats "Does not invent a section 7.1 recipe.", "Does not invent a D9 code.", "Does not
author fixture bytes.", "Does not take over G21, G23, G24, or G30."

**From `IMPLEMENTATION-FREEZE.md` §7.1** (`e809d4395f394a507c36c23c069d566c838b2283c931b6d2d13797be53406dbd`),
line 1681 and line 1711, verbatim:

> "Naming them makes escalation compliant. It does not make them optional and
> it does not authorise anyone to invent one."

> "Every row above must be closed by a binding artifact before signature. None may
> be closed by this record, by the blueprint, by a checker, or by an implementer."

**From the D9 class list** `docs/coop/artifacts/d9-exit-contract.v1.14.json`
sha256 `8dd3303855f49bfdbb2751ee65f54a906405f0654159ebe815472f73cdf7da31`,
`$.codeVocabulary.closed` = `true`, `$.codeVocabulary.rule`:

> "A code outside this vocabulary is a contract violation. Codes are grouped by remedy: two codes
> with the same remedy are a smell, two remedies behind one code is a defect."

**The D-293 constraint clause that applies to this gate.**
`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md`
sha256 `f530cedca1c799097ed0fc30cf8ec6f0480abe9a56d495236909a3a23b84fc33`, line 3, verbatim:

> "Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new
> D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep
> their recorded prohibitions (adapter, CI encoding, journal)."

and line 6 (the default policy), verbatim:

> "**Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit
> platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved;
> for delegated byte sets, concrete witness bytes may be selected only within already-recorded
> schemas and fates, and any choice that would create a new semantic member, identifier, value, list
> or implementation stays outside the grant (recorded as a named open decision instead)."

## 4. Dependencies

**(a) Sequencing dependency — DR-117 Class A opening precedes G29 fixture authoring.**
COORD `docs/coop/COORDINATOR-DECISIONS.md`
sha256 `1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, heading
`## D-293 — User decisions: adopt the agreed recommendations on DECISIONS-NEEDED A–F` (line 16169),
Decision 5 at line 16228, verbatim:

> "5. **B1 (DR-131), B2 (DR-133), B3 (DR-117).** The agreed
>    programmes are authorized in the agreed order — for DR-117:
>    `preview-product-boundary-successor.v9` re-citing the twelve
>    current joins and stating its relationship to
>    `product-boundary-successor-contract.v8` (D-116); a fresh
>    application-grade dual review bound to v9's final digest; then
>    the owner-controlled opening entry; then G29/G30 fixture
>    authoring; then a separate SATISFIED-GRADE + MF-6 cycle. The
>    D-056 Class A openings themselves are separate owner-controlled
>    entries; this entry opens none of them."

The same programme in `DECISIONS-RECOMMENDED.md` (repo root)
sha256 `42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`, heading
`## B3 — DR-117 Class A opening (preview-product-boundary-successor.v8)` (line 164),
`### Recommendation (Claude, round 2)` (line 171), line 174, verbatim:

> "**All four amendments adopted. Agreed recommendation:** Option C with **both C1 and C2 on the
> current record, C1 first**: author `preview-product-boundary-successor.v9` re-citing the twelve
> current joins and stating its relationship to `product-boundary-successor-contract.v8` (D-116);
> then a fresh application-grade dual review bound to v9's final digest; then the owner-controlled
> opening entry (which lifts D-137's express reservation with the owner's word and restates the
> v9/contract.v8 relationship); then G29/G30 fixture authoring (D1); then a separate DR-117
> SATISFIED-GRADE + MF-6 cycle. The shared gate-2 entry carries a distinct DR-117 finding."

Measured state of that chain at HEAD:

- `preview-product-boundary-successor.v9.json` **does not exist** in `docs/coop/artifacts/`. The
  lineage head on disk is `preview-product-boundary-successor.v8.json`
  sha256 `f2e788e51c347e1033073f0718e701d164affe51e7f667da9bcd49a08837144c`, `$.status` =
  `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`, `$.binds` =
  `"NOTHING"`.

  > **Correction, remeasured 2026-08-28 13:14 (same HEAD `8bc9963`).** The bullet above was true
  > when measured. It is now stale: a concurrent session authored
  > `docs/coop/artifacts/preview-product-boundary-successor.v9.json` at 12:58 on 2026-08-28,
  > sha256 `e0221a1c095f688dcd5b127bce9f712543165599c71dc8415b94fb7bfdea4dd5`, untracked, with
  > `$.status` = `"CANDIDATE-NOT-APPLIED"`, `$.reviewStatus` = `"AWAITING-INDEPENDENT-REVIEW"`,
  > `$.binds` = `"NOTHING"`. **Both independent reviews returned REJECT**
  > (`preview-product-boundary-successor.v9.review-independent.claude2.json` and
  > `.review-independent.codex.json`), and there is no
  > `^## D-.*preview-product-boundary-successor.v9` heading in COORD, so v9 is neither accepted nor
  > recorded. The dependency and the standing below are unchanged, and on these bytes are
  > strengthened rather than weakened: the first step of D-293 Decision 5's DR-117 order has been
  > attempted and has not yet passed review.
- The owner-controlled opening entry is unrecorded; D-293's own closing words at COORD line 16300,
  verbatim: "It does not open D-056 Class A."
- The G29 occupancy records the same standing at `$.eligibilityEffect`: "A section 7.1 recipe is not
  invented. Class A is not opened. DR-117 is not SATISFIED." The sibling G30 occupancy states the
  reservation's source explicitly at `harness.DR-G30…preview.v2.json#$.dr117Split`: "Class A remains
  reserved on v5 (D-137)."

This is an **ordering** dependency, not a byte dependency: nothing in the G29 corpus needs a v9 byte
to exist. It bars *proceeding*, not *constructing*.

**(b) Byte dependency — a section 7.1 parked identity recipe, if and only if a cell needs one.**
The prohibition is `…preview.v3#$.doesNot[3]` and `$.failsIf[17]`/`[31]`. The dependency sentence,
`IMPLEMENTATION-FREEZE.md` line 1711, verbatim:

> "Every row above must be closed by a binding artifact before signature. None may
> be closed by this record, by the blueprint, by a checker, or by an implementer."

Measured effect on this corpus: every G29 exactByteIntent asks for **"digest-pinned"**
representations and for **negative** identity facts — EV-1 requires "a record that no ExecutionId
was minted", EV-2 requires "rejection before any stage runs", EV-3 requires a record equal to the
pinned v1-slice sentence. None of the five requires *producing* a RunId, EvidenceDigest, Finding
fingerprint, FactViewId, cache key, `executionPlanCommitment` or `subjectScopeCommitment` value. The
"digest-pinned" wording is satisfied by a SHA-256 over the payload bytes, which is the recorded
practice at `g23-fixture-corpus.v4.json#$.authoredCatalog.members[i].payloadSha256` and
`g21-fixture-corpus.v1.json#$.authoredCatalog.members[i].sha256`. So §7.1 is a live **prohibition**
here, and **not** an unmet byte dependency. See OQ-G29-4 for the one place this could flip.

**(c) Byte dependency — a D9 code. Not blocking.** The prohibition is `…preview.v3#$.doesNot[4]`
("Does not invent a D9 code"). The class list exists and is closed:
`docs/coop/artifacts/d9-exit-contract.v1.14.json`
sha256 `8dd3303855f49bfdbb2751ee65f54a906405f0654159ebe815472f73cdf7da31`, pinned live by file 08
line 40 (DR-007 source-pin cell: "`d9-exit-contract.v1.14`
`8dd3303855f49bfdbb2751ee65f54a906405f0654159ebe815472f73cdf7da31`"). `$.codeVocabulary.closed` =
`true`; `$.codeVocabulary.errorCodes` contains `"EXTENSION.ADMISSION_REJECTED"`;
`$.exitClasses[2]` is `{"class":"request-rejected","code":2,"meaning":"Usage, configuration,
admission, compatibility, lookup-address, resolver failure, mutation-precondition rejection, or an
admitted Run refused before final commit because a HOST-DERIVED finalization precondition proved its
frozen authoritative target unsatisfiable."}`. No G29 retained-evidence member names a D9 code at
all; the corpus can be authored without touching one, and if a witness cell wants a typed outcome
token it selects from the closed vocabulary rather than minting. Not blocking.

**(d) Axis dependency — the platform token list. Unresolved.** `…preview.v3#$.proposedLaterWork[2]`
names "the four D-002 platforms" but the occupancy carries no `$.platforms` array and no
`$.windowsStanding`. The recorded practice for a gate whose own occupancy lacks the array is
`g21-fixture-corpus.v8`, which quoted G10 occupancy v2 and checked ORDERED-EQUAL against G23
occupancy v2. The G29 record does not say which occupancy G29 must quote. This blocks only the
*per-platform copy version*, not the first authoring (`g21-fixture-corpus.v1` authored payloads with
no platform subdirectory). See OQ-G29-2.

**(e) Not a dependency.** There is no reserved list, reserved number, undecided threshold, other
gate's corpus, or unauthored schema that the ten G29 cells require. `$.basedOn.inputCorpus` is
already Claude ACCEPT 0/0; the classes are defined at
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[]`; the v1-slice sentence
is pinned at a digest that re-verifies at HEAD.

**Standing: BLOCKED-ON-DR-117-CLASS-A-OPENING (sequencing)**

## 5. Recommended artifact shape

**Closest precedent: `docs/coop/artifacts/g21-fixture-corpus.v1.json`**
sha256 `861bb4e7d26a80158cc1cc3a0518c5e8e95311bee4d8c8ce63acd1e60d6c906d` (D-241), the
**first-authoring** shape — payloads under `docs/coop/artifacts/fixtures/g21.v1/` with **no platform
subdirectory**, and a `failsIf` roster alongside `doesNot`. Its per-platform successor
`g21-fixture-corpus.v8.json` sha256 `e8149a865e49bdcda9eda923e9918f332a83078f43ab6a3af9a10d6d31ef6359`
(D-247) is the separate later version, and `g23-fixture-corpus.v4.json`
sha256 `b3fce9f5bab6764919f5dc43c28a43f3d9c3b6be310e45c2c1bd08a617c755c5` (D-239) is the
per-platform-copy shape.

G29 is a **first authoring**: no G29 fixture corpus of any version exists in
`docs/coop/artifacts/`, and OQ-G29-2 leaves the platform token list unresolved, which the
first-authoring shape does not need.

Top-level fields carried by `g21-fixture-corpus.v1.json` (verified key list):

`artifact, version, date, documentClass, registerRow, registerRowNote, namedGate, status,
reviewStatus, sealRecommendation, binds, authorityClaim, purpose, basedOn, parentReview, file08Pin,
head, requiredNowUnchanged, file08StatusToken, cborConstruction, pinnedHostContext, whatIsAuthored,
whatIsNotAuthored, authoredCatalog, leftoverDesignClosedIfAcceptedAndRecorded,
leftoverDesignRemainingOnG21, remainderAfterThisCorpus, summary, doesNot, failsIf, proposedLaterWork,
recordedInputs, remeasurementClause`

`$.authoredCatalog` keys: `standing, doesNotMutate, executionRemains, members`.
`$.authoredCatalog.members[i]` keys: `id, class, inputCorpusId, path, sha256, mutation, expected`.

For comparison, `g23-fixture-corpus.v4.json` top-level adds
`platformsQuotedFromOccupancyV2, windowsStandingQuotedFromOccupancyV2` and its
`$.authoredCatalog` keys are `standing, doesNotMutate, executionRemains, payloadCount (4),
platformCount (4), copyCount (16), members`, with
`$.authoredCatalog.members[i]` keys `id, class, inputCorpusId, payloadSha256, snapshotMembership,
mutation, expected, platformCopies`. Those two platform fields are exactly what a G29 successor
**cannot** fill until OQ-G29-2 resolves.

**Proposed artifact name:** `docs/coop/artifacts/g29-fixture-corpus.v1.json`
**Proposed fixture directory:** `docs/coop/artifacts/fixtures/g29.v1/<id>.json`, mode 0444, no
platform subdirectory (first-authoring shape).

**G29-specific fields the successor should carry beyond the v1 skeleton**, each traceable to a
governing byte rather than invented:

- `v1SlicePin` — path + sha256 + selector + verbatim, mirroring
  `…preview.v3#$.section7Item8Standing.pin`, because `$.failsIf[31]` bites on exactly that.
- `retainedEvidenceCoverage` — a member-by-member map EV-1..EV-5 → authored ids, because
  `$.retainedEvidence` has five members and none may be dropped.
- `platformAxisNotAuthored` — an explicit statement that the four D-002 platform tokens are not
  quoted here because the G29 occupancy carries no `platforms` array (OQ-G29-2), so the omission
  reads as a recorded open question rather than a silent drop.
- `doesNotInventSection71Recipe` / `doesNotInventD9Code` — the two named prohibitions, restated the
  way `g29-input-corpus.v1.json#$.whatIsNotAuthored` restates them.
- `leftoverDesignRemainingOnG29` — must remain non-empty unless a per-platform successor and a
  second join land; see the reviewer-attack pattern in §6.

## 6. Effort and risk

**Number of cases:** 10 payload cells at first authoring (8 named initial states + 2 not-that-gate
records). Zero platform copies in v1. A later per-platform version would be 40 copies if OQ-G29-2
resolves to four tokens.

**(a) Witness-byte selections inside recorded schemas — permitted under the grant.**

1. The concrete well-formed representation of each of the six excluded forms (Axis C), so long as it
   is "otherwise well-formed" per `preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[0].input`
   and the analogous `input` strings at `classes[1]`, `[3]`, `[4]`, `[5]`, `[7]`.
2. The concrete pre-stage substitution payload for EE-5b, within
   `classes[6].input` ("A PlanIntent, manifest, or protocol frame that substitutes an imperative
   contribution (not merely a named hook/root/probe) after admission but before any stage executes.").
3. The serialization of each payload and its `sha256` over those bytes (the precedent's
   `payloadSha256` / `sha256` field).
4. Fixture ids, following the `g29-input-corpus.v1.json#$.initialStates[i].id` names already
   recorded (`G29.ee.*`, `G29.ee5b.*`, `G29.v1-slice-section-7-item-8`) rather than minting a new
   naming scheme.
5. The two not-that-gate records for EV-4 and EV-5, whose content is fixed by their own
   exactByteIntent ("a not-that-gate record that this refusal is admission/pre-stage validation, not
   G21 process-tree containment" / "…not G23 FactCandidate/Coverage admission").
6. `mutation` and `expected` values drawn from the recorded passProperties, not composed anew.

**(b) Choices that would create new semantics — record as a named open decision, do not choose.**

1. **Enumerating the four D-002 platform tokens.** Violates the D-293 default policy clause "any
   choice that would create a new semantic member, identifier, value, list or implementation stays
   outside the grant", and `g21-leftover-join.v13.json#$.obligations[3].reason` records the parallel
   prohibition "does not invent a D-002 platform list". Record as a named open decision, do not
   choose.
2. **Supplying a derivation for any identity/digest/commitment/ref-typed field** (RunId,
   ExecutionId, EvidenceDigest, Finding fingerprint, FactViewId, cache key,
   `executionPlanCommitment`, `subjectScopeCommitment`). Violates `…preview.v3#$.doesNot[3]`,
   `$.failsIf[17]`, `$.failsIf[31]`, and `IMPLEMENTATION-FREEZE.md` §7.1 line 1711. Record as a
   named open decision, do not choose.
3. **Minting a D9 code, exit number, or HostTermination for a refusal.** Violates
   `…preview.v3#$.doesNot[4]`, `g29-leftover-join.v4.json#$.doesNot[6]`, the D-293 constraint "no …
   new D9/HostTermination/pack semantics … is authorised", and
   `d9-exit-contract.v1.14.json#$.codeVocabulary.rule`. Record as a named open decision, do not
   choose.
4. **Collapsing EV-1 into EV-2, or EV-4/EV-5 into the corpus classes.** `$.retainedEvidence` keeps
   five members separate; the sibling gate states the rule explicitly at
   `harness.DR-G30…preview.v2.json#$.liveCellMemberSplit.notCollapsed`. Record as a named open
   decision, do not choose.
5. **Classifying any witness under EE-7a, EE-7b, or EE-7d.** `…preview.v3#$.failsIf[16]`: "EE-7a,
   EE-7b, or EE-7d is treated as a G29 class". Those ride G30. Record as a named open decision, do
   not choose.
6. **Inventing an EE number, or an FX/G29-* identifier scheme.** `$.namedExecutesLimbNote`: "This
   specification does not invent EE numbers." Record as a named open decision, do not choose.
7. **Claiming `leftoverDesignClosedIfAcceptedAndRecorded: ["OBL-G29-FX-AUTHORING"]`.** Ten cells at
   first authoring do not exhaust the corpus while the platform axis is unresolved and EV-4/EV-5
   have no named initial state. Record as a named open decision, do not choose.

**Risk.** The reviewer-attack pattern on the two recorded corpus lineages was (a) inventing a
vocabulary or encoding the record does not carry, (b) collapsing two classes the occupancy keeps
separate, (c) classifying a witness under a closed identifier it does not belong to, (d)
over-claiming closure. G29's exposure maps onto all four. On (a), the sharpest trap is the
"digest-pinned" wording in every exactByteIntent: `g23-fixture-corpus.v1.review-independent.claude2.json`
was REJECTed on "M2 — coverageCommitment is a SHA-256 over compact JSON, not deterministic-CBOR, and
its construction is undeclared", so the G29 successor must declare its digest construction the way
`g21-fixture-corpus.v1.json#$.cborConstruction` does, or a reviewer will find the same defect. On
(b), the EV-1/EV-2 split at the seventh excluded form ("represented pre-stage imperative
substitution" belongs to EV-2, not EV-1) is exactly the shape of the `g23-fixture-corpus.v2` reject
("M1 — the EV-2 narrows fixture also converts unknown to covered, collapsing EV-2 into EV-3 against
the governing occupancy's explicit non-collapse requirement"). On (c), the closed identifier here is
the EE set: putting a witness under EE-3a, EE-6b, EE-7c or EE-7e — all of which exist at
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.classes[]` and none of which is a
G29 class — reproduces the `G21FXV3-M1` defect ("The non-object-top-level payload is an RF-2 case
but is not authorized as a member of the closed CC-5 corpus"). On (d), `$.failsIf[1]` already names
the over-claim: "leftover-design of OBL-G29-FX-AUTHORING is claimed closed by this file existing".

## 7. Open questions

**OQ-G29-1:** Which occupancy does the obligation's own reason cite?
`g29-leftover-join.v4.json#$.obligations[3].reason` opens "G29 v2 names unauthored fixture cells …",
while the same file's `$.basedOn.occupancyV3.role` says "Current G29 occupancy remasurement" of v3
and `$.purpose` says "Cite occupancy v3 as the current occupancy remasurement." The reason string
was carried forward from `g29-leftover-join.v3` without re-pointing. A successor must decide whether
it quotes exactByteIntent from occupancy v3 (the current remasurement, dual ACCEPT 0/0) or from v2
(the artifact the obligation reason names). The bytes point both ways.

**OQ-G29-2:** Which occupancy would a G29 corpus quote the four D-002 platform tokens from?
`harness.DR-G29…preview.v3.json` has no `$.platforms` and no `$.windowsStanding`; its
`$.proposedLaterWork[2]` says "on the four D-002 platforms" without enumerating them. The recorded
practice at `g21-fixture-corpus.v8` was to quote G10 occupancy v2 and check ORDERED-EQUAL against
G23 occupancy v2 (`harness.DR-G23.provider-well-formed-admission.preview.v2.json`
sha256 `f48ba637bdf193785c05906a1686ce268b27b6ce7355de07fa5effefdd84fb0b`, `$.platforms` =
`["macos/arm64","macos/x86_64","linux/x86_64","linux/arm64"]`). The G29 record names no source. This
does not block a first authoring; it blocks a per-platform copy version.

**OQ-G29-3:** Which byte form of the v1-slice §7 item 8 sentence must EV-3 reproduce?
`preview-product-boundary-successor.v5.json#$.enforcementEvidence.v1SlicePin.verbatim` carries
markdown backticks (`` `PlanIntent` ``, `` `ExecutionId` ``, `` `AttemptRecord` ``,
`` `ExecutionPlan` ``); `harness.DR-G29…preview.v3.json#$.section7Item8Standing.pin.verbatim` and
`$.retainedEvidence[2].exactByteIntent` carry the identical sentence with the backticks removed.
`$.failsIf[31]` requires the item to be "pinned with path, digest, and verbatim" and
`$.retainedEvidence[2].passProperty` requires "The cited item equals the pinned v1-slice section 7
item 8 verbatim above." The two "verbatim" strings in the record are not byte-equal to each other,
so "equals … verbatim" is ambiguous about which one is the target.

**OQ-G29-4:** Does any G29 cell require a parked §7.1 identity recipe after all?
`…preview.v3#$.retainedEvidence[0].exactByteIntent` requires "a record that no ExecutionId was
minted". A *negative* record needs no derivation rule. But `IMPLEMENTATION-FREEZE.md` §7.1's
governing property (line 1728) parks "Any identity-, digest-, commitment- or ref-typed field for
which no binding artifact states a rule producing its value from real inputs", and the record does
not say whether an admission-refusal fixture must carry any positive identity field (for example a
request id or an AttemptRecord absence proof carrying a typed key). If a chosen witness turns out to
need one, that cell becomes BLOCKED-ON a §7.1 escalation rather than a witness selection.

**OQ-G29-5:** What initial states supply EV-4 and EV-5?
`g29-input-corpus.v1.json#$.whatIsAuthored` names eight states covering EV-1 (six), EV-2 (one) and
EV-3 (one). `harness.DR-G29…preview.v3.json#$.retainedEvidence[3]` and `[4]` each say "Once
authored: a not-that-gate record …" and each is `"suppliedBy": "excluded-form admission corpus
class. Not authored."` — but no initial state was named for either. Whether the two not-that-gate
records are additional corpus cells or annotations on the existing eight is not settled by the
record.

**OQ-G29-6:** Is `preview-product-boundary-successor.v5` still the governing class definition?
`harness.DR-G29…preview.v3.json#$.productLawOwner` pins v5. The lineage head on disk is v8
(sha256 `f2e788e51c347e1033073f0718e701d164affe51e7f667da9bcd49a08837144c`), and D-293 Decision 5
orders a **v9** before the opening entry and before G29 fixture authoring. Whether a G29 corpus
authored after v9 lands must re-cite v5 (the occupancy's pin) or v9 (the then-current contract) is
not stated anywhere in the G29 record.
