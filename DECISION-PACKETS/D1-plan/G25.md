# G25 — OBL-G25-FX-AUTHORING — D1 fixture-authoring plan record

Recorded at HEAD `8bc9963f68784842de643d5dbb1269bd4cf4411a`. This is an inventory of already-recorded
bytes. It authors no fixture byte, records no successor, and decides nothing.

## 1. Governing bytes

### 1.1 GATE leftover-join

Path: `docs/coop/artifacts/g25-leftover-join.v5.json`
sha256: `9f2b137fe0b01830b4113ef26c8283214a75982f588f164391d61c5510f67aa3`

| field | value (verbatim) |
|---|---|
| `$.artifact` | `g25-leftover-join.v5` |
| `$.date` | `2026-08-23` |
| `$.registerRow` | `DR-G25` |
| `$.status` | `CANDIDATE-NOT-APPLIED` |
| `$.reviewStatus` | `AWAITING-INDEPENDENT-REVIEW` |
| `$.sealRecommendation` | `DO-NOT-SEAL` |
| `$.binds` | `NOTHING` |
| `$.head` | `825616145ea768a3394965f2e21129396e188e78` |
| `$.requiredNowUnchanged` | `28` |
| `$.file08StatusToken` | `OPEN` |
| `$.liveGateOwners.DR-G25` | `Product + CLI / output + semantic owners` |

`$.file08Pin` = `{"path": "docs/v2/architecture/08-decision-and-readiness-register.md", "sha256":
"e503b75b5599444ee4d77c0c7d7c82a74026b2c1848ad214e0ac738c0aa40c3e"}` — recomputed at this HEAD and
equal.

COORD heading that recorded this join (`docs/coop/COORDINATOR-DECISIONS.md` sha256
`1ee9def72c44acd96f36da3392d4980d0e06afb731b0a4003b5bde73247e136c`, line 12127, verbatim):

> `## D-249 — Record g25 leftover-join.v5 as G25 leftover remasurement`

`$.summary.leftoverDesign` = `["OBL-G25-FX-AUTHORING"]`. `$.summary.ntReadingsCollapsed` = `false`.

### 1.2 The obligation object (verbatim)

JSON path `$.obligations[3]`:

```json
{
  "id": "OBL-G25-FX-AUTHORING",
  "leftoverDesign": true,
  "existingGate": "none as authored implementations",
  "namedCorpusNotAuthored": [
    "missing-required-rung corpus (DR-131 NT-3)"
  ],
  "namedNt3Readings": [
    "NT-3.missing-required-rung",
    "NT-3.universe-unconstructible"
  ],
  "executionObligationOwnerToday": "none",
  "rideStanding": "not-capable-of-riding as execution-only remainder",
  "reason": "G25 v2 names two unauthored fixture cells of one NT-3 class. D-056 Decision clause 5: authoring fixtures remains design work, distinct from the harness specification, distinct from live-cell naming, and distinct from execution. This join does not invent those fixture bytes and does not collapse the two readings. The INPUT-state corpus exists and is Claude ACCEPT 0/0; that INPUT layer is OBL-G25-INPUT-CORPUS, leftoverDesign false. This join does not invent fixture bytes."
}
```

Called out explicitly:

- `$.obligations[3].reason` — quoted in full above. Its operative sentence for coverage is
  `G25 v2 names two unauthored fixture cells of one NT-3 class.` Its operative prohibition is
  `This join does not invent those fixture bytes and does not collapse the two readings.`
- `$.obligations[3].namedCorpusNotAuthored` = `["missing-required-rung corpus (DR-131 NT-3)"]`.
- `$.obligations[3].namedNt3Readings` = `["NT-3.missing-required-rung",
  "NT-3.universe-unconstructible"]`. **This field is unique to G25 among the five preview-analyze
  gates.** It is the byte that makes the two readings a coverage requirement rather than a stylistic
  choice.
- `remainingNotAuthored` / `namedCases` / `namedNotAuthored` — **absent** on this obligation.
- `$.obligations[3].existingGate` = `"none as authored implementations"` (the gate DR-G25 exists at
  file 08 line 361; the authored implementations do not).
- `$.obligations[3].rideStanding` = `"not-capable-of-riding as execution-only remainder"`.
- `$.obligations[3].executionObligationOwnerToday` = `"none"`. Contrast `$.obligations[4]`
  (`OBL-G25-EXECUTION`) whose owner is `"Product + CLI / output + semantic owners"`.

### 1.3 Occupancy file and the fields that name the fixture cells

Path: `docs/coop/artifacts/harness.DR-G25.preview-analyze-missing-rung.preview.v3.json`
sha256: `4f124cd763974b603fb307e13830cc7f79bc559c3b05ab7d39c59194d2f5dfde`
`$.artifact` = `harness.DR-G25.preview-analyze-missing-rung.preview.v3`

**`$.liveRowVerbatim`**

> `| DR-G25 PREVIEW-ANALYZE-MISSING-RUNG | A missing required TypeScript semantic rung is typed Coverage-indeterminate; silent syntax fallback fails | named: harness.DR-G25.preview-analyze-missing-rung.preview (D-151; not authored; not QUALIFIED). missing-required-rung corpus (DR-131 NT-3) | typed Coverage-indeterminate; no silent syntax fallback; not authoritative success | Product + CLI / output + semantic owners | PROPOSED; not QUALIFIED | pass all; no waiver for silent fallback | OPEN |`

Byte-identical to file 08 line 361.

**`$.liveHarnessCellVerbatim`**

> `named: harness.DR-G25.preview-analyze-missing-rung.preview (D-151; not authored; not QUALIFIED). missing-required-rung corpus (DR-131 NT-3)`

**`$.liveCellVerbatim`**

> `typed Coverage-indeterminate; no silent syntax fallback; not authoritative success`

**`$.claimVerbatim`**

> `A missing required TypeScript semantic rung is typed Coverage-indeterminate; silent syntax fallback fails`

**`$.waiverRuleVerbatim`**

> `pass all; no waiver for silent fallback`

**`$.namedCorpusNotAuthored`** = `["missing-required-rung corpus (DR-131 NT-3)"]`

**`$.namedCorpusClasses[0]` (the single NT-3 class, with its two cells)**

- `.id` = `NT-3`
- `.nameVerbatim` = `missing-rung-not-success`
- `.memberVerbatim` = `missing-required-rung corpus (DR-131 NT-3)`
- `.inputVerbatim` = `Required TypeScript semantic universe cannot be constructed.`
- `.passVerbatim` = `Typed Coverage-indeterminate. Not authoritative success. No silent syntax fallback.`
- `.source` = `preview-analyze-contract.v2 negativeTests.NT-3`
- `.registerFormVerbatim` = `A missing required TypeScript semantic rung is typed Coverage-indeterminate; silent syntax fallback fails`
- `.corpusTakesBothReadings` = `true`
- `.readingNote` (verbatim — this is the sentence that forbids collapse):

> `The register names a missing required TypeScript semantic rung. v2 names an unconstructible required TypeScript semantic universe. Those are two cells of this one NT-3 class, not two NT numbers. A fixture author must pin both. This specification does not collapse them.`

- `.cells[0]` = `{"id": "NT-3.missing-required-rung", "reading": "register-claim", "inputNamed": "a
  required TypeScript semantic rung is absent while other universe construction may still be
  possible"}`
- `.cells[1]` = `{"id": "NT-3.universe-unconstructible", "reading": "v2-input", "inputNamed":
  "Required TypeScript semantic universe cannot be constructed."}`
- `.exactByteIntent` (full, untruncated):

> `Once authored: two digest-pinned cells. Cell NT-3.missing-required-rung: analyze-request bytes whose required TypeScript semantic rung is absent. Cell NT-3.universe-unconstructible: analyze-request bytes for which the required TypeScript semantic universe cannot be constructed (v2 inputVerbatim). Each cell is paired with pinned Coverage and terminal-result bytes showing NT-3 passVerbatim. Fixtures are unauthored.`

- `.passProperty`:

> `NT-3 passVerbatim holds on both cells. Typed Coverage-indeterminate. Not authoritative success. No silent syntax fallback. Silent fallback fails. Waiver of silent fallback is forbidden.`

**`$.retainedEvidence[0]` (EV-1)**

- `.memberVerbatim` = `typed Coverage-indeterminate`
- `.exactByteIntent` (full, untruncated):

> `Once authored: digest-pinned analyze requests for both NT-3 cells (missing required rung; unconstructible required TypeScript semantic universe), paired with pinned Coverage bytes whose standing for that domain is indeterminate — not covered, and not omitted. Unknown-to-covered conversion is G23 and is not retained or scored here. Fixtures are unauthored.`

- `.passProperty`:

> `A missing required rung, and an unconstructible required TypeScript semantic universe, are typed Coverage-indeterminate. Silent omission of the domain fails. Unknown-to-covered conversion is G23, not this member, and is not retained here.`

- `.suppliedBy` = `NT-3 both cells. Not authored.`

**`$.retainedEvidence[1]` (EV-2)**

- `.memberVerbatim` = `no silent syntax fallback`
- `.exactByteIntent` (full, untruncated):

> `Once authored: the same pinned requests, paired with a record that no syntax-only evaluation ran in place of the missing rung and that no syntax-only result was emitted under the same check or Coverage id. Fixtures are unauthored.`

- `.passProperty`:

> `Silent syntax fallback fails. Waiver of silent fallback is forbidden.`

- `.suppliedBy` = `NT-3 both cells. Not authored.`

**`$.retainedEvidence[2]` (EV-3)**

- `.memberVerbatim` = `not authoritative success`
- `.exactByteIntent` (full, untruncated):

> `Once authored: the same pinned requests, paired with the terminal preview result showing the invocation is not labeled authoritative success. Sealed-Run labelling is G27 and is not retained or scored here. Fixtures are unauthored.`

- `.passProperty`:

> `A missing required rung, and an unconstructible required TypeScript semantic universe, are not authoritative success. Silent promotion / sealed-Run labelling is G27 and is not retained here.`

- `.suppliedBy` = `NT-3 both cells. Not authored.`

**`$.undeterminedVsIndeterminate`** (a semantic boundary the fixture must not blur):

> `Doctor UNDETERMINED is a check status about doctor's reach. Analyze Coverage-indeterminate is a Coverage-domain standing about a missing required semantic rung. Collapsing them is forbidden.`

**`$.namedExecutesLimbs`** = `["NT-3"]`
**`$.namedExecutesLimbNote`** = `Use only NT-3 as the live cell name. This is DR-131 NT-3, not DR-133 NT-3.`

**`$.discipline`**

> `SPECIFICATION OF EXACT-BYTE INTENT. Occupancy remasurement of the already-named identifier. No harness runs here. Naming is not execution. No fixture bytes authored. DR-131 NT-3 is not executed by this file existing. The two NT-3 cells are not collapsed. DR-133 NT-3 is G23, not this gate.`

**`$.liveGateOwner`** = `{"DR-G25": "Product + CLI / output + semantic owners"}`
**`$.executionObligationOwner`** = `Product + CLI / output + semantic owners`
**`$.productLawOwner`** = `DR-131 / preview-analyze-contract.v2`
**`$.palV1A1`** = `PAL-V1-A1: Product + CLI / output + semantic owners is the execution-obligation owner. Product-law ownership remains DR-131 / preview-analyze-contract.v2. This specification does not transfer product law onto the gate owner.`

**`$.proposedLaterWork[2]`**

> `A later D-000 cycle may author and independently pin fixture implementations for DR-131 NT-3 both cells and the three live-cell members, on the four D-002 platforms. This v3 occupancy does not invent those bytes.`

**Fields checked for and ABSENT on this occupancy:** `platforms`, `windowsStanding`, `passProperty`
(the gate-level roll-up G24 carries), `liveCellMemberSplit`, `d002Standing`. The only platform byte
is `$.proposedLaterWork[2]` above and `$.basedOn.d002.role` = `Language roles: TypeScript, alone.
This specification does not mint Rust-as-core and does not name G13 into required-now.` — which says
nothing about platforms.

**INPUT-state corpus** (`$.basedOn.inputCorpus`; measured `leftoverDesign: false` at
`g25-leftover-join.v5.json#$.obligations[2]`): `docs/coop/artifacts/g25-input-corpus.v1.json`
sha256 `d7dfef9504e3b2bb0378e6a18c9a4a44b1066a8eb88f1963a6e6051868aa1362`.
`$.status` = `CANDIDATE-NOT-APPLIED`; `$.binds` = `NOTHING`; `$.head` =
`5d5d77819ae3019d9e6e02f1e66de3d93c060402`; `$.requiredNowUnchanged` = `26` (stale against live 28).

`$.whatIsAuthored`:

> `Named initial states drawn from live G25 v2 exactByteIntent: NT-3.missing-required-rung and NT-3.universe-unconstructible. Two cells of one class, not two NT numbers. No fixture bytes.`

`$.initialStates` (2 items, both `.fixtureBytes` = `NOT-AUTHORED`):

1. `G25.nt3.missing-required-rung` — `.stateNamed` = `NT-3.missing-required-rung: analyze-request
   bytes whose required TypeScript semantic rung is absent`; `.source` = `G25 v2 exactByteIntent Cell
   NT-3.missing-required-rung`
2. `G25.nt3.universe-unconstructible` — `.stateNamed` = `NT-3.universe-unconstructible:
   analyze-request bytes for which the required TypeScript semantic universe cannot be constructed`;
   `.source` = `G25 v2 exactByteIntent Cell NT-3.universe-unconstructible`

`$.whatIsNotAuthored` = `["fixture bytes", "unknown-to-covered conversion (G23)", "sealed-Run
labelling (G27)", "a D9 code"]`.

### 1.4 ROW twin join (or: none)

**None.** No ROW-side leftover-join measures `OBL-G25-FX-AUTHORING`.

Measured: a search of `docs/coop/artifacts/*.json` (excluding `*.review-*`) for the literal
`OBL-G25-FX-AUTHORING` returns exactly six files, all in the G25 lineage:
`g25-leftover-join.v1.json` through `.v5.json` and `g25-input-corpus.v1.json`.

`DECISION-PACKETS/D1-fixture-authoring-delegation.md` sha256
`bc8484cc7159af26a142b97a55b1095049f7ea2ac10c283cd1e2428ba2569ea9`, section 3.2 (line 161) lists the
nine ROW twins and none is a G24–G28 id; line 152 of section 3.1 carries the G25 row with a final
column of `none`.

The cross-custody rule that would otherwise apply, verbatim from the end of that section 3.2:

> Closing a fixture obligation would require a successor on *both* the GATE join and its ROW twin, since the same id is measured true on both.

Because there is no ROW twin, closing `OBL-G25-FX-AUTHORING` needs a successor on
`g25-leftover-join` alone.

**A near-miss worth naming, since it is a real cross-lineage citation on this join.** G25's join
carries `$.basedOn.g23LeftoverJoinV8` and its `$.leftoverDesignOpenStanding` records
`leftover-design of OBL-G23-FX-AUTHORING is leftoverDesign [] on leftover-join.v8 (D-240) / DR-G23.
Remainder of G23 is execution.` That is a *sibling gate's* join cited for the NT-3 disambiguation
(see §3), not a ROW twin of this obligation, and `$.doesNot` includes `Does not take over G23.`

### 1.5 Governing contract/spec the occupancy pins

**Shared DR-131 contract, common to G24, G25, G26, G27 and G28.**

Path: `docs/coop/artifacts/preview-analyze-contract.v2.json`
sha256: `081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970`

Pinned by this occupancy at `$.basedOn.v2Contract.path` and at
`$.recordedInputs["docs/coop/artifacts/preview-analyze-contract.v2.json"]` (same digest). The role
string, verbatim (`$.basedOn.v2Contract.role`):

> `Accepted DR-131 design-contract candidate. Product law for NT-3 remains here. This specification does not replace v2.`

Contract standing in its own bytes: `$.registerRow` = `DR-131`; `$.status` =
`CANDIDATE-NOT-APPLIED`; `$.reviewStatus` = `AWAITING-INDEPENDENT-REVIEW`; `$.sealRecommendation` =
`DO-NOT-SEAL`; `$.binds` = `NOTHING`; `$.negativeTests.status` = `specified-classes-not-executed`.
`$.authorityClaim`:

> `This artifact PROPOSES the DR-131 preview non-authoritative analyze product contract. It applies nothing, edits no register row, seals nothing, marks nothing SATISFIED, and does not authorize docs/v2/implementation/. D-056 Class A is not opened. File 12 has no authority.`

**The NT-1..NT-8 definitions, verbatim, at `$.negativeTests`.** All eight are quoted here once
because the five preview-analyze gates share this one contract lineage.

| path | name | input | pass |
|---|---|---|---|
| `$.negativeTests.NT-1` | `user-or-third-party-pack-refused` | `An analyze request that names a pack other than opensip.preview.typescript.pack version 1, or supplies a user/third-party pack.` | `Request refused at admission. No evaluation.` |
| `$.negativeTests.NT-2` | `imperative-rule-refused` | `A pack or contribution that is not declarative-only.` | `Refused at admission. ARCH.PROBE-CONTRACT dependents stay excluded.` |
| `$.negativeTests.NT-3` | `missing-rung-not-success` | `Required TypeScript semantic universe cannot be constructed.` | `Typed Coverage-indeterminate. Not authoritative success. No silent syntax fallback.` |
| `$.negativeTests.NT-4` | `component-finding-not-analyze-finding` | `Provider payload that would be a finding under file 01's general may-return.` | `Refused under DR-133. Analyze findings, if any, come only from host-owned rules over admitted facts.` |
| `$.negativeTests.NT-5` | `sarif-not-advertised` | `A request or flag that advertises SARIF 2.1.0 for preview analyze.` | `Refused or not offered. D-077 drop stands.` |
| `$.negativeTests.NT-6` | `preview-result-not-sealed-run` | `A consumer treats the preview terminal result as a sealed authoritative Run.` | `The contract forbids that label. No silent promotion.` |
| `$.negativeTests.NT-7` | `host-must-not-mint-verdict` | `A host path that derives fail or warn other than from the core policyOutcome in CoreCompletion.` | `Refused. The only policy result is core-evaluated policyOutcome. Host does not mint fail/warn.` |
| `$.negativeTests.NT-8` | `host-must-not-mint-d9-or-termination-from-policy` | `A host path that mints a D9 class, exit number, or HostTermination from the pack threshold or from policyOutcome.` | `Refused. Host maps only existing D9 v1.14 classes. policyOutcome is not HostTermination.` |

**The class this gate owns: `$.negativeTests.NT-3`.** The occupancy's `$.namedCorpusClasses[0]`
reproduces that `name`/`input`/`pass` byte-for-byte and names its own source as
`preview-analyze-contract.v2 negativeTests.NT-3`.

**Contract sections that supply the semantics NT-3 tests.**

`$.missingRung` (verbatim, both fields):

- `.rule` = `An unavailable required semantic rung is typed Coverage-indeterminate.`
- `.forbidden` = `Silent syntactic fallback as if the missing rung were sufficient (C-1 / freeze law 3).`

`$.registerRowQuoted.obligationDecomposition[4]` (`OBL-5`):
`.cellFragmentVerbatim` = `typed-indeterminate on a missing required rung`;
`.answeredAt` = `["missingRung"]`;
`.readAs` = `A missing required semantic rung is typed Coverage-indeterminate. Silent syntax fallback is forbidden.`

`$.inputs.coverage` = `Only host-admitted Coverage. The provider does not choose sufficiency.`

**The register-form vs contract-form split that creates the two readings.** The contract's NT-3
`input` is `Required TypeScript semantic universe cannot be constructed.` The file 08 DR-G25 claim
cell (line 361, quoted in `$.claimVerbatim`) is `A missing required TypeScript semantic rung is typed
Coverage-indeterminate; silent syntax fallback fails`. Those two are the `register-claim` and
`v2-input` readings the occupancy names at `$.namedCorpusClasses[0].cells[0].reading` and
`.cells[1].reading`. Neither the contract nor the occupancy declares one to subsume the other; the
occupancy's `.readingNote` says `A fixture author must pin both.`

### 1.5b DR-131 NT-3 is a different NT-3 from DR-133 NT-3 (G23)

**Checked and confirmed: they are different classes on different contracts at different gates.**

| | DR-131 NT-3 (this gate, G25) | DR-133 NT-3 (G23) |
|---|---|---|
| contract | `docs/coop/artifacts/preview-analyze-contract.v2.json` sha256 `081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970` | `docs/coop/artifacts/provider-only-output-contract.v3.json` sha256 `ef2a7416700cc8197486c6e29450673c60e3b94512be3ad278d1995f7d281309` |
| JSON path | `$.negativeTests.NT-3` | `$.negativeTests.NT-3` |
| `name` | `missing-rung-not-success` | `fact-candidate-masquerade` |
| `input` | `Required TypeScript semantic universe cannot be constructed.` | `A FactCandidateV1 whose relationSchemaId or payload is provider-minted or mismatched so as to carry a findingLikeResult.` |
| `pass` | `Typed Coverage-indeterminate. Not authoritative success. No silent syntax fallback.` | `Host-owned relation registry rejects before fact admission. Value is not reinterpreted as a Finding.` |
| gate | DR-G25 (D-151), file 08 line 361 | DR-G23 (D-147), file 08 line 359 |

The governing bytes say so in four places:

1. `harness.DR-G25…preview.v3.json#$.namedExecutesLimbNote`:
   > `Use only NT-3 as the live cell name. This is DR-131 NT-3, not DR-133 NT-3.`
2. `harness.DR-G25…preview.v3.json#$.discipline` (last sentence):
   > `DR-133 NT-3 is G23, not this gate.`
3. `harness.DR-G25…preview.v3.json#$.failsIf[15]`:
   > `DR-133 NT-3 is treated as executing here`
4. `g25-leftover-join.v5.json#$.registerRowNote` (verbatim excerpt):
   > `Those NT-3 identifiers are row-local. This join does not take over G23, does not reopen D-151's NT-3 closure, does not invent fixture bytes, and does not SATISFY DR-131.`

The same distinction is drawn from the G23 side at
`preview-analyze-nt-gate-join.v2.json#$.g23Quoted.whyNotAssignedToNT1NT2NT3` (sha256
`4081c7400b3b9eae61089bb807140b4f75f5dd512b664c1f6657553a7da03813`), verbatim:

> `G23 owns provider FactCandidate and Coverage admission. It does not own pack-name admission, imperative-pack admission, or missing-rung Coverage-indeterminate typing.`

and at `preview-analyze-nt-gate-join.v2.json#$.classes[2].leftoverReason` (DR-131 NT-3), verbatim:

> `Well-formed analyze whose required semantic rung is missing. G21 Coverage goldens are post-fault containment. G23 Coverage class is domain mutation, not missing-rung indeterminate. G19 owns durable state-byte class, not Coverage typing.`

Practical consequence for the fixture: DR-133 NT-3's already-authored payload
`docs/coop/artifacts/fixtures/g23.v3/G23.nt3.factcandidate-findingLikeResult.json` (catalogued at
`g23-fixture-corpus.v3.json#$.authoredCatalog.members[0]`, sha256
`3576e2e606b3eed68feced5b83a34247263d3b563274ef3fd9054c8b2a2ba6a7` for the catalog) is **not** a G25
witness and must not be reused, copied, or cited as one.

**DR-131's own D-056 Class A standing (shared across all five gates).**
`DECISION-PACKETS/B1-DR-131-class-A.md` line 116, verbatim:

> **Gate 1 (Class A) — does NOT hold today.**

This join records the same at `$.proposedLaterWork[2]`, verbatim:

> `A later dedicated SATISFIED-GRADE cycle may test DR-131 only when D-056 five gates hold. Gate 1 Class A remains unopened. This join is not that cycle.`

Whether that is a *dependency for the fixtures* is answered in section 4.

## 2. Coverage set

**Axis A — named corpus classes.**

1. `NT-3` — `harness.DR-G25…preview.v3.json#$.namedCorpusClasses[0].id`;
   `g25-leftover-join.v5.json#$.namedClassesThisGate[0]`;
   `harness.DR-G25…preview.v3.json#$.namedExecutesLimbs[0]`. Exactly one class.

**Axis B — the two NT-3 readings (cells). This is the axis a reviewer will check first.**

2. `NT-3.missing-required-rung` — `…preview.v3.json#$.namedCorpusClasses[0].cells[0].id`;
   `.reading` = `register-claim`; `.inputNamed` = `a required TypeScript semantic rung is absent
   while other universe construction may still be possible`; also
   `g25-leftover-join.v5.json#$.obligations[3].namedNt3Readings[0]`.
3. `NT-3.universe-unconstructible` — `…preview.v3.json#$.namedCorpusClasses[0].cells[1].id`;
   `.reading` = `v2-input`; `.inputNamed` = `Required TypeScript semantic universe cannot be
   constructed.`; also `g25-leftover-join.v5.json#$.obligations[3].namedNt3Readings[1]`.

Mandatory, not optional: `…preview.v3.json#$.namedCorpusClasses[0].corpusTakesBothReadings` = `true`,
and `.readingNote` (full, untruncated, also quoted in §1.3):

> `The register names a missing required TypeScript semantic rung. v2 names an unconstructible required TypeScript semantic universe. Those are two cells of this one NT-3 class, not two NT numbers. A fixture author must pin both. This specification does not collapse them.`

**Axis C — live retained-evidence members (file 08 "Required retained evidence" cell, semicolon-split).**

4. `typed Coverage-indeterminate` — `…preview.v3.json#$.retainedEvidence[0].memberVerbatim`
5. `no silent syntax fallback` — `…preview.v3.json#$.retainedEvidence[1].memberVerbatim`
6. `not authoritative success` — `…preview.v3.json#$.retainedEvidence[2].memberVerbatim`

Each of the three carries `.suppliedBy` = `NT-3 both cells. Not authored.` — so all three are
required on both cells, not one per cell.

**Axis D — named initial states (INPUT layer, already accepted).**

7. `G25.nt3.missing-required-rung` — `g25-input-corpus.v1.json#$.initialStates[0].id`
8. `G25.nt3.universe-unconstructible` — `g25-input-corpus.v1.json#$.initialStates[1].id`

Note EV-2 and EV-3 both say `the same pinned requests` in their `exactByteIntent`, so the payload set
is two request files, not six.

**Axis E — mutations / transitions / fates.** No mutation axis is named. The fate is one per class,
`passVerbatim` = `Typed Coverage-indeterminate. Not authoritative success. No silent syntax
fallback.` The negative fate is `$.waiverRuleVerbatim` = `pass all; no waiver for silent fallback`.
One additional non-collapse quantifier applies to the *result typing*, not to a case:
`…preview.v3.json#$.undeterminedVsIndeterminate` = `Doctor UNDETERMINED is a check status about
doctor's reach. Analyze Coverage-indeterminate is a Coverage-domain standing about a missing required
semantic rung. Collapsing them is forbidden.`

**Axis F — platforms.** The occupancy has **no `platforms` array** and no `windowsStanding`. The one
platform byte is `…preview.v3.json#$.proposedLaterWork[2]`: `A later D-000 cycle may author and
independently pin fixture implementations for DR-131 NT-3 both cells and the three live-cell members,
**on the four D-002 platforms**.` (emphasis added; string verbatim). The four tokens are **not
enumerated anywhere in the G25 record**. The recorded practice for that situation is
`g21-fixture-corpus.v8.json` sha256 `e8149a865e49bdcda9eda923e9918f332a83078f43ab6a3af9a10d6d31ef6359`
(`$.g21OccupancyV4HasNoPlatformsArray` = `true`; `$.platformsQuotedFromG10OccupancyV2` =
`["macos/arm64", "macos/x86_64", "linux/x86_64", "linux/arm64"]`;
`$.g23OccupancyV2PlatformsOrderedEqual` = `true`; `$.failsIf[1]` = `a D-002 platform list is invented
rather than quoted from G10 occupancy v2`). Source arrays, both live and equal:
`harness.DR-G10.provider-conformance.ts-major-1.v2.json` sha256
`b0cbce06487b96bbe7f6af1dae62ba3b3ca55aaa41305cb96f531099e86bf7c9` `$.platforms`, and
`harness.DR-G23.provider-well-formed-admission.preview.v2.json` sha256
`f48ba637bdf193785c05906a1686ce268b27b6ce7355de07fa5effefdd84fb0b` `$.platforms`. See OQ-G25-1.

**Total coverage members: 2 first-authoring payloads; 6 (cell × evidence-member) obligations;
8 per-platform copies in a later copy version.**

Arithmetic:

- **Payloads (first-authoring, no platform subdirectory):** 2 named initial states = 2 analyze-request
  payload files (axis D). EV-2 and EV-3 add paired records over "the same pinned requests", not new
  requests.
- **Evidence obligations:** 2 NT-3 cells × 3 live retained-evidence members = **6**. Each of the three
  `retainedEvidence[*].suppliedBy` reads `NT-3 both cells`, so the product is required, not a choice.
  Adding the class-level `passProperty` (1, which itself says "holds on both cells") and the three
  member-level `passProperty` obligations gives 6 + 1 + 3 = 10 distinct pass predicates over 2
  payloads.
- **Per-platform copy version:** 2 payloads × 4 D-002 platforms = **8** copy files, following the
  `g21-fixture-corpus.v8` pattern (2 payloads × 4 platforms = 8 files) exactly.

The 2 and the 6 are determined by the record. The 8 depends on OQ-G25-1.

## 3. Prohibitions bounding the authoring

**From the GATE join `g25-leftover-join.v5.json`.**

`$.obligations[3].reason`:

> `This join does not invent those fixture bytes and does not collapse the two readings.`

`$.doesNot` (all 23 items, verbatim):

> `Does not SATISFY DR-131.` · `Does not reopen leftover-design of DR-131 NT-3.` · `Does not take over G23.` · `Does not collapse the two NT-3 readings.` · `Does not open D-056 Class A.` · `Does not close leftover-design of fixture implementations.` · `Does not close OBL-G25-FX-AUTHORING.` · `Does not invent a D9 code.` · `Does not name G13 into required-now.` · `Does not add a DR-G* row.` · `Does not change live required-now 28.` · `Does not execute fixtures.` · `Does not claim QUALIFIED.` · `Does not edit file 08.` · `Does not invent a section 7.1 recipe.` · `Does not authorize docs/v2/implementation/.` · `Does not record leftover-join.v3 as current after this successor is recorded.` · `Does not record leftover-join.v4 as current G23 leftover-join.` · `Does not record leftover-join.v1 as current G23 leftover-join.` · `Does not SATISFY DR-133.` · `Does not invent fixture bytes.` · `Does not record leftover-join.v4 as current.` · `Does not invent a finding identifier.`

`$.leftoverDesignOpenStanding` (verbatim):

> `The live DR-G25 token is OPEN. leftover-design of unnamed G25 initial states is stale as a naming claim after g25-input-corpus.v1. leftover-design of G25 fixture implementations remains. leftover-design of OBL-G23-FX-AUTHORING is leftoverDesign [] on leftover-join.v8 (D-240) / DR-G23. Remainder of G23 is execution. The two NT-3 cells are not collapsed. DR-131 is not SATISFIED. This join does not take over G23.`

**From the occupancy `harness.DR-G25…preview.v3.json`.**

`$.doesNot` — 24 items. The fixture-content ones, verbatim:

> `Does not author fixture bytes.` · `Does not collapse UNDETERMINED with Coverage-indeterminate.` · `Does not take over G23, G24, G26, G27, G28, or G30.` · `Does not mint Rust-as-core.` · `Does not name G13 into required-now.` · `Does not invent a D9 code or a section 7.1 recipe.` · `Does not treat DR-133 NT-3 as executing here.` · `Does not collapse the two NT-3 cells.`

`$.failsIf` — 32 items. The fixture-content ones, verbatim:

> `this specification is read as authoring fixture bytes` · `DR-131 NT-3 is treated as executed by this file existing` · `DR-133 NT-3 is treated as executing here` · `the two NT-3 cells are collapsed` · `doctor UNDETERMINED is collapsed with analyze Coverage-indeterminate` · `live G25 harness cell is not quoted byte-exact` · `CLAUDE-G25-V1-SF1 is not landed (EV-1 or EV-3 still retain a neighbouring gate's property as scored bytes)` · `CLAUDE-G25-V1-B1 is not landed (NT-3 name/input/pass not quoted, or the corpus class is absent, or the two readings are collapsed)`

The last two are the repaired defects of this lineage and are the sharpest constraints on the corpus:
EV-1 must not score unknown-to-covered conversion (that is G23) and EV-3 must not score sealed-Run
labelling (that is G27), per `$.retainedEvidence[0].passProperty` and `[2].passProperty`.

**From the contract `preview-analyze-contract.v2.json`.**

`$.doesNot` (verbatim, all nine):

> `Does not SATISFY DR-131.` · `Does not open D-056 Class A.` · `Does not invent §7.1 identity recipes.` · `Does not advertise SARIF.` · `Does not freeze public rule IR.` · `Does not rewrite DR-123.` · `Does not authorize docs/v2/implementation/.` · `Does not rewrite D-002 commands, platforms, or independent-release machinery.` · `Does not SATISFY DR-117 or DR-133.`

`$.missingRung.forbidden` = `Silent syntactic fallback as if the missing rung were sufficient (C-1 /
freeze law 3).` — this is the fate the fixture must make fail, not a shape it may author.

**From the INPUT corpus `g25-input-corpus.v1.json`.**

`$.whatIsNotAuthored` = `["fixture bytes", "unknown-to-covered conversion (G23)", "sealed-Run
labelling (G27)", "a D9 code"]`. `$.doesNot` includes `Does not collapse the two NT-3 readings into
one state.`, `Does not take over G23 unknown-to-covered conversion.`, `Does not take over G27
sealed-Run labelling.`

**The D-293 constraint clause that applies to this gate.**

`DECISION-PACKETS/D1-fixture-authoring-delegation.claude-recommendation.r2.md`
sha256 `f530cedca1c799097ed0fc30cf8ec6f0480abe9a56d495236909a3a23b84fc33`, verbatim:

> Effective only within already-recorded semantics: no adapter, CI encoding, journal, SDK API, new D9/HostTermination/pack semantics, reserved list, or number is authorised; G15, G16 and G18 keep their recorded prohibitions (adapter, CI encoding, journal).

> **Default policy (replaces round 1):** coverage, not one-case-per-member — every explicit platform, matrix, mutation, transition and fate quantifier in the governing bytes is preserved; for delegated byte sets, concrete witness bytes may be selected only within already-recorded schemas and fates, and any choice that would create a new semantic member, identifier, value, list or implementation stays outside the grant (recorded as a named open decision instead).

For G25 the operative half of the default policy is **"every explicit … quantifier in the governing
bytes is preserved"**: the `namedNt3Readings` pair is exactly such a quantifier, and preserving it is
the difference between a corpus that survives review and `g23-fixture-corpus.v2`'s single-MUST-FIX
rejection.

Adopted by the owner at COORD `## D-293 — User decisions: adopt the agreed recommendations on
DECISIONS-NEEDED A–F` Decision 8, verbatim:

> **D1 (fixture authoring).** Delegated to the orchestrator under D-000 review for the enumerated G15, G16, G18, G19, G20, G21, G24–G30, `OBL-HOSTILE-GOLDENS` and the two DR-122 SARIF fixture obligations, under the agreed semantic, coverage, dependency and D-000 constraints.

## 4. Dependencies

**(a) Sequencing dependency — the DR-131 Class A opening. Not a byte gap.**

This is an ordering constraint on *when the authoring act may be recorded*, not a missing input to
the fixture. Every byte the fixture needs (class name, two cell ids, inputs, pass, three evidence
members, two initial states) exists and is quoted in §1.3 and §1.5.

`DECISIONS-RECOMMENDED.md` (repo root) sha256
`42f27394746d4aac569a09a01da719c0cf318cf114086635e73097a6add97370`, heading
`## B1 — DR-131 Class A opening (preview-analyze-contract.v2)` (line 114), subsection
`### Recommendation (Claude, round 2)`, numbered item 4, verbatim:

> 4. G24–G28 fixture authoring (D1) before the separate DR-131 SATISFIED-GRADE + MF-6 cycle — as conservative sequencing, not as a precedent-proven automatic reopening of gate 2.

Items 1–3 of the same numbered list, verbatim:

> 1. One shared gate-2 entry that states a common interpretation of D-056 gate 2 (declaring whether it interprets D-056 or is a scoped successor amendment) and then measures each of DR-131, DR-133, DR-117 separately from bytes; it reconciles the row-level findings (D-154; D-147; D-207) with the current gate joins' FX-AUTHORING flags expressly, and does not describe D-249..D-253 as silently overturning D-154.
> 2. A fresh application-grade dual review with the NEUTRAL question of the exact final DR-131 contract bytes the opening entry will cite (v2 `081ff7fb…` unless a successor is authored, in which case the successor).
> 3. The T2-02 opening entry per row, citing those verdicts and the owner's product word.

The Codex round-2 concurrence in the same section, verbatim:

> Then use a separate DR-131 opening entry, author the G24-G28 fixtures as conservative sequencing, and conduct a later separate DR-131 SATISFIED-GRADE plus MF-6 cycle.

Adopted at COORD `## D-293 — …` Decision 5, verbatim:

> **B1 (DR-131), B2 (DR-133), B3 (DR-117).** The agreed programmes are authorized in the agreed order — for DR-117: `preview-product-boundary-successor.v9` re-citing the twelve current joins and stating its relationship to `product-boundary-successor-contract.v8` (D-116); a fresh application-grade dual review bound to v9's final digest; then the owner-controlled opening entry; then G29/G30 fixture authoring; then a separate SATISFIED-GRADE + MF-6 cycle. The D-056 Class A openings themselves are separate owner-controlled entries; this entry opens none of them.

Confirmed: `preview-analyze-contract.v2` at digest prefix `081ff7fb` **is** the DR-131 contract
pinned in §1.5 of this file (full digest
`081ff7fb529b34a3db3ac9f4c7505848e2253be12b5a867d39b49b3a26d56970`). Item 2 of the B1 list makes it
conditional ("unless a successor is authored, in which case the successor"), so a later DR-131
contract successor would move this pin — see OQ-G25-5.

**My reading.** D-293 Decision 5 authorizes the programmes "in the agreed order"; the DR-131 list
places the opening entry at item 3 and fixture authoring at item 4; the spelled-out DR-117 sequence
and Codex's round-2 sentence both order opening-then-fixtures; and Decision 5 says the openings "are
separate owner-controlled entries; this entry opens none of them." So on the bytes the opening has
not happened and the agreed order places it first. I read that as blocking the *recording act*, not
the *drafting*: item 4's own text constrains fixture authoring only relative to the SATISFIED-GRADE
cycle, and D-293 Decision 8 delegates D1 for G24–G30 with no express Class A precondition. The record
does not reconcile the two readings. Recorded as OQ-G25-4; the standing line takes the conservative
reading.

**(b) Byte dependencies: none.**

No reserved list, reserved number, reserved axis, unauthored schema, other gate's corpus, or
undecided threshold is required. Measured:

- The class name, input and pass are closed: `preview-analyze-contract.v2.json#$.negativeTests.NT-3`.
- Both cell ids and their `inputNamed` strings are closed:
  `harness.DR-G25…preview.v3.json#$.namedCorpusClasses[0].cells[0..1]`.
- The two initial states are already named and independently accepted:
  `g25-input-corpus.v1.json#$.initialStates`, measured `leftoverDesign: false` at
  `g25-leftover-join.v5.json#$.obligations[2]`.
- No `RESERVED` token appears in `g25-leftover-join.v5.json`, `harness.DR-G25…preview.v3.json`, or
  `g25-input-corpus.v1.json`.

Two things the record does not determine, neither a missing byte: the platform copy axis (§2 axis F,
OQ-G25-1) and the concrete encoding of "Coverage bytes whose standing for that domain is
indeterminate" (OQ-G25-2). The first-authoring version is constructible today without settling the
platform question; the second is a witness-byte selection whose vocabulary is bounded, but whose
domain-encoding surface is exactly what got `g23-fixture-corpus.v1` rejected (see §6).

**Standing: BLOCKED-ON-DR-131-CLASS-A-OPENING (sequencing; no byte dependency)**

## 5. Recommended artifact shape

**Closest precedent: `docs/coop/artifacts/g23-fixture-corpus.v3.json`**
sha256 `3576e2e606b3eed68feced5b83a34247263d3b563274ef3fd9054c8b2a2ba6a7` (recorded D-237) — the
first-authoring version of the G23 lineage. G25 is closer to G23 than to G21 because both are
Coverage-typing corpora over analyze-side JSON payloads; `g23-fixture-corpus.v3` is the only
precedent that carries `cborConstruction` and `pinnedHostContext`, and G25's EV-1 needs both (see
OQ-G25-2).

Fields the successor would carry, from `g23-fixture-corpus.v3.json`:

`artifact, version, date, documentClass ("DESIGN-CONTRACT-CANDIDATE"), registerRow, registerRowNote,
namedGate, status ("CANDIDATE-NOT-APPLIED"), reviewStatus ("AWAITING-INDEPENDENT-REVIEW"),
sealRecommendation ("DO-NOT-SEAL"), binds ("NOTHING"), authorityClaim, purpose, basedOn, parentReview,
file08Pin, head, requiredNowUnchanged, file08StatusToken, appliesFindingsOf, cborConstruction,
pinnedHostContext, whatIsAuthored, whatIsNotAuthored, authoredCatalog,
leftoverDesignClosedIfAcceptedAndRecorded, leftoverDesignRemainingOnG23, remainderAfterThisCorpus,
summary, doesNot, proposedLaterWork, recordedInputs, remeasurementClause`

`authoredCatalog` keys: `standing, doesNotMutate, executionRemains, members`; each member carries
`id, class, inputCorpusId, path, sha256, snapshotMembership, mutation, expected`.

For G25 the member fields map as: `id`/`inputCorpusId` = the two
`g25-input-corpus.v1.json#$.initialStates[*].id` values; `class` = `NT-3` for both, with a **cell**
field (or an `id` that carries the cell suffix, as the INPUT ids already do) so the two readings stay
visibly distinct; `expected` = the class `passVerbatim` reproduced byte-exactly. A `nt3ReadingsSplit`
field mirroring `g24`'s `liveCellMemberSplit` shape is the obvious place to recite
`namedNt3Readings` and assert `ntReadingsCollapsed: false` in `summary`, matching the join's own
`$.summary.ntReadingsCollapsed` = `false`.

**Proposed artifact name:** `docs/coop/artifacts/g25-fixture-corpus.v1.json`
**Proposed fixture directory:** `docs/coop/artifacts/fixtures/g25.v1/`
with two files named after the INPUT ids:
`G25.nt3.missing-required-rung.json`, `G25.nt3.universe-unconstructible.json`, mode `0444`
(measured: `docs/coop/artifacts/fixtures/g23.v3/*` are `-r--r--r--`).

**It is a first-authoring: no platform subdirectories.** The per-platform copy version is a separate
later artifact (`g25-fixture-corpus.v2` under
`fixtures/g25.v2/<platform-with-slash-replaced-by-dash>/`), following D-239 for G23 and D-243/D-247
for G21, and would have to quote its four platform tokens from
`harness.DR-G10.provider-conformance.ts-major-1.v2.json#$.platforms` ORDERED-EQUAL to
`harness.DR-G23.provider-well-formed-admission.preview.v2.json#$.platforms`, never inventing the
list.

## 6. Effort and risk

**Number of cases: 2** payload files in the first-authoring version, carrying **6** (cell ×
evidence-member) obligations; **8** files in the later per-platform copy version. Two review cycles
(Stage A dual ACCEPT, Stage B dual CONSENT) per version, plus one `g25-leftover-join.v6`
remasurement successor per version. No ROW twin successor is needed (§1.4).

**(a) Witness-byte selections inside recorded schemas — choices allowed under the grant.**

1. Which required TypeScript semantic relation is the "missing rung" in
   `G25.nt3.missing-required-rung` — selectable from the already-recorded relation set. The G23
   lineage pinned that set at `g23-fixture-corpus.v3.json#$.pinnedHostContext.typescriptSemanticRelations`
   = `["declares", "imports", "references", "calls", "types", "reachability"]`, cited to
   `docs/coop/artifacts/delivery.v2.json` and `fact-plane.v1.json`. Selecting one is a witness
   choice; adding one is not.
2. The concrete file paths and byte lengths in `snapshotMembership`, following
   `g23-fixture-corpus.v3.json#$.authoredCatalog.members[0].snapshotMembership` (a list of
   `{path, contentSha256, byteLength}`).
3. Which of the two cells carries the fuller universe (the `.cells[0].inputNamed` string explicitly
   allows "other universe construction may still be possible", so the two cells differ in degree, and
   the degree is a witness choice).
4. File names, ordering, and the `mutation` prose, as long as `expected` reproduces
   `$.negativeTests.NT-3.pass` byte-exactly.

**(b) Choices that would create new semantics — record as a named open decision, do not choose.**

1. **A Coverage-domain encoding vocabulary for "indeterminate"** — EV-1 requires "pinned Coverage
   bytes whose standing for that domain is indeterminate — not covered, and not omitted", but no
   pinned artifact in the G25 record supplies a `coverageState` token for that standing. Inventing
   one violates the D-293 default policy's "new semantic member, identifier, value, list". This is
   the exact defect `g23-fixture-corpus.v1.review-independent.claude2.json` rejected as
   `M3 — hostRequestedDomainBaseline invents a Coverage-domain encoding vocabulary and
   under-determines the requested domain`.
   **Record as a named open decision, do not choose.**
2. **Collapsing the two NT-3 cells** into one payload — violates
   `harness.DR-G25…preview.v3.json#$.failsIf[16]` (`the two NT-3 cells are collapsed`),
   `#$.doesNot` (`Does not collapse the two NT-3 cells.`), `g25-leftover-join.v5.json#$.doesNot`
   (`Does not collapse the two NT-3 readings.`), and `#$.obligations[3].namedNt3Readings`.
   **Record as a named open decision, do not choose.**
3. **Scoring unknown-to-covered conversion in EV-1** — violates
   `#$.retainedEvidence[0].passProperty` (`Unknown-to-covered conversion is G23, not this member, and
   is not retained here.`) and `#$.doesNot` `Does not take over G23, G24, G26, G27, G28, or G30.`
   **Record as a named open decision, do not choose.**
4. **Scoring sealed-Run labelling in EV-3** — violates `#$.retainedEvidence[2].passProperty`
   (`Silent promotion / sealed-Run labelling is G27 and is not retained here.`).
   **Record as a named open decision, do not choose.**
5. **Using `UNDETERMINED` (the doctor check status) as the analyze Coverage typing** — violates
   `#$.undeterminedVsIndeterminate` and `#$.failsIf[17]` `doctor UNDETERMINED is collapsed with
   analyze Coverage-indeterminate`.
   **Record as a named open decision, do not choose.**
6. **Reusing DR-133 NT-3's payload** `fixtures/g23.v3/G23.nt3.factcandidate-findingLikeResult.json`
   or classifying a G25 witness under DR-133 NT-3 — violates `#$.failsIf[15]` `DR-133 NT-3 is treated
   as executing here` and `#$.namedExecutesLimbNote`. See §1.5b.
   **Record as a named open decision, do not choose.**
7. **A D9 code or a section 7.1 recipe** in the terminal-result bytes — violates `#$.doesNot`
   `Does not invent a D9 code or a section 7.1 recipe.` and D-293's `new D9/HostTermination …
   semantics`.
   **Record as a named open decision, do not choose.**
8. **A finding identifier** — violates `g25-leftover-join.v5.json#$.doesNot` `Does not invent a
   finding identifier.` and the contract's `$.unstableIdentities.notDurable` = `["finding
   fingerprint", "RunId as a sealed authoritative identity", "EvidenceDigest"]`.
   **Record as a named open decision, do not choose.**

**Risk.** The reviewer-attack pattern on the two recorded fixture-corpus lineages is: (a) inventing a
vocabulary or encoding the record does not carry, (b) collapsing two classes the occupancy keeps
separate, (c) classifying a witness under a closed identifier it does not belong to, (d) over-claiming
closure. **G25 is the highest-risk of the five preview-analyze gates on both (a) and (b)
simultaneously.**

On (a): `g23-fixture-corpus.v1.review-independent.claude2.json` returned `REJECT` with five MUST-FIX
and four SHOULD-FIX, of which three are Coverage-encoding defects —
`M2 — coverageCommitment is a SHA-256 over compact JSON, not deterministic-CBOR, and its construction
is undeclared`, `M3 — hostRequestedDomainBaseline invents a Coverage-domain encoding vocabulary and
under-determines the requested domain`, and `M4 — file and package are not typescript-semantic
coverage relations; both NT-5 baselines are unconstructible for this provider`. G25's EV-1 needs the
same kind of Coverage bytes and will be attacked the same way. The mitigation the record itself
supplies is `g23-fixture-corpus.v3.json#$.cborConstruction`, which declares the profile explicitly
(`RFC 8949 definite-length canonical CBOR`, key order, the `coverageCommitment` construction string,
and the note that it `is not a live Run coverageCommitment verification and is not a section 7.1
recipe`) — a G25 corpus that omits an equivalent declaration invites M2 verbatim.

On (b): `g23-fixture-corpus.v2.review-independent.claude2.json` returned `REJECT` on one MUST-FIX,
`M1 — the EV-2 narrows fixture also converts unknown to covered, collapsing EV-2 into EV-3 against
the governing occupancy's explicit non-collapse requirement`. G25 carries *two* non-collapse
requirements, not one: the NT-3 cells (`$.namedCorpusClasses[0].readingNote`) and
UNDETERMINED-vs-indeterminate (`$.undeterminedVsIndeterminate`). Either one violated is a MUST-FIX.

On (c): the DR-133 NT-3 / DR-131 NT-3 name collision is a live trap; §1.5b exists so the corpus can
recite the distinction rather than trip it.

On (d): `g23-fixture-corpus.v1`'s `S4 — leftoverDesignClosedIfAcceptedAndRecorded over-claims closure
of OBL-G23-FX-AUTHORING` is the template. Because G25's corpus phrase is a *single* class with two
cells and three evidence members, a corpus that pins both cells and all three members arguably does
exhaust the named phrase — which makes over-claiming *more* tempting here than at G24 or G28. The
safe form is `g23-fixture-corpus.v3.json#$.leftoverDesignClosedIfAcceptedAndRecorded` = `[]`, leaving
the closure measurement to the successor leftover-join.

## 7. Open questions

**OQ-G25-1:** Does the first G25 fixture corpus carry per-platform copies, or is it a platform-free
first authoring? The occupancy has no `platforms` array, no `windowsStanding`, and no `d002Standing`;
its only platform byte is `$.proposedLaterWork[2]`'s phrase `on the four D-002 platforms`, and the
four tokens are not enumerated in the G25 record. The G23 lineage split first-authoring (v3, D-237)
from per-platform copies (v4, D-239) and the G21 lineage did the same (v7 → v8), but no G25 byte
records that split as this gate's answer, nor names which occupancy supplies the tokens.

**OQ-G25-2:** What Coverage bytes express "indeterminate — not covered, and not omitted"?
`harness.DR-G25…preview.v3.json#$.retainedEvidence[0].exactByteIntent` requires them, but no artifact
pinned by this occupancy carries a `coverageState` value for that standing. The neighbouring closed
vocabulary is `docs/coop/artifacts/d9-exit-contract.v1.14.json#$.codeVocabulary.reasonCodes`
(sha256 `8dd3303855f49bfdbb2751ee65f54a906405f0654159ebe815472f73cdf7da31`), which carries
`COVERAGE.REQUIRED_RELATION_MISSING` and `VERDICT.INDETERMINATE` — but that is the D9 *exit* contract,
which G25's `$.doesNot` forbids this gate from inventing into, and no byte routes NT-3's Coverage
typing to it. Whether the fixture may cite those reason codes, must cite a Coverage-side token that
does not yet exist, or must record the encoding as open, is not settled.

**OQ-G25-3:** `harness.DR-G25…preview.v3.json#$.retainedEvidence[1].exactByteIntent` requires "a
record that no syntax-only evaluation ran in place of the missing rung and that no syntax-only result
was emitted under the same check or Coverage id." No schema for such a negative-observation record
exists in the pinned inputs, and "the same check or Coverage id" presupposes a check-id namespace
that no cited artifact defines. Whether that record is a field of the terminal-result payload, a
sibling file, or catalog prose is not settled.

**OQ-G25-4:** Does D-293 Decision 5's "authorized in the agreed order" make the DR-131 Class A
opening entry a precondition for *recording* the G25 fixture corpus, or only for the SATISFIED-GRADE
cycle? `DECISIONS-RECOMMENDED.md` §B1 item 4 constrains fixture authoring only relative to "the
separate DR-131 SATISFIED-GRADE + MF-6 cycle", and D-293 Decision 8 delegates D1 for G24–G30 with no
express Class A precondition; but item 4 sits in a list whose item 3 is the opening entry, and
Codex's round-2 sentence orders opening-then-fixtures. The record does not reconcile the two.

**OQ-G25-5:** `DECISIONS-RECOMMENDED.md` §B1 item 2 makes the cited DR-131 contract conditional:
`the exact final DR-131 contract bytes the opening entry will cite (v2 081ff7fb… unless a successor
is authored, in which case the successor)`. If a `preview-analyze-contract.v3` is authored before the
opening entry, every `.source` string in `harness.DR-G25…preview.v3.json#$.namedCorpusClasses[0]`
(`preview-analyze-contract.v2 negativeTests.NT-3`) and every digest pin in §1.5 would need
remeasurement. Nothing in the record says whether an authored G25 corpus would then need a successor
or would stand as measured.
